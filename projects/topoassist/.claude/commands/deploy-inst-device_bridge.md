Deploy device_bridge.py to Mac. Run this on the Mac:

```bash
scp bus-home:~/claude/projects/topoassist/device_bridge.py ~/device_bridge.py
```

Then restart the bridge on the Mac:
```bash
python3 ~/device_bridge.py
```
