================================================================================
  TOPOASSIST — AI REBUILD PROMPT / PROJECT INSTRUCTIONS
  Arista EOS Lab Workbench (Google Apps Script)
  Last updated: 2026-05-17 (fix: selectDevice() link highlight not cleared on device click — added restoreLinkStyle(linkMap[selectedLineId]) before clearing selectedLineId and requestAnimationFrame(redrawLinks) at end of function; matches pattern already in _deselectAll() and selectLine(). FILES: Sidebar-js.html)
  Previous: 2026-05-17 (ui: dynamic Cancel→Close/Abort* label — all edit modals: Cancel button now shows "Close" when clean, "Abort *" when dirty. Tier 1 (live): networkStackModal, autoConfigModal, deviceManagerContainer, sheetColumnManagerContainer — _updateCancelBtn wired alongside updateDirtyButton in checkTechDirty/checkIpDirty/DeviceManagerSaveButton/SheetColumnManagerSaveButton. Tier 2 (open+close): guiEditModal, groupRectModal — reset on open, label computed in close function before _confirmDirtyClose. New helper _updateCancelBtn(id,isDirty) added near updateDirtyButton. Button IDs added: btnCancelTech/Ip/Edit/GroupRect/DeviceManager/SheetColumnManager. FILES: Sidebar.html, Sidebar-js.html)
  Previous: 2026-05-17 (ui: snake accordion label rename + alignment — Sidebar-css.html: .snake-sec-body .ip-label { width:140px } pins all labels to same width so inputs align; Sidebar-js.html: renamed Bridge MAC→DUT MAC, EP1/EP2 sublabels→TrafficGen Endpoint1/2, Port IP→TrafficGen NH IP, Port MAC→TrafficGen NH MAC, Subnets→TrafficGen Subnets; removed --label-w:110px inline (no longer needed with CSS rule). FILES: Sidebar-css.html, Sidebar-js.html)
  Previous: 2026-05-17 (fix+ui: snake accordion consistency + arp spacing — (1) Sidebar-css.html: added .snake-sec/.snake-sec-hdr/.snake-sec-body/.snake-sec-sublabel CSS block mirroring .br-sec pattern (theme CSS vars, rotating .br-chev chevron, collapsed class); (2) Sidebar-js.html: accordion builder replaced native <details>/<summary> with div-based .snake-sec toggle; added _toggleSnakeSec(dev) near _toggleBrSec; removed all inline font-family from accordion HTML (covered by CSS); (3) Sidebar.html: updated snake info-box text (per-user request); (4) Code.gs generateSnakeStaticConfig(): fixed extra spaces in `arp vrf ${pVrf}   ${remoteIp}` (3 spaces → 1) — typo from initial implementation. FILES: Sidebar-css.html, Sidebar-js.html, Sidebar.html, Code.gs)
  Previous: 2026-05-17 (feat: per-device EP1/EP2 in snake accordion — (1) Code.gs: getSnakeDeviceMacs→getSnakeDeviceConfigs (SNAKE_DEVICE_MACS→SNAKE_DEVICE_CONFIGS); value format changed from {devName:macString} to {devName:{bridge_mac,ep1_nh,ep1_mac,ep1_subnet,ep2_nh,ep2_mac,ep2_subnet}}; migration on first read converts legacy format; saveIpPreferences routes snake_configs; getDeviceConfig section 080 applies all 7 per-device fields to _snakeIpPrefs with global fallbacks; snakeBridgeMacConfigured uses Object.values().some(c=>c.bridge_mac); (2) Sidebar.html: removed static .snake-inputs EP1/EP2 block; info-box updated; (3) Sidebar-js.html: accordion builder expanded to 7 fields per device (Bridge MAC + EP1 Port IP/MAC/Subnets + EP2 Port IP/MAC/Subnets) with snake-dev-{field} css classes and data-dev; saveIpConfig collects snake_configs from accordion; resetIpConfig clears all [data-dev] inputs; checkSnakeTrafficWarning reads .snake-dev-ep1-nh/.snake-dev-ep2-nh; EP NH oninput uses querySelectorAll post-build override. FILES: Code.gs, Sidebar.html, Sidebar-js.html)
  Previous: 2026-05-17 (fix: _selectiveConfigInvalidate removed-device path (line ~10239) was missing _markDeviceConfigFetching — added to the Object.keys(oldDevs).forEach loop so split buttons showing removed devices also turn orange. Also confirmed snapshot restore covered via fetchData(true) path, schema change covered via fetchData(true) from Force Sync. FILES: Sidebar-js.html)
  Previous: 2026-05-17 (feat: complete config refresh orange state — _markConfigsFetching() extended to cover all 3 split buttons (delegates to _markDeviceConfigFetching, SSoT); new _markDeviceConfigFetching(devName) marks only buttons showing that device; _markConfigsFetching() added to: saveIpPreferences, saveGlobalCfgFromSidebar, saveTechSettings, fetchData(force=true); _markDeviceConfigFetching() added to: _selectiveConfigInvalidate invalidate.forEach loop + safety-net full clear, _generateSingleDevice. FILES: Sidebar-js.html)
  Previous: 2026-05-17 (feat: per-device snake Bridge MAC accordion — (1) Code.gs: new getSnakeDeviceMacs()/saveSnakeDeviceMacs() store {devName:mac} in UserProperties as 'SNAKE_DEVICE_MACS'; getIpPreferencesForModal() adds prefs.snake_macs; saveIpPreferences() extracts+saves snake_macs separately; _seedPropsSheet() candidate added; getDeviceConfig() section 080 reads per-device MAC via getSnakeDeviceMacs()[deviceName] with fallback to global bridge_mac; snakeBridgeMacConfigured checks both global bridge_mac and Object.keys(getSnakeDeviceMacs()).length>0; (2) Sidebar.html: removed static #ip_bridge_mac row from .snake-inputs; added #snakeDeviceAccordion container div; (3) Sidebar-js.html: openIpConfigModal() builds per-device <details> accordion from _snakeDevs with class='snake-dev-mac' data-dev='${dev}' inputs; saveIpConfig() collects snake_macs via querySelectorAll('#snakeDeviceAccordion .snake-dev-mac') IIFE instead of single bridge_mac field; resetIpConfig() clears accordion inputs; auto-detect fallback pre-fills only when exactly one snake device and no saved MAC. FILES: Code.gs, Sidebar.html, Sidebar-js.html)
  Previous: 2026-05-17 (fix: snakeDeviceNamesSet scope — was declared inside calculateGlobalTopology() but read in getTopologyData(); now returned in calculateGlobalTopology() return object as topo.snakeDeviceNamesSet; getTopologyData() reads Array.from(topo.snakeDeviceNamesSet||[]). FILES: Code.gs)
  Previous: 2026-05-17 (fix: snake CAT/ARP/device-list — (1) Code.gs getTopologyData(): details.category='P2P' now set AFTER details.isSnakePrimary=true and AFTER details.isSnakeSecondary=true (determineCategory() ran before flags existed — CAT always showed L2); (2) Code.gs getDeviceConfig() snakePairsForStatic loop: removed ip_type_ p2p check that silently excluded all snake pairs (snake ports have blank ip_type_ by design) — ARP + static routes were never generated; (3) Code.gs getTopologyData(): snakeDeviceNamesSet tracks devices with at least one snake pair; returned as snakeDeviceNames:[...] alongside snakeBridgeMacConfigured; (4) Sidebar-js.html: _snakeDeviceNames module variable; populated from both topology load handlers; openIpConfigModal() displays 'Snake detected on: X, Y — enter DUT system MAC'; (5) Sidebar.html: #snakeDeviceList banner (green) added above traffic warning; placeholder text renamed to 'No snake connections detected...'. FILES: Code.gs, Sidebar-js.html, Sidebar.html)
  Previous: 2026-05-16 (feat: openConfigFromButton orange fetching state — all three paths (device/left/right) now call _setSplitBtnLabel(...,'fetching',...) at start before any cache check; cached paths restore 'ready' immediately after showConfigModal; uncached paths rely on processDeviceConfig to restore 'ready' on success. FILES: Sidebar-js.html)
  Previous: 2026-05-16 (feat: Auto Config snake section conditional — #snakeTestSection wrapper added in Sidebar.html; shown only when allNodesData contains isSnakePrimary||isSnakeSecondary port (checked in openIpConfigModal() success handler); hidden for topologies with no snake ports; info-box--dim added with Bridge MAC instructions (show version | grep MAC, Device Bridge auto-detect, EP1/EP2 only needed when not directly connected). FILES: Sidebar.html, Sidebar-js.html)
  Previous: 2026-05-16 (fix: snake port config generation — (1) Code.gs getIpBlock() outer condition changed from `if(ipType.includes("p2p"))` to `if(ipType.includes("p2p")||d.isSnakePrimary||d.isSnakeSecondary)` — snake ports have blank ip_type_ (intentional), so the P2P block (IP address + TTL sub-commands) was silently skipped; (2) Code.gs determineCategory() now returns "P2P" for snake ports before ipType-based checks — was returning "L2" since blank ip_type_ matched the L2 fallback; (3) Sidebar-js.html runValidation() Check Group 9 added: flags snake Bridge MAC not set when any snake port exists and snakeBridgeMacConfigured=false (ARP/routes will be placeholder-only); (4) Code.gs getTopologyData() returns snakeBridgeMacConfigured:!!(globalIpPrefs&&globalIpPrefs.bridge_mac) alongside snakeTrafficFlags; (5) Sidebar-js.html: _snakeBridgeMacConfigured module variable + auditFlagSnakeBridgeMac flag + checkbox in Snake/IXIA audit section. FILES: Code.gs, Sidebar-js.html, Sidebar.html)
  Previous: 2026-05-16 (fix: CF grey lost on blank rows — proper fix: CF rules rebuilt ONLY on fullRebuild (Force Sync/schema change); auto-refresh skips setConditionalFormatRules entirely so Force Sync's lastRow+CF_ROW_BUFFER coverage is preserved for blank rows. CF formulas re-evaluate dynamically without needing a rebuild. Previous fix (always extend buffer) caused ~1-2s slowness per edit — reverted and replaced. FILES: Code.gs)
  Previous: 2026-05-16 (fix: snake audit false-positives — (1) Check E (Incomplete L3): added isSnakePrimary/isSnakeSecondary guard — blank ip_type_ is correct for snake ports; (2) Check R (Wrong IP Type): only fires when ip_type_ is explicitly set to a non-p2p value (not blank); (3) VLAN Check Group 6: snake ports skip vlanUsage tracking — primary+secondary share VLAN intentionally, L3 Conflict must not fire. FILES: Sidebar-js.html)
  Previous: 2026-05-16 (fix: CF grey lost on blank rows after any edit — root cause: applyVisualFormatting(fullRebuild=false) used cfLastRow=lastRow (no buffer), causing setConditionalFormatRules to replace Force Sync rules and strip coverage from blank rows beyond lastRow; blank rows lost INACTIVE GRAY on every subsequent edit. Fix: always use Math.min(lastRow+CF_ROW_BUFFER, maxRows) regardless of fullRebuild — buffer always extended, not just on Force Sync. FILES: Code.gs)
  Previous: 2026-05-16 (fix: snake secondary port order + SNAKE description — (1) Code.gs SNAKE SELF-LOOP LINKS block: secondary port now gets order=primaryOrder+0.5 (was 9999) so SHEET sort places it immediately after its primary in the device card (et2→et3→et4→et5 not et2→et4→et3→et5); (2) Code.gs generateAttributesBlock() description Rule S added: snake ports (isSnakePrimary||isSnakeSecondary) emit `-> SNAKE-{peerDev}-{peerPort} __TA` instead of `-> {peerDev}-{peerPort} __TA`, checked before Rule 4/3/2 so snake takes priority. FILES: Code.gs)
  Previous: 2026-05-16 (fix: snake port SSoT — (1) Sidebar-js.html linkMap entry now includes type:link.type so getPeerId() snake-preference loop (linkObj.type==='snake') fires correctly; (2) Sidebar-js.html selectPortOrLink() SSoT: instead of selectLine(lines[0]) (always the regular cable), uses getPeerId() peer result to find the link ID that connects to that peer via portMap scan — keeps snake-preference logic in one place (getPeerId), selectPortOrLink just resolves link from peer identity; (3) Code.gs generateSnakeTtlPbrConfig() + generateSnakeTtlTrafficPolicyConfig() now emit global ACL/route-map/traffic-policies blocks only — per-interface stanzas removed (were duplicating interface blocks with getIpBlock()); (4) Code.gs getIpBlock() snake P2P branch now emits TTL sub-commands (no ip policy, no traffic-policy input/output, ip policy route-map SNAKE_SET_TTL, traffic-policy input/output SNAKE_TTL_POLICY) — SSoT for interface-level TTL config; (5) Code.gs buildConditionalRules() SNAKE-PEER replaced with SNAKE-ROW: single multi-range rule covers all SCHEMA_KEYS columns for the device (same allDevRanges pattern as INACTIVE GRAY) — A-SNAKE RED and N/A-SNAKE GRAY remain higher-priority and override blue per-column. FILES: Sidebar-js.html, Code.gs)
  Previous: 2026-05-16 (refactor: cfgPanel-view button labels SSoT — btnCfgViewCopyIface and btnCfgViewPushIface text removed from static HTML; _openCfgPanelView now sets both via _COPY_IFACE_LABEL / _PUSH_IFACE_LABEL constants (same source as configModal). FILES: Sidebar.html, Sidebar-js.html)
  Previous: 2026-05-16 (fix: snake topology — (1) Code.gs getTopologyData() SNAKE secondary loop: after setting details.peerDev/peerPort, now syncs details back into devicePorts[devName][snakePort].details (replaces empty {} created by globalLinkMap.forEach block) so node.ports has full sp_mode_/ip_type_/peerDev/peerPort — prevents secondary port from being hidden by INT SORT mode filter and ensures showTooltip strict-sync fires on secondary; (2) Sidebar-js.html getPeerId(): now iterates all portMap links and returns snake-type link first before falling back to lines[0] — prevents wrong peer when primary snake port also has a regular cable link to another device; Fix C (addSnakePair client-side guards for ip_type_/sp_mode_/vlan_) was already present from prior session. FILES: Code.gs, Sidebar-js.html)
  Previous: 2026-05-16 (fix: remove Tooltip button from configModal — #btnCfgShowTooltip button removed from action bar (Sidebar.html); _showCfgModalTooltip(e) function deleted; ttBtn management removed from _clearPushBtns and _setPushBtnVisibility. Hover tooltips on canvas interfaces already show interface config via details.config (processDeviceConfig) — no button needed. FILES: Sidebar.html, Sidebar-js.html)
  Previous: 2026-05-16 (ux: rename "Base+Ifs" → "Base+Ints" — all 6 user-visible locations: _COPY_IFACE_LABEL + _PUSH_IFACE_LABEL constants (Sidebar-js.html), btnCfgCopyInterface + btnCfgPushIface static labels (Sidebar.html), btnCfgViewCopyIface + btnCfgViewPushIface static labels (Sidebar.html), Config View panel description × 2 (UserGuide.html). FILES: Sidebar-js.html, Sidebar.html, UserGuide.html)
  Previous: 2026-05-16 (refactor: extract _renderConfigIntoView shared helper — (1) _renderConfigIntoView(configText, preId, searchInputId, clearBtnId, badgeId) pure render helper: expands ta-clean markers → sets currentRawConfig → renders to <pre> via _renderWithLineNums → resets search input → hides badge + clearBtn → wires _makeConfigSearchHandler; returns {body, inp, clearBtn, badge}; (2) showConfigModal() slimmed — delegates all render+search-wire logic to _renderConfigIntoView; retains: title set, modal open, searchInput.focus(), Esc keydown handler; (3) _openCfgPanelView() slimmed — delegates render+search-wire to _renderConfigIntoView; retains: _configViewContext set, title HTML, genSub label, Esc keydown, non-EOS push visibility, _switchCfgPanel; (4) 7 new test cases in test-js.js for _renderConfigIntoView; 72/72 tests pass. FILES: Sidebar-js.html, test-js.js)
  Previous: 2026-05-16 (feat: snake peer marking — (1) buildConditionalRules() SNAKE-PEER CF: when snake_int_<dev> is filled, highlights int_<dev> and snake_int_<dev> with blue (#dbeafe bg/#1e40af text) to show they are a paired snake connection, lower priority than audit/amber; (2) getTopologyData() SNAKE SELF-LOOP LINKS block: after links.push, iterates both directions and sets peerDev/peerPort on whichever port node is already in allCollectedNodes (the primary int_ port); (3) getTopologyData() secondary port addition: looks up globalLinkMap.get(devName+":"+snakePort) and sets details.peerDev/details.peerPort so showTooltip strict-sync override fires and config description shows the connected peer port. FILES: Code.gs)
  Previous: 2026-05-16 (refactor: config view SSoT — (1) _isNonEosDev(devName) helper extracted (replaces inline allDevicesData[devName].type==='non-arista' checks in _setPushBtnVisibility and _openCfgPanelView); (2) _buildCopyInterfaceText(devName,portFilter) pure builder extracted — shared by copyInterfaceConfig() and _cfgViewCopyInterface() (eliminates duplicate copy-interface algorithms); (3) non-EOS path now hides ALL configModal action buttons (Copy+Push, was: only Push); (4) #btnCfgShowTooltip added to configModal action bar — visible in interface view only; onmouseenter→_showCfgModalTooltip(e) routes to showLoopbackTooltip/showVx1Tooltip/showTooltip based on portFilter; onmouseleave→hideTooltip(); (5) _clearPushBtns and _setPushBtnVisibility extended to manage Tooltip button. FILES: Sidebar.html, Sidebar-js.html)
  Previous: 2026-05-16 (docs: UserGuide Device Bridge section — added info-box documenting eAPI push error detail limitation and automatic SSH PTY diagnostic fallback. FILES: UserGuide.html)
  Previous: 2026-05-16 (fix: addSnakePair() guards + ip_type_ forced — server: validates vlan_ is non-empty before writing row (error if missing; snake VRF chain requires it); forces ip_type_=p2p after attrs write so it always holds regardless of user input; client: pre-fills ip_type_="p2p" if blank in snake branch; client-side vlan_ guard shows setStatus error before GAS call. FILES: Code.gs, Sidebar-js.html)
  Previous: 2026-05-16 (ux: context-aware config modal buttons — _setPushBtnVisibility now controls all 4 buttons (copy+push); device view: Copy Base+Ifs/Copy All/Push Base+Ifs/Push All; interface view: Copy Int/Push Int only (Copy All+Push All hidden); new constants _COPY_IFACE_LABEL/_COPY_INT_LABEL/_PUSH_INT_LABEL; _clearPushBtns resets all 4; copyInterfaceConfig uses _extractInterfaceBlock(currentRawConfig, portFilter) in interface view. FILES: Sidebar-js.html, UserGuide.html)
  Previous: 2026-05-16 (refactor: createTopologySnapshot() — snapshot tab name now uses YYMMDDHHMMSS timestamp instead of Unix ms (getTime()). Human-readable, sorts chronologically, second-level granularity is sufficient since creation requires a manual ui.prompt interaction. FILES: Code.gs)
  Previous: 2026-05-16 (fix: addSnakePair() now bumps DATA_VERSION after writing the snake row — previously adding a snake port left the GAS server-side cache stale (10-min TTL) so the smart poll never detected a topology change and the new snake port did not appear in the topology diagram. Fix: PropertiesService.getScriptProperties().setProperty('DATA_VERSION', ...) added before return {success:true}. FILES: Code.gs)
  Previous: 2026-05-15 (refactor: cleanup UI uses server-side command generation via new /reconcile/cleanup endpoint — _doCleanDevItems now sends structured items [{type,data}] to /reconcile/cleanup instead of building raw EOS CLI in JS (_buildCleanupConfig) and posting to /pushconfig; server converts via new _items_to_orphans(items) helper → _orphans_to_cmds() → _batch_orphan_cmds() → push; JS batching loop and _CLEANUP_BATCH_SIZE removed; _buildCleanupConfig retained for command preview display only; VERSION 260515.10→260515.12. FILES: device_bridge.py, Sidebar-js.html)
  Previous: 2026-05-15 (ux: push confirm modal distinguishes orphan-only vs config push — _showPushConfirmModal gains 5th param orphansOnly=false; when orphans present and diff is empty: title→'Orphan Cleanup', body→'Config already up to date — no new commands to push.', Commit button→'Clean up'; when diff is non-empty the modal stays as 'Push' + 'Commit'; call site in pushDeviceConfig() computes _orphansOnly = !!orphans_pending && !diff.trim() and passes it. FILES: Sidebar-js.html)
  Previous: 2026-05-15 (fix: copy-flash restore uses _SPLIT_STATE_BG SSoT — handleCopy and _copyGenRow both restore wrapper.style.background from _SPLIT_STATE_BG[wrapper.dataset.splitState] after 2s flash; previously handleCopy re-derived color from currentDeviceConfig/currentLeftConfig/currentRightConfig vars (stale path); _copyGenRow hardcoded '#10b981' (always green regardless of actual state). FILES: Sidebar-js.html)
  Previous: 2026-05-15 (refactor: push button label SSoT — extracted _PUSH_IFACE_LABEL/'Push Base+Ifs' and _PUSH_ALL_LABEL/'Push All' constants; _clearPushBtns() hides+resets both btnCfgPushIface/btnCfgPushAll; closeConfigModal now calls _clearPushBtns() instead of inline duplicate; _setPushBtnVisibility non-eos path calls _clearPushBtns(). FILES: Sidebar-js.html)
  Previous: 2026-05-15 (fix: ok=false Phase 1 push now falls through to Commit modal when session is open — EOS skipped failing commands but session remains PENDING; committing applies only valid commands (identical to partial staging Case 2); orphan cleanup runs in Phase 2 as normal; _injectOrphanPendingBanner(orphans, pushOk) simplified to _injectOrphanPendingBanner(orphans) — pushOk param removed, action text hardcoded; _p1EosErrs derived before ok check: ok=false reads from res.error.split('\n'), ok=true reads from _eosErrors(res); hard-failure exit only when !res.session_name; empty-diff early exits guard on !res.orphans_pending so orphan-only commits still reach modal; device_bridge.py _push_config() ok=False return now includes session_name; VERSION bumped to 260515.6. FILES: device_bridge.py, Sidebar-js.html)
  Previous: 2026-05-15 (refactor: setSplitBtnState — full SSoT for split-btn color+state; signature changed from (splitId,labelText,bg,actionsDisabled,state) to (splitId,labelText,state,actionsDisabled); bg param removed; color now derived from _SPLIT_STATE_BG const map {ready:'#10b981',fetching:'#f59e0b','non-eos':'#64748b',error:'#ef4444'}; dataset.splitState always written unconditionally; _setSplitBtnLabel drops bg computation; _updateStripFetchError drops errColor var; 10 tests in test-js.js updated to new signature, also verify bg values. FILES: Sidebar-js.html, test-js.js)
  Previous: 2026-05-15 (fix: addDevicesToList originalBg SSoT — removed style.background read-back (originalBg); resetAddBtn now clears btn.style.background='' to let .btn-primary-mono CSS take over instead of baking #3b82f6 as a persistent inline style. FILES: Sidebar-js.html)
  Previous: 2026-05-15 (fix: setSplitBtnState gains optional 5th param state ('ready'|'fetching'|'non-eos'|'error') — writes wrapper.dataset.splitState when provided; _setSplitBtnLabel now passes state to setSplitBtnState instead of writing dataset.splitState separately; _updateStripFetchError passes state='error' so refreshConfigAges cannot overwrite red error labels with age text; 6 new tests in test-js.js (setSplitBtnState section). FILES: Sidebar-js.html, test-js.js)
  Previous: 2026-05-15 (fix: refreshConfigAges isGreen() dual color-check workaround replaced with dataset.splitState — was checking both 'rgb(16,185,129)' and '#10b981' as a fragile workaround for Chrome color normalization; now uses w.dataset.splitState==='ready'. _setSplitBtnLabel writes dataset.splitState on every call; all split-btn state checks must use this attribute. FILES: Sidebar-js.html)
  Previous: 2026-05-15 (fix: orphan banner suppressed when Phase 1 push has EOS errors — _injectOrphanPendingBanner now called inside the ok=false branch after _updatePushPreviewError fills the body, with pushOk=false; banner text changes to "fix the push error above, then re-push"; function signature updated to _injectOrphanPendingBanner(orphans, pushOk=true). Root cause: bodyEl.innerHTML assignment in _updatePushPreviewError replaces content, so orphan injection must follow it; ok=true path unchanged. FILES: Sidebar-js.html)
  Previous: 2026-05-15 (fix: _markConfigsFetching background check was broken — Chrome normalizes style.background '#10b981' to 'rgb(16, 185, 129)' so the equality check always failed and the card never flipped. Fix: _setSplitBtnLabel now writes wrapper.dataset.splitState = state after every call; _markConfigsFetching checks dataset.splitState === 'ready' instead of style.background. FILES: Sidebar-js.html)
  Previous: 2026-05-15 (ux: device card flips to Fetching immediately on sheet change — smart poll version-change handler now calls configCache={} + _markConfigsFetching() before fetchData(false); _markConfigsFetching() helper (near _selectiveConfigInvalidate, ~line 10109) checks if splitDevice is green (#10b981), reads devData.sheetIndex from allDevicesData[selectedDeviceId], calls _setSplitBtnLabel with state 'fetching' to flip card amber and disable Copy/View/Push; after rebuild+prefetch, card returns to green Cached as before; manual Refresh (fetchData(true)) already cleared configCache — unchanged. FILES: Sidebar-js.html)
  Previous: 2026-05-15 (fix: push session timeout resilience — _finalize_or_restage() added to device_bridge.py; if EOS configure session expires during user review (~30s idle timeout), finalize handler now re-stages and commits the full config immediately instead of failing; JS push modal shows amber "re-staged + committed — session had timed out" + marks Phase 1 "session expired" in amber; 7 new tests in test_device_bridge.py TestFinalizeOrRestage. FILES: device_bridge.py, Sidebar-js.html, tests/test_device_bridge.py)
  Previous: 2026-05-15 (perf: move CF_ROW_BUFFER extension to Force Sync only — applyVisualFormatting(optionalSheet, fullRebuild) gains fullRebuild param; all 5 steps run on every refresh (backgrounds, borders, CF rules, fonts); only the cfLastRow calculation changes: auto-refresh uses lastRow (covers current rows), Force Sync uses lastRow+CF_ROW_BUFFER (pre-covers 300 new rows). The buffer is what made setConditionalFormatRules slow on every edit — removing it on auto-refresh saves ~1-2s per reload. isColorEnabled behavior fully preserved. applyGlobalFormatting(fullRebuild) passes flag through. FILES: Code.gs)
  Previous: 2026-05-15 (ux: rename "Push Interface" → "Push Base+Ifs" — all 5 user-visible locations: btnCfgPushIface + btnCfgViewPushIface button labels (Sidebar.html), two piBtn.textContent assignments in _setPushBtnVisibility/_clearPushBtns (Sidebar-js.html), Config View panel description (UserGuide.html). FILES: Sidebar.html, Sidebar-js.html, UserGuide.html)
  Previous: 2026-05-15 (docs: UserGuide — snake VRF breakout naming + snake_int_ color note; added breakout-port note to 001_SYSTEM bullet (Et61/1 → SNAKE_Et61-1) and snake_int_ shade note to Sheet Formatting section. FILES: UserGuide.html)
  Previous: 2026-05-15 (fix: snake_int_ column header color — snake_int_ row-2 sub-header now gets a distinct shade (same device hue, sat=55/lig=82 vs row-1 sat=85/lig=88) so it is visually separate from int_ and other device columns; snakeIntRow2Cols tracking added to applyVisualFormatting(). Requires Force Sync + Save & Sync to apply. FILES: Code.gs)
  Previous: 2026-05-15 (fix: snake VRF names with breakout ports — _snakeVrf(portName) helper added to Code.gs; replaces '/' with '-' in canonical port names before building EOS VRF name (e.g. SNAKE_Et61/1 → SNAKE_Et61-1); applied at all 5 call sites: getIpBlock() ×3, collectDeviceData() vrfs.add ×2, generateSnakeStaticConfig() forEach body. EOS rejects '/' in vrf instance names. FILES: Code.gs)
  Previous: 2026-05-14 (fix: Unicode icons → SVG/text — replaced all ⚠/✗/🚀/✓ Unicode chars in Sidebar-js.html with plain text (status messages) or inline SVG (GW caution divs, generate-all success header); fix: applyVlanFilter VTEP range chip — filterVlans now built by expanding each topoVlanFilter token through parseVlanInput() so VTEP range chips like "1-4000" correctly match individual port VLANs. FILES: Sidebar-js.html)
  Previous: 2026-05-14 (fix: _TA_PROPS auto-seed on sidebar open — _seedPropsSheet() added to showTopologyWindow() alongside _restorePropsFromSheet(); this ensures _TA_PROPS is backfilled from existing DocumentProperties the first time the sidebar opens, without requiring a manual Device Manager/Tech Settings save first. Copy flow now: open sidebar on original (seeds _TA_PROPS) → make copy → open sidebar on copy (restores from _TA_PROPS). FILES: Code.gs)
  Previous: 2026-05-14 (fix: _TA_PROPS coverage + first-open restore — TARGET_SHEET_NAME now wired to _writePropsSheet in promptRenameSheet (both paths); _restorePropsFromSheet() added to showTopologyWindow() so copy-restored properties are applied on first sidebar open without needing a second reopen. FILES: Code.gs)
  Previous: 2026-05-14 (docs: UserGuide full structural audit — fixed snapshot tab name from "Snapshot_<timestamp>" to "SNAP_<timestamp>_<name>"; expanded checkpoint restore description to cover all device settings restored (not just roles+MLAG); fixed "Custom View" menu-path badge to "Custom View..." matching actual menu label. FILES: UserGuide.html)
  Previous: 2026-05-14 (feat: _TA_PROPS_SNAP — snapshots now embed full property backup in row 3 as "_TA_PROPS_SNAP:{doc:{},user:{},script:{}}" by reading all _TA_PROPS rows at creation time via _readPropsSheetForSnapshot(). restoreFromSnapshot detects new prefix and calls _restorePropsFromSnapshot() to unconditionally restore all three property stores. Legacy "PROPS:" snapshots still restore roles+mlag only. getSnapshotDiff updated to recognize both prefixes as 3-row format. New helpers: _readPropsSheetForSnapshot(), _restorePropsFromSnapshot(propsSnap). CLAUDE.md updated with _TA_PROPS_SNAP invariant. FILES: Code.gs)
  Previous: 2026-05-14 (revert: CF_ROW_BUFFER 10→300 — Sheets does NOT auto-extend CF ranges when rows are appended beyond the current range; buffer of 300 is required to pre-cover new rows. FILES: Code.gs)
  Previous: 2026-05-14 (docs: update help modal (Sidebar.html) and UserGuide.html for regex search — search placeholder updated to "Filter, regex, or v10 to trace VLAN…"; strict shortcut ctx updated to "whole-word match"; examples section adds leaf,spine (OR), leaf[12] (regex), et[0-9]+/1$ (regex), 10\.0\.[12]\. (IP regex); UserGuide adds comma-OR, regex examples and corrects strict description. FILES: Sidebar.html, UserGuide.html)
  Previous: 2026-05-14 (feat: regex support in global search (filterTopology) — searchPatterns now always contains RegExp objects; each comma-separated term is compiled with new RegExp(term,'i') (try/catch falls back to escapeRegExp on invalid syntax); strict mode wraps in word-boundary anchors (?:^|\s|\/|:)(?:pattern)(?:$|\s|\/|:); device name/role check uses pat.test(); port row check uses pattern.test(rowText) — removes old rowText.includes() branch. Commas are still OR separators. VLAN trace mode (v10,v20-30) unchanged. FILES: Sidebar-js.html)
  Previous: 2026-05-14 (feat: _seedPropsSheet() — one-time forward-fill of _TA_PROPS from live properties on first open; covers all 55 backed-up keys; no-op on subsequent opens. Called from onOpenInstallable() after _restorePropsFromSheet(). FILES: Code.gs)
  Previous: 2026-05-14 (fix: VLAN chip filter now hides device nodes and group rects — applyVlanFilter() adds/removes .hidden-vlan on .node[data-id] elements via devicePortsMap after hiding port rows; CSS .node.hidden-vlan{display:none!important} in Sidebar-css.html means group rects with all-hidden members auto-skip (renderGroupRectsInto _computeGroupClusters already skips el.style.display=none nodes, and .hidden-vlan overrides inline style via !important); refreshGroupRects+redrawLinks called via requestAnimationFrame after. fix: _devVisRestoreAll() now resets ALL DevVis filter state on close (was only clearing hiddenDevices/selectedInDevVis): resets topoVlanFilter=[]/topoSviFilter/topoIpFilter/topoIntModeFilter to defaults, removes .hidden-vlan from all .node elements, calls applyTopoSviFilter/applyTopoIpTypeFilter/applyTopoIntModeFilter/applyVlanFilter, re-renders all filter strips. Interaction with global search: applyVlanFilter uses CSS class with !important so VLAN filter wins over search/dimmed inline styles (no conflict). FILES: Sidebar-js.html)
  Previous: 2026-05-14 (feat: extend _TA_PROPS copy-safe backup to ALL topology properties — previously only DEVICE_ROLES/MLAG_PEERS/NON_ARISTA_DEVICES were backed up; now covers: 20 tech-settings keys (saveNetworkSettings→_writePropsSheetBatch), GLOBAL_DEVICE_CONFIG_TEMPLATE (saveGlobalConfig), GW_DEVICE_OVERRIDES (saveGwDeviceOverrides), DEVICE_HOSTNAMES (saveDeviceHostnames + inline in processDeviceBatch), DEVICE_METADATA (saveDeviceMetadata), SCHEMA_CONFIG_ARRAY (saveSchemaConfig) all as store='doc'; IP prefs 21 keys (saveIpPreferences→_writePropsSheetBatch) and CUSTOM_VIEW_PREFS (applyCustomView + migration path) as store='user'; DEVICE_LABELS (saveDeviceLabels) and TOPOLOGY_HIDDEN_DEVICES (saveDeviceVisibility) as store='script'. _writePropsSheet gains optional store param (3rd col); new _writePropsSheetBatch for batch writes; _restorePropsFromSheet now routes by store and restores ALL keys (no allowlist). Rule added to CLAUDE.md: every new saveX() must call _writePropsSheet. FILES: Code.gs, CLAUDE.md)
  Previous: 2026-05-14 (chore: increase CF_ROW_BUFFER 200→300. FILES: Code.gs)
  Previous: 2026-05-14 (fix: xcvr/schema columns missing CF and dropdown validation after adding new interfaces — root cause: buildConditionalRules() and setDataValidation() both bounded to lastRow at rebuild time; rows added after the last rebuild fall outside the range. Fix: added CF_ROW_BUFFER=300 constant; rebuildSheet() validation range extended to Math.min(Math.max(finalRowCount-2, CF_ROW_BUFFER), maxRows-2); applyVisualFormatting() CF range extended to Math.min(lastRow+CF_ROW_BUFFER, maxRows); updateSheetDropdownsOnly() updated same. Also removed misleading "requires Force Sync" tooltip from Non-EOS toggle buttons in Device Manager (Non-EOS toggle IS a structural change → triggers rebuildSheet with full schema — Force Sync not needed). FILES: Code.gs, Sidebar-js.html)
  Previous: 2026-05-14 (fix: Edit Interface dirty warning fires on no-op open+close — moved `initialEditModalState = _captureFormState('guiEditModal')` to AFTER `modal.style.display = "flex"` in all three open functions (openEditModal, openConnectModal, openAddLinkModal). Root cause: capture was taken while modal was still display:none; browser select-value normalization differs in hidden vs visible state, causing open-state ≠ close-state even with zero user changes. FILES: Sidebar-js.html)
  Previous: 2026-05-14 (fix: Sheet Checkpoint Management — delete confirm() blocked in cross-origin GAS iframes; replaced with inline in-page confirmation panel (#delConfirm, cancelDelete/doDelete); dialog height 470→520. _writePropsSheet silent catch now logs + shows toast on error for diagnostics. FILES: Code.gs)
  Previous: 2026-05-14 (feat: _TA_PROPS sheet — DocumentProperties survive File→Make a copy. _writePropsSheet(key,val) mirrors DEVICE_ROLES+DEVICE_MLAG_PEERS+NON_ARISTA_DEVICES to a hidden "_TA_PROPS" sheet on every save. _restorePropsFromSheet() reads it back and sets missing DocumentProperties — called first in onOpenInstallable(). Write-through wired in saveDeviceRoles, saveDeviceMlagPeers, saveNonAristaList. Legacy sheets without _TA_PROPS tab work unchanged. FILES: Code.gs)
  Previous: 2026-05-14 (feat: Sheet Checkpoint Management — menu item renamed from "Restore Sheet Checkpoint"; createTopologySnapshot stores DEVICE_ROLES+DEVICE_MLAG_PEERS as PROPS:JSON in row 3 (was 2 metadata rows, now 3); restoreFromSnapshot detects format via "PROPS:" prefix, restores roles+mlag on restore; getSnapshotDiff updated for both legacy (2-row) and new (3-row) formats using computed metaRows/snapHeaderRow/snapDataStart; wizard title and modal title updated to "Sheet Checkpoint Management"; UserGuide updated. FILES: Code.gs, UserGuide.html)
  Previous: 2026-05-14 (feat: Delete Checkpoint — deleteSnapshot(snapshotName) added to Code.gs; showRestoreWizard gets "Delete Checkpoint" button below Restore (red outlined, disabled until selection, confirm dialog); on success removes option from dropdown and resets UI; dialog height 430→470; test_deleteSnapshot: 3 cases (throw on missing, exists before, gone after); registered in runAllTests. FILES: Code.gs, Test-gs.gs)
  Previous: 2026-05-14 (docs(userguide): full structural audit — fixed Cabling Export and VLAN Summary section descriptions (both said "secondary toolbar row" but buttons are in primary row VIEW section); added Reset Snapshot button option to DID shift warning. FILES: UserGuide.html)
  Previous: 2026-05-14 (feat: File→Version history→Restore now triggers sidebar reload — onStructuralChange handles changeType='OTHER' (fired by native version history restore) by setting _RELOAD_AFTER_RESTORE='1'; sidebar _checkRestoreReload 5s poll picks it up, clears configCache, calls fetchData(true); status message generalized to "Sheet restored — reloading topology…" to cover both checkpoint and version history restore paths. FILES: Code.gs, Sidebar-js.html)
  Previous: 2026-05-14 (fix: L2 trunk GW SVIs now use plain ip address — getIpBlock() gains 4th param _vtepSvi (default true); L2 trunk SVI caller passes false, bypassing the LEAF anycast/VARP branch; Vx1 SVI loop generates inline, unaffected. Condition: !_vtepSvi || deviceRole!='LEAF' → plain ip address. FILES: Code.gs)
  Previous: 2026-05-14 (fix: gwVlans no longer populated from L2 trunk/access rows — analyzeRow() removed gwVlans.add(v) entirely; L2 trunk rows with ip_type_=gw-* generate regular SVI IP config (not anycast/virtual) and must NOT feed VXLAN VNI mappings; gwVlans is now exclusively populated from Vx1 interface rows (local + MLAG peer), which are the authoritative declaration of what gets VXLAN-extended. FILES: Code.gs)
  Previous: 2026-05-14 (feat: auto config reload after checkpoint restore — restoreFromSnapshot() sets DocumentProperty _RELOAD_AFTER_RESTORE='1' after syncSchemaPreservingOrder(); new checkAndClearReloadFlag() reads+deletes flag atomically, returns bool; Sidebar-js.html 5s setInterval(_checkRestoreReload) polls it, clears configCache and calls fetchData(true) when flag is consumed; GAS modal blocking ensures poll fires naturally after dialog closes. Test: test_checkAndClearReloadFlag in Test-gs.gs — 3 cases: flag absent→false, flag set→true+clear, second call→false. FILES: Code.gs, Sidebar-js.html, Test-gs.gs)
  Previous: 2026-05-14 (fix: ensureDummyColumn() — insertColumnBefore(1) now only runs when col 1 has non-empty, non-_sys_ content. After sheet.clear() getLastColumn() returns 0 and col 1 is blank; previously an unconditional insert created a ghost column (the old _sys_ position at col 2, same 36px width) visible as a duplicate sys column after 0-device reset. Fix: guard insert with lastCol>=1 && col1Val!=''. FILES: Code.gs)
  Previous: 2026-05-14 (fix: two bugs — (1) onEdit DATA_VERSION bump moved to BEFORE LockService.tryLock(): rapid paste events could fire concurrent onEdit executions; if tryLock timed out, no bump was recorded and the smart poll never triggered a refresh; (2) VXLAN/Lo1 VTEP guard changed from gwVlans.size>0 to vx1Vlans.size>0: gwVlans is populated from GW SVI rows (ip_type_=gw-*) which exist without a Vx1 row — the guard incorrectly generated VTEP config for devices with GW SVIs but no Vx1 interface; vx1Vlans is a new Set in collectDeviceData populated only from explicit Vx1 rows (local + MLAG peer); gwVlans unchanged for VNI mapping + BGP. FILES: Code.gs)
  Previous: 2026-05-14 (feat: delete device ID snapshot — deleteDeviceIdSnapshot() added to Code.gs; "Reset Snapshot" button added to push modal ID-shift banner; _resetDeviceIdSnapshot() clears banner + _lastIdShiftResult + calls GAS. Shift warning now dismissible without regenerating all configs. FILES: Code.gs, Sidebar-js.html)
  Previous: 2026-05-13 (refactor: SSoT — extracted _applyExplicitMlagOverride(topo, headers, rows) helper; was copy-pasted identically in getTopologyData, getDeviceConfig, and applyVisualFormatting; now one call per site. FILES: Code.gs)
  Previous: 2026-05-13 (fix: getTopologyData never applied explicit MLAG peer override — calculateGlobalTopology returns empty peerLinkPorts; without the override block, isPeerLink=false on all nodes → links never got type='peer' → canvas showed no peer-link styling and audit check "MLAG Peer-Link Missing" always fired. Fix: added same explicitMlagPeers override block (+ _extendPeerLinkPortsWithPo + mlagConfigPorts rebuild) to getTopologyData immediately after calculateGlobalTopology, mirroring getDeviceConfig and applyVisualFormatting. FILES: Code.gs)
  Previous: 2026-05-13 (fix: MLAG peer-link visual and config-gen bugs — (1) _extendPeerLinkPortsWithPo() helper added: after peerLinkPorts is built from Et-keyed globalLinkMap entries, it scans data rows and adds Po-keyed entries ("Leaf1:Po4000") so calculateConnectionBackgrounds can detect peer-link on rows where colorKey is Po-keyed; called in both applyVisualFormatting and getDeviceConfig after the explicit peer forEach; (2) detectMlagState: after extracting peerLinkPort from Et key, scans rows via indices['po'] to find the Port-Channel value and converts to "Port-Channel4000" form for mlag configuration peer-link command. FILES: Code.gs)
  Previous: 2026-05-13 (fix: ta-clean-* markers not expanded in strip copy buttons (copyLeft/copyRight/copyDevice) — all now call _expandTaCleanDisplay(); 8 tests added to test-js.js for _expandTaCleanDisplay. CLAUDE.md invariant tightened. FILES: Sidebar-js.html, test-js.js, CLAUDE.md)
  Previous: 2026-05-13 (fix: onEdit DATA_VERSION bump moved BEFORE the auto-clean processing loop — was at end of try block so any exception/timeout silently prevented the bump, causing smart poll to never detect the change and auto-refresh to fail after paste. Bump now fires right after guard checks (wrong sheet / col A / header row) so it always runs for valid data-row edits regardless of what the processing loop does. FILES: Code.gs)
  Previous: 2026-05-13 (fix: ta-clean-* markers showing verbatim in port hover tooltip, loopback tooltip, and Vx1 tooltip — all three config render paths (showTooltip, showLoopbackTooltip, showVx1Tooltip) now call _expandTaCleanDisplay() before escapeHtml(). FILES: Sidebar-js.html)
  Previous: 2026-05-13 (ui: audit checkbox label "VLAN mismatch on PO link" → "VLAN mismatch between interface" — check runs on all link types, not just PO. FILES: Sidebar.html)
  Previous: 2026-05-13 (feat: audit check N extended to cover l2-et-access and l2-po-access — multi-VLAN in vlan_ now fires auditFlagStaleVlan for access modes (was L3 only); message updated to 'Multi-VLAN: only a single VLAN is valid here'. FILES: Sidebar-js.html)
  Previous: 2026-05-13 (fix: svi_vlan_='no'/'−' now treated as no-SVI in all JS consumers — (1) audit check D 'Incomplete SVI' no longer fires; (2) applyTopoSviFilter maps them as 'blank' not 'active'. Other consumers (line 13921, 20073) already safe via /^\d+$/ filter. FILES: Sidebar-js.html)
  Previous: 2026-05-13 (fix: audit check D "Incomplete SVI" no longer fires when svi_vlan_='no' or '-' — isSviOn now excludes these explicit no-SVI markers (same as _parseSviVlans and CF rules). FILES: Sidebar-js.html)
  Previous: 2026-05-13 (fix: CF rules A3/A5/N/A-3/N/A-6 now treat svi_vlan_='no' and svi_vlan_='-' as 'no SVI' — ip_type_ and vrf_ correctly grey when L2 mode and svi_vlan_ is empty OR 'no' OR '-'; all 4 rules updated via replace_all. FILES: Code.gs)
  Previous: 2026-05-13 (fix: hostname dot (bridge-hn-dot) never lit — show version | json does not include hostname on all EOS versions; added show hostname as 5th command in _check_devstatus; _build_devstatus_ssh gains hn=None param; hostname field now reads hn.get("hostname","") instead of ver.get("hostname",""); test suite updated: _VER loses hostname key, _HN fixture added, _run() passes hn=_HN, test_ver_none corrected, test_hn_none added, test_all_none passes hn=None. VERSION 260513.4→260513.6. FILES: device_bridge.py, Sidebar-js.html [embedded template], tests/test_device_bridge.py)
  Previous: 2026-05-13 (fix: bridge modal SSH status dot (bridgeDot_) was always grey on modal reopen — _restoreBridgeDevDots() added; called after _populateLldpDevTable() in openLldpModal() to reapply dot-green/dot-red from devStatusMap; also moved dev-bridge-dot card indicator from right:5px to left:5px (right side overlapped with expand affordance). FILES: Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-05-13 (feat: refined per-device bridge reachability indicators — (1) bridge modal per-device dot reverted to dot-red for SSH-failed devices (was dot-orange; red=device unreachable, distinct from orange=auth-expired toolbar state); (2) card header dev-bridge-dot shows dot-green when ds.ok===true (reachable), dot-orange when ds.ok===false; (3) _bridgeHasDeviceFailures flag: reset to false at start of each runBridgeCheck(); set true on any dot-red (SSH fail); persists across health-check polls so _checkBridgeHealth()/_silentBridgeCheck() re-assert 'partial' vs 'up' correctly; (4) bridge-partial toolbar state (dark-yellow #d97706, CSS class bridge-partial): set by _updateBridgeToolbarIcon after runBridgeCheck completes with ≥1 device unreachable; _bridgeUpState now treats both 'up' and 'partial' as up-state for auto-Bridge/autoCheck. FILES: Sidebar-js.html)
  Previous: 2026-05-13 (feat: _parseSviVlans now accepts 'no' and '-' as explicit no-SVI markers (same result as blank — returns []) so users can document intent in the sheet without affecting config gen. FILES: Code.gs)
  Previous: 2026-05-13 (fix: smart poll visibility guard removed — getDataVersion() now polls every 4s regardless of tab focus so sheet edits while backgrounded are detected and configCache stays current. FILES: Sidebar-js.html)
  Previous: 2026-05-13 (fix: INACTIVE GRAY now includes int_ column — full row greys when int_ is empty; removed key!=='int' exclusion from SCHEMA_KEYS reduce. FILES: Code.gs)
  Previous: 2026-05-13 (feat: per-device bridge unreachable indicator — orange dot (top-right of card header, position:absolute) added to every device card via dev-bridge-dot span with dataset.devId; applyDevStatusToCards() sets dot-orange when ds.ok===false; runBridgeCheck() resets all dots before check; bridge modal per-device dot changed from dot-red→dot-orange for unreachable (distinguishes per-device failure from global bridge-down red). FILES: Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-05-13 (ui: exportCablingModal width 700px → 1050px (50% wider) for single-line cabling display. FILES: Sidebar-css.html)
  Previous: 2026-05-13 (fix: Cabling Export view panel textarea now fills full modal height — added height:100%;min-height:0 to .cabling-view-body .cabling-preview-text override (was only min-height:200px, leaving empty space below content). FILES: Sidebar-css.html)
  Previous: 2026-05-13 (fix: Cabling Export — Topology List and Device List tabs now call _viewCablingReport instead of _cablingShowTab, so clicking the tab populates the textarea and switching tabs refreshes content for the selected mode. FILES: Sidebar.html)
  Previous: 2026-05-13 (feat: buildConditionalRules — (1) add A-Vx RED rules: et_speed_/xcvr_speed_/xcvr_type_/encoding_/sp_mode_/po_/ip_type_ show red when int_ starts ^Vx and field is filled (paired with existing N/A-5/N/A-Vx gray; gray when empty, red overrides when filled); (2) add W1 REMOVED comment with reason (was: et_speed_=xcvr_speed_ amber — removed because equal speeds is correct for native non-breakout ports; W2 is the refined surviving rule); (3) fix stale INSTRUCTIONS: N/A-5 desc, N/A-Vx undocumented, A8 scope, N/A GRAY color, W1 entry. FILES: Code.gs, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-05-13 (feat: SFP10 added to xcvr_type_ — schema preset after SFP+; FORMAT_CONFIG color #f1f5f9 (same as SFP+, 10G tier); UserGuide swatch updated to "SFP+ / SFP10". FILES: Code.gs, UserGuide.html)
  Previous: 2026-05-13 (fix: _autoMinimizeOpenModal skips full-screen containers; showAuditResults removes _scanInternalVlanConflicts — internal VLAN conflicts belong in audit mode only, not schema sync. FILES: Sidebar-js.html)
  Previous: 2026-05-13 (revert: _oc_iface_status admin-status check — reverted 42b6818; only SSH/eAPI transport is used, which already returns "disabled" from EOS for admin-shut ports; REST/gNMI change was not needed. VERSION 260513.4. FILES: device_bridge.py, Sidebar-js.html [downloadBridgeScript() embedded template])
  Previous: 2026-05-13 (feat: xcvr_type_ schema defaults + SFP56 color — added preset dropdown options ['OSFP','QSFP-DD','QSFP56','QSFP28','QSFP100','DSFP','QSFP+','SFP56','SFP28','SFP25','SFP+','SFP','BASE-T'] to _DEFAULT_SCHEMA xcvr_type entry (was empty []); added SFP56→#ccfbf1 (teal-100, 50G speed color) to FORMAT_CONFIG xcvr_type rules between QSFP+ and SFP28; updated UserGuide color swatches + dropdown note. FILES: Code.gs, UserGuide.html)
  Previous: 2026-05-13 (docs: UserGuide — EVPN SERVICE description expanded: Per-VLAN default (RT=VLAN:VLAN); Bundle sub-modes split into "No bundle ranges" (single EVPN_VLAN_AWARE_BUNDLE, RT=ASN_BASE:1) and "Named bundles" (EVPN_BUNDLE_N, RT=ASN_BASE:N, VLANs intersected per device). FILES: UserGuide.html)
  Previous: 2026-05-13 (fix: add flex:1;min-height:0 to pushConfirmBody, helpModal body, vlanSummaryBody — missing scroll trio caused silent content clipping. FILES: Sidebar.html)
  Previous: 2026-05-13 (docs: UserGuide — new "VLAN Summary" h2 section (id=vlan-summary) with TOC link; documents table columns (VLAN/Type/VRF/Ports+Devices), 4 type colors (L2=blue/L3=amber/VTEP=teal/INT=purple), [CONFLICT] badge, sort buttons (ID/Type/VRF/Ports), Conflicts filter toggle, filter box, and INT-type bridge dependency note. FILES: UserGuide.html)
  Previous: 2026-05-13 (docs: UserGuide — new "Cabling Export" h2 section added (id=cabling-export) with TOC link; documents Export tab (bridge filter bar pills: Mismatch/Miss/ErrDis/Down/Shut/Error; Topology List: Copy MD/Copy CSV/View/Download; Device List: View/Download; AI Validate: NotebookLM integration with editable prompt), Topology List and Device List tabs (find bar, Copy, Download), and Model/Rack field info box. FILES: UserGuide.html)
  Previous: 2026-05-13 (docs: UserGuide Config Center — toolbar table entry updated (accurate panel list + Targets strip mention); new "Config Center — Overview" h3 added at top of config-generation section: documents Targets strip (blue chips, All/None, role filter, Shift+click), 4 panels (Generate/Template/Cleanup/Deploy) in a table, and Config View panel (filter bar, :N jump, Copy Base+Ifs, Copy All, Push Interface, Push All). FILES: UserGuide.html)
  Previous: 2026-05-13 (docs: UserGuide Installation Step 3 updated — "Configure & run" → "Run the bridge"; removed METHOD/JUMP_HOST/SSH_USER bullet list; now says transport settings are in the Device Bridge modal. FILES: UserGuide.html)
  Previous: 2026-05-13 (docs: UserGuide Device Bridge section updated — Setup Step 2 now says "run, no editing needed" (transport configured in modal); added Modal Settings subsection documenting Bridge card (enable on bring-up, auto-reconnect, auto-check interval, L1 threshold) and Transport card (SSH: USER/PASS/PORT/JUMP; eAPI: USER/PASS/PROTO/PORT); Running a Check updated to mention Device Login collapsible and IP field. FILES: UserGuide.html)
  Previous: 2026-05-13 (fix: Device Manager always opens at max height — autoResizePanelHeight() manager branch uses maxH directly; was HDR+n*ROW which gave 380px with 0 devices. FILES: Sidebar-js.html)
  Previous: 2026-05-13 (docs: UserGuide Audit Engine section restructured — Live Issues (Bridge/LLDP, Bridge/Int State, Bridge/Other) moved above Design Issues; blue/green section header rows added; 6 always-active checks (Orphan Po, Stale VLAN, Stale SVI, Missing VLAN, Stale VRF, Incomplete xcvr speed) marked with "always active" inline label; intro updated to explain 32-configurable + 6-always-active = 38 total structure. FILES: UserGuide.html)
  Previous: 2026-05-13 (docs: UserGuide multi-update — Keyboard Shortcuts section expanded to full Help modal content (search/reset/config search/device cards/navigation/edit mode); GW Address Formula: added super-wide prefix tier (IPv4 /8-/15, IPv6 /32-/47, oct2 dropped, collision warning); "Per-VLAN VRF" renamed to "Per-VLAN & Per-Sub-Interface VRF" throughout; "IP Config"/"IP Configuration Modal" renamed to "Auto Config" to match Sidebar modal title; .inline-code CSS class added. FILES: UserGuide.html)
  Previous: 2026-05-13 (docs: UserGuide Typical Workflow Step 6 — remove MLAG from Tech Stack list; add note that MLAG is declared in Device Manager. FILES: UserGuide.html)
  Previous: 2026-05-13 (docs: UserGuide — remove all REST/gNMI references (intro para, interface state note, internal VLAN note, push note); only ssh/eapi mentioned now. FILES: UserGuide.html)
  Previous: 2026-05-13 (docs: UserGuide — METHOD default corrected to "eapi"; rest/gnmi removed from METHOD docs and transport options box; "Device Bridge (optional — for live device checks)" h3 renamed to "Device Bridge (for live device checks)". FILES: UserGuide.html)
  Previous: 2026-05-13 (docs: UserGuide remove pip install pygnmi from Installation Step 2 (no pip deps for ssh/eapi/rest/gnmi in practice); gnmi transport note updated to just "requires pygnmi package". FILES: UserGuide.html)
  Previous: 2026-05-13 (feat: range-aware VRF positional mapping — vrf_ now supports ranges in vlan_ (e.g. vlan_: 10,20-30,40 / vrf_: vrf1,vrf2,vrf3 maps token-by-token: vrf1→VLAN 10, vrf2→VLANs 20-30, vrf3→VLAN 40). _buildVrfMap(vrfVal,vlanVal) builds Map<vlanInt,vrf|null> by pairing VRF tokens to raw VLAN tokens before expansion; _resolveVrfForVlan(vrfMap,vrfList,vlan) replaces _resolveVrfAtIndex at all 3 config-gen call sites (Vx1 SVI, l2-trunk SVI, sub-int). _auditVrfIssues: removed range-ambiguous warn block; count checks now use comma-token count not expanded-VLAN count. test_buildVrfMap added (13 cases); test_auditVrfIssues updated (removed 4 range-warn, added 4 range-OK). FILES: Code.gs, Test-gs.gs, UserGuide.html — per-VLAN VRF audit bullet updated: range expands as one token, example added, stale "expand range first" note removed; UserGuide h1 version: replaced GAS scriptlet <?= APP_VERSION ?> with hardcoded version string — stamp.sh Python block now keeps it in sync on every version bump; fix(bridge-sync): downloadBridgeScript() embedded template was 499 lines behind device_bridge.py — synced now; topoassist-bridge-version.sh hook extended to auto-sync full content on every device_bridge.py edit)
  Previous: 2026-05-13 (docs: UserGuide remove stale §6 section number from Config Center quick-ref link — Config Generation is now §10; dropped number entirely to avoid future drift. FILES: UserGuide.html)
  Previous: 2026-05-13 (ux: UserGuide Esc clears search — keydown handler on ugSearchInput now handles Escape: calls ugClear() + blurs input. FILES: UserGuide.html)
  Previous: 2026-05-13 (fix: UserGuide.html missing <meta charset="UTF-8"> — em dashes and other non-ASCII rendered as Latin-1 garbage (â€" etc.) in browsers that default to Latin-1. FILES: UserGuide.html)
  Previous: 2026-05-12 (docs(userguide): document per-interface idempotency reset commands — table of ta-clean-et/po/vl expansions (default/no sub-commands injected first inside each interface block) added to Config Push Layer 1 section. FILES: UserGuide.html)
  Previous: 2026-05-12 (feat: ta-clean-* config viewer expansion — markers replaced with paste-ready EOS commands in display; _expandTaCleanDisplay() JS mirrors Python _expand_ta_cleanup(); _TA_CLEAN_MAP const mirrors TA_ALIAS_EXPANSIONS; _annotateTaClean() removed; cfg-ta-clean CSS removed; no ipv6 address added to ta-clean-et and ta-clean-po in both device_bridge.py and embedded template. FILES: device_bridge.py, Sidebar-js.html, Sidebar-css.html, tests/test_device_bridge.py)
  Previous: 2026-05-12 (fix: _renderLldpResults sub-section labels + order aligned with audit modal — LLDP group first (New LLDP neighbor / LLDP Mismatch / INT Up - no LLDP neighbor), then Int State group (L1 errors (CRC/FCS) / No link (notconnect) / Err-Dis (errdisabled) / Disabled (admin shut)). Previously: New LLDP link / L1 Quality / Interface errdisabled / Interface down / Interface shut / No LLDP neighbour (up). FILES: Sidebar-js.html)
  Previous: 2026-05-12 (feat: annotate ta-clean-* idempotency markers in config viewer — _annotateTaClean() replaces raw ta-clean-et/po/vl lines with human-readable "! [TopoAssist] IFACE: clear stale X before push" in _renderWithLineNums and highlightEosConfig; cfg-ta-clean CSS class (dim italic) applied in both configBody and cfgViewBody. FILES: Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-05-11 (feat: bridgeAuditFlagOrphan — new dedicated audit flag for LLDP-discovered neighbors not in topology (default ON). Decoupled from bridgeAuditFlagMismatch. Changes: (1) var bridgeAuditFlagOrphan declared; (2) added to _ALL_AUDIT_FLAGS; (3) toggleAuditCheck re-renders on orphan toggle; (4) runValidation orphan block: auditCheckHadIssue.bridgeAuditFlagOrphan count + gated by bridgeAuditFlagOrphan (was bridgeAuditFlagMismatch); message "New LLDP neighbor" (was "Extra cable"); (5) _logBridgeIssuesToStatusLog same gate+message fix; (6) Sidebar.html audit modal: orphan checkbox added first in Bridge-LLDP; bridgeAuditFlagMissing label renamed "INT Up - no LLDP neighbor". FILES: Sidebar-js.html, Sidebar.html)
  Previous: 2026-05-11 (fix: canvas background click now deselects disconnected-port selection — added _selectedPortId tracker; _deselectAll guard + selectDevice + selectLine all clear it. FILES: Sidebar-js.html)
  Previous: 2026-05-11 (fix: _populateBridgePortSubTypes peer-disabled classification — when local port is notconnect and peer interface linkStatus === 'disabled', classify as 'shut' not 'down'. Adds peerPortName/peerDs/peerIfLs/peerIsDisabled locals to notconnect branch; peerIsDisabled OR peerIsUnavailable OR peerIsNonArista all map to 'shut'. Aligns with CLAUDE.md rule: if either side is disabled, link is shut. FILES: Sidebar-js.html)
  Previous: 2026-05-11 (fix: bridge audit modal + legend alignment — (1) Sidebar.html audit modal: bridgeAuditFlagMissing moved from Bridge-LLDP to Bridge-Int-State as "Up (no LLDP neighbor)" — first row, matching Port-Live legend order Up/No link/Err-Dis/Disabled; Bridge-LLDP now has only Mismatch/Internal VLAN/L1 Errors. (2) _renderBridgeLldpLegend items: errdis entry added between Missing and Down (sub:'errdis', label:'Err-Dis'). (3) _bridgeEffectiveClass: errdis branch added before shut — errdis port with flag OFF → missing-quiet, flag ON → missing (red). FILES: Sidebar.html, Sidebar-js.html)
  Previous: 2026-05-11 (fix: legend L1 item — label "+L1"→"L1 Error"; dot reverted to bare has-l1 (ring only, no base color — the overlay indicator itself); background:none restored on .bridge-port-dot base rule (no fallback). FILES: Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-05-11 (feat: LLDP port dot SSoT — (1) _bridgePortDotClass now appends has-l1 suffix internally (removed from all callers). (2) _bridgePortTitle new helper owns all 4 title strings (replaces inline ternaries at devcard builder and applyLldpIndicators). (3) _linkL1Cls(u,v) new helper for link-level has-l1 suffix. (4) _renderBridgeLldpLegend() renders Port-LLDP legend row via SSoT helpers + MOCK port ID trick; called from initApp(). (5) Sidebar.html lldpLegendRow changed from 6 hardcoded items to empty placeholder. FILES: Sidebar-js.html, Sidebar.html, Sidebar-css.html)
  Previous: 2026-05-10 (feat: bridge errdis + peer-check — (1) bridgePortSubType now has 4 values: 'errdis'|'shut'|'down'|'other'; priority errdis>shut>down>other everywhere. (2) _populateBridgePortSubTypes: errdisabled→'errdis'; notconnect peer-check: if peer device missing/failing in dsData or peer is non-Arista, reclassify 'down'→'shut' (expected no-link, peer disabled). (3) var bridgeAuditFlagErrDis (default OFF) added + registered in _ALL_AUDIT_FLAGS. (4) All consumers updated: runValidation bridge checks, _auditIssues, _logBridgeIssuesToStatusLog, _renderLldpResults (noNbrErrDisR bucket + svgErrDis icon + sub-section), toggleAuditCheck, _bridgePortDotClass/Title, _cableGroupBridgeCategories, _CABLING_BRIDGE_CATS. (5) Sidebar.html audit modal: down/shut rows moved from Bridge-LLDP to new "Bridge — Int State" audit-cat-hdr; errdis row added. (6) Sidebar-css.html: bridge-port-errdis + bridge-row-errdis CSS classes. FILES: Sidebar-js.html, Sidebar.html, Sidebar-css.html, CLAUDE.md)
  Previous: 2026-05-14 (fix: interface idempotency atomicity — cleanup and push must be in one configure session. _push_ta_cleanup() separate cleanup session REMOVED; _expand_ta_cleanup(lines) now inlines cleanup commands as first sub-commands of each interface block (returns flat list, not tuple). _push_config() call site: `cleanup_cmds, lines = _expand_ta_cleanup(lines)` + `_push_ta_cleanup call` → `lines = _expand_ta_cleanup(lines)`. _push_ta_cleanup method deleted from both device_bridge.py and Sidebar-js.html embedded template. _make_push_handler() mock removed. TestExpandTaCleanup tests rewritten: 7 tests now verify inline expansion (cleanup cmds injected with leading space after interface line, before original sub-commands). 133 tests pass. FILES: device_bridge.py, Sidebar-js.html, tests/test_device_bridge.py, CLAUDE.md)
  Previous: 2026-05-14 (fix: bridge-port-dot CSS safety net — added background:#94a3b8 fallback to .bridge-port-dot base rule; color modifiers override it so existing states unchanged; prevents any dot without a color class from being invisible. FILES: Sidebar-css.html)
  Previous: 2026-05-14 (fix: interface idempotency — EOS eAPI does not resolve user-defined aliases so ta-clean-* marker calls inside a configure session fail with 'invalid command'. Replaced alias mechanism: TA_ALIASES_CMDS → TA_ALIAS_EXPANSIONS dict (maps marker name → flat commands); _ensure_ta_aliases() → _push_ta_cleanup(ip, cleanup_cmds) instance method (same SSH/eAPI dispatch as _abort_stale_sessions); _expand_ta_cleanup(lines) new module-level pure function strips ta-clean-* markers from config lines and returns (cleanup_cmds, filtered_lines); _push_config() calls _expand_ta_cleanup first then _push_ta_cleanup before main session. generateGlobalBlock() alias definitions removed (ta-clean-* markers remain in per-interface config as Python-intercepted sentinels). Tests: _ensure_ta_aliases mock → _push_ta_cleanup mock; 7 new TestExpandTaCleanup tests (133 total). FILES: Code.gs, device_bridge.py, Sidebar-js.html, tests/test_device_bridge.py, CLAUDE.md)
  Previous: 2026-05-14 (fix: +L1 legend dot now uses bridge-port-match has-l1 (green+ring) via _bridgePortDotClass('match', MOCK) with mock L1 error entry — was bare has-l1 with no base class, rendering as invisible ring only. FILES: Sidebar-js.html)
  Previous: 2026-05-14 (refactor: legend SSoT — Port—LLDP legend row now rendered by _renderBridgeLldpLegend() called from initApp(); hardcoded items removed from Sidebar.html (id="lldpLegendRow" placeholder); uses same _bridgePortDotClass/_bridgePortTitle helpers so class/title changes propagate automatically. FILES: Sidebar.html, Sidebar-js.html)
  Previous: 2026-05-11 (feat: parameterized EOS alias cleanup — ta-clean-et/po/vl aliases with `interface %1` format; pre-committed by _ensure_ta_aliases() before main push. generateGlobalBlock() adds 3 aliases (numbered subcommand format). generateConfig() emits `ta-clean-et Et1/1` at global level before each Et interface block; emits `ta-clean-po Port-Channel${N}` before each PO block. generateAttributesBlock() L2 block simplified: 3 VLAN cleanup lines removed (alias handles them). getIpBlock() takes `_isSvi=false` param; when true skips 6-line inline IP cleanup (SVI loop handles via alias). SVI loop: GW SVIs emit `ta-clean-vl Vlan${v}` before interface, call getIpBlock(...,true). vx1 SVI array: same pattern. device_bridge.py: TA_ALIASES_CMDS constant (multi-line numbered format, interface %1 on every alias), _ensure_ta_aliases() method commits in dedicated session, call in _push_config() when not dry_run. Sidebar-js.html embedded template: all 3 items synced. Tests: _ensure_ta_aliases mocked in _make_push_handler(). CLAUDE.md: interface idempotency section rewritten with correct alias behavior. VERSION 260511.11. FILES: Code.gs, device_bridge.py, Sidebar-js.html, tests/test_device_bridge.py, CLAUDE.md)
  Previous: 2026-05-14 (refactor: complete SSoT for push modal titles — _buildPushTitle(label,deviceName,state) now calls _fmtDev(label||deviceName) internally so all states show [XX] device index consistently; _showPushConfirmModal uses _buildPushTitle for all 3 states (Push/In Sync/Verify) and loading state; _devLabel local var removed; every push modal titleEl assignment now goes through _buildPushTitle (except closePushConfirmModal reset 'Config Push' which has no device context). FILES: Sidebar-js.html)
  Previous: 2026-05-14 (revert: alias approach abandoned — EOS aliases do not resolve in configure session context and do not expand in interface submode without explicit 'interface %1' parameterization. Reverted all alias machinery: generateGlobalBlock() 3 alias lines removed; generateAttributesBlock() L2 block restored with 3 VLAN cleanup lines (default switchport trunk allowed vlan / no switchport trunk native vlan / default switchport access vlan); generateConfig() ET section: ta-clean-et call removed; generateConfig() PO section: ta-clean-po call removed; getIpBlock() LEAF and non-LEAF: ta-clean-vl replaced with 6-command inline block; vx1 SVI array: ta-clean-vl replaced with 6 inline entries; device_bridge.py: TA_ALIASES constant + _ensure_ta_aliases method + pre-commit call removed; Sidebar-js.html embedded template: all 3 alias items removed; tests: _ensure_ta_aliases mock removed from _make_push_handler(). CLAUDE.md updated: interface idempotency section rewritten to document inline approach + note why aliases were rejected. FILES: Code.gs, device_bridge.py, Sidebar-js.html, tests/test_device_bridge.py, CLAUDE.md)
  Previous: 2026-05-14 (feat: device name in all push modal stage titles — _updatePushPreviewError(msg) → _updatePushPreviewError(label, msg); title now "(label ? label+': ' : '')+'Preview Failed'"; all 10 call sites updated to pass label; 7 error titles in commitPushConfig now prefixed with "(label||deviceName)+': '": Push Timed Out, EOS Rejection on Verify, Partial Commit, Verify Failed ×2, Push Failed, Push Error. FILES: Sidebar-js.html)
  Previous: 2026-05-14 (refactor: SSoT for LLDP bridge-port-dot helpers — _bridgePortDotClass(status,portId) now returns full class string including ' has-l1' suffix (callers no longer compute it separately); _bridgePortTitle(status,portId) new helper replaces identical title ternary duplicated in devcard build + applyLldpIndicators(); _linkL1Cls(u,v) new helper replaces duplicate link-level has-l1 computation in _renderLldpResults down/shut rows. FILES: Sidebar-js.html)
  Previous: 2026-05-13 (feat: alias pre-commit in device_bridge.py — EOS aliases defined inside a configure session are NOT resolvable within that same session (only after commit). Fix: TA_ALIASES list constant (3 alias strings) added after CLEANUP_BATCH_SIZE; _ensure_ta_aliases(ip) method commits all 3 aliases in a dedicated topoassist_aliases_<epoch> configure session before _push_config opens the main session — same pre-commit pattern as _abort_stale_sessions + _find_bgp_asn_change. Called as 'if not dry_run: self._ensure_ta_aliases(ip)' in _push_config() after BGP ASN prepend, before session creation. Best-effort: logs on failure, never blocks push. Tests: _ensure_ta_aliases mocked in _make_push_handler() (same pattern as _abort_stale_sessions) so push-logic tests aren't disrupted. Embedded template in downloadBridgeScript() (Sidebar-js.html) fully synced: TA_ALIASES constant, _ensure_ta_aliases method (using -- for em-dash in JS template literal), and the pre-commit call in _push_config. FILES: device_bridge.py, Sidebar-js.html, tests/test_device_bridge.py)
  Previous: 2026-05-13 (feat: interface idempotency aliases — 3 EOS aliases defined in generateGlobalBlock() (000_BASE): ta-clean-et (ET physical: clears VLAN trunk/access/native + channel-group), ta-clean-po (PO: clears VLAN trunk/access/native), ta-clean-vl (GW SVI: clears all ip/ipv6 address variants). ta-clean-et emitted in generateConfig() ET section before if(hasPo); ta-clean-po emitted before generateAttributesBlock(d) in PO section; ta-clean-vl replaces the previous 6-command inline block in getIpBlock() LEAF branch, non-LEAF branch, and vx1 SVI array. The 3 VLAN cleanup lines removed from generateAttributesBlock() L2 block — now handled by ta-clean-et/ta-clean-po. CLAUDE.md updated (GW SVI idempotency section replaced with Interface idempotency aliases). FILES: Code.gs, CLAUDE.md)
  Previous: 2026-05-12 (cleanup: removed dead btnCancelPushHdr refs from Sidebar-js.html — element never existed in HTML; added two-choice confirmation dialog pattern to CLAUDE.md Rule 21. FILES: Sidebar-js.html, CLAUDE.md)
  Previous: 2026-05-11 (refactor: true SSoT for split-btn labels — _buildSplitLabel(sheetId,dev,port,state,ageOrTime) is the pure label-string builder for all 3 states; _setSplitBtnLabel delegates format to it; refreshConfigAges uses _buildSplitLabel directly (no DOM side-effects). _buildSplitBase removed. FILES: Sidebar-js.html)
  Previous: 2026-05-11 (fix: clear _commitInFlight + delete _pendingPush.sessionName on Phase 2 success — × during Phase 3 no longer shows "commit in progress" dialog or fires spurious abort. FILES: Sidebar-js.html)
  Previous: 2026-05-10 (fix: push button label — after Phase-1 diff resolves button now shows "Pending…" instead of stale "Previewing…"; restores to origText on commit/cancel/timeout. FILES: Sidebar-js.html)
  Previous: 2026-05-10 (fix: push button stuck "Previewing…" when cancel clicked during Phase-1 fetch — root cause: _pendingPush not set until after fetch resolves, so cancelPushConfig() saw null and closed the modal without restoring button; in-flight return path also returned without restoring. Fix: _previewingPush={btnId,origText} set before fetch in pushDeviceConfig + _pushConfigText; cancelPushConfig uses it when _pendingPush===null; all existing early-return paths in both functions clear _previewingPush when restoring button. FILES: Sidebar-js.html)
  Previous: 2026-05-10 (cleanup: remove dead _showOrphanConsentInModal + _detectOrphansAndConsent + _emptyOrphans — replaced by Phase 2 orphan flow. FILES: Sidebar-js.html)
  Previous: 2026-05-10 (feat: orphan cleanup moved to Phase 2 — single-device push now detects orphans in Phase 1 (returns orphans_pending) and cleans them in Phase 2 finalize after user clicks Commit; blue pending banner shows count before commit, green cleaned banner shows count after; pushAllConfigs bulk push unaffected. FILES: device_bridge.py, Sidebar-js.html)
  Previous: 2026-05-10 (docs: UserGuide full structural audit — Config Center Deploy nav label (was "Push"), Push All section updated to "Deploy" nav item; Holistic-View Floating Controls table now includes Group List Toggle button. FILES: UserGuide.html)
  Previous: 2026-05-10 (rename: 9 Config Center element IDs shed stale generateAll*/genAll* prefix from old two-column layout: generateAllLeft→genResultsBody, generateAllRight→pushResultsBody, genAllPushWarn→pushWarnBanner, btnRunGenerateAll→btnGenerateConfigs, btnGenAllCleanup→btnCleanupTargets, genallDeviceFilter→cfgCenterDevFilter, genAllDeviceList→cfgCenterDevList, genAllSelCount→cfgCenterSelCount, genAllRoleFilters→cfgCenterRoleFilters. FILES: Sidebar.html, Sidebar-js.html)
  Previous: 2026-05-10 (rename: cfgModalPushInterface→_pushCurrentDeviceInterface, cfgModalPushAll→_pushCurrentDeviceAll, _setCfgModalPushBtns→_setPushBtnVisibility — names were scoped to cfgModal but functions are called from both configModal and Config Center View panel. FILES: Sidebar.html, Sidebar-js.html)
  Previous: 2026-05-09 (rename: JS _orphansToCmdLines → _orphansToCmds to match Python _orphans_to_cmds naming; added // DUPLICATED sync comments to JS function and both Python copies (device_bridge.py + embedded template). FILES: Sidebar-js.html, device_bridge.py)
  Previous: 2026-05-10 (refactor: pushAllConfigs _allArista now uses _getTopologyAristaIds() — third inline duplicate eliminated; all three flows (Cleanup UI, single-device push, bulk push) now share one SSOT for the topology Arista device list. FILES: Sidebar-js.html)
  Previous: 2026-05-10 (refactor: rename configPushModal → pushConfirmModal for consistency with child element IDs (pushConfirmBody, pushConfirmTitle, btnPushConfirmClose); update CLAUDE.md bypass-open rule to require _autoMinimizeOpenModal() in addition to modal-minimized strip. FILES: Sidebar.html, Sidebar-js.html, Sidebar-css.html, CLAUDE.md)
  Previous: 2026-05-10 (cleanup: remove dead-code fallback from allDeviceNames in _doRunCleanup — runCleanup() guards on currentData.nodes so fallback was unreachable. FILES: Sidebar-js.html)
  Previous: 2026-05-10 (fix: configModal not closing — two bugs: (1) orphan consent countdown timer used wrong ID 'pushConfirmModal' (should be 'configPushModal') so minimize-pause never worked; (2) _showPushConfirmModal() now calls _autoMinimizeOpenModal() first so configModal is minimized before configPushModal appears — single Esc closes configPushModal correctly. FILES: Sidebar-js.html)
  Previous: 2026-05-10 (fix: Cleanup UI allDeviceNames used selected-only devices — BGP orphan check falsely flagged valid neighbors pointing to unselected-but-present topology devices; now uses full allArista set from currentData.nodes. FILES: Sidebar-js.html)
  Previous: 2026-05-09 (fix: consent modal "Clean up & Push" fetch was sending flat {ip,config} body — server reads ipMap only → empty results dict → data.ok undefined → "Cleanup failed". Fixed to send {ipMap:{[ip]:{ip,config}},push_timeout} matching _doCleanDevItems pattern; result read as results[ip]; auth_expired handled. FILES: Sidebar-js.html)
  Previous: 2026-05-09 (refactor: push consent cleanup now JS-driven via /pushconfig (same path as Cleanup UI) — _showOrphanConsentInModal(orphans, detectionErrors, ip) runs fetch on "Clean up & Push" click; shows spinner/retry-on-error; _detectOrphansAndConsent returns _emptyOrphans() on proceed so Phase 1 skips _batch_orphan_cmds; _batch_orphan_cmds remains for bulk pushAllConfigs. FILES: Sidebar-js.html)
  Previous: 2026-05-09 (refactor: consent modal and Cleanup UI now share _orphansToCmdLines(orphans) — converts orphans dict to _buildCleanupConfig items format; cmds[] removed from /pushconfig/detect_orphans response; both views driven by same client-side command-gen path. FILES: device_bridge.py, Sidebar-js.html)
  Previous: 2026-05-09 (Cleanup UI: live EOS command preview below the button bar — updates on every checkbox change via updateReconSelCount(); shows _buildCleanupConfig(selected) grouped by device in a dark code block; hidden when nothing selected. FILES: Sidebar-js.html)
  Previous: 2026-05-09 (orphan consent modal: show EOS commands that will execute (range-collapsed, e.g. "no interface Vlan1-3999") in dark code block below the count table; /pushconfig/detect_orphans now includes cmds[] in orphans_pending; table reverted to count-only 2 columns. FILES: device_bridge.py, Sidebar-js.html)
  Previous: 2026-05-09 (fix: orphan consent name preview was showing [object Object] — each category uses the correct field key: interfaces→x.name, vlans→x.vid, bgp→x.neighbor, vrfs→x.name, ospf→x.iface. FILES: Sidebar-js.html)
  Previous: 2026-05-09 (fix: ta_total and matched now included in orphans_pending response from /pushconfig/detect_orphans so the push-phase log shows correct ta_total instead of 0 when using precomputed orphan data. FILES: device_bridge.py, Sidebar-js.html)
  Previous: 2026-05-09 (orphan consent table now shows first 5 names + "… and N more" per section (was count-only); 3-column table: label | count | names preview. FILES: Sidebar-js.html)
  Previous: 2026-05-09 (fix: orphan consent 300s auto-cancel countdown now pauses while pushConfirmModal is minimized — setInterval skips decrement when modal has modal-minimized class. FILES: Sidebar-js.html)
  Previous: 2026-05-09 (fix: _detect_orphans parallel workers all now have VERBOSE logging — _w_bgp, _w_vrf, _w_ospf each log before command, after success (with counts: neighbor lines / vrf instances / no passive-interface lines), and on failure. Previously only _w_vlan had logging; silent failures in the other 3 workers made detection errors invisible in the bridge terminal. Same pattern applied to embedded template. FILES: device_bridge.py, Sidebar-js.html)
  Previous: 2026-05-09 (orphan consent UX + cleanup timeout: section-by-section breakdown in _showOrphanConsentInModal instead of comma list; 300s auto-cancel countdown with color shift at 60s/30s; _emptyOrphans() sentinel so no-orphan detection skips Phase 1 re-detection thread; _bridgeCleanupTimeout global read from /health cleanup_timeout at both health-check sites; Cleanup UI (runCleanup) now passes push_timeout:_bridgeCleanupTimeout so range-collapsed cmds get CLEANUP_PUSH_TIMEOUT headroom; /pushconfig handler reads push_timeout from body and forwards to _push_config; -c CLI flag for CLEANUP_PUSH_TIMEOUT with help text + startup banner; all changes mirrored in embedded template. FILES: device_bridge.py, Sidebar-js.html)
  Previous: 2026-05-09 (fix: revert Option A orphan consent (deferred cleanup) — orphan cleanup must run before Phase 1 staging or EOS rejects pushes when new GW addresses (super-wide /14) overlap existing orphan SVI IPs. New Option B flow: new /pushconfig/detect_orphans endpoint detects orphans without pushing; JS calls it before Phase 1 and shows blocking "Orphan Cleanup Required" consent modal (_showOrphanConsentModal → Promise<bool>); user clicks "Clean up & Push" or Cancel; cleanup runs in Phase 1 as before (batch sessions before main config); orphans_cleaned banner shown in push confirm modal after Phase 1. /pushconfig/finalize reverted to simple commit/abort with no orphan logic. Port-filter (single-interface) pushes skip orphan consent. FILES: device_bridge.py, Sidebar-js.html)
  Previous: 2026-05-09 (fix: modal ghosting — 3 fixes: (1) dock chip onclick now calls setActiveModalChip(modal.id) after restore so _activeModalChipId is correct (prevents guard-timeout ghost); (2) openGenerateAllModal early-return (minimized→restore) now calls _autoMinimizeOpenModal()+setActiveModalChip so open modal gets minimized and chip is tracked; (3) openHelpModal uses display:'flex' not 'block'. FILES: Sidebar-js.html)
  Previous: 2026-05-09 (fix: remove redundant IXIA per-tier caution lines from super-wide caution — swIxia4/swIxia6 removed; generic IXIA note now under GW IPv4/IPv6 headers covers all tiers. FILES: Sidebar-js.html)
  Previous: 2026-05-09 (fix: pre-push log "(commit)" label wrong for open-session push — added _log_action = "dry-run"|"open"|"commit" separate from final_cmd so log correctly shows "(open)" for single-device open-session push; day-one bug. FILES: device_bridge.py, Sidebar-js.html)
  Previous: 2026-05-09 (feat: IXIA reserved range always visible in GW example
  Previous: 2026-05-09 (fix: pushAllConfigs no status bar message after completion — added setStatus() at end of pushAllConfigs() after Promise.allSettled(); status bar now shows "Push complete — N/N successful", "Push partial — N/N successful", or "Push failed — 0/N successful" after every bulk push; previously only the Generate panel's inline result rows updated (user may have scrolled away during long push, seeing no notification). FILES: Sidebar-js.html)
  Previous: 2026-05-09 (fix: spurious [orphan-detect] detection failed log on batch push — orphan result block gated on `all_ifaces and not dry_run`; recursive batch calls (all_ifaces=False) no longer log false detection failure since _orphan_result[0] is intentionally None. Applied to device_bridge.py + embedded template. FILES: device_bridge.py, Sidebar-js.html)
  Previous: 2026-05-09 (fix: IP config example section now copyable — removed user-select:none from .auto-fixed-label; added user-select:text to .auto-fixed-formula, .auto-fixed-eg, .ip-ex-key, .ip-ex-val. FILES: Sidebar-css.html)
  Previous: 2026-05-09 (fix: orphan cleanup always in separate session + pre-push log — (1) Removed orphan-prepend path: orphan cmds are ALWAYS pushed in their own configure session(s) before the main config, regardless of command count. Range-collapsed cmds (e.g. 4 cmds → 7998 SVI/VLAN deletions) would block the main session beyond PUSH_TIMEOUT when prepended; now each batch has its own 300s window. (2) Added [push] ip: sending N lines to session X (commit/abort) log before every configure session so push start is visible. Applied to device_bridge.py + embedded template. FILES: device_bridge.py, Sidebar-js.html)
  Previous: 2026-05-09 (fix: device_bridge timestamps + eAPI push timeout — (1) _vlog(msg) helper added: prefixes every log line with HH:MM:SS timestamp; all 37 diagnostic print(f"  [") calls replaced with _vlog(f"[") in both device_bridge.py and embedded template. (2) _eapi_push now uses PUSH_TIMEOUT (default 300s) for configure sessions >5 commands instead of hardcoded TIMEOUT (15s) — mirrors the existing SSH-path fix; fixes pushes timing out at 15s on eAPI-connected devices with large configs. (3) topoassist-bridge-version.sh hook regex fixed — PCRE variable-length lookbehind unsupported on this system; replaced with sed-based VERSION extraction. VERSION bumped to 260509.2. FILES: device_bridge.py, Sidebar-js.html, hooks/topoassist-bridge-version.sh)
  Previous: 2026-05-09 (feat: consistent preferred-VLAN notes for all 3 GW tiers — dedicated gw-ex{4|6}-{std|wide|sw}-pref divs (same dim-italic style) added to Sidebar.html and populated by JS in _updateGwExample(); preferred text removed from calc labels and super-wide caution; formula key info-box shows single-line preferred summary + fixed IXIA note (IPv4-only 10.0.x.x/16); IXIA caution always visible for super-wide regardless of demo VLAN oct3. FILES: Sidebar.html, Sidebar-js.html)
  Previous: 2026-05-09 (feat: IXIA reserved range caveat added to all 3 GW tiers — static red italic note "⚠ 10.0.x.x/16 and 10:0::/16 reserved for IXIA — verify no GW address falls in this range across all tiers" added at bottom of the "Configurable — VLAN key" formula key info-box (always visible when Example section is expanded, independent of which tier is active). FILES: Sidebar.html)
  Previous: 2026-05-09 (test: 34 new unit tests — TestContiguousRanges, TestOrphansToCmds, TestBatchOrphanCmds; covers SVI range collapse (7998→1 cmd), VLAN range, sub-int/PO/physical routing, BGP/VRF/OSPF blocks, block-split safety, empty inputs, mixed categories; 126 total. FILES: tests/test_device_bridge.py)
  Previous: 2026-05-09 (fix: orphan cleanup diagnostics + [orphan-detect] counts + [orphan-prepend] log. FILES: device_bridge.py, Sidebar-js.html)
  Previous: 2026-05-09 (fix: orphan cleanup diagnostics — [orphan-detect] always printed after detection with per-category counts (iface/bgp/vlan/vrf/ospf) or failure reason; [orphan-detect] when 0 cmds generated; [orphan-prepend] when range cmds prepended silently to main push (was completely invisible). Applied to device_bridge.py + embedded template. FILES: device_bridge.py, Sidebar-js.html)
  Previous: 2026-05-09 (fix: VERSION bump missed prior commit — device_bridge.py and embedded template now correctly read 260509.2. FILES: device_bridge.py, Sidebar-js.html)
  Previous: 2026-05-09 (fix: Port-Channel orphan cleanup used wrong EOS command — 'default interface PoN' only resets config, leaving the PO interface; 'no interface PoN' actually deletes it. _orphans_to_cmds() now checks Port-Channel/Po name pattern before the physical else-branch and emits 'no interface' instead of 'default interface'. Same fix applied to JS _buildCleanupConfig(). VERSION bumped 260509.1→260509.2 in device_bridge.py and embedded template. FILES: device_bridge.py, Sidebar-js.html)
  Previous: 2026-05-09 (fix: super-wide caution always shows all 3 notes — (1) Collision note now dynamic using example VLAN (1520 & 20 → both oct3=20). (2) IXIA reserved range warning (10.0.x.x/16) always visible — was conditional on oct3=0 so never showed with VLAN 1520. (3) Preferred note always appended. No branch on oct3 anymore. FILES: Sidebar-js.html)
  Previous: 2026-05-09 (feat: EOS range syntax for orphan cleanup — (1) _contiguous_ranges(nums) helper added (pure fn, converts sorted int list to [(start,end),...] tuples); (2) _orphans_to_cmds() rewrote SVI section: collects VlanN VIDs, sorts, emits "no interface VlanX-Y" ranges instead of N individual commands — 7998 SVIs → ~10-20 range cmds; VLAN table section similarly emits "no vlan X-Y"; sub-int/physical unchanged (individual). (3) orphans_cleaned false-success bug fixed: _asn_extra["orphans_cleaned"] now set AFTER batch loop and only when _orphan_committed=True — failed batch no longer reports all orphans as cleaned. (4) _buildCleanupConfig() (Cleanup UI JS) updated with _jsContiguousRanges() helper + same SVI/VLAN range logic. All applied to device_bridge.py + embedded template + Sidebar-js.html. FILES: device_bridge.py, Sidebar-js.html)
  Previous: 2026-05-09 (fix: CLEANUP_BATCH_SIZE reverted to 300 everywhere — 1000 timed out at 300s on GW-SVI-heavy device (each no interface VlanN ~0.3s due to route-table cleanup); 300 cmds ≈ 90s, 3× safety margin. JS _CLEANUP_BATCH_SIZE also 300. FILES: device_bridge.py, Sidebar-js.html)
  Previous: 2026-05-09 (fix: CLEANUP_BATCH_SIZE unified to 1000 everywhere — Python push-path and embedded template both bumped 300→1000; _batch_orphan_cmds is block-aware so no mid-block risk at 1000. FILES: device_bridge.py, Sidebar-js.html)
  Previous: 2026-05-09 (fix: bridge push logging — (1) Orphan batch loop: print planned count + per-batch start/done/fail unconditionally (was if VERBOSE or silent). (2) eAPI and SSH commit/abort/pending prints now unconditional (removed if VERBOSE guard). Both changes applied to device_bridge.py and embedded template. FILES: device_bridge.py, Sidebar-js.html)
  Previous: 2026-05-09 (fix: cleanup UX — (1) [detect-orphans/eapi] log line added before show interfaces description in _detect_orphans (was silent because _run_cmds uses JSON eAPI, not _text_cmd); applied to device_bridge.py + embedded template. (2) _CLEANUP_BATCH_SIZE bumped 300→1000 (item-level JS split, pure no-interface-VlanN commands); Python CLEANUP_BATCH_SIZE stays 300 (push-path, mixed block types). (3) Per-batch elapsed-seconds ticker in _doCleanDevItems — setInterval updates status bar every 1s during fetch, cleared on response or error. FILES: device_bridge.py, Sidebar-js.html)
  Previous: 2026-05-09 (feat: GW example preferred-VLAN notes + super-wide formula corrected to oct3 — (1) Standard ≥/24 and ≥/64 labels now say "— any VLAN". (2) Wide /16–/23 and /48–/63 calc labels append "— VLANs ≥100 preferred". (3) Super-wide /8–/15 and /32–/47 calc labels append "— VLANs 1–99 preferred". (4) Super-wide caution block shows "💡 VLANs 1–99 preferred — oct3 = VLAN itself" on every VLAN (below IXIA warning when oct3=0, below collision note otherwise). (5) CLAUDE.md GW assembly rule updated: super-wide formula corrected to {first}{oct3}.0.0.last (was oct2); preferred-VLAN guidance and oct3=0 IXIA conflict added. FILES: Sidebar.html, Sidebar-js.html, CLAUDE.md)
  Previous: 2026-05-09 (feat: EVPN per-vlan disjoint VTEP VLAN audit check — new auditFlagEvpnVlanMismatch flag (default ON). runValidation() checks all full-device VTEP pairs for VLAN set intersection when EVPN IPv4/IPv6 is on and evpnService=per-vlan; MLAG pairs skipped. Fires [Audit - Design] error naming both devices and their vtepVlans. Checkbox added to Audit modal IP/Gateway section. UserGuide audit table row added (rowspan bumped 6→7). FILES: Sidebar-js.html, Sidebar.html, UserGuide.html)
  Previous: 2026-05-09 (fix: _asn_extra UnboundLocalError in push path — initialize _asn_extra={} alongside other state vars; replace reassignment with conditional update so orphans_cleaned key is not wiped. Applied to device_bridge.py and embedded template. FILES: device_bridge.py, Sidebar-js.html)
  Previous: 2026-05-09 (fix: spurious dirty-close warnings on networkStackModal and autoConfigModal — same race condition as Config Center. openTechModal() now resets initialTechState='' before the async GAS load; closeTechModal() resets it after confirm passes. openIpConfigModal() now resets initialIpState='' before the async GAS load; closeIpModal() resets it after confirm passes. Full audit: initialDeviceManagerState and initialEditModalState set synchronously (safe); initialGroupRectState set synchronously (safe); SheetColumnManager uses isSheetColumnManagerDirty boolean reset synchronously (safe). FILES: Sidebar-js.html)
  Previous: 2026-05-09 (fix: concurrent _detect_orphans + _find_bgp_asn_change in push phase — both now run in parallel threads (TIMEOUT*3 join) eliminating the extra sequential 'include router bgp' round-trip; embedded template in downloadBridgeScript() synced with identical pattern. FILES: Sidebar-js.html)
  Previous: 2026-05-09 (feat: Auto Config modal Example section UX — (1) Header simplified to just "Example" (removed long VLAN/oct2/oct3 subtitle). (2) Color legend moved inside #ipExampleGrid as first element (collapses with the section). (3) "Auto-derived — not configurable" (MLAG Ctrl/L3) and "Configurable — VLAN key" info-boxes moved inside #ipExampleGrid (now collapse/expand with toggle). (4) All user-select:none removed from example grid inline styles and .ip-ex-key CSS — all example IPs and formulas are now copyable. FILES: Sidebar.html, Sidebar-css.html)
  Previous: 2026-05-09 (fix: Config Center spurious "changes lost" dirty warning — _loadGlobalCfgTemplate() now resets initialGlobalCfgState='' before the async GAS load; closeGenerateAllModal() also resets it after close. Race condition: openGenerateAllModal always called _loadGlobalCfgTemplate on open (sets textarea to 'Loading...'); if closed before GAS returned, stale initialGlobalCfgState from prior session caused 'Loading...' !== old_config → spurious warning. FILES: Sidebar-js.html)
  Previous: 2026-05-09 (fix: GW example section UX — (1) Example grid collapsed by default: #ipExampleGrid starts display:none, toggle button starts at rotate(-90deg). (2) Removed secOp() opacity dimming from _updateGwExample() — all three tiers (standard/wide/super-wide) now always show at full opacity; active tier is evident from the configured mask value. (3) Range/tier reference table moved into the existing info-box alongside "Override GW octets per device" text (table inside the same div, margin-top:6px), instead of a separate second info-box after #gwDeviceOverrideTable. FILES: Sidebar.html, Sidebar-js.html)
  Previous: 2026-05-09 (fix: orphan cleanup batching — prevents push timeout when large orphan sets (e.g. 7998 stale SVIs) exceed CLEANUP_BATCH_SIZE=300. Python: CLEANUP_BATCH_SIZE constant added; _push_config now pushes orphan_cmds in sequential batched sessions (recursive _push_config calls with all_ifaces=False) when len(orphan_cmds)>CLEANUP_BATCH_SIZE, instead of prepending all commands into one session. Embedded template synced identically. JS: _doCleanDevItems rewritten as async, splits devItems into _CLEANUP_BATCH_SIZE=300 chunks per device, sends sequential fetch /pushconfig calls with batch N/M progress in status bar, aggregates per-device ok/error. FILES: device_bridge.py, Sidebar-js.html)
  Previous: 2026-05-09 (feat: GW example grid — 3-tier layout (standard/wide/super-wide all visible simultaneously, inactive tiers dimmed at 0.35 opacity). Section wrappers gw-ex4-std-sec / gw-ex4-wide-sec / gw-ex4-sw-sec (and IPv6 equivalents) use grid-column:1/4 + inner grid, opacity toggled by _updateGwExample(). New super-wide IDs: gw-ex4-sw-{mphy1,mphy2,solo,anycast,vip,calc,caution}. Mask variables split: m4s/m4w/m4sw and m6s/m6w/m6sw each tracking their tier range. Calc labels now always show tier-specific formula (no toggle). Caution updated to VLAN 1215+1250 collision example (both oct2=12 → 112.0.0.1). Range/tier reference table added inside #gwAdvancedSection below #gwDeviceOverrideTable. FILES: Sidebar.html, Sidebar-js.html)
  Previous: 2026-05-09 (feat: SheetAssistPanel EOS internal VLANs + chip legend redesign. (1) Code.gs getSheetVlanSummary() now reads sp_mode_ columns and classifies VLANs as hasL2/hasL3 instead of trunkCount/nativeOnly — chip priority: vtep>svi>l2>l3. (2) Code.gs: saveInternalVlans(vlansMap) merges all device internal VLAN arrays to sorted unique set, saves {vlans,updatedAt} to DocumentProperties INTERNAL_VLANS; getInternalVlans() reads it back. (3) Sidebar-js.html: after deviceInternalVlans population at both auto-bridge and manual bridge call sites, fires google.script.run.saveInternalVlans(deviceInternalVlans) (fire-and-forget). (4) SheetAssistPanel.html: chip-trunk→chip-l2 (blue), chip-native removed, chip-l3 added (orange #fff7ed/#c2410c), chip-internal added (purple #faf5ff/#7c3aed, non-clickable); legend row updated vtep/svi/l2/l3/internal with #internalVlanTs timestamp span; buildSapVlanStrip() merges sheet VLANs + internal-only VLANs sorted numerically; _updateInternalVlanTimestamp() shows "EOS internal · Xm ago"; _pollInternalVlans() polls every 30s. FILES: Code.gs, Sidebar-js.html, SheetAssistPanel.html)
  Previous: 2026-05-09 (feat: Cleanup panel — collapsed-by-default device groups with lazy DOM rendering. Device headers always shown; item rows rendered only on first expand (fixes browser hang on large topologies). Expand All / Collapse All buttons added. Select All auto-expands all before checking. Per-device checkbox expands+checks in one click. _renderCleanupResults now emits header-only HTML; _cleanupRenderDevBody does lazy item render; _toggleCleanupDev / _cleanupDevCheck / _cleanupExpandAll / _cleanupCollapseAll added. CSS: .cleanup-hdr-clickable, .cleanup-chevron (90deg on .cleanup-open), .bridge-recon-dev-body. FILES: Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-05-09 (_detect_orphans: BGP/VLAN/VRF/OSPF/OSPF3 queries now run in parallel via threading.Thread after the serial interface query; reduces pre-push check time from ~5-10s sequential to ~1-2s. Same change applied to embedded template in downloadBridgeScript(). FILES: device_bridge.py, Sidebar-js.html)
  Previous: 2026-05-09 (fix: topology deselect and audit message persistence — (A) added _deselectAll() function (extracts inline deselect from viewport click handler); card body onclick now calls _deselectAll() so clicking empty space in a device card clears selection; (B) selectDevice() now clears #selectionStatus at the start and removes the self-defeating statusLog "△" overwrite (lines that immediately cleared audit messages after showing them); (C) selectLine() no-error case now clears statusEl.innerHTML (was only clearing color/title, leaving prior error text visible). FILES: Sidebar-js.html)
  Previous: 2026-05-09 (device_bridge: EAPI _push_config timeout RuntimeError messages now include len(lines) and PUSH_TIMEOUT; earlier change added same to SSH subprocess.TimeoutExpired path in run_push. FILES: device_bridge.py, Sidebar-js.html)
  Previous: 2026-05-08 (ui: infoVgw popup — removed Virtual Router MAC entry (redundant with inline field; was inaccurate "required for both modes"); VARP line now notes MAC is configured below. FILES: Sidebar.html)
  Previous: 2026-05-08 (ui: infoEvpnService popup — Bundle description updated to cover both default (single EVPN_VLAN_AWARE_BUNDLE, RT ASN_BASE:1) and multi-bundle groups (EVPN_BUNDLE_N, RT ASN_BASE:N). FILES: Sidebar.html)
  Previous: 2026-05-08 (ui: EVPN BUNDLES hint text — updated to clarify default (empty = all Vx1 VLANs → single bundle RT asnBase:1) + per-range behavior. FILES: Sidebar.html)
  Previous: 2026-05-08 (ui: TOPO EDIT — [New Group]+[Toggle] grouped together in segmented-group with margin-left:auto, flush right; [Size-][+] left-aligned. FILES: Sidebar.html)
  Previous: 2026-05-08 (ui: TOPO EDIT — toggle/expand button moved to extreme right via margin-left:auto; [Size-][+] and [New Group] left-aligned; hc-btns justify-content:flex-start. FILES: Sidebar.html, Sidebar-css.html)
  Previous: 2026-05-08 (ui: EVPN BUNDLES info popup — added i btn-info-popup to BUNDLES label in evpnBundleGroupsSection; popup id=infoBundleGroups shows 3-device worked example (DevA/B/C, 2 bundle ranges, BGP stanzas per device, RT sharing explanation). FILES: Sidebar.html)
  Previous: 2026-05-08 (feat: EVPN multi-bundle groups — Tech Stack modal BUNDLES panel (shown when vlan-aware-bundle selected) lets user define N VLAN ranges; each becomes EVPN_BUNDLE_N with RT asnBase:N; device VLANs intersected per bundle so only relevant bundles are emitted. Stored as EVPN_BUNDLE_GROUPS JSON in DocumentProperties. Helpers: _bundleVlanIntersection() in Code.gs. Falls back to single EVPN_VLAN_AWARE_BUNDLE when no groups defined. FILES: Code.gs, Sidebar.html, Sidebar-js.html, test-js.js)
  Previous: 2026-05-08 (fix: vlan-aware-bundle VLAN list now range-compressed — added _compressVlanList() helper near _compressVniLines(); both generateBGP() and generateBGPEvpnOverlay() bundle branches use it instead of .join(","). Prevents 12k-char CLI lines for large VLAN sets. FILES: Code.gs)
  Previous: 2026-05-08 (fix: TOPO EDIT group list defaults to collapsed — _groupListCollapsed init changed from === '1' to !== '0'; collapses on first load unless user explicitly expanded it. FILES: Sidebar-js.html)
  Previous: 2026-05-08 (ui: TOPO EDIT button order — size [-][+] left, [New Group][Toggle List] right. FILES: Sidebar.html)
  Previous: 2026-05-08 (fix: TOPO EDIT group-list expand button stays fixed — #holistic-controls flex-direction:column-reverse pins label+buttons at bottom; group list grows upward above them; #groupListPanel border-top→border-bottom for correct separator placement. FILES: Sidebar-css.html)
  Previous: 2026-05-08 (docs: UserGuide §9 Per-VLAN VRF — added 'default' placeholder section with example showing mixed default/named VRF list; note that 'default' counts toward positional count. FILES: UserGuide.html)
  Previous: 2026-05-08 (fix: floatingLegend expand button stays fixed — flex-direction:column-reverse on .topo-legend pins the header/toggle at bottom; content grows upward; max-height:calc(100vh-60px)+overflow-y:auto on .legend-content caps height; chevron now rotate(180deg) expanded / rotate(0deg) collapsed. FILES: Sidebar-css.html)
  Previous: 2026-05-08 (fix: VRF 'default' handling — 'default' (any case) now treated as no-VRF at all three output points: (1) _resolveVrfAtIndex() returns null when resolved value is 'default'; (2) getIpBlock() effectiveVrf guard skips 'default' on legacy d.vrf_ fallback path; (3) analyzeRow vrfs.add() now parses comma-list individually and skips 'default' entries — fixes spurious `vrf default` on interfaces and `vrf instance default` in global config. Test-gs.gs: 6 new _resolveVrfAtIndex cases covering 'default' single/mixed-case/positional. FILES: Code.gs, Test-gs.gs)
  Previous: 2026-05-08 (fix: Auto Config false dirty-state warning — moved _updateGwExample() call to BEFORE initialIpState=getCurrentIpState() in openIpConfigModal success handler; applyHint() modifies per-device override input .value from "" to hint strings, so snapshot must be taken after hints are settled. FILES: Sidebar-js.html)
  Previous: 2026-05-08 (feat: super-wide GW prefix tier /8–/15 (IPv4) and /32–/47 (IPv6) — oct3 dropped; 3-tier formula in getIpBlock() + vx1 SVI block; mask validation min /8; _updateGwExample() super-wide formula, dynamic calc labels, VLAN 40 caution; legend updated; CLAUDE.md GW assembly rule updated. FILES: Code.gs, Sidebar.html, Sidebar-js.html, CLAUDE.md)
  Previous: 2026-05-07 (docs: UserGuide — add missing 🛡️ emoji to Audit Engine h2 header; FILES: UserGuide.html)
  Previous: 2026-05-07 (docs: UserGuide quality fixes — W1: Cabling Export toolbar row now describes 3 tabs (Export/Topology List/Device List); W2: New Project Reset section added under Sheet & Schema (menu path + description); W3: Restore Sheet Checkpoint now has its own menu-path span in Appendix; FILES: UserGuide.html)
  Previous: 2026-05-07 (docs: UserGuide §6 Global Config Template rewritten — Config Center→Template panel access, 000_BASE (auto-generated: hostname+ip routing) vs 000_GTPL (user textarea), Copy Base+Ifs = 000_BASE+000_GTPL+interface blocks, Save & Apply; FILES: UserGuide.html)
  Previous: 2026-05-07 (docs: UserGuide audit section — count "24 checks" → "35 checks"; table now has 35 rows (was 25): added 3 Bridge/LLDP rows (Interface down OFF-default, Interface shut OFF-default, L1 errors ON-default), 3 VLAN rows (Stale VLAN, Stale SVI, Missing VLAN), 1 IP/Gateway row (Stale VRF), 1 Mode/Speed row (Incomplete xcvr speed), new Snake/IXIA category (2 rows); rowspans corrected. FILES: UserGuide.html)
  Previous: 2026-05-07 (feat: add ! --- SECTION --- headers to formatted config output via _cfgSectionLabel(key) helper in both formatConfigText() (client) and formatConfigMap() (server); rename 000_CTPL → 000_GTPL (Global Template, G < L sorts between 000_BASE and 000_LO0); sections labeled: BASELINE, GLOBAL TEMPLATE, LOOPBACKS, SYSTEM, MLAG, SNAKE TESTING, OSPF UNDERLAY, OSPFv3 UNDERLAY, BGP; interface blocks (050_*/055_VXLAN/060_*) unlabeled — interface X is the header; FILES: Code.gs, Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-05-07 (fix: openCablingModal() now calls _cablingShowTab('export') after display:flex — ensures modal always reopens on Export tab regardless of which tab was active when it was last closed. FILES: Sidebar-js.html)
  Previous: 2026-05-07 (docs: UserGuide legend sync — LL/St column bullet points rewritten: LL now shows 6 states (match/mismatch/missing/down/shut/+L1) with correct 6px inline dots; St column documents both phases (design=square, live=circle); Interface Operational State table relabeled Up/No Link/Err-Dis/Disabled with 6px dots + glow; line 833 "Red dots" → "Dashed square dots"; +L1 legend item now has-l1 only (no bridge-port-shut base). FILES: UserGuide.html, Sidebar.html)
  Previous: 2026-05-07 (ui: .dot.connected cabled square — no fill, solid border only (transparent bg, 1.5px solid #64748b, border-radius:2px). Green fill removed to avoid redundancy with op-up live state. FILES: Sidebar-css.html)
  Previous: 2026-05-07 (fix: exportCablingModal width 480px→700px for readable cabling text. FILES: Sidebar-css.html)
  Previous: 2026-05-07 (fix: legend dot size consistency — .l-item .dot + .l-item .bridge-port-dot + .l-item .dot.disconnected all use box-sizing:border-box so borders don't inflate 10px target. .bridge-port-dot devcard size 7px→6px to match .dot. FILES: Sidebar-css.html)
  Previous: 2026-05-07 (fix: exportCablingModal size — 480px wide, 82vh tall (matches Config Center feel); all 3 tabs share same fixed size. FILES: Sidebar-css.html)
  Previous: 2026-05-07 (ui: port dot shape overhaul — design-phase cabled=green filled square (border-radius:2px), uncabled=dashed gray square outline (transparent bg, 1.5px dashed #94a3b8, no ring). op-* live states reset to circle (border-radius:50%;border:none). has-l1 ring changed from box-shadow to outline (2px solid #eab308, outline-offset:1px) so glow from bridge-port-* states shows through. LL legend items now use .bridge-port-dot CSS classes (no inline styles, no .dot border). .l-item .bridge-port-dot sizing rule added (10×10px). Highlight .dot.connected now uses outline instead of box-shadow. UserGuide: uncabled port wording updated to "dashed gray square". FILES: Sidebar-css.html, Sidebar.html, UserGuide.html)
  Previous: 2026-05-07 (fix: bridge-scanning toolbar icon animation — @keyframes bridge-scan now pulses blue (#3b82f6→muted) + deeper opacity (1→0.2) + shorter cycle (0.65s); #btnBridgeToolbar.bridge-scanning adds transition:none to defeat .icon-btn transition:all conflict. FILES: Sidebar-css.html)
  Previous: 2026-05-07 (ui: legend group labels show ST/LL column tag — .col-tag badge added to Port—Design(ST), Port—Live(ST), Port—LLDP(LL) group labels. CSS: .group-label gains display:flex+gap; .col-tag = 9px mono badge. FILES: Sidebar.html, Sidebar-css.html)
  Previous: 2026-05-07 (fix: exportCablingModal fixed size 500px×620px — all 3 tabs share same modal dimensions; removed cabling-view-active class toggle (no longer needed). FILES: Sidebar-css.html, Sidebar-js.html)
  Previous: 2026-05-07 (refactor: remove bridge_mismatch canvas layer — redundant with audit error red-dashed; bucket/colorMap/lineWidth/draw-order all cleaned. FILES: Sidebar-js.html)
  Previous: 2026-05-07 (fix: cabling view panel height — added #exportCablingModal.cabling-view-active{height:min(600px,75vh)} so flex:1 on .cabling-panel-view fills the modal; _cablingShowTab toggles cabling-view-active on the modal when isView. FILES: Sidebar-css.html, Sidebar-js.html)
  Previous: 2026-05-07 (ui: port dot color system overhaul — two-phase dot model: Design phase (.dot.connected=green, .dot.disconnected=gray+red-ring), Live phase (op-up=green, op-down=amber, op-errdisabled=red, op-disabled=gray). Bridge-port-dot full state: _bridgePortDotClass(status,portId) added (no audit flag gating) — match=green, mismatch=amber, missing+other=red, missing+down=amber(#f59e0b aligns with op-down), missing+shut=gray; has-l1 class adds amber ring (box-shadow:0 0 0 2px #eab308) for L1 errors. applyLldpIndicators() + devcard build now use _bridgePortDotClass()+has-l1. rowHtml call sites: down→bridge-port-down, shut→bridge-port-shut, L1 rows get has-l1 + base class reflects down vs mismatch. Legend restructured: Port—Design (Cabled/No Cable), Port—Live (Up/No Link/Err-Dis/Disabled), Port—LLDP (Match/Mismatch/Missing/Down/Shut/+L1). UserGuide: "red dot" → "uncabled port dot". FILES: Sidebar-css.html, Sidebar-js.html, Sidebar.html, UserGuide.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-05-07 (feat: split 000_GLOBAL into 000_BASE + 000_TEMPLATE; refactor generateGlobalBlock() to return only mandatory commands (ip routing, service routing protocols, varp mac); getGlobalConfig() output now stored separately in 000_TEMPLATE (omitted when empty); rename "Copy Interface" → "Copy Base+Ifs" in both config modal and Config Center panel; add _getBaseBlock() helper that returns 000_BASE + 000_TEMPLATE concatenated; copyInterfaceConfig() and _cfgViewCopyInterface() now prepend _getBaseBlock() in full-device view (portFilter=null), exclude "interface defaults" block (now in 000_TEMPLATE) from interface extraction; sort order: 000_BASE < 000_LO0 < 000_LO1 < 000_TEMPLATE < 001_SYSTEM < 050_Et*; FILES: Code.gs, Sidebar.html, Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-05-07 (feat: exportCablingModal tab design — replaced split-pane with 3-tab layout (Export/Topology List/Device List); tab bar (.cabling-tab-bar/.cabling-tab/.cabling-tab-active) sits below modal header; Export tab shows all controls (filter, topology section, device section, AI Validate); Topology List/Device List tabs show find bar + textarea; _cablingShowTab(tab) is the unified switch — controls panel hide/show, tab highlight, view panel show/hide, pill active state; _viewCablingReport calls _cablingShowTab; _closeCablingViewPanel calls _cablingShowTab('export'). CSS: .cabling-panel-controls (overflow-y:auto,padding) + .cabling-panel-view (flex column). Section 26 updated. FILES: Sidebar.html, Sidebar-js.html, Sidebar-css.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-05-07 (fix: exportCablingModal always-visible bug — removed erroneous #exportCablingModal{display:flex} CSS override that defeated display:none initial state; openCablingModal now sets display:'flex' not 'block'. FILES: Sidebar-css.html, Sidebar-js.html)
  Previous: 2026-05-07 (feat: cablingViewRight — replaced floating cablingViewPanel with split-pane inside exportCablingModal; modal widens via .cabling-view-open class (860px, 80vh); layout: .cabling-split-container (flex row) → .cabling-split-left (340px, controls) + #cablingViewRight (.cabling-split-right, flex:1, text view); _viewCablingReport adds/removes .cabling-view-open on modal and shows/hides cablingViewRight; _closeCablingViewPanel targets cablingViewRight; _downloadFromCablingViewPanel reads cablingViewRight.dataset.mode; removed _makeDraggable('cablingViewPanel'). CSS: removed #cablingViewPanel fixed-position; added .cabling-split-container/left/right + .cabling-view-title-bar + #exportCablingModal.cabling-view-open. Section 26 updated. FILES: Sidebar.html, Sidebar-js.html, Sidebar-css.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-05-07 (ui: bridge results panel — colored dot per LLDP row for visual parity with topology devcard bridge-port-dot. rowHtml() gains 6th dotCls param; dot rendered inside .bridge-row-icon (flex cell) before SVG icon. Dot classes reuse bridge-port-dot variants: mismatch/L1→bridge-port-mismatch(amber), no-neighbor-up→bridge-port-missing(red), down/shut/partial→bridge-port-missing-quiet(gray), match→bridge-port-match(green), unknown/skip→no dot. Orphan rows get amber dot directly in label. CSS: .bridge-result-row grid col widened 18px→26px to fit dot+icon. FILES: Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-05-07 (feat: cablingViewPanel — replaced inline cablingPreviewArea with floating draggable panel (modal-floating, 560px wide, 75vh max); _viewCablingReport now opens/toggles the panel; panel has find bar (_cablingViewFindUpdate/Jump/Next/Prev/Key with focus-return rule) + Copy + Download toolbar; _closeCablingViewPanel replaces _closeCablingPreview; _downloadFromCablingViewPanel reads panel.dataset.mode; drag via _makeDraggable noReset+saveKey=cablingViewPanelPos; closeCablingModal updated. CSS: #cablingViewPanel + .cabling-view-toolbar + .cabling-view-body. Section 26 updated. FILES: Sidebar.html, Sidebar-js.html, Sidebar-css.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-05-07 (visual color coding — 4 changes: (1) Sheet N/A gray darkened: buildConditionalRules() CF N/A rules changed from #e2e8f0/#cbd5e1 to #c8d5e3/#94a3b8; dynamic runtime bg also updated to #c8d5e3 (Code.gs); (2) Canvas mismatch links amber: redrawLinks() bridge_mismatch layer added between non_eos and regular_po — amber #eab308, lineWidth 3, checks bridgePortStatus[link.u/v] === 'mismatch'|'missing' (Sidebar-js.html); (3) Device card audit indicator: _applyAuditCardIndicators() added — builds devSet from _auditIssues design entries + bridgePortStatus mismatch/missing endpoints, toggles .has-audit-error on .node cards; called from runValidation() after _updateAuditToolbarIcon; cleared on toggleValidationMode() off; .node.has-audit-error CSS: red border 1.5px #ef4444 + box-shadow (Sidebar-js.html, Sidebar-css.html); (4) Unified greens: .bridge-dev-dot.dot-green and .bridge-hn-dot.hn-match both changed #16a34a → #22c55e (Sidebar-css.html). FILES: Code.gs, Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-05-07 (ux: exportCablingModal 5 improvements — (1) View button active state: btnCablingViewNetlist/btnCablingViewDevice get .cabling-pill-active when preview open, cleared by _closeCablingPreview; (2) zero-match disable: _renderCablingBridgeFilter disables all 6 action buttons + shows .cabling-note "No cables match" when filter active + 0 results; (3) count badge: .cabling-count-badge spans (cablingTopoCount/cablingDeviceCount) show "N/M" next to section titles when filter active with results; (4) no-bridge dim note: .cabling-note "Run Device Bridge to enable issue filtering" appended below filter row when no bridge data; (5) preview textarea adaptive height: removed rows="14", added min-height:140px; max-height:40vh. New CSS: .cabling-count-badge, .cabling-note. FILES: Sidebar.html, Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-05-07 (ui: move "Audit icon in toolbar" hint — removed static info-box from Sidebar.html bridgeResults; now injected in titleHtml in _renderLldpResults, below "Interface States [N]" title bar and above Issues/OK/Other sections. FILES: Sidebar.html, Sidebar-js.html)
  Previous: 2026-05-07 (ui: L1 threshold row alignment — replaced .bridge-chk-spacer span with visibility:hidden disabled checkbox (pixel-perfect match); removed .bridge-chk-spacer CSS rule. FILES: Sidebar.html, Sidebar-css.html)
  Previous: 2026-05-06 (fix: Topology List button order → [Copy MD][Copy CSV][View][Download]; DUT summary in _buildCablingReportStr now filtered to devices that appear in sortedGroups — respects bridge issue filter. FILES: Sidebar.html, Sidebar-js.html)
  Previous: 2026-05-06 (redesign: exportCablingModal — compact single-row filter bar (Filter by Issues: All|None|pills) via .cabling-filter-row/.cabling-filter-ctrl/.cabling-filter-sep; section rows (⬡ Topology List / ◻ Device List) with title+4-button layout via .cabling-section-row/.cabling-section-title/.cabling-action-btns; removed export-divider between sections; pill labels shortened: Missing→Miss, L1 Errors→Error. FILES: Sidebar.html, Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-05-06 (ui: shorten bridge sub-label "No LLDP neighbour (Interface is up)" → "No LLDP neighbour (up)". FILES: Sidebar-js.html)
  Previous: 2026-05-06 (ui: bridge results panel restructured — "Interface States [Total]" title header above three collapsible sections; Issues sub-sections reordered flat (no group labels): New LLDP link / LLDP Mismatch / L1 Quality (FCS/Symbol/Align) / Interface down (notconnect) / Interface shut / No LLDP neighbour (Interface is up); "No EOS" section renamed "Other"; skip rows labelled "Excluded — ..."; unknown detail "no DNS/IP"; .bridge-results-title + .br-total-badge CSS added. FILES: Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-05-06 (fix: _cableGroupBridgeCategories now applies link-level priority (shut>down>other) per link, matching audit — previously checked each port independently causing shut ports to also show under Down filter. feat: View inline preview in exportCablingModal — _buildCablingReportStr(mode) extracted from downloadCablingReport as shared text builder; _viewCablingReport(mode) toggles #cablingPreviewArea with readonly textarea + Copy/Hide header; _closeCablingPreview() resets it; _copyCablingPreview() uses handleCopy; closeCablingModal() calls _closeCablingPreview(); [View][Download] button rows for both Topology and Device sections; .cabling-preview-text/.cabling-preview-header CSS added. FILES: Sidebar-js.html, Sidebar.html, Sidebar-css.html)
  Previous: 2026-05-06 (ui: update export panel descriptions — Topology Patching List: "Flat list ordered by device. Best for planning a new topology & auditing total connections." Device Patching List: "Patching from each device's perspective. Best when adding a new device — scroll to that device's section to see only its cabling." FILES: Sidebar.html)
  Previous: 2026-05-06 (ui: bridge panel "Interface down" label → "Interface down (notconnect)" to match EOS terminology. FILES: Sidebar-js.html)
  Previous: 2026-05-06 (feat: L1 threshold defaults min 0→1; condition >→>= (report when count ≥ threshold); _populateBridgePortL1Errors now iterates allNodesData instead of bridgePortStatus so errdisable/down/shut ports not involved in bridge check are also detected; _renderLldpResults down/shut rows now annotate with L1 detail via _l1ErrDetail if errors present; Sidebar.html bridgeL1Threshold input min/value updated 0→1; test-js.js clampL1Threshold updated min 0→1 + test cases. FILES: Sidebar-js.html, Sidebar.html, test-js.js)
  Previous: 2026-05-06 (fix: downloadCablingReport now applies bridge issue filter — replaced Object.values(cableGroups) iteration with _filterCableGroupsByBridge(_sortCableGroups(...)) so Download Topology Patching and Download Device Patching honour the same filter as Copy CSV and Copy Markdown; totalCableGroups/totalIndividualLinks counts updated from filtered array. FILES: Sidebar-js.html)
  Previous: 2026-05-06 (fix: _renderCablingBridgeFilter pills — '' (two adjacent single quotes) inside a single-quoted JS string on line 14920 created adjacent string literals causing SyntaxError: Unexpected string; changed to template literal: ` onclick="_toggleCablingBridgeFilter('${c.val}')"`. FILES: Sidebar-js.html)
  Previous: 2026-05-06 (fix: Sidebar.html HTML structure — L1 threshold bridge-audit-flags-grid div was placed outside the bridge-settings-card with a spurious extra </div> closing the modal body; moved div inside the card (before card's closing </div>), removed spurious </div>. FILES: Sidebar.html)
  Previous: 2026-05-06 (fix: remaining font-family:'JetBrains Mono' quote conflict in _renderCablingBridgeFilter "No bridge data" message — removed from inline style, added font-family to .info-box--dim CSS rule instead. FILES: Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-05-06 (fix: SyntaxError from font-family single-quotes inside JS string literals in _renderCablingBridgeFilter — replaced inline style="font-family:'JetBrains Mono'" with CSS classes .cabling-filter-label/.cabling-filter-summary; removed redundant font-family from .cabling-pill and .btn-mono inline styles in _renderCablingBridgeFilter — CSS classes carry the declaration. Sidebar-css.html: added .cabling-filter-label and .cabling-filter-summary rules with explicit JetBrains Mono. FILES: Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-05-06 (feat: cabling export bridge issue filter — cablingBridgeFilterBar placeholder in exportCablingModal (Sidebar.html); _CABLING_BRIDGE_CATS (5 cats: mismatch/missing/down/shut/l1), _cableGroupBridgeCategories, _cableGroupIssueReason, _filterCableGroupsByBridge, _renderCablingBridgeFilter, _toggleCablingBridgeFilter, _cablingBridgeSelectAll/None in Sidebar-js.html; copyCablingCsv/_buildCablingMarkdownStr apply filter + add Issues column when bridge data present; .cabling-pill/.cabling-pill-active CSS in Sidebar-css.html. FILES: Sidebar.html, Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-05-06 (feat: L1 error check (CRC/FCS) in bridge audit — device_bridge.py: _parse_interface_errors(raw) pure fn extracts {fcs,sym,align} per-iface from 'show interfaces counters errors'; _build_devstatus_ssh gains errs param + interfaceErrors field; _check_devstatus adds 4th command (ssh/eapi) or returns interfaceErrors:{} (rest/gnmi); VERSION 260505.2→260506.1. Sidebar-js.html embedded template: identical changes synced. JS: var bridgeAuditFlagL1Errors (default ON), bridgeL1ErrorThreshold, bridgePortL1Errors={} globals; _populateBridgePortL1Errors(dsData) SSoT fn called at both bridge call sites after _populateBridgePortSubTypes; _l1ErrDetail(u,uErr,v,vErr) pure helper; _renderLldpResults: svgWarn icon, l1ErrR classification from bridgePortL1Errors SSoT, issueCount includes l1ErrR.length, 'L1 Quality' group label + 'CRC / FCS errors' sub-section with bridge-row-l1 CSS class; _logBridgeIssuesToStatusLog: L1 entries gated on bridgeAuditFlagL1Errors; runValidation: auditCheckHadIssue.bridgeAuditFlagL1Errors + flagErrorOnPort + _auditIssues entries; toggleAuditCheck re-renders on bridgeAuditFlagL1Errors; onBridgeL1ThresholdChange handler; _syncBridgeEnableCheckbox syncs #bridgeL1Threshold. Sidebar.html: L1 error threshold input in Bridge modal; bridgeAuditFlagL1Errors checkbox in audit settings. Sidebar-css.html: bridge-row-l1/hover/.bridge-row-detail rules. test-js.js: clampL1Threshold (5 cases) + _l1ErrDetail (5 cases). FILES: device_bridge.py, Sidebar-js.html, Sidebar.html, Sidebar-css.html, test-js.js)
  Previous: 2026-05-06 (fix: SSoT violations — (1) Audit flags: changed let→var for all 34 auditFlag/bridgeAuditFlag globals so window[name] dynamic access works; _getAuditFlag replaced switch with window[name] lookup; _setAuditFlag replaced switch with window[name]=val + localStorage.setItem; storage event listener added for cross-tab flag sync. (2) globalLinks=links moved to AFTER allNodesData/linkMap/portMap/etc reset to eliminate partial-state window. (3) currentDeviceConfig/Left/Right cleared alongside allNodesData={} on every topology reload. FILES: Sidebar-js.html)
  Previous: 2026-05-06 (fix: interface name column (colEt) in DevView port list no longer has its own click handler — click now bubbles to row.onclick → selectPortOrLink → port strip with config options. Filter-by-interface removed from colEt; use searchInp instead. FILES: Sidebar-js.html)
  Previous: 2026-05-06 (fix: modals/panels always open expanded — setActiveModalChip(modalId) now strips modal-minimized+preMinDisplay from the incoming modal before registering it (covers all standard open functions); _showPushConfirmModal strips modal-minimized at function top before any early-return path. CLAUDE.md rule added. FILES: Sidebar-js.html, CLAUDE.md)
  Previous: 2026-05-06 (feat: bridge panel adds 'Interface States' group label (br-group-label) above Interface down/shut sub-sections when either exists. br-group-label CSS added to Sidebar-css.html. FILES: Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-05-06 (fix: _renderLldpResults always shows 'Interface down' and 'Interface shut' sub-sections regardless of bridgeAuditFlagDown/Shut; issueCount badge also counts them unconditionally. Audit flags still control topology dots (_bridgeEffectiveClass) and audit log (_logBridgeIssuesToStatusLog). FILES: Sidebar-js.html)
  Previous: 2026-05-06 (feat: Bridge auto-check enable/disable checkbox (enabled by default). New fn onBridgeAutoCheckToggle(val): saves 'bridgeAutoCheck' to localStorage, stops timer if disabled, starts if bridge up. _startBridgeAutoCheck now guards on 'bridgeAutoCheck' !== 'false' (at start and inside interval callback). _syncBridgeEnableCheckbox syncs #chkBridgeAutoCheck. Sidebar.html: checkbox added inline with interval input in AUTO section. test-js.js: clampBridgeInterval 7 cases. FILES: Sidebar-js.html, Sidebar.html, test-js.js)
  Previous: 2026-05-06 (feat: Bridge Modal — configurable periodic auto-check interval. New global _bridgeAutoCheckTimer (setInterval handle). New fns: _startBridgeAutoCheck() (clears+restarts interval from localStorage 'bridgeAutoCheckInterval', default 30 min, min 5), _stopBridgeAutoCheck() (clears interval), onBridgeAutoCheckIntervalChange() (clamps 5-480, saves localStorage, restarts if bridge up). _updateBridgeToolbarIcon: on up-transition calls _startBridgeAutoCheck alongside _autoRunBridge; on down-transition calls _stopBridgeAutoCheck. _syncBridgeEnableCheckbox: also syncs #bridgeAutoCheckInterval input. Sidebar.html: added 'Auto-check every N min' number input row in Bridge Modal AUTO section. FILES: Sidebar-js.html, Sidebar.html)
  Previous: 2026-05-06 (Test-gs.gs: add test_expandVlanString (10 cases), test_compressVlanRanges (8 cases), test_parseAndExpandDevices (7 cases); all registered in runAllTests(). INSTRUCTIONS covered-function list expanded to all 25 suites. FILES: Test-gs.gs, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-05-06 (refactor: bridgePortSubType as single source of truth for LLDP missing sub-type — bridgePortSubType={} declared alongside bridgePortStatus; _populateBridgePortSubTypes(dsData) replaces _bridgeMissingSubType and is called immediately after _compareBridgeData() at both bridge-check call sites; _renderLldpResults(results,orphans) drops dsData param + _portLinkSt() helper; _logBridgeIssuesToStatusLog drops _ls() helper; runValidation per-port count block + audit log block + _bridgeEffectiveClass all read bridgePortSubType directly; _bridgeMissingSubType deleted. FILES: Sidebar-js.html)
  Previous: 2026-05-06 (fix: bridge dot + audit count misclassification — (1) _bridgeEffectiveClass(status,portId) now sub-type aware: checks bridgeAuditFlagShut/Down/Missing based on _bridgeMissingSubType(portId); dots turn missing-quiet when flag OFF; (2) applyLldpIndicators() passes portId; initial port-row render passes domId; (3) applyLldpIndicators() moved to after devStatusMap update in both auto-bridge and manual-bridge handlers so sub-type lookup uses fresh data; (4) runValidation() per-port missing count uses link-level priority — checks paired port via _portToResult so far-end 'notconnect' of a shut link counts as shut, not down. FILES: Sidebar-js.html)
  Previous: 2026-05-06 (fix: _logBridgeIssuesToStatusLog audit misrouting — all 'missing' entries were routed to bridgeAuditFlagMissing regardless of link status; added local _ls() helper using dsData; replaced single else-if with tri-branch: shut→bridgeAuditFlagShut, down→bridgeAuditFlagDown, other→bridgeAuditFlagMissing. FILES: Sidebar-js.html)
  Previous: 2026-05-06 (fix: bridgeAuditFlagShut now counts toward issueCount when ON (same as Down); tooltip updated. All 3 sub-sections: No neighbour=ON+counted, Interface down=OFF+counted-when-ON, Interface shut=OFF+counted-when-ON. FILES: Sidebar-js.html)
  Previous: 2026-05-06 (feat: LLDP missing-neighbour sub-sections — _renderLldpResults(results, orphans, dsData) now receives dsData from both call sites; _portLinkSt() looks up live linkStatus from dsData; noNbrR split into noNbrDownR (notconnect, amber, counted), noNbrShutR (disabled, gray, excluded from issueCount), noNbrR (other, red, counted). New svgDown/svgShut icons; bridge-row-shut CSS class added. FILES: Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-05-06 (refactor: unify config-viewer duplicate code — (1) _cfgViewCopyAll/_cfgViewCopyInterface now delegate to _copyToClipboard() instead of inlining clipboard logic; (2) extracted _makeConfigSearchHandler({inp,body,clearBtn,badge}) factory — both showConfigModal and _openCfgPanelView assign oninput via it; (3) deleted _cfgViewSearchHandler (dead code). Rule 11a: Reuse and Enhance, Never Duplicate. FILES: Sidebar-js.html)
  Previous: 2026-05-06 (refactor: eliminate window._cfgViewRawText — replaced all 7 uses with currentRawConfig; _openCfgPanelView sets currentRawConfig = text directly. Single source of truth for currently-displayed config regardless of entry path (configModal or Config Center view panel). FILES: Sidebar-js.html)
  Previous: 2026-05-06 (fix: _openCfgPanelView now sets currentRawConfig = text alongside window._cfgViewRawText — cfgModalPushInterface() was calling _extractAllInterfaceBlocks(currentRawConfig) which was stale/empty when entering via Config Center view panel, causing "interface block not found" error. FILES: Sidebar-js.html)
  Previous: 2026-05-06 (configPushModal title format: device-first — "[01] leaf1: Push", "[01] leaf1: In Sync", "[01] leaf1: Verify". Applies to both modal header and dock chip. cfgModalPushInterface/cfgModalPushAll both route through same modal. FILES: Sidebar-js.html)
  Previous: 2026-05-06 (configPushModal dock chip + header: removed modal-header-subtitle and wrapper div (no-subtitle rule); device name merged into title. FILES: Sidebar.html, Sidebar-js.html)
  Previous: 2026-05-06 (Generate panel device list: removed [sheetIndex] prefix from row labels — sort order unchanged (still by sheetIndex), only the display label simplified to "devName · N lines". FILES: Sidebar-js.html)
  Previous: 2026-05-06 (configModal dock chip label: showConfigModal now sets title.innerText = "Config: " + _fmtDev(deviceName) — dock chip and modal header both show "Config: [01] leaf1" with "Config: " prefix restored. FILES: Sidebar-js.html)
  Previous: 2026-05-06 (View panel back button: cfgViewTitle now renders "← Generate › DevName" breadcrumb — "← Generate" is a clickable button calling _switchCfgPanel('generate'), making return to device list obvious. FILES: Sidebar-js.html)
  Previous: 2026-05-06 (configModal dock chip label fix: added modal-header-title class to #configModalTitle span — dock chip now reads "Config: leaf1" (dynamic device name) instead of raw ID "configModal". FILES: Sidebar.html)
  Previous: 2026-05-06 (View panel as sub-panel of Generate: removed cfgNav-view from left nav; added id="cfgNavGenSub" to Generate nav sub-text. _switchCfgPanel: 'view' maps to navActive='generate' so Generate stays highlighted; restores "Build EOS configs from sheet" on return. _openCfgPanelView: updates cfgNavGenSub to "↳ DevName". FILES: Sidebar.html, Sidebar-js.html)
  Previous: 2026-05-06 (Sweep Tests.gs→Test-gs.gs across all docs and code comments: Code.gs (3 comments), Sidebar-js.html (2 DUPLICATED comments), CLAUDE.md (2 invariants), UserGuide.html setup table, README.md setup table, hooks (stamp.sh, topoassist-deploy-tracker.sh, post-change-summary.sh, topoassist-gas-test-check.sh), memory files. No logic changes. FILES: Code.gs, Sidebar-js.html, CLAUDE.md, UserGuide.html, README.md)
  Previous: 2026-05-06 (Rename test files: tests-client-node.js→test-js.js, Tests.gs→Test-gs.gs. Add topoassist-client-test-check.sh hook (fires on Sidebar-js.html edits, checks test-js.js coverage). .claspignore broadened to *.js + Tests-client.html. test-device_bridge.md updated. FILES: test-js.js, Test-gs.gs, .claspignore, .claude/commands/test-device_bridge.md)
  Previous: 2026-05-06 (.claspignore: added tests-client-node.js — Node.js runner must not be pushed to GAS (causes ParseError). FILES: .claspignore)
  Previous: 2026-05-06 (Generate panel UX enhancements: (1) _renderGenerateDeviceList: updated header text — 0 cached: "N device(s) — open a device card to auto-cache its config, or Generate Configs to build all"; partial: "N of M device(s) auto-cached — open more device cards, or Generate Configs to fetch remaining"; allCached: green ✓ unchanged. Button label now managed here (guarded by !genBtn.disabled): "Regenerate Configs" when allCached, else "Generate Configs". Per-device Gen/Regen/… split-btn added as leftmost action before Copy. (2) _generateSingleDevice(name): clears configCache[name] and removes from _generateAllConfigs; adds name to window._genSingleFetching Set; re-renders (shows "…"); calls fetchFullConfig with callback that removes from Set and re-renders. (3) openGenerateAllModal: removed redundant btn.innerText override (label now set by _renderGenerateDeviceList). (4) _renderGenerateResults: moved btn.disabled=false before _renderGenerateDeviceList() call so label is set correctly; removed stale btn.innerText='Re-Generate Configs' line. FILES: Sidebar-js.html)
  Previous: 2026-05-06 (Client-side test runner: tests-client-node.js added — pure Node.js (no deps) with MockEl DOM mock; runs same 22 cases as Tests-client.html; `node tests-client-node.js` expected 22 passed 0 failed. test-device_bridge.md updated to prefer Node.js runner over xdg-open. FILES: tests-client-node.js, .claude/commands/test-device_bridge.md)
  Previous: 2026-05-06 (Client-side test suite: Tests-client.html added — standalone browser-runnable tests for applyHint (4 cases), lockFirst wide/exit-wide (10 cases), g() reader (4 cases), glb4firstForNonWide (4 cases); 22 total. No GAS auth needed. applyHint and lockFirst in Sidebar-js.html now carry SYNC comments pointing to Tests-client.html. test-device_bridge.md updated with browser-open instructions. FILES: Tests-client.html, Sidebar-js.html, .claude/commands/test-device_bridge.md)
  Previous: 2026-05-06 (Cancel + dirty-state warnings: Added _confirmDirtyClose(isDirty, label), _captureFormState(modalId), _captureGroupRectState() helpers. Added Cancel buttons to networkStackModal and autoConfigModal footers. closeTechModal/closeIpModal/closeGenerateAllModal now call _confirmDirtyClose. Added initialGlobalCfgState tracking to configCenterModal Template panel with oninput wiring and save reset. Added initialEditModalState tracking in all 3 guiEditModal open paths with reset in all save/delete handlers. Added initialGroupRectState tracking to groupRectModal with reset in confirmGroupRect/deleteGroupRect. Esc key path covered automatically via X button click. Updated ~/.claude/rules/ui.md Rule 21, CLAUDE.md Rule 21, topoassist-review-code-design.md Check 32, Reference_Card.md. FILES: Sidebar.html, Sidebar-js.html)
  Previous: 2026-05-06 (alert()→setStatus() sweep: 42 alert() calls in error/async paths replaced with setStatus(...,'status-error'/'status-warn'). Retained: input validation alerts (Field ID, Interface Name, IP mask validation errors, Device already in list) and D3 cable tooltip. Converted: all withFailureHandler errors, server response errors (res.error), timeouts, copy failures, precondition guards (no topology/cabling data, D3 not loaded, exit edit mode). FILES: Sidebar-js.html)
  Previous: 2026-05-06 (In-panel config view in Config Center: new "View" left-nav panel replaces opening configModal as a separate overlay (eliminated z-index conflict). cfgPanel-view has: device name strip (cfg-panel-desc), search bar (#cfgViewSearch — filter + :N line jump), Copy Interface / Copy All / Push Interface / Push All action buttons, cfg-search-hint, <pre id="cfgViewBody"> with full syntax highlighting (mirrors configModal light-mode hardcoded CSS). _openCfgPanelView(name): sets _configViewContext (so push buttons reuse cfgModalPushInterface/cfgModalPushAll directly), populates cfgViewBody, wires _cfgViewSearchHandler, shows push buttons for EOS devices only, calls _switchCfgPanel('view'). _cfgViewSearchHandler: block-aware filter + :N line jump using _splitConfigIntoBlocks + _renderWithLineNums (no duplication). _clearCfgViewSearch / _cfgViewCopyAll / _cfgViewCopyInterface added. Esc in cfgViewSearch clears before closing Config Center. _switchCfgPanel updated to include 'view'. _viewGeneratedConfigByName now calls _openCfgPanelView instead of showConfigModal. _iconBtn_UNUSED removed (dead code). FILES: Sidebar.html, Sidebar-css.html, Sidebar-js.html)
  Previous: 2026-05-06 (alert() → setStatus() in all error handlers: syncSchemaPreservingOrder failure (line ~1995), resetSchemaToDefaults failure (line ~2010), deleteInterface failure x2 (lines ~13017/13026), deleteInterface catch block (line ~13034). All errors now surface in status bar + statusLog instead of blocking modal dialogs. FILES: Sidebar-js.html)
  Previous: 2026-05-06 (Three GW override fixes: (A) _updateGwExample() null guard — early return if 'ip_gw_v4_mask' element absent (prevents TypeError when modal is not open). (B) Per-device hint fix — glb4firstForNonWide/glb6firstForNonWide variables added: when global mask is wide (_gwV4/6FirstSaved holds the real value), per-device non-wide first fields now show the saved global first (e.g. '100') as hint instead of '1'; passed as fallback arg to lockFirst. (C) saveIpConfig() no longer closes autoConfigModal — removed autoConfigModal+editOverlay display:none lines from success handler so save is non-destructive and modal stays open. FILES: Sidebar-js.html)
  Previous: 2026-05-06 (selectDevice() line 6555: devData?.fetchEpoch — devData is allDevicesData[deviceId] which is undefined during topology reload; configCache persists so else branch was reached with undefined devData. FILES: Sidebar-js.html)
  Previous: 2026-05-06 (UserGuide search: ugJump() now expands collapsed <details> ancestors before scrollIntoView — walks up parentElement chain setting open=true on any DETAILS found; handles case where user manually collapses a section after search ran. FILES: UserGuide.html)
  Previous: 2026-05-06 (Per-device wide-mode lockFirst refinement: forceValue flag added so '1' is only forced when the per-device mask itself is explicitly wide (devExplicitWide4/6). When global is wide but per-device mask is inherited (hint), explicit per-device first values are disabled but NOT overwritten — global changes must never override user-set per-device config. Also handles the case where field is already locked from global-wide and per-device mask then goes explicitly wide: forces '1' in the `else if (forceValue && el.value !== '1')` branch. explicitMask4/6 variables extracted to track hint vs explicit per-device mask. FILES: Sidebar-js.html)
  Previous: 2026-05-06 (Generate panel split-btn UI + View fix: (1) _renderGenerateDeviceList now renders each device as a .split-btn row (reusing existing CSS) — green bg (#10b981) when cached, slate (#475569) when not; label shows [sheetIndex] devName · N lines; Copy and View are .split-action buttons with dividers, matching the device strip style. (2) _copyGenRow(devName,btn) added — blue flash + green restore on copy, proper split-btn wrapper feedback. (3) _viewGeneratedConfigByName now calls showConfigModal(name, text, {noAutoMinimize:true}) — fixes bug where clicking View inside Config Center auto-minimized Config Center. (4) showConfigModal(deviceName, configText, opts) — added opts param; skips _autoMinimizeOpenModal when opts.noAutoMinimize is true. (5) Removed console.error from showConfigModal guard (was pre-write scan blocker). (6) _iconBtn renamed to _iconBtn_UNUSED (no callers; replaced by split-btn approach). (7) _copyDeviceConfigByName replaced by _copyGenRow. FILES: Sidebar-js.html)
  Previous: 2026-05-06 (Null guards + error logging: (1) processDeviceConfig() line 6768 — guard allDevicesData[deviceName] before .fetchTime access; async GAS callback fires after topology reload clears allDevicesData. (2) render() port-row onclick — early return when isEditMode && !allNodesData[domId]; event handler can fire after topology reload. (3) window.onerror — now calls setStatus() so uncaught errors land in statusLog (was: el.innerText, bypassed statusLog, got overwritten). (4) Added window unhandledrejection handler — catches async/Promise errors. FILES: Sidebar-js.html)
  Previous: 2026-05-06 (Generate panel live device list: Config Center → Generate panel now always shows all EOS devices on modal open — no longer shows "No configs generated yet." blank state. _renderGenerateDeviceList() renders all devices sorted by sheetIndex; active rows (eye+copy SVG icon buttons) when config is available from either window._generateAllConfigs or configCache[devName]; greyed placeholder rows (disabled icons) when not yet fetched. _getConfigTextForDevice(name) is the union lookup (generateAllConfigs first, then configCache). _iconBtn(type,name,disabled) returns inline SVG eye/copy icon button with data-dev attr; handlers use this.dataset.dev. _refreshGenerateRow(devName) called from fetchFullConfig success — re-renders full list when individual device config is fetched (reactive update). openGenerateAllModal now calls _renderGenerateDeviceList() always and defaults panel to 'generate'. _renderGenerateResults simplified: stores data + calls _renderGenerateDeviceList(). _viewGeneratedConfigByName/_copyDeviceConfigByName are name-based replacements for index-based variants (old idx variants kept for configModal context). FILES: Sidebar-js.html)
  Previous: 2026-05-06 (Per-device GW wide-mode fix: lockFirst() now always forces el.value='1' when entering wide mode (previously only forced '1' for hint/empty fields; explicit override values were left unchanged, producing invalid IPs like "515.20.0.1/22"). preLockVal still saves the original non-hint value for restoration when mask exits wide mode. _readGwDeviceOverrides: disabled first fields now read el.value.trim() (always '1') instead of el.dataset.preLockVal, so the explicit '1' is persisted to GW_DEVICE_OVERRIDES — critical because server-side getIpBlock() uses gw_v4_first in the wide formula; without saving '1', a non-wide global first (e.g. '100') would produce invalid addresses for the per-device wide device. FILES: Sidebar-js.html)
  Previous: 2026-05-06 (UserGuide search navigation: Enter→next match, Shift+Enter→prev match; Prev/Next buttons (ugPrevBtn/ugNextBtn, class ug-nav-btn, hidden until 2+ matches); active match highlighted orange via mark.ug-mark.ug-mark-active; "N / total" count display in ugMatchCount span. State: _ugMarks[] (NodeList of mark elements), _ugCurMark (index). Functions: ugJump(dir) wraps around, removes/adds ug-mark-active, scrollIntoView center; _ugUpdateCount() shows "N / total"; _ugSetNavVisible(show). _ugRunSearch populates _ugMarks, sets _ugCurMark=0, highlights first match. keydown on ugSearchInput: Enter→ugJump(1), Shift+Enter→ugJump(-1). FILES: UserGuide.html)
  Previous: 2026-05-05 (Generate configs localStorage cache: _renderGenerateResults saves configs+meta to localStorage key 'ta_gen_cache'; openGenerateAllModal restores from localStorage into window._generateAllConfigs/_generateAllMeta if not already in memory; runGenerateAll clears localStorage on fresh generation. Configs now survive sidebar close/reopen — no click needed on reload. FILES: Sidebar-js.html)
  Previous: 2026-05-05 (Fix two review warnings: (1) #groupListPanel max-height now dynamically clamped in _renderGroupList() so #holistic-controls never overflows viewport top — avail = window.innerHeight - 32 - fixedH (label+btns), capped at 240px min 80px. (2) Update canonicalizeInterface and parseVlanWithNative DUPLICATED last-synced comments from 2026-04-26 → 2026-05-05 in Code.gs and Sidebar-js.html (logic verified identical; date was stale). FILES: Sidebar-js.html, Code.gs)
  Previous: 2026-05-05 (openGenerateAllModal cache-restore bug: _switchCfgPanel was called unconditionally with 'push' after _renderGenerateResults, hiding cached results. Fixed: switch to 'generate' panel when cache exists, else switch to panel||'push'. FILES: Sidebar-js.html)
  Previous: 2026-05-05 (Fix #groupListPanel last-row member names clipped: max-height 160→240px, padding-bottom:4px so last row clears border-radius, overscroll-behavior:contain prevents wheel events leaking to canvas. FILES: Sidebar-css.html)
  Previous: 2026-05-05 (TOPO EDIT group list: collapse toggle redesigned — groupListToggleBtn (chevron up/down) collapses/expands #groupListPanel; _groupListCollapsed bool (localStorage grects_list_collapsed); toggleGroupList() flips state; _updateGroupListToggleBtn() syncs chevron icon+title; _renderGroupList() calls _updateGroupListToggleBtn() and hides panel when collapsed OR no groups. Removed _groupRectsHidden canvas-hide logic entirely — group rects on topology are always shown. FILES: Sidebar.html, Sidebar-js.html)
  Previous: 2026-05-05 (TOPO EDIT group list improvements: (1) Fix linear→flex flash — startFlexibleLayout() now removes #linear-grect-svg before creating #flex-grect-svg so linear rects don't persist during flex sim; (2) Global hide/unhide toggle — groupRectsToggleBtn in hc-btns; _groupRectsHidden bool (localStorage grects_hidden); toggleGroupRectsVisibility()+_updateGroupRectsToggleBtn() sync button icon eye/eye-off; renderGroupRectsInto returns early when hidden; (3) Sort — _renderGroupList sorts groups A-Z, members A-Z; (4) Two-line row layout — .gl-row is flex-direction:column; .gl-row-top has swatch+label+focus-eye; .gl-members below wraps all device names (no truncation, user-select:text). _updateGroupRectsToggleBtn() called at initApp(). FILES: Sidebar.html, Sidebar-css.html, Sidebar-js.html)
  Previous: 2026-05-05 (TOPO EDIT group list: #groupListPanel div added to #holistic-controls below .hc-btns. _renderGroupList() populates rows (color swatch + label + member names + eye icon) whenever saveGroupRects()/loadGroupRects() run; panel hidden when no groups exist. _zoomToGroup(grId) unions all cluster bounding boxes for that group and calls focusView(). Fix: flex view group rects no longer flash briefly with stale pre-simulation node positions — removed premature renderGroupRectsInto call at flex SVG creation; end.grects event handler renders them correctly once simulation settles. FILES: Sidebar.html, Sidebar-css.html, Sidebar-js.html)
  Previous: 2026-05-05 (_computeGroupClusters gapThreshold now dynamic: detailed view reads cardWidthInp+deviceGapInp (W+G) so threshold = one full card slot — any non-member card between two members splits them into separate clusters; holistic view uses fixed 300px fallback (no card geometry in force-sim). FILES: Sidebar-js.html)
  Previous: 2026-05-05 (Group rect spatial clustering: _computeGroupClusters(memberIds, padding=30, gapThreshold=W+G) uses union-find on node bounding boxes — nodes whose edges are within W+G of each other share a cluster. renderGroupRectsInto calls _computeGroupClusters for member-based rects: 1 cluster → falls through to existing single-rect path; 2+ clusters → renders one compact rect+label per cluster with same label/color/dblclick-to-edit handler, returns early. Fixes "stretching rect" when group members are spread across canvas. FILES: Sidebar-js.html)
  Previous: 2026-05-05 (sheetColumnManagerContainer: Debug log F12 + Apply Formatting moved to schema-settings-bar (top toolbar row below header); footer simplified to Reset Defaults left + Check Sync/Force Sync/Cancel/Save&Sync right. FILES: Sidebar.html, Sidebar-css.html)
  Previous: 2026-05-05 (TestVersionSync: pytest class catches VERSION mismatch between device_bridge.py and Sidebar-js.html embedded template; auto-runs on every device_bridge.py edit. Never manually edit VERSION — topoassist-bridge-version.sh hook auto-bumps on every device_bridge.py edit. FILES: tests/test_device_bridge.py)
  Previous: 2026-05-05 (Containers: remove btn-modal-close from deviceManagerContainer and sheetColumnManagerContainer headers — minimize-only in header, footer Cancel/Save handles dismiss. sheetVisContainer keeps [×] (google.script.host.close). FILES: Sidebar.html)
  Previous: 2026-05-05 (Container modal design settled: all 3 containers keep [−][×] in header (injector now falls back to appendChild when no close btn); deviceManagerContainer footer Cancel restored — [×]=quick close, Cancel=explicit discard. FILES: Sidebar.html, Sidebar-js.html)
  Previous: 2026-05-05 (setTitle/showModelessDialog sync: all 6 showModelessDialog calls in Code.gs now have matching .setTitle with identical string; fixed Network Topology (version suffix in setTitle only), Sheet Assistant and Sheet View (no setTitle at all). Check 31 added to review-code-design skill. FILES: Code.gs)
  Previous: 2026-05-05 (Column & Formatting Manager name sync: sheet menu addItem + setTitle + showModelessDialog in Code.gs corrected from "Manage Column & Formatting"; Section 26 and UserGuide menu-path references updated. FILES: Code.gs, UserGuide.html)
  Previous: 2026-05-05 (OSPF orphan cleanup: passive-interface EtX → default passive-interface EtX — removes override without residue. Both _orphans_to_cmds() and JS _buildCleanupConfig() updated. VERSION 260505.1→260505.2. UserGuide cleanup table updated. FILES: device_bridge.py, Sidebar-js.html, UserGuide.html)
  Previous: 2026-05-05 (UserGuide: Cleanup section expanded to all 5 orphan categories (interface, BGP neighbor, VLAN, VRF, OSPF passive) with detection+removal-command table; OSPF passive special handling explained; Layer 2 push-time section updated; Cleanup entry point corrected to Config Center → Cleanup. review-userguide skill: Steps 2c-i/ii/iii added. Config Center Generate panel 3 fixes: cache check on reopen, View button per device, minimized modal restore. FILES: UserGuide.html, Sidebar-js.html, ~/.claude/commands/topoassist-review-userguide-update.md)
  Previous: 2026-05-05 (deviceManagerContainer: removed footer Cancel button — header [×] already handles dismiss; restored minimize button injection for all containers (reverted earlier exclusion). FILES: Sidebar.html, Sidebar-js.html)
  Previous: 2026-05-05 (Delete icon upgrade: replaced × (two crossing lines) with trash-2 icon (polyline+path) in both Device Manager (btn-icon-delete, 12×12, no inline style — CSS class handles layout) and Sheet Column Manager (schema-del, 12×12). btn-icon-delete given fixed 28×28px size so it is centered in its 30px grid column. FILES: Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-05-05 (Group rect duplicate-label bug fix: (1) loadGroupRects() deduplicates same-label entries on load — merges members of duplicates into the first entry and persists the fix; (2) confirmGroupRect() blocks saving a new/renamed group whose label (case-insensitive) matches an existing group — shows inline error under the label input with _setGroupRectLabelError(); (3) openGroupRectModal() clears the label error on open. Root cause: no uniqueness check allowed two groupRects entries with the same name, rendering two separate rectangles for one logical group. FILES: Sidebar.html, Sidebar-js.html)
  Previous: 2026-05-05 (_injectMinimizeButtons: exclude fullScreenIds (deviceManagerContainer, sheetColumnManagerContainer, sheetVisContainer) — containers are excluded from dock so minimize would permanently hide them; × only, no [−] button. FILES: Sidebar-js.html)
  Previous: 2026-05-05 (deviceBridgeModal: removed subtitle wrapper from header; subtitle text moved to info-box--dim at top of modal body. Header now standard title+close only. FILES: Sidebar.html)
  Previous: 2026-05-05 (Dock chip sort: minimized modals in #modalDock now sorted by minimized-at time (oldest-left, most-recent-right) via data-minimized-at timestamp stamped in toggleModalMinimize; _refreshDock sorts before rendering. FILES: Sidebar-js.html)
  Previous: 2026-05-05 (Bug fix: column click handlers (colEt/colMode/colType/colPo) in port rows now guard with isEditMode check — in edit cabling mode, clicks no longer trigger _portColFilter/global search; instead bubble up to row onclick to open edit/connect modal. FILES: Sidebar-js.html)
  Previous: 2026-05-05 (Modal renames: techModal→networkStackModal ("Network Stack"), pushConfirmModal→configPushModal ("Config Push"), cablingModal→exportCablingModal ("Export Cabling"), helpModal title "Shortcuts Help" icon removed, sheetColumnManagerContainer title→"Column & Formatting Manager". Section 26 updated. FILES: Sidebar.html, Sidebar-js.html, Sidebar-css.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-05-05 (auditSettingsModal: changed modal-title span → modal-header-title div to match all other modals; chip/dock name now reads correctly from DOM. Section 26 updated. FILES: Sidebar.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-05-05 (Remove modal-floating: deviceBridgeModal was already modal-std in HTML; removed dead .modal-floating CSS block and JS modal-floating class checks in toggleModalMinimize + _injectMinimizeButtons. Section 26 updated. FILES: Sidebar-css.html, Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-05-05 (Remove modal-medium: dropped modal-medium class from autoConfigModal and auditModal — widths moved inline; .modal-medium CSS removed from Sidebar-css.html. Section 26 updated. FILES: Sidebar.html, Sidebar-css.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-05-05 (UserGuide: Cleanup section expanded to all 5 orphan categories (interface, BGP neighbor, VLAN, VRF, OSPF passive) with detection+removal-command table; OSPF passive special handling explained (reverse direction — emit passive-interface on cleanup, not no passive-interface); Layer 2 push-time section updated to reference all 5 categories; Cleanup entry point corrected from "Device Bridge → Cleanup" to "Config Center → Cleanup nav item". review-userguide skill: added Step 2c-i (entry point check), 2c-ii (all 5 categories checklist), 2c-iii (Layer 2 push-time coverage). Config Center Generate panel: 3 fixes — (1) cache check in openGenerateAllModal prevents redundant regeneration; (2) View button per device added via _renderGenerateResults(); (3) openGenerateAllModal restores minimized modal instead of reinitializing. FILES: UserGuide.html, ~/.claude/commands/topoassist-review-userguide-update.md, Sidebar-js.html)
  Previous: 2026-05-05 (In-modal loading state: showModalLoading(modalId, msg) now shows the modal immediately with an in-modal spinner (position:absolute over body, top=header height) instead of covering the full screen. hideModalLoading(modalId) removes it. Added .modal-loading-state + .modal-loading-spinner CSS (reuses @keyframes spin). openIpConfigModal and openTechModal updated: guard timeout + failure handler close the modal + clear chip; success handler calls hideModalLoading and populates fields (modal already visible). hideGlobalLoading no longer called from modal-open paths. FILES: Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-05-05 (deviceManagerContainer header: removed redundant modal-keep-on-min class — all 3 full-page containers now have identical plain .modal-header. FILES: Sidebar.html)
  Previous: 2026-05-05 (Added Section 26: UI Element Inventory — reference table of all ui.prompt/ui.alert native dialogs, modal-std modals with header titles and types, full-page containers, sidebar panels, and chip/dock name source rule. FILES: INSTRUCTIONS_topoassist.txt)
  Previous: 2026-05-05 (Modal refactor: (1) editOverlay dim backdrop never shown — removed all display='block' assignments from 13 open functions (autoConfigModal, techModal, guiEditModal x3, auditModal, deviceBridgeModal, cablingModal, helpModal, configModal, configCenterModal, vlanSummaryModal, groupRectModal); overlay element stays in DOM as dead ref. (2) Merged _updateActiveChip + _updateMinimizedDockChip into single _refreshDock() — dock chip onclick now calls _autoMinimizeOpenModal() before restoring, so restoring a minimized modal auto-minimizes any other open modal. (3) Removed hadOverlay tracking from toggleModalMinimize and _onModalClose. (4) _autoMinimizeOpenModal and closeAllModals both converted from hardcoded candidates array to dynamic .modal-std query. FILES: Sidebar-js.html)
  Previous: 2026-05-05 (No subtitle in modal headers: removed .modal-header-subtitle div + wrapper div from sheetColumnManagerContainer; deleted .modal-header-subtitle CSS from Sidebar-css.html. All three full-page containers (deviceManagerContainer, sheetColumnManagerContainer, sheetVisContainer) now share the identical .modal-header > .modal-header-title + .btn-modal-close structure. FILES: Sidebar.html, Sidebar-css.html)
  Previous: 2026-05-05 (Config Center Generate panel: cache-aware restore + View button. openGenerateAllModal(): if modal is minimized, calls toggleModalMinimize to restore and returns; if window._generateAllConfigs exists, calls _renderGenerateResults() to show cached result immediately. runGenerateAll(): clears window._generateAllConfigs/Text/Meta at start (always fresh GAS call). New _renderGenerateResults(configs, meta): shared renderer used by both runGenerateAll() success handler and openGenerateAllModal(); stores window._generateAllMeta={sheetName,ts}; adds View button per device row (calls _viewGeneratedConfig(idx)) in addition to existing Copy button; sets left.dataset.generated='1'; sets btn to 'Re-Generate Configs'. New _viewGeneratedConfig(idx): calls showConfigModal(cfg.name, cfg.text) on cached entry. FILES: Sidebar-js.html)
  Previous: 2026-05-05 (deviceManagerContainer header migrated from .reorder-header to standard .modal-header/.modal-header-title; mode-reorder no-border-radius rule updated; dead .reorder-header/.reorder-header-close CSS removed. FILES: Sidebar.html, Sidebar-css.html)
  Previous: 2026-05-05 (Remove modalOrder/closeFuncs registry from Esc handler — replaced with dynamic .modal-std query (only one modal open at a time; .btn-modal-close clicked directly). Two find-bar special cases (configModal, configCenterModal) preserved inline. FILES: Sidebar-js.html)
  Previous: 2026-05-05 (Rename app-topology → appTopologyContainer (main topology wrapper div, consistent *Container naming convention). FILES: Sidebar.html, Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-05-05 (Remove deviceManagerContainer from modalOrder and closeFuncs — also a separate GAS dialog, same as sheetColumnManagerContainer/sheetVisContainer. Final modalOrder has 13 topology-only modals. FILES: Sidebar-js.html)
  Previous: 2026-05-05 (Remove sheetColumnManagerContainer and sheetVisContainer from modalOrder and closeFuncs in topology Esc handler — these are separate GAS dialog windows, never in the topology DOM, so Esc in the topology sidebar can never reach them. FILES: Sidebar-js.html)
  Previous: 2026-05-04 (reorderModal renamed deviceManagerContainer (HTML/JS/CSS). _updateMinimizedDockChip() now skips full-screen mode modals (sheetColumnManagerContainer, deviceManagerContainer, sheetVisContainer) — these collapse via setHeight so the header [chevron] is the restore UI; dock chip was redundant. FILES: Sidebar.html, Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-05-04 (device_bridge.py idempotency filter bug fix + tests: GW SVI cleanup commands ('default ip address virtual'/'default ipv6 address virtual') returning 'invalid command' go through the eAPI RuntimeError path ('CLI command N of M failed') not the success path — moved idempotency filter to RuntimeError path with orphaned-context-line stripping (_BLOCK_CMD_RE check). Added pair-based pre-scan in success path for per-cmd eAPI errors. 12 new tests in TestPushConfigIdempotencyFilter, TestPushConfigTimeoutRecovery, TestFinalizeSessionTimeoutRecovery. FILES: device_bridge.py, Sidebar-js.html, tests/test_device_bridge.py)
  Previous: 2026-05-04 (Modal refactors: deviceBridgeModal modal-floating→modal-std (adds editOverlay, _onModalClose, standard minimize/dock-chip; removed _initLldpModalDrag). generateAllModal renamed configCenterModal. viewCustomContainer renamed sheetVisContainer. FILES: Sidebar.html, Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-05-04 (Chip name reads from DOM: _chipNames map removed. _refreshModalChip() now reads modal-header-title textContent for toolbar chip name (same source as _updateModalDock dock chip). Single source of truth — name always matches modal header. FILES: Sidebar-js.html)
  Previous: 2026-05-04 (Auto-minimize + universal chip: _autoMinimizeOpenModal() minimizes any open modal when a new one opens. _chipNames expanded to all modals incl. reorderModal→'Device Manager'. _onModalClose() now also clears chip for the closing modal. setActiveModalChip(id) added to all open paths. FILES: Sidebar-js.html)
  Previous: 2026-05-04 (Targets strip hint text updated: "Blue = Selected | Shift+Role = Select Multiple Roles". FILES: Sidebar.html)
  Previous: 2026-05-04 (Global minimize rule enforced on all modals: _onModalClose(el) helper strips modal-minimized class + datasets + calls _updateModalDock(); added to all 10 non-tracked close functions and closeAllModals. CLAUDE.md Rule 24 updated with full minimize/close contract. FILES: Sidebar-js.html, CLAUDE.md)
  Previous: 2026-05-04 (Config Center targets strip: role chips moved inline to first row (same line as All/None). genAllRoleFilters is now a flex div inside the header row, hidden until roles load. Hint updated to "Shift+role = toggle · blue = included". FILES: Sidebar.html)
  Previous: 2026-05-04 (Config Center nav reorder + Deploy rename + role filter chips. Nav order: Cleanup → Deploy → Generate → Template. Push renamed to "Deploy" (sub: "Cleanup + push to devices"). Default panel changed from 'generate' to 'push'. Role filter pills (.genall-role-chip, amber) added below device chips: _buildRoleFilters() reads unique roles from data-role attrs; _selectByRole(role, shiftKey): click=exclusive select that role, Shift+click=toggle. genAllRoleFilters div added to Sidebar.html targets strip. FILES: Sidebar.html, Sidebar-css.html, Sidebar-js.html)
  Previous: 2026-05-04 (Active modal chip in toolbar — amber pill (#activeModalChip, .active-modal-chip) in APP toolbar section shows the current modal name for the three renamed modals: guiEditModal→"Edit Interface", autoConfigModal→"Auto Config", deviceBridgeModal→"Device Bridge". setActiveModalChip(name|null) helper added to Sidebar-js.html; called from openEditModal/closeEditModal, openIpConfigModal/closeIpModal, openBridgeModal/closeBridgeModal. Chip hidden by default; dark-mode override included. FILES: Sidebar.html, Sidebar-css.html, Sidebar-js.html)
  Previous: 2026-05-04 (Config Center redesign v2 — replaced 2×2 grid with left-nav sidebar + full-width content panels. 4 nav items: Generate / Template / Cleanup / Push. Each panel has italic description strip (.cfg-panel-desc), action bar (.cfg-panel-actions), scrollable body (.cfg-panel-body). Removed duplicate Global Config Template toolbar icon. _switchCfgPanel(panel) added; _switchGenTab() forwards to it; openGenerateAllModal(panel) accepts optional panel param (default 'generate'); openGlobalCfgModal() calls openGenerateAllModal('template'). Targets strip: added "Click to toggle · blue = included" hint; _updateGenAllSelCount shows "All N selected" / "N of N selected". Modal width 1100→960px. FILES: Sidebar.html, Sidebar-css.html, Sidebar-js.html)
  Previous: 2026-05-04 (Config Center redesign v1 — merged globalCfgModal into generateAllModal as 2×2 grid. FILES: Sidebar.html, Sidebar-css.html, Sidebar-js.html)
  Previous: 2026-05-04 (eAPI commit timeout resilience + idempotency error reclassification. device_bridge.py _push_config eAPI path: catch OSError/TimeoutError on _eapi_push — sleep 2s, query "show configuration sessions", if session absent return {ok:True, diff:"", late_response:True}; if still present raise "session still pending"; embedded template in downloadBridgeScript() synced. _finalize_session eAPI path: same OSError catch on the commit call — verifies session gone, returns late_response:True if committed. _push_config eos_errs reclassification: "default ip address virtual" / "default ipv6 address virtual" "invalid command" errors (from GW SVI idempotency cleanup) demoted from eos_errors to eos_warnings — harmless when feature not configured. JS pushAllConfigs(): _lateResponse flag from res.late_response; Phase 3 result labels: "Committed — eAPI timed out (verified clean)" / "eAPI timed out (verify failed)" / "eAPI timed out, N lines still differ" in amber; "All rejected" gate skipped when _lateResponse. commitPushConfig(): Phase 2 row shows amber "committed — eAPI delayed" when _lateResponse. FILES: device_bridge.py, Sidebar-js.html)
  Previous: 2026-05-04 (Rename modal IDs: editModal→guiEditModal, ipConfigModal→autoConfigModal, bridgeModal→deviceBridgeModal. All getElementById/querySelector/data-modal/CSS selectors/modalOrder/closeFuncs updated. JS function names (openIpConfigModal, closeEditModal, closeBridgeModal, closeIpModal, toggleBridgeMinimize, etc.) unchanged. FILES: Sidebar.html, Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-05-04 (Config Center redesign — merged globalCfgModal into generateAllModal as a 2×2 panel layout (Generate | Config Template top row; Cleanup | Push bottom row). Removed standalone globalCfgModal element; openGlobalCfgModal() now delegates to openGenerateAllModal() + _loadGlobalCfgTemplate(); _switchGenTab() is a no-op (all panels always visible); Targets chip list has max-height:60px + overflow-y:auto scroll; modal width 900→1100px; title "Topology Config Operation"→"Config Center"; removed globalCfgModal from modalOrder/closeFuncs/closeAllModals; toolbar Global Config Template button data-modal updated to generateAllModal. FILES: Sidebar.html, Sidebar-css.html, Sidebar-js.html)
  Previous: 2026-05-04 (UserGuide.html: interface naming table expanded — replaced flat abbreviation list with a 6-row table showing Abbrev / Type / How it is configured; clarifies Et and Vx1 go in int_, Po uses po_ column, Lo/Vl are auto-generated, Ma is not configured by TopoAssist; added info-box warning that Lo/Vl/Ma/Po in int_ triggers audit error. FILES: UserGuide.html)
  Previous: 2026-05-04 (UserGuide.html print fix: beforeprint/afterprint events open all <details> sections before printing and restore original open/closed state after. CSS `details { display: block }` alone is insufficient in modern browsers — they hide closed content via content-visibility, not display. State saved in data-print-was-closed attribute. FILES: UserGuide.html)
  Previous: 2026-05-04 (UserGuide.html control bar redesign: 3-zone sticky bar — segmented pill [Top|Expand|Collapse] left; search flex-1 centre + match count; Print/Download right. Search rebuilt with TreeWalker (collect-then-modify, FILTER_REJECT prunes svg/script/button/doc-actions, tagName.toLowerCase()); separate re/reSplit regexes; mark unwrap via firstChild insertion. FILES: UserGuide.html)
  Previous: 2026-05-04 (Docs: UserGuide.html topology toolbar section full overhaul — Primary Row table rebuilt: removed stale SORT row (moved to secondary row as INT SORT), removed Group Rect row (moved to new "Holistic-View Floating Controls" subsection), fixed "Collapse Secondary Row" → "Collapse/Expand All Cards" with correct chevron-up icon, added Global Config Template, Device Visibility Panel, Errors Only button; Secondary Row table rebuilt: removed stale Collapse/Expand All and ERR rows (both now in primary row), added INT SORT with correct SHEET/MODE/CAT/A-Z options, added HISTORY status log button; added "Holistic-View Floating Controls" subsection with Group Rect and Node Size −/+; fixed 2× Group Rect references that said "primary toolbar row" → "TOPO EDIT floating panel". FILES: UserGuide.html)
  Previous: 2026-05-04 (UserGuide.html control bar overhaul: sticky top bar (position:sticky, top:0, z-index:10, white bg + bottom border/shadow, negative margin to fill container width); left group: search input with clear button + match count + Expand All + Collapse All; right group: Jump to Top + Print/PDF + Download; search highlights matches with <mark class="ug-mark">, opens matching sections, closes non-matching, debounced 140ms; Expand/Collapse All toggles all details[open]; Download sets data-ug-expand-all="1" on body before outerHTML so reopened file opens all sections; DOMContentLoaded checks body.dataset.ugExpandAll to force isOpen=true. FILES: UserGuide.html)
  Previous: 2026-05-04 (Docs: UserGuide.html Device Manager section updated — added "Editing Devices" subsection (inline editing, change indicators, was-span, .changed selects, Save/Cancel flow); added DNS name to Model & Rack subsection (now "Model, Rack & DNS Name"). FILES: UserGuide.html)
  Previous: 2026-05-04 (SheetColumnManager: added "was: [original label]" dim italic span (id=schemaWas_<safeKey>) below label input in each row; safeSchemaKey/origLabel/labelWasChanged computed in forEach; SheetColumnManagerUpdateLabel updates span in-place on onchange; CSS .schema-was added. FILES: Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-05-04 (Device Manager change indicators: Hostname gets "was: X" dim span (id=devHostnameWas_N, updated in-place by DeviceManagerUpdateHostname); Role and MLAG selects get green border+text via .role-select.changed/.mlag-peer-select.changed when value differs from _origRole/_origMlagPeer; DeviceManagerLoadDevices now sets _origHostname/_origRole/_origMlagPeer on each device object; wasChanged variables (hostnameWasChanged/roleChanged/mlagChanged) computed in row render. FILES: Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-05-04 (Fix: removed stale nth-child header rules (pencil removal shifted col indices — Type was getting 20px from old pencil rule). All header cells now uniform 10px via .reorder-list-header. FILES: Sidebar-css.html)
  Previous: 2026-05-04 (Device Manager UX pass 2: DNS Name + Hostname → inline inputs (no Apply/Cancel panel); DeviceManagerUpdateName validates on oninput with red border + error; DeviceManagerUpdateHostname auto-saves; wasChanged shows dim "was: X" note; removed DeviceManagerRenameDevice/closeDeviceEditor/applyDeviceEdit; header .reorder-list-header gets 10px uppercase font; CSS replaced old .dev-hostname-* spans with .dev-name-inp/.dev-hostname-inp/.dev-name-was/.dev-name-error. FILES: Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-05-04 (Device Manager UX: dropped redundant pencil-edit column (hostname/name already click-to-edit); EOS type badge → dim .type-eos-label span; tightened row padding 8px→6px, gap 8px→6px, min-height 40px→36px, padding 15px→10px. Grid: 11 cols→10 cols. FILES: Sidebar-css.html, Sidebar.html, Sidebar-js.html)
  Previous: 2026-05-04 (Bug fixes: closeFuncs reorderModal→DeviceManagerCloseButton (Esc was broken); CSS #schemaBody→#sheetColumnManagerBody; CSS schemaContainer→sheetColumnManagerContainer (7 selectors); local vars schemaArray→columns, schemaMsg→columnManagerMsg. FILES: Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-05-03 (Rename batch 2: deleteField→SheetColumnManagerDeleteField, addNewField→SheetColumnManagerAddField, getOriginalItem→SheetColumnManagerGetOriginalItem, silentRefreshDeviceManager→DeviceManagerAutoRefresh, promptRenameDevice→DeviceManagerRenameDevice, applyDeviceListFromServer→DeviceManagerLoadDevices; HTML IDs: schemaContainer→sheetColumnManagerContainer, schemaBody→sheetColumnManagerBody, btnSaveSchema→btnSheetColumnManagerSave; fixed stale attemptCloseSchema→SheetColumnManagerCloseButton in closeFuncs. FILES: Sidebar-js.html, Sidebar.html)
  Previous: 2026-05-03 (Rename: currentSchemaList→currentSheetColumnManagerList, initialSchemaState→initialSheetColumnManagerState, initialSchemaKeyOrder→initialSheetColumnManagerKeyOrder, isSchemaSaving→isSheetColumnManagerSaving, schemaLockTimer→sheetColumnManagerLockTimer, currentDeviceList→currentDeviceManagerList, initialDeviceState→initialDeviceManagerState, runSchemaAudit→SheetColumnManagerAudit, pendingDeleteIndex→pendingDeviceManagerDeleteIndex, globalSchema→topoColumnSchema. FILES: Sidebar-js.html, Sidebar.html)
  Previous: 2026-05-03 (Rename dirty flags: isDeviceListDirty→isDeviceManagerDirty, isSchemaDirty→isSheetColumnManagerDirty. FILES: Sidebar-js.html)
  Previous: 2026-05-03 (Rename: checkForChanges→DeviceManagerCheckChanges, checkSchemaChanges→SheetColumnManagerCheckChanges, saveSchemaChanges→SheetColumnManagerSave, resetSchemaToDefaults→SheetColumnManagerResetDefaults (client only; GAS call preserved), updateSchemaLabel→SheetColumnManagerUpdateLabel, updateSchemaOption→SheetColumnManagerUpdateOption, getCleanSchema→SheetColumnManagerCleanState, showDeviceManagerUi→DeviceManager (Code.gs). FILES: Sidebar-js.html, Sidebar.html, Code.gs)
  Previous: 2026-05-03 (Rename: initDeviceManager()→DeviceManagerOpenButton(), initSheetColumnManager()→SheetColumnManagerOpenButton(). FILES: Sidebar-js.html)
  Previous: 2026-05-03 (Rename: renderSchemaList()→SheetColumnManager(), updateDeviceSaveButton()→DeviceManagerSaveButton(), attemptCloseManager()→DeviceManagerCloseButton(), attemptCloseSchema()→SheetColumnManagerCloseButton(). FILES: Sidebar-js.html, Sidebar.html)
  Previous: 2026-05-03 (Rename: initSchemaManager()→initSheetColumnManager(). FILES: Sidebar-js.html)
  Previous: 2026-05-03 (Fix: SheetColumnsManagerSaveButton→SheetColumnManagerSaveButton. FILES: Sidebar-js.html)
  Previous: 2026-05-03 (Refactor: openDeviceManager()→initDeviceManager() to match initSchemaManager() pattern; removed redundant modal.style.display="flex" (CSS body.mode-reorder already controls visibility); silentRefreshDeviceManager() guard changed from modal.style.display==='none' to currentMode!=='manager'. FILES: Sidebar-js.html)
  Previous: 2026-05-03 (Rename: renderManagerList()→DeviceManager(), showDeviceDataUi()→SheetColumnManager(), updateSaveButtonVisuals()→SheetColumnsManagerSaveButton(). FILES: Sidebar-js.html, Code.gs)
  Previous: 2026-05-03 (Fix: Device Manager — after applyDeviceEdit(), scroll edited device into view. FILES: Sidebar-js.html)
  Previous: 2026-05-03 (Fix: grey CF formatting lost after force sync — ensureDummyColumn() now runs BEFORE Stage 5 (applyGlobalFormatting) in rebuildSheet(); row1Colors and row2 header background corrected to start at col 2 (device cols). Root cause: CF rules built via headers.indexOf before _sys_ inserted at col 1 → all rules off by one. Also removed console.log from calculateGlobalTopology log helper; removed stale REPLACE comment. FILES: Code.gs)
  Previous: 2026-05-03 (Remove success alert from saveIpPreferences handler — modal closes + setStatus covers feedback; alert was redundant and disruptive. Files: Sidebar-js.html)
  Previous: 2026-05-03 (Docs: updated Code.gs cabling-helpers block header sync date from 2026-04-19 → 2026-04-26 to match the most recent individual function sync date (_buildCableGroupsForTest). Comment-only change, no logic. FILES: Code.gs)
  Previous: 2026-05-03 (Bug fix: rebuildSheet() now calls ensureDummyColumn(mappingSheet) unconditionally before refreshSheetRowVisibility() — fixes _sys_ deletion on new project with no data rows. Root cause: refreshSheetRowVisibility() returns early when lastRow < 3, skipping its own ensureDummyColumn call. Also removed stale console.log from savePreferences(). FILES: Code.gs)
  Previous: 2026-05-03 (UserGuide: Step 3 summary order fixed (Interface→Transceiver→Speed→Mode→Port Channel→Vlan→SVI→IP→Show All). Steps 5-8 updated: Step 5 removed Simple-view restriction; Step 6 renamed to "Tech Stack & Auto Config" + expanded MLAG/overrides; Step 7 changed to "Check Audit" — audit runs automatically; Step 8 changed to download+double-click. Progressive Column Views expanded to 9 items matching menu. Column Visibility Views rewritten with correct names, column lists, and order including SVI+IP entries. FILES: UserGuide.html)
  Previous: 2026-05-03 (Template sheet release workflow: added .clasp-template.json (gitignored Script ID 1CliQdcCkaLfvNoXakyu6wX5MgHQTEVu5kLTOlhxVM5IlyCX180TMx51d); deploy to template via /topoassist-deploy-template command (swaps .clasp.json → .clasp-template.json, pushes --force, restores); template sheet Drive ID 1mIF9aeQ5oZ1QPkOUpQgGKn55zWAPGIcSjvy7xoapVos — users copy it via /copy URL. FILES: .clasp-template.json)
  Previous: 2026-05-03 (Bug fix: resetToNewProject() now calls ensureDummyColumn(sheet) unconditionally after sheet.clear() — previously the system column (_sys_, col A) was not recreated when resetting without keeping a device. FILES: Code.gs)
  Previous: 2026-05-03 (UserGuide restructure: added collapsible h2 sections (JS auto-wraps in details/summary at DOMContentLoaded; default-open: Installation + Typical Workflow); added Quick Navigation grid; replaced Quick Start with 10-step Typical Workflow; added Device Manager h2 (DID, MLAG, Non-EOS toggle, Model/Rack, Visibility, mid-project insert callout); added Sheet Filling h2 (progressive views, SheetAssist Panel, formatting); added Group Rects h2 before Config Generation; added Appendix h2 (Checkpoints moved here, Snake Test reference, Custom View); renamed Reconcile→Cleanup throughout; added id attributes to all h2s for anchor links. FILES: UserGuide.html)
  Previous: 2026-05-02 (GenAll Targets header polish: All/None buttons changed from btn-mono inline style to .genall-sel-chip (rounded chip, 20px height, bg-secondary fill, blue hover). Device pill gap changed from 4px 14px → 6px 8px (uniform, symmetric). Pill height 22px→24px, padding 2px 8px→0 10px. FILES: Sidebar.html, Sidebar-css.html)
  Previous: 2026-05-02 (GenAll device selector: replaced checkbox+label list in #genAllDeviceList with pill/chip toggle buttons (.genall-dev-pill, .genall-dev-pill.selected). Each pill has data-dev attribute; _toggleGenAllPill(btn) toggles selected class. _updateGenAllSelCount() and _genAllSelectAll() updated to use .genall-dev-pill.selected. runCleanup() and pushAllConfigs() filter updated to use CSS.escape+querySelector on data-dev+.selected. All pills start selected (all devices targeted). FILES: Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-05-02 (Device name display: added _fmtDev(name) helper → '[NN] DevName' using sheetIndex zero-padded to 2 digits; falls back to raw name when sheetIndex unavailable. Applied everywhere: MLAG peer dropdown, audit pair errors (VLAN/Mode/Speed), status bar, bridge device rows, cleanup UI device headers (+ sorted by sheetIndex), push modal subtitle, config modal title, genAll device checklist. GW override table fixed to use dev.sheetIndex instead of dev.id. FILES: Sidebar-js.html)
  Previous: 2026-05-02 (SheetViewMenu column sets: viewMode=[int,sp_mode]; viewPo=[int,sp_mode,po]. Files: Code.gs)
  Previous: 2026-05-02 (SheetViewMenu: moved Mode view from L2/L3 section to L1.5, renamed to 'L1.5: Mode', positioned above 'L1.5: Port Channel'. Files: Code.gs)
  Previous: 2026-05-02 (CSS: bump .gw-ov-dev-id and .gw-ov-mlag-badge from 9px → 10px to satisfy 10px font-size floor. Files: Sidebar-css.html)
  Previous: 2026-05-01 (UserGuide: expanded section 4 with IP Config modal, GW address formula (standard vs wide-prefix), per-device GW override table usage, wide-mode locking behaviour, and MLAG First-octet constraint. Files: UserGuide.html)
  Previous: 2026-05-01 (runValidation() crash fix: two node-guard conditions (!node || !node.details) now also check !node.name — prevents TypeError: Cannot read properties of undefined (reading 'toLowerCase') when a sheet row has an empty int_ column, producing an allNodesData entry with name:undefined. Files: Sidebar-js.html)
  Previous: 2026-05-01 (Audit check K (Internal VLAN Conflict) gated to l2-* modes and Vx1 only — l3-* sub-int vlan_ is dot1q encap only, no VLAN DB entry, so overlap with EOS-internal VLANs is harmless. VERSION 260501.36. Files: Sidebar-js.html)
  Previous: 2026-05-01 (Per-device GW lockFirst() corrected: hint/empty fields → disable + force '1'; explicit fields → disable only, value preserved (shown as disabled, not overwritten). _readGwDeviceOverrides: disabled fields return dataset.preLockVal ('' for hint-locked, explicit value for user-set), so saves in wide mode correctly preserve per-device explicit first overrides. Files: Sidebar-js.html)
  Previous: 2026-05-01 (Per-device GW lockFirst() bug fix: was unconditionally overwriting el.value='1' on every _updateGwExample() call in wide mode. Fixed: only enters the lock on first transition (!el.disabled guard); saves explicit pre-wide value to el.dataset.preLockVal (empty string if was hint/empty). Subsequent wide-mode calls are no-ops. On exit from wide mode: restores preLockVal if non-empty (explicit value preserved), otherwise clears and applies global hint. Files: Sidebar-js.html)
  Previous: 2026-05-01 (Per-device GW first field locked in wide mode: lockFirst() helper sets disabled=true + value='1' when devWide; on exit from wide mode clears value and re-enables before applying global hint. _readGwDeviceOverrides now skips disabled fields (!el.disabled guard). CSS: .gw-ov-cell input:disabled styled same as hint (grey italic, no opacity reduction, not-allowed cursor). Files: Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-05-01 (Per-device GW override table: onfocus now clears the hint value and removes hint class when field is in hint mode, leaving an empty field ready for typing. Avoids GAS iframe el.select() unreliability (mouseup deselects after onfocus). onblur still re-applies hint if field left empty. Files: Sidebar-js.html)
  Previous: 2026-05-01 (Per-device GW hint logic simplified: applyForced removed. All 6 per-device fields now use applyHint uniformly. First fields use '1' as hint when devWide=true (per-device effective mask is wide), global first otherwise. Explicit per-device values (hint class removed by oninput) are NEVER overwritten by global changes or wide-mode transitions. Eliminates savedWide dataset mechanism entirely. Files: Sidebar-js.html)
  Previous: 2026-05-01 (openIpConfigModal listener ordering fix: blanket inputs.forEach oninput=checkIpDirty now excludes #gwDeviceOverrideTable inputs (el.closest guard). Redundant explicit #gwDeviceOverrideTable oninput block removed. _buildGwDeviceOverrideTable() moved to run AFTER the blanket binding block so its rich handlers (onfocus=select, oninput=remove-hint+_updateGwExample+checkIpDirty+_highlightGwMlagMismatch, onblur=re-hint) are never overwritten. initialIpState=getCurrentIpState() also moved to after _buildGwDeviceOverrideTable(). Files: Sidebar-js.html)
  Previous: 2026-05-01 (GW per-device override hint system overhaul: applyForced (first fields) always overwrites with "1" on wide-mode entry, saves pre-wide value (empty if it was a hint itself), restores on exit; applyHint (last, mask) only applies on empty/hinted fields. onfocus=el.select() when hint-class so typing replaces rather than appends. onblur calls _updateGwExample() when field emptied to re-apply hint. _readGwDeviceOverrides skips hint-class fields. Files: Sidebar-js.html)
  Previous: 2026-05-01 (GW Auto Config layout: stacked — standard group on top (4 rows: GW MLAG phy / GW phy / GW Anycast / GW VARP, each grid-column:2/4), then calc hint divider (gw-ex4/6-calc, always visible), then wide-prefix group (same 4 rows below). Single MLAG phy row per group (id=3, last+3). IDs: gw-ex4-std-mphy / gw-ex4-wide-mphy (singular, no -1/-2 suffix). Files: Sidebar.html, Sidebar-js.html)
  Previous: 2026-05-01 (GW Auto Config section restructured: always shows both standard (col2) and wide-prefix (col3) columns side-by-side. "GW IPv4/6 phy" renamed to "GW MLAG phy" (last+id, MLAG pair device IDs 3&4). New "GW phy" row added showing just last (non-LEAF/SPINE formula, no device-id offset). Calc hint (gw-ex4/6-calc) always visible with static annotation. Per-device first-field guard: _updateGwExample() locks all gw_ov_{dev}_v4/v6_first override inputs to "1" in wide mode, saving/restoring via data-saved-wide attribute. Standard column uses configured first (_gwV4FirstSaved in wide mode); wide column always uses "1". Masks: std col uses actual if >=24 else /24; wide col uses actual if <24 else /16. Files: Sidebar.html, Sidebar-js.html)
  Previous: 2026-05-01 (Wide-prefix calc hint: gw-ex4-calc / gw-ex6-calc hidden divs added to Auto Config GW section (grid-column:1/4, display:none by default), appearing between the GW IPv4/IPv6 section headers and the phy rows. In wide mode (mask < /24 or < /64), _updateGwExample() shows amber-colored oct2 (f-dyn span) with the concatenated leading group (1·15=115) and oct3 (f-dyn span). Files: Sidebar.html, Sidebar-js.html)
  Previous: 2026-05-01 (Formula key box cleaned up: example rows removed from grid; separate dim annotation block added below grid showing "Wide-prefix e.g. VLAN 1520 (oct2=15, oct3=20): IPv4 /16 → 1·15=115.20.0.last/16 / IPv6 /32 → 1·15=115:20:0::last/32". Files: Sidebar.html)
  Previous-2: 2026-05-01 (Wide-prefix first-field lock: in wide mode (IPv4 < /24, IPv6 < /64) the GW first-field is auto-set to "1" and disabled — "1" is the only universally valid prefix digit for VLANs 1-4094 (any larger digit overflows 255 for VLANs ≥ 1000). Switching back to standard mode restores the saved value. State tracked via _gwV4WideMode/_gwV6WideMode booleans and _gwV4FirstSaved/_gwV6FirstSaved; labels switch "First Octet"↔"Prefix" and "First Hextet"↔"Prefix". lbl_gw_v4_first / lbl_gw_v6_first IDs added to Sidebar.html labels. Files: Sidebar.html, Sidebar-js.html)
  Previous: 2026-05-01 (Auto Config panel GW examples now dynamic: IDs gw-ex4-{phy1,phy2,vip,anycast} and gw-ex6-{phy1,phy2,vip,anycast} added to Sidebar.html; mode badges gw-ex4-mode/gw-ex6-mode show 'standard'/'wide-prefix'. _updateGwExample() rebuilds innerHTML using current first/last/mask inputs on every field change and modal open. _onMaskInput() validates mask range with inline error-msg span + has-error class. All 4 mask inputs now have title tooltips and error-msg spans. oninput wired in openIpConfigModal() — GW first/last inputs also trigger _updateGwExample(). Files: Sidebar.html, Sidebar-js.html)
  Previous: 2026-05-01 (Mask range validation in saveIpConfig(): GW IPv4 /16–/32, GW IPv6 /32–/128, P2P IPv4 /24–/32, P2P IPv6 /64–/128 — alert+return on out-of-range. Files: Sidebar-js.html)
  Previous: 2026-05-01 (GW formula: IPv4 and IPv6 now have independent prefix cutoffs. IPv4 cutoff: gw_v4_mask < /24. IPv6 cutoff: gw_v6_mask < /64. Wide-prefix assembles {first}{oct2}.{oct3}.0.{last} (IPv4) or {first}{oct2}:{oct3}:0::{last} (IPv6). Standard assembles {first}.{oct2}.{oct3}.{last} or {first}:{oct2}:{oct3}::{last}. getIpBlock() uses gwPfx4/gwPfx6 separately; vx1 block uses gwPfx4_vx1/gwPfx6_vx1. Files: Code.gs)
  Previous: 2026-05-01 (Per-device GW override inputs: placeholders now show the current global GW value (v4_first/last/mask, v6_first/last/mask) read from the modal DOM at build time; _gwDeviceRow() gains optional globalGw param; _buildGwDeviceOverrideTable() reads ip_gw_* fields and passes globalGw to renderDev. Files: Sidebar-js.html)
  Previous: 2026-05-01 (GW override table group sort: groups now sorted by sheet position of their first member (not creation order); sortedGroups = groupRects.slice().sort by devices.findIndex of first member. Files: Sidebar-js.html)
  Previous: 2026-05-01 (Group rects not visible in linear/flex on initial load: loadGroupRects() was only called when switching to holistic view — changed to unconditional call in toggleViewMode() so groupRects is always populated from localStorage before any view renders. Files: Sidebar-js.html)
  Previous: 2026-05-01 (Group rect modal member checklist sort: sort moved from _getTopologyDevices() into openGroupRectModal() where allDevicesData is guaranteed populated; uses parseInt(sheetIndex) with NaN→MAX_SAFE_INTEGER fallback so non-Arista devices sort to end; Arista devices sorted ascending by sheetIndex. Files: Sidebar-js.html)
  Previous: 2026-05-01 (Group rect modal member checklist: each device now shows a [sheetIndex] badge (gray, 10px) between the checkbox and device name, sourced from allDevicesData[devId].sheetIndex. Files: Sidebar-js.html)
  Previous: 2026-05-01 (Group rect member checklist + GW override table: _getTopologyDevices() now sorts by allDevicesData[id].sheetIndex (ascending) instead of alphabetically — matches visible sheet order in all views. _buildGwDeviceOverrideTable() non-adjacent members: builds deviceIndexMap, splits each group's members into contiguous runs (a gap exists when any device between two consecutive members by sheet position is not in the group), emits a separate group header per run with the same label. Files: Sidebar-js.html)
  Previous: 2026-05-01 (Per-device GW override: group rows by topology group rects (holistic view groups stored in localStorage as holistic_group_rects). _buildGwDeviceOverrideTable() calls loadGroupRects() then renders devices per group with group label headers; ungrouped devices shown under "Other" when groups exist, or flat when no groups defined. Removed rack grouping. Files: Code.gs, Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-05-01 (Per-device GW override UI refinements: added v4_mask/v6_mask per-device override fields (applied in getDeviceConfig()); device table now shows [id] before device name; devices grouped by rack (from Device Manager) with rack group headers; toggle uses .gw-subsection-toggle so it reads as part of the GATEWAY section, not a new top-level section; getIpPreferencesForModal() now calls getExistingDevices() for full id/rack/hostname data. Files: Code.gs, Sidebar.html, Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-05-01 (Per-device GW first/last override: new GW_DEVICE_OVERRIDES DocumentProperty stores {devName:{v4_first,v4_last,v6_first,v6_last}}. getDeviceConfig() applies overrides to ipPrefs before passing to generateComplexL3Block(). ipConfigModal now has collapsible PER-DEVICE GW OVERRIDES section — table of all Arista devices with per-device first/last inputs, MLAG pair visual grouping [M] badge + blue left border, mismatch warning on save. New Code.gs: getGwDeviceOverrides(), saveGwDeviceOverrides(), getIpPreferencesForModal(). New Sidebar-js: _toggleGwAdvanced(), _buildGwDeviceOverrideTable(), _gwDeviceRow(), _readGwDeviceOverrides(), _highlightGwMlagMismatch(), _checkGwMlagMismatches(). Files: Code.gs, Sidebar.html, Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-04-30 (GW SVI idempotency: all GW SVI blocks — getIpBlock() LEAF branch, getIpBlock() non-LEAF branch, and vx1 SVI block — now unconditionally emit 6 cleanup commands before any IP assignment: `default ip address`, `default ip address virtual`, `default ip virtual-router address`, `default ipv6 address`, `default ipv6 address virtual`, `default ipv6 virtual-router address`. Makes GW config fully idempotent for IP changes, anycast↔VARP switches, and LEAF↔non-LEAF role changes. Files: Code.gs)
  Previous: 2026-04-30 (Reverted topology window size cycling feature — removed TOPOLOGY_SIZES, TOPOLOGY_SIZE_KEY, getTopologySizeIdx(), setTopologySizeIdx(), reopenTopologyAtSize() from Code.gs; removed TOPOLOGY_SIZES, _topologySizeIdx, _updateSizeButtons(), cycleTopologySize() from Sidebar-js.html; removed .tb-size-label from Sidebar-css.html; showTopologyWindow() back to fixed setWidth(1600).setHeight(900). VERSION 260430.96. Files: Code.gs, Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-04-30 (Fix black screen: removed <?= defaultSizeIdx ?> template var from Sidebar-js.html — GAS template evaluation failure was causing a SyntaxError that prevented ALL JavaScript from loading. _topologySizeIdx now hardcoded to 2 (L) as default; actual saved preference loaded async via google.script.run.getTopologySizeIdx() in initApp() and applied to buttons on response. VERSION 260430.93. Files: Sidebar-js.html)
  Previous: 2026-04-30 (defaultSizeIdx now set on ALL Sidebar template callers: showCustomViewUi, showDeviceManagerUi, showDeviceDataUi each now call getTopologySizeIdx() and assign template.defaultSizeIdx — ensures template var is always defined; simplified Sidebar-js.html back to plain <?= defaultSizeIdx ?>. VERSION 260430.94. Files: Code.gs, Sidebar-js.html)
  Previous: 2026-04-30 (Fix SyntaxError on non-topology Sidebar modes: _topologySizeIdx template var now uses fault-tolerant expression `(typeof defaultSizeIdx !== 'undefined' ? defaultSizeIdx : 2)` — avoids blank output when Sidebar opened as Device Manager/Schema/etc. which don't set defaultSizeIdx. VERSION 260430.91. Files: Sidebar-js.html)
  Previous: 2026-04-30 (Topology window size cycling: TOPOLOGY_SIZES presets [S 1100×650, M 1400×800, L 1600×900, XL 1920×1060] in Code.gs + client mirror in Sidebar-js.html. APP toolbar: − [label] + buttons cycle size via google.script.host.setWidth/Height (in-place, no reload); preference saved to UserProperties via setTopologySizeIdx(). _updateSizeButtons() called at initApp(). Fit button removed from VIEW toolbar. VERSION 260430.92. Files: Code.gs, Sidebar.html, Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-04-30 (Vx1 VLAN stanza generation: collectDeviceData() Vx1 block now adds vlan_ values to allVlans (generates vlan N/name VLANNNNN__TA) in addition to gwVlans — previously Vx1 VLANs were VNI-mapped but no system VLAN table entry was created. Both own-device and MLAG-peer Vx1 paths updated. VERSION 260430.88. Files: Code.gs)
  Previous: 2026-04-30 (UserGuide audit section updates: removed stale "MLAG Destination Conflict" bullet (no corresponding audit flag exists); added "Orphan Po (Arista only)" bullet after "Stale Po value on Ethernet mode"; updated Audit Check Settings table — Port-Channel rowspan 4→5, added "Orphan Po (Arista only)" row; removed stale "All 24 checks" count from prose. VERSION 260430.87. Files: UserGuide.html)
  Previous: 2026-04-30 (Fit button + default zoom increase: (1) "Fit topology to screen" icon-btn added as first button in VIEW toolbar — calls zoomToFit(), expand-arrows SVG icon; (2) focusView() padding 100→60px (tighter fit = larger apparent size), maxZoom clamp 1.2→1.5 (small topologies now zoom in further by default). VERSION 260430.84. Files: Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-30 (Po audit refinement: dropped auditFlagPoNoPeerPo (redundant with auditFlagPoAsymmetry which already catches one-sided Po declarations at the link level); kept auditFlagPoNoPeer (orphan Po — Po mode with no peer device) gated to Arista-only devices via `allDevicesData[node.device].type !== 'non-arista'` (non-EOS single-device rows are a valid pattern). _seenPoNoPeerPo dedup Set also removed. VERSION 260430.83. Files: Sidebar-js.html)
  Previous: 2026-04-30 (Two new audit flags for invalid Po configs: auditFlagPoNoPeerPo and auditFlagPoNoPeer. Both fire at most once per device:Po pair via dedup Sets. Checks L and L1 in runValidation() CHECK GROUP 2. VERSION 260430.81. Files: Sidebar-js.html)
  Previous: 2026-04-30 (Po peer-Po description fix: (1) Root cause: poGroup is always deleted during JSON sanitization (circular ref guard) — d.poGroup branch in generateConfig was dead code; (2) Fix: rescue peerPo scalar from poGroup before deletion (`if pp.startsWith('Po') → cleanDetails.peerPo = pp`); (3) remoteMlagPair branch now appends d.peerPo when available (`-> bkd566-Po10 __TA`), omits when absent (`-> bkd566 __TA`); (4) Reverts wrong poVal append from prior commit (local Po ≠ peer Po for regular Pos). VERSION 260430.80. Files: Code.gs)
  Previous: 2026-04-30 (Po remoteMlagPair description: append local poVal (peer shares same MLAG ID) — `-> bkd566 __TA` → `-> bkd566-Po4062 __TA`. Consistent with poGroup path that already includes remotePo. VERSION 260430.79. Files: Code.gs)
  Previous: 2026-04-30 (Topology Config Operation modal redesign — tab layout: 3 horizontal action cards replaced with compact genall-action-row (3 groups + separators); Targets moved above action row; tab strip [Generate|Push|Cleanup] added; each tab panel is position:absolute inset:0 overflow-y:auto filling all remaining modal height (~60vh min); #generateAllLeft=Generate tab, #generateAllRight=Push tab, #reconResultsBody=Cleanup tab (reconResults wrapper removed); _switchGenTab(tab) toggles active tab and panel display; runGenerateAll/pushAllConfigs/runCleanup each auto-switch to their tab; _doRunCleanup signature drops reconPanel param. VERSION 260430.78+. Files: Sidebar.html, Sidebar-css.html, Sidebar-js.html)
  Previous: 2026-04-30 (eAPI multi-error split + diagnostic filter: (1) _push_config except RuntimeError block now splits raw_msg by 'CLI command N of M' boundaries before calling _annotate_eapi_errors — each per-command entry gets its own correct block-context header instead of one shared header from the first failing command only; (2) _diagnose_eapi_errors skips entries that already have a specific % reason from eAPI (no 'could not run command') — avoids redundant SSH for commands EOS already explained. Mirrored in embedded template. VERSION 260430.49→260430.50. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-30 (_eapi_push stopOnError:False: added "stopOnError":False to runCmds params so EOS continues executing remaining configure session commands after a failure instead of aborting at the first error. % errors for all failed commands appear in each command's text output; _extract_eos_errors finds all of them in one joined pass. Degrades gracefully: if EOS ignores it for configure sessions, RuntimeError path still catches the top-level error. Mirrored in embedded template. VERSION 260430.48→260430.49. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-30 (Click-to-filter on port columns ET/MODE/CAT/PO: added `_portColFilter(val)` helper before `filterTopology()`; click handlers on colEt/colMode/colPo with stopPropagation; colType always clickable (P2P/GW/L2); colMode/colPo only clickable when non-empty; rowText now includes `'po'+poId` for unambiguous PO matching. VERSION 260430.75. Files: Sidebar-js.html)
  Previous: 2026-04-30 (_diagnose_eapi_errors % reason formatting: % reason now on its own indented line — re.sub replacement changed from ': ' + reason to ':\n  ' + reason (with each \n in multi-line reasons also indented by 2 spaces). Mirrored in embedded template. VERSION 260430.47→260430.48. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-30 (Interface description completeness — __TA on all TA-managed interfaces: (1) Lo0: added `description ROUTER_ID __TA`; (2) Lo1 (MLAG VTEP): `VTEP_MLAG_SHARED` → `VTEP_MLAG_SHARED __TA`; (3) Vxlan1 (MLAG + standalone): added `description VTEP __TA`; (4) Vlan4094: `MLAG_CONTROL_PLANE` → `MLAG_CONTROL_PLANE __TA`; (5) Vlan4093: `MLAG_L3_PEERING` → `MLAG_L3_PEERING __TA`. CLAUDE.md: __TA tagging constraint updated. VERSION 260430.71. Files: Code.gs, CLAUDE.md)
  Previous: 2026-04-30 (Config modal search hint: placeholder updated to "Filter… or :N to jump to line"; .cfg-search-hint dim strip added between toolbar and <pre> body — italic 11px left-accent dim note with inline <code> styling for :N. VERSION 260430.70. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-30 (_diagnose_eapi_errors rewrite — index-based % grouping: replaced fragile PTY prompt-tracking with group-based collection: consecutive % lines between non-% output lines form one group; groups paired to failed commands by index; multiple % lines per command joined by \n. Also switched abort pattern to end + re-enter + abort (mirrors real push) for clean session teardown from any sub-mode depth. VERSION 260430.46→260430.47. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-30 (Config modal line-number jump: type :N (e.g. ":12314") in configSearch to jump to that line — full config rendered with the target line highlighted in amber (cfg-line-hl: yellow-tinted bg + amber left border + amber gutter number) and scrolled into view; badge shows "Line N". _renderWithLineNums() gains optional highlightLine param; wraps the target row in <span class="cfg-line-hl">. VERSION 260430.66. Files: Sidebar-css.html, Sidebar-js.html)
  Previous: 2026-04-30 (_diagnose_eapi_errors root cause fix: eAPI configure session failures surface as top-level JSON-RPC "error" — _eapi_push raises RuntimeError before _extract_eos_errors or _diagnose_eapi_errors are reached. Fix: wrap _eapi_push in try/except RuntimeError in _push_config eAPI branch; if message matches _EAPI_FAILED_CMD_RE run _annotate_eapi_errors + _diagnose_eapi_errors on [raw_msg] and return {"ok":False,"error":'\n'.join(eos_errs)}; re-raise all other RuntimeErrors. Mirrored in template ("\\n".join). VERSION 260430.45→260430.46. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-30 (DevVis + search interaction fix: (1) applyDeviceVisibility() now unconditionally clears .dimmed and sets display:'' for non-hiddenDevices nodes — stale .dimmed from a prior search run no longer blocks showing a DevVis-re-shown device; if search is active, filterTopology() is re-invoked to re-apply dimming cleanly; (2) redrawLinks() nodeCache loop skips hiddenDevices nodes even if they lack .dimmed — prevents ghost (0,0) entries in nodeCache from hidden nodes. VERSION 260430.61. Files: Sidebar-js.html)
  Previous: 2026-04-30 (_diagnose_eapi_errors VERBOSE print: added VERBOSE terminal log before SSH diagnostic session — "[diag] ip: SSH diagnostic for N error(s) — session_name". Mirrored in embedded template. VERSION 260430.44→260430.45. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-30 (_diagnose_eapi_errors: new DeviceBridge instance method that fires after _annotate_eapi_errors when eAPI errors contain 'could not run command'. Opens a throwaway configure session (topoassist_<epoch>) via SSH, replays each failed command in its block context (block header + failed cmd), captures the % reason EOS emits on the PTY, then aborts — nothing ever committed. percent_map keyed by (block_cmd, failed_cmd) tuple; 're.sub r':could not run command' → '% reason' replaces the generic suffix in each matching eos_errs entry. Falls through (returns eos_errs unchanged) on any exception — SSH unavailable or timed out. Call site: _push_config eAPI branch, after _annotate_eapi_errors, before setting _asn_extra["eos_errors"]. Mirrored in embedded template (re.sub uses \\s not \s in template). VERSION 260430.43→260430.44. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-30 (P2P SVI description: SVI loop now emits `P2P_{vrf}_{vlan} __TA` for SVIs where ip_type_ includes 'p2p' (vrf omitted when absent); bare `__TA` reserved for all other non-GW ip_types. `_sviIsP2p` check added alongside `_sviIsGw`; `_resolveVrfAtIndex` extracted to `_resolvedVrf` to avoid double call. CLAUDE.md: SVI description naming constraint updated to cover all three cases (GW/P2P/other). VERSION 260430.68. Files: Code.gs, CLAUDE.md)
  Previous: 2026-04-30 (GW SVI description naming overhaul: (1) Reverted non-LEAF virtual IP cleanup (`no ip address virtual` / `no ipv6 address virtual` removed — unnecessary); (2) getIpBlock GW branch now emits full description for ALL roles: LEAF+EVPN→`ANYCAST_GW_{vrf}_{vlan} __TA`, LEAF non-EVPN→`GW_{vrf}_{vlan} __TA`, non-LEAF→`GW_{vrf}_{vlan} __TA`; VRF included when set, omitted when absent; (3) SVI loop: `_sviIsGw` check skips generic `description __TA` base for GW SVIs — getIpBlock provides the full label. CLAUDE.md: removed reverted non-LEAF cleanup constraint; added GW SVI description naming constraint. Files: Code.gs, CLAUDE.md)
  Previous: 2026-04-30 (Non-LEAF GW cleanup: non-LEAF path in getIpBlock now emits `no ip address virtual` before `ip address` and `no ipv6 address virtual` before `ipv6 address` to evict stale virtual IPs left from a prior LEAF config — EOS is additive for ip address virtual and ipv6 address virtual. VERSION 260430.56. Files: Code.gs)
  Previous: 2026-04-30 (Bridge auto-reconnect interval reduced 60s → 30s. VERSION 260430.54. Files: Sidebar-js.html)
  Previous: 2026-04-30 (Signal dot on #btnGenerateAllToolbar: 7px absolute-positioned dot (top:2px right:2px) shows operation state — orange pulsing while running (gen-sig-running), green/orange/red glow for 5s on completion (gen-sig-ok/warn/err). _setGenToolbarSignal(state) sets class + auto-clears after 5s. _setGenToolbarActive(on, signalState) extended with optional signalState param. _setResultRow() now downgrades _genSignalState ('ok'→'warn' on #f59e0b, →'err' on #ef4444/#f87171). Push completion uses accumulated _genSignalState; stops yield 'warn'; generate success/failure yield 'ok'/'err'. VERSION 260430.53. Files: Sidebar.html, Sidebar-css.html, Sidebar-js.html)
  Previous: 2026-04-30 (Topology Config Operation modal: Generate Configs description updated; per-device Copy button added to result rows — _copyDeviceConfigByIndex(idx, btnEl) looks up window._generateAllConfigs[idx] and calls _copyToClipboard. VERSION 260430.52. Files: Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-30 (_print_transport_status: Jump host line suppressed when jump_host is empty — only printed when a jump host is actually configured. VERSION 260430.51. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-30 (blank line before _print_transport_status() in startup banner — one blank line between "Ctrl+C to stop." and transport status. VERSION 260430.50. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-30 (remove _print_transport_status() from startup banner — transport no longer shown at boot to avoid stale display; only printed on transport change via UI. VERSION 260430.48. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-30 (3 changes: (1) writeConfigsToSheet column-per-device layout: each device gets its own column — row 1 = device names (bold, dark header, frozen), rows 2+ = config lines; 500px column width uniform; trims sheet to exactly numRows×numDevices to stay under 10M cell limit; (2) generateAllModal renamed to "Topology Config Operation"; Generate Configs + Stop buttons moved from header into first genall-action-card; header now only has title + [−][×]; (3) allVlans L2-only guard: sp_mode_ isL2 check gates vlan_ and nv<N> from entering allVlans. VERSION 260430.47. Files: Code.gs, Sidebar.html)
  Previous: 2026-04-30 (Error/info handling coverage — 3 fixes: (1) Phase 3 verify eos_warnings: bulk pushAllConfigs Phase 3 now captures _vEosWarns=_eosWarnings(vRes); combined with _pushEosWarns into combinedWarnText shown in amber box across all Phase 3 outcomes (clean/partial/verify-failed); (2) Bulk push display parity: _setResultRow() gains 8th infoDetails param (teal #f0f9ff box); _orphanDetailText(orphans) helper builds "Auto-removed orphans: Et1, BGP ..." text; orphan details shown in teal box on all non-skip outcomes; warnSuffix in label even when eos_errors present; eos_warnings always shown (removed !_pushEosErrs guard); (3) _detect_orphans per-category error surfacing: bare except-pass in BGP/VLAN/VRF/OSPF detection blocks now captures to detection_errors{category:msg[:80]}; returned as detection_errors:{}; _renderCleanupResults renders amber warning box per device listing failed categories + error; all mirrored in embedded template. VERSION 260430.43. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-30 (_print_transport_status() helper extracted in device_bridge.py and embedded template: replaces inline transport block in startup banner and /settings change log — terminal always shows live transport state at boot and on every transport change via UI. VERSION 260430.43. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-30 (configModal always shows line numbers: showConfigModal initial render + oninput clear path both changed from highlightEosConfig(text) to _renderWithLineNums(text.split('\n').map((text,i)=>({text,lineNum:i+1}))). Line numbers now visible in full config view for error correlation (e.g. "CLI command 12314 of 12447 failed"). VERSION unchanged. Files: Sidebar-js.html)
  Previous: 2026-04-30 (embedded template startup banner: fix stale (-P) → (-p) in Timeout line — device_bridge.py already used -p but embedded template still showed -P. VERSION 260430.39. Files: Sidebar-js.html)
  Previous: 2026-04-30 (eAPI error context annotation: _annotate_eapi_errors(errors, lines) added to device_bridge.py and embedded template. eAPI push errors report 'CLI command N of M <cmd> failed: reason' with no block context (no PTY prompt echoes). _annotate_eapi_errors parses the failed command name from each error via _EAPI_FAILED_CMD_RE, locates it in lines[], scans backwards for the nearest _BLOCK_CMD_RE match (interface/router bgp/ospf/vlan/address-family), and injects that block command as a context line immediately before the error. Deduplicates: same block command not re-injected if already added for the previous error. Called in _push_config eAPI branch after _extract_eos_errors: eos_errs = _annotate_eapi_errors(eos_errs, lines). _BLOCK_CMD_RE also added to embedded template (was missing). VERSION 260430.39 in both device_bridge.py and embedded template. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-30 (configModal block-aware search + line numbers: (1) _cfgLineClass(line) extracted from highlightEosConfig — returns CSS class string or null; highlightEosConfig() now calls _cfgLineClass() (no logic change); (2) _splitConfigIntoBlocks(rawConfig) — splits config into [{header,lines,sep}] block objects, each line as {text,lineNum}; (3) _renderWithLineNums(lineObjs) — renders filtered lines with right-aligned line number gutter (cfg-lno span, non-selectable) + syntax highlighting via _cfgLineClass; (4) searchInput.oninput rewritten — uses _splitConfigIntoBlocks + block filter (ANY line match → whole block shown) + _renderWithLineNums; badge shows "N blocks · M lines"; (5) Sidebar-css.html — .cfg-lno added (color:#94a3b8, font-size:10px, non-selectable). VERSION 260430.36. Files: Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-04-30 (device_bridge.py + embedded template startup banner/help cleanup: (1) startup banner — removed "Options" and "Legacy" lines (cluttered, now in -h only); (2) help text (-h) — restructured: core options first (-b/-t/-p/-v), then "Transport and credentials via sidebar UI" note, then Legacy section (-m/-u/-eu/-ep/--eapi-port/--eapi-http) with clear description; (3) /settings change log — transport change now prints "Transport → EAPI (eAPI user: admin)" instead of bare "Transport CHANGE: ssh → eapi"; field changes print "key: old → new" instead of "key CHANGE: old → new"; (4) VERSION bumped 260430.28 → 260430.36 in both device_bridge.py and embedded template. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-30 (Device Operations modal overhaul: (1) Modal renamed "All Device Config" → "Device Operations"; (2) Generate All card removed — "Generate Configs" + "Stop" buttons moved into modal header; info-box--dim description added at top of body explaining Generate Configs; (3) "Push All" → "Push Targets" with updated description (cleanup + push, no diff review) + SVG ⚠ warning replacing Unicode &#9888;; (4) "Cleanup" → "Clean-up Targets" with updated description; (5) "Push Targets" device filter label → "Targets" (scope shared by both operations); (6) Post-cleanup per-device result panel: _doCleanDevItems success handler replaces reconResultsBody with per-device rows (N items removed / Error) + summary line with timestamp; (7) JS text: btn texts updated — Generate Configs/Re-Generate Configs/Push Targets/Scanning…/Clean-up Targets/Stop Clean-up; status messages updated — "check Targets", "Push Targets is in progress", "Clean-up done/error". VERSION 260430.34. Files: Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-30 (Modal dock for minimized modals: (1) Sidebar.html — #modalDock div added after #editOverlay (fixed bottom strip, hidden by default); (2) Sidebar-css.html — #modalDock (position:fixed bottom:0, z-index:29000, flex row, auto-shown/hidden) + .modal-dock-chip (22px chip with JetBrains Mono 11px, hover blue border); (3) Sidebar-js.html — _updateModalDock() scans .modal-std.modal-minimized elements (skipping display:none), builds chips with .modal-header-title text + upward chevron SVG, chip onclick calls toggleModalMinimize(id); dock style.display toggled flex/none based on chip count; toggleModalMinimize() calls _updateModalDock() at end. VERSION 260430.32. Files: Sidebar.html, Sidebar-css.html, Sidebar-js.html)
  Previous: 2026-04-30 (Orphan cleanup visibility in push UI: _push_config now inlines _detect_orphans + _orphans_to_cmds instead of calling _find_ta_orphans wrapper (wrapper removed from both device_bridge.py and embedded template). When orphans are cleaned, response dict includes orphans_cleaned:{interfaces,bgp,vlans,vrfs,ospf}. Single-push confirm modal: _injectOrphanCleanedBanner(orphans) appended to body — teal info banner "Auto-removed N orphan(s): Et1, BGP 10.0.0.1, VLAN 100…". Push All: orphanSuffix added to result row label "· N orphan(s) removed". VERSION device_bridge.py + template 260430.28. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-30 (allVlans L2-only guard: collectDeviceData() analyzeRow() now reads sp_mode_ and sets isL2 = spMode.includes('l2'). vlan_ values only added to allVlans when isL2=true (l2-et-access/trunk, l2-po-access/trunk). Native VLAN (nv<N>) also gated on isL2. L3 ports (l3-et-int, l3-et-sub-int, l3-po-int, l3-po-sub-int) no longer generate spurious vlan N/name entries for encap dot1q tags. GW VLANs unaffected — GW ports are l2-et-trunk/l2-po-trunk so isL2=true. VERSION 260430.27. Files: Code.gs, CLAUDE.md)
  Previous: 2026-04-30 (Fix minimized modal disappearance + active-push guard: (1) closeAllModals() — skips modals with modal-minimized class (overlay onclick no longer destroys minimized modals); adds _isOperationActive() guard that returns early when btnStopPush/btnStopCleanup/btnStopGenerate has stop-active class, preventing overlay click from dismissing a modal mid-push/generate/cleanup; (2) Escape LIFO handler — adds !el.classList.contains('modal-minimized') to the display check, so Escape closes the foreground modal without touching any minimized modal in the background. VERSION 260430.26. Files: Sidebar-js.html)
  Previous: 2026-04-30 (Bridge docstring + VERSION fixes: (1) transport options docstring reordered — eapi listed first as default, ssh second in both device_bridge.py and embedded template; (2) /health endpoint docstring hardcoded version "260429.1" replaced with "<VERSION>" placeholder in both files; (3) embedded template VERSION bumped 260430.16→260430.17 to match device_bridge.py. FILES: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-30 (Cleanup overhaul: unified _detect_orphans + _orphans_to_cmds architecture. _detect_orphans(ip, config_text, all_device_names, expected_ports) is the single detection function for all 5 orphan categories (interfaces/bgp/vlans/vrfs/ospf) — used by push path via thin _find_ta_orphans wrapper and by Cleanup UI via /reconcile handler. _orphans_to_cmds(orphans) is a module-level pure function converting structured dict to flat EOS CLI. _reconcile_device removed. Cleanup UI renamed from Reconcile: runCleanup/_doRunCleanup/_renderCleanupResults replace runReconcile/_doRunReconcile/_renderReconcileResults; buttons btnGenAllCleanup/btnStopCleanup; new checkbox UI with Select All / per-device checkbox / per-item checkbox / Clean Selected(N) / Clean All buttons; _cleanupItemStore[] index avoids JSON serialization in HTML attributes; _buildCleanupConfig(items) is JS mirror of _orphans_to_cmds for selected items; _cleanupResults/_cleanupIpMap state vars; _doCleanDevItems() dispatches to /pushconfig. All changes synced to embedded template in downloadBridgeScript(). FILES: device_bridge.py, Sidebar-js.html, Sidebar.html, CLAUDE.md)
  Previous: 2026-04-30 (OSPF orphan cleanup in _find_ta_orphans: EOS configure sessions are additive — pushing a fresh router ospf block does NOT remove pre-existing 'no passive-interface' sub-commands for deleted interfaces. Added 5th cleanup category to _find_ta_orphans: after identifying orphan interfaces, build orphan_ifaces set, query 'show running-config | section router ospf' and 'router ospf3', parse each router ospf[3] N [vrf VRF] context block, find 'no passive-interface X' entries where X is in orphan_ifaces, emit 'router ospf[3] N [vrf VRF] / passive-interface X / !' cleanup. Handles default VRF and per-VRF OSPF contexts, both OSPFv2 and OSPFv3. Best-effort (exception silently skipped). Same change applied to embedded template in Sidebar-js.html. VERSION 260430.15→260430.16. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-30 (eAPI default + jump host UI: (1) Default transport changed from ssh→eapi in _cfg and _loadBridgeTransportPrefs(); default eapi_proto https, default eapi_port 443 in both UI HTML attrs and JS fallbacks; (2) jump_host/jump_user added to _cfg (jump_host initialized from JUMP_HOST module var; jump_user from BRIDGE_JUMP_USER env); _jump_args() helper returns ['-J','user@host'] or [] based on _cfg; all 3 SSH subprocess call sites now use *_jump_args() instead of inline JUMP_HOST conditional; (3) /settings allowed set extended with jump_host/jump_user; terminal log skips ssh_pass/eapi_pass only — jump_host/jump_user are logged; (4) Sidebar.html: eAPI radio checked by default; HTTPS radio checked; port value/placeholder=443; jump host checkbox (id=chkJumpHost) + HOST (id=bridgeJumpHost, default bus-home) + USER (id=bridgeJumpUser) fields added to SSH section, hidden until checkbox checked; (5) Sidebar-js.html: onJumpHostToggle() shows/hides bridgeJumpFields; _applyBridgeSettings() includes jump_host/jump_user (empty string when unchecked); _loadBridgeTransportPrefs() restores checkbox+fields from localStorage; startup banner uses _cfg['jump_host']/_cfg['jump_user'] not JUMP_HOST; same changes synced to embedded template. FILES: device_bridge.py, Sidebar-js.html, Sidebar.html)
  Previous: 2026-04-30 (__TA ownership tag rename: changed all config ownership stamps from `#TA` to `__TA` across all files. Rationale: EOS grep treats `#` as a special character — `grep #TA` fails, `grep __TA` works. Double underscore is collision-resistant (impossible to appear accidentally in user descriptions) and consistent (VLAN names already used `_#TA` separating the tag from content). Changes: (1) Code.gs — all interface/SVI/VRF descriptions, BGP neighbor descriptions, VLAN names (VLAN0010__TA), MLAG VLAN names (MLAG_L3_UNDERLAY_PEERING__TA, MLAG_CONTROL_PLANE__TA), desc_ guard check; (2) device_bridge.py — all _find_ta_orphans/_reconcile_device string matches and regex patterns; (3) Sidebar-js.html — embedded template + UI strings (reconcile results, orphan display, _buildExpectedPortsPerDevice comment); (4) Sidebar.html — Reconcile description; (5) UserGuide.html — all user-facing examples; (6) CLAUDE.md — Push sync invariant + __TA tagging constraint. device_bridge.py VERSION 260430.14→260430.15. Files: Code.gs, device_bridge.py, Sidebar-js.html, Sidebar.html, UserGuide.html, CLAUDE.md)
  Previous: 2026-04-30 (HTTP/HTTPS radio + settings terminal log: (1) Sidebar.html: eAPI HTTP checkbox replaced by explicit HTTP|HTTPS radio buttons (name="eapiProto", id="rbEapiHttp"/"rbEapiHttps") with auto-port change onchange="onEapiProtoChange()"; PORT row is now separate from PROTO row; (2) Sidebar-js.html: added onEapiProtoChange() — reads radio, auto-sets port (HTTP→80, HTTPS→443), calls _applyBridgeSettings(); _applyBridgeSettings() reads radio via document.querySelector('input[name="eapiProto"]:checked') instead of chkEapiHttp checkbox; _loadBridgeTransportPrefs() restores rbEapiHttp/rbEapiHttps radio state from localStorage; (3) device_bridge.py + embedded template: /settings handler now logs changed fields to terminal with flush=True — transport change: "[settings] Transport: old → new", other non-password fields: "[settings] key: old → new"; passwords never logged. VERSION 260430.14. Files: Sidebar.html, Sidebar-js.html, device_bridge.py)
  Previous: 2026-04-30 (Fix holistic-view regression in group rect visibility: renderGroupRectsInto() coordinate-based rect anyVisible check now guards with !_isHolisticView() — holistic nodes are SVG circles not .node divs, so the #canvas-surface .node[data-id] query always returns false and incorrectly hides all coordinate-based rects in holistic view. VERSION 260430.15. Files: Sidebar-js.html)
  Previous: 2026-04-30 (Transport settings redesign — credentials move from CLI to UI: (1) device_bridge.py + embedded template: METHOD/SSH_USER/EAPI_USER/EAPI_PASS/EAPI_PORT/EAPI_PROTO module-level constants replaced by _cfg dict; _ssh_base() now reads _cfg['ssh_pass'] and _cfg['ssh_port'] (sshpass gets actual password, port_arg added when non-22); POST /settings endpoint added — sidebar can update _cfg at runtime with no bridge restart; startup banner updated (Endpoints includes /settings, Options shows 'credentials via sidebar UI', Legacy line shows old CLI flags); (2) Sidebar.html: Transport card gains SSH fields (USER/PASS/PORT) shown when SSH selected; eAPI fields unchanged; all onchange handlers call _applyBridgeSettings(); (3) Sidebar-js.html: _getBridgeCmdLine() simplified to plain 'python3 device_bridge.py'; onBridgeTransportChange() shows/hides both SSH and eAPI field blocks; _saveBridgeTransportPrefs() + _loadBridgeTransportPrefs() replaced by _applyBridgeSettings() (saves to localStorage + POSTs to /settings fire-and-forget) and _loadBridgeTransportPrefs() (restores all fields, calls _applyBridgeSettings). Keys stored as bridgeCfg_* in localStorage. VERSION 260430.13. Files: device_bridge.py, Sidebar-js.html, Sidebar.html)
  Previous: 2026-04-30 (startup Options banner: added -m ssh|eapi to the one-line Options hint in both device_bridge.py and embedded template. VERSION 260430.11. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-30 (Global Config find bar: _gcfgJumpTo() always restores focus to the search input after every jump — oninput, Enter, Shift+Enter, and prev/next buttons. Removed restoreFocus parameter; inp.focus() is now unconditional (Ctrl+F bar behaviour: focus never leaves the find input). Fixes second-Enter deleting matched text from textarea. VERSION 260430.10. Files: Sidebar-js.html)
  Previous: 2026-04-30 (Global Config Template modal: (1) width widened from modal-medium (450px) to 700px via #globalCfgModal CSS override in Sidebar-css.html; (2) search focus-jump bug fixed — _gcfgJumpTo() now accepts restoreFocus param; globalCfgSearchUpdate() passes restoreFocus=true so typing in the find bar no longer steals focus to the textarea; Enter/Shift+Enter navigation leaves focus in textarea as intended; (3) Escape key now clears the find bar first (before closing the modal) — mirrors the configModal fix already in the global topologyKeyHandler. VERSION 260430.9. Files: Sidebar-css.html, Sidebar-js.html)
  Previous: 2026-04-30 (Fix coordinate-based group rects showing when search has no matches: renderGroupRectsInto() — non-member (coordinate-based) group rects now check if any .node[data-id] elements are visible before rendering; returns early if none visible, consistent with member-based rects which already returned null from _computeGroupBounds. VERSION 260430.7. Files: Sidebar-js.html)
  Previous: 2026-04-30 (eAPI transport mode: (1) device_bridge.py — CLI flags added: -m ssh|eapi (METHOD), -eu user (EAPI_USER), -ep pass (EAPI_PASS), --eapi-port N (EAPI_PORT), --eapi-http (EAPI_PROTO=http); _eapi_push/_eapi_cmds URLs updated to use EAPI_PROTO; _text_cmd(ip, cmd) dispatcher added — eAPI path calls _eapi_push, SSH path calls subprocess; 5 hardcoded SSH blocks in _find_ta_orphans (bgp-orphan, vrf-orphan), _find_bgp_asn_change, _reconcile_device (bgp-recon, vrf-recon) replaced with self._text_cmd(); (2) Sidebar-js.html embedded template — same changes as device_bridge.py (dual-copy sync invariant); (3) Sidebar.html bridgeModal — Transport settings card added after Bridge auto card: SSH/eAPI radio buttons; eAPI fields (user, pass, port, HTTP checkbox) shown only when eAPI selected; (4) Sidebar-js.html JS — _getBridgeCmdLine() builds the command string based on current transport selection; onBridgeTransportChange() shows/hides eAPI fields; _saveBridgeTransportPrefs()/_loadBridgeTransportPrefs() persist all transport prefs to localStorage; _loadBridgeTransportPrefs() called in openBridgeModal(); _checkBridgeHealth() "Start the bridge" hint now uses _getBridgeCmdLine() for dynamic command. VERSION 260430.6. Files: device_bridge.py, Sidebar-js.html, Sidebar.html)
  Previous: 2026-04-29 (Reconcile false-stale + Remove SyntaxError: (1) _reconcile_device expected_set now also parses config_text for 'interface X' lines — fixes SVI interfaces (Vlan1 from svi_vlan_) being flagged as orphans since they are not physical topology ports; same fix applied to embedded template; (2) Remove button onclick SyntaxError — JSON.stringify(dev/ip/name) produces double-quoted strings embedded inside double-quoted HTML attribute, breaking parser; fixed by wrapping all JSON.stringify() args in _esc() which converts " to &quot;; applied to all four buttons: unconfigureOrphan, unconfigureBgpOrphan, unconfigureVlanOrphan, unconfigureVrfOrphan. VERSION 260429.6→260429.7. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-29 (Three bridge fixes: (1) _extract_eos_errors PTY echo filter — '!' lines containing '#' and '(config' are PTY prompt echoes (not EOS warnings); guard added to elif branch in both device_bridge.py and embedded template; fixes garbage interleaved lines in EOS notes display; (2) runReconcile health pre-check removed — /health fetch with AbortSignal.timeout(4000) caused false "Device Bridge not running" on first attempt; now calls _doRunReconcile directly, consistent with runBridgeCheck and pushAllConfigs behavior; (3) _doRunReconcile hostname DNS fallback added — 'if (!ip) ip = n.id' mirrors Push All behavior so devices without explicit IP use hostname for DNS resolution. VERSION 260429.5→260429.6. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-29 (VLAN name #TA separator fix: EOS rejects spaces in vlan name command — changed VLAN0037 #TA → VLAN0037_#TA and MLAG vlan names MLAG_L3_UNDERLAY_PEERING #TA → MLAG_L3_UNDERLAY_PEERING_#TA, MLAG_CONTROL_PLANE #TA → MLAG_CONTROL_PLANE_#TA. Orphan detection unaffected (uses '#TA' substring check). Files: Code.gs, CLAUDE.md)
  Previous: 2026-04-29 (Reconcile silent-failure fix: anyFailed guard — when any device returns ok:false, _renderReconcileResults is called to show per-device errors instead of masking them as "All #TA config accounted for"; previously total===0 was true for all-failed runs, showing false clean. File: Sidebar-js.html)
  Previous: 2026-04-29 (Reconcile VLAN/VRF + BGP ASN + embedded template sync: (1) device_bridge.py _reconcile_device expanded — now accepts config_text=None; VLAN orphan detection (parse expected vlans from config_text, show vlan | json → VLANs with #TA not in expected set → vlan_orphans:[{vid,name}]); VRF orphan detection (SSH show running-config | section vrf instance → VRFs with '#TA' description not in expected set → vrf_orphans:[{name}]); BGP orphans now include asn field (parsed from 'router bgp ASN' line in bgp_text); /reconcile handler extracts config_text from each ipMap entry; VERSION 260429.4→260429.5; (2) Sidebar-js.html embedded template fully synced: /reconcile added to allowed paths, /reconcile handler added (parallel threads with config_text extraction), _reconcile_device method added (full VLAN/VRF/BGP detection matching device_bridge.py); (3) _renderReconcileResults updated: Stale VLANs + Stale VRFs sub-sections with Remove buttons; (4) unconfigureBgpOrphan(dev,ip,neighbor,asn,btn) — now accepts asn param, generates 'router bgp ASN / no neighbor X / !' config, pushes via _pushOrphanConfig; call site updated to pass o.asn; (5) Push Target filter applied to runReconcile via genAllDev_* checkbox filter; Stop button added with AbortController; _resetStopReconBtn helper added. Files: device_bridge.py, Sidebar-js.html, Sidebar.html)
  Previous: 2026-04-29 (#TA tagging expanded to VLANs, VRFs, and orphan cleanup extended to match: (1) Code.gs generateSystemBlocks — replaced 'default vlan 1-4094' + compressed range with individual 'vlan N / name VLAN0010 #TA / !' blocks per VLAN (4-digit zero-padded); VRF blocks now include ' description VRFNAME #TA'; (2) generateMlagConfig — MLAG VLAN names updated to 'MLAG_L3_UNDERLAY_PEERING #TA' and 'MLAG_CONTROL_PLANE #TA' for consistency (no cleanup change — skip list unchanged); (3) generateConfig desc_ guard — physical and PO description Rule 1 now strips double-#TA (guard: if desc already contains #TA, don't append again); (4) device_bridge.py + embedded template _find_ta_orphans expanded: VLAN orphan cleanup (show vlan | json → find #TA-named VLANs not in expected set from config_text → no vlan X; skips 4093/4094) + VRF orphan cleanup (SSH show running-config | section vrf instance → find VRFs with '#TA' in description not in expected set → no vrf instance VRFNAME); VERSION 260429.3→260429.4. Files: Code.gs, device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-29 (BGP #TA orphan cleanup extended to push: (1) _find_ta_orphans(ip, config_text, all_device_names=None) — after interface cleanup, SSHes show running-config | section router bgp, splits on global-only context (stops at first vrf sub-context), finds neighbor descriptions matching both underlay (To DevName via iface #TA) and overlay (Overlay to DevName #TA) patterns where device not in all_device_names, emits router bgp ASN / no neighbor X cleanup cmds; (2) _push_config now accepts all_device_names=None, threads into _find_ta_orphans; (3) /pushconfig handler extracts allDeviceNames from POST body; (4) pushDeviceConfig + pushAllConfigs both send allDeviceNames in body; (5) _reconcile_device BGP regex updated to match both underlay and overlay patterns (was Overlay-only); VERSION 260429.2→260429.3. Files: Sidebar-js.html, device_bridge.py)
  Previous: 2026-04-29 (Reconcile overhaul + push sync fix: (1) runReconcile() now uses #btnGenAllReconcile + #reconResults/#reconResultsBody — lives in Generate All modal; (2) /health check (AbortSignal.timeout 4s) before starting — shows clear error if bridge not running; (3) allDeviceNames passed in POST body for BGP orphan check; (4) _reconcile_device() now accepts all_device_names — SSHes show running-config | section router bgp and finds neighbor descriptions with #TA referencing devices not in topology, returns bgp_orphans:[{neighbor,description}]; (5) _renderReconcileResults() split into Stale Interfaces + Stale BGP Neighbors sub-sections; BGP orphans are informational (no Unconfigure button); (6) pushAllConfigs fix: now sends all_ifaces:true (was missing, so orphan cleanup was skipped on Push All unlike single-device push); VERSION 260429.1→260429.2. Files: Sidebar-js.html, Sidebar-css.html, device_bridge.py)
  Previous: 2026-04-29 (GAS guard timeout fixes: (1) fetchData guard 30s→60s — cold GAS start + large topology can exceed 30s; (2) syncSchemaPreservingOrder missing _guard added (_colSyncGuard, 60s, clears syncPollTimer + resets button); (3) timeout tier table in gas.md + design-review Check 17 updated. Files: Sidebar-js.html)
  Previous: 2026-04-29 (check-constraints.md: added check #4 — device_bridge.py ↔ embedded template logic sync; CLAUDE.md: added dual-file sync hard rule above VERSION rule. Files: .claude/commands/check-constraints.md, CLAUDE.md)
  Previous: 2026-04-29 (Device Bridge panel polish: (1) Device Login header now uses br-sec-hdr pattern — chevron at start (br-chev#svgBridgeDevChevron), title br-sec-title, no trailing button. (2) DNS hint moved out of header into info-box--dim (#bridgeDevHint) below device table; hidden/shown with table collapse via _syncBridgeDevCollapse. (3) #bridgeResults gets border-top separator via CSS. (4) Audit icon hint added inside #bridgeResults. Files: Sidebar.html, Sidebar-css.html, Sidebar-js.html)
  Previous: 2026-04-29 (Device Bridge panel redesigned to grouped collapsible sections: Issues (New link / Mismatch / No neighbour) / OK / No EOS. _renderLldpResults() rewrites #bridgeResultSections via secHtml/subHtml helpers. Classification uses bridgePortStatus enum (not r.msg parsing). _toggleBrSec(key) handles collapse/expand with localStorage persistence (bridgeSec_issues/ok/noeos). Issues open by default; OK and No EOS collapsed. HTML: #bridgeDiscovered + #bridgeResultList + #bridgeSummaryBadge removed, replaced with #bridgeResultSections. CSS: .br-sec-*, .br-sub-*, .br-chev, .br-empty-msg added. Files: Sidebar.html, Sidebar-css.html, Sidebar-js.html)
  Previous: 2026-04-29 (device_bridge.py synced with Sidebar-js.html embedded template — same _extract_eos_errors tuple return, eos_warnings in both call sites, unconditional _abort_stale_sessions, VERSION bumped to 260429.1 in both device_bridge.py and embedded template. Tests updated: TestExtractEosErrors uses _errs/_warns helpers; 4 new warning tests added. Files: device_bridge.py, Sidebar-js.html, tests/test_device_bridge.py)
  Previous: 2026-04-29 (EOS ! vs % differentiation + session cleanup: (1) _extract_eos_errors() now returns (errors, warnings) tuple — % lines → eos_errors (command rejected); ! lines with len>1 → eos_warnings (command accepted with note, e.g. transceiver not present). Both callers in _push_config (eAPI + SSH) unpack the tuple and set eos_warnings in response. (2) _abort_stale_sessions() guard removed — was only run when not dry_run; now runs unconditionally so verify passes also clean up stale sessions. (3) JS: _eosWarnings(res) mirrors _eosErrors(); _injectEosWarningBanner(warns) injects amber banner (beforeend, #fffbeb/#fcd34d/#92400e); _setResultRow() gets optional 7th warnDetails param rendered as amber box. Wired into: single-device Phase 1 (_p1EosWarns), push-from-tooltip Phase 1 (_ptEosWarns), single-device verify Phase 3 (step label + detail), Generate All success row (label suffix + warnDetails). File: Sidebar-js.html)
  Previous: 2026-04-29 (Push flow bug fixes — 3 issues: (1) Partial commit: after Phase 3 verify shows remaining diff, a "Recommit" button is now appended inside pt_detail using DOM createElement (closure captures deviceName/btnId before finally clears _pendingPush); clicking Recommit calls closePushConfirmModal()+pushDeviceConfig(). (2) Cancel slow: cancelPushConfig() now fire-and-forgets the EOS session abort (fetch+.catch(()=>{}) then immediately calls closePushConfirmModal()); removed the "Aborting session…" intermediate modal state that held the modal open until device round-trip. (3) Terminal-state Close button: all 7 terminal failure branches (timeout, EOS rejection, partial, verify-failed, verify-error, Phase2-failed, network-error) now set cancelFtr.onclick=closePushConfirmModal instead of relying on cancelPushConfig() (which has session-abort logic). closePushConfirmModal() resets cancelFtr.onclick=cancelPushConfig to restore for next open. File: Sidebar-js.html)
  Previous: 2026-04-29 (configCache-as-sole-truth refactor: removed allDevicesData.fullConfig, allDevicesData.configLoaded, and allNodesData.details.configLoaded — configCache is now the only config state store. processDeviceConfig() no longer writes fullConfig/configLoaded; wasAlreadyLoaded checks !!configCache[dev] instead. selectDevice() checks !configCache[deviceId] instead of !devData.fullConfig; reads formatConfigText(configCache[deviceId].data) instead of devData.fullConfig. selectLine() uses !!configCache[dev] for isCfg1Ready/isCfg2Ready. showTooltip link section uses !myData.details.config (not .configLoaded) to decide whether to fetch. openViewConfig() left/right branches compute _fullCfgL/R from configCache on demand. Save handlers (global config, tech settings) — redundant per-device fullConfig=null/configLoaded=false loops deleted; configCache={} is sufficient. File: Sidebar-js.html)
  Previous: 2026-04-29 (_selectiveConfigInvalidate fingerprint: added dev.vtepVlans to _fp() — Vx1 row changes (vlan_, svi_vlan_) update node.vtepVlans in topology but Vx1 is excluded from dev.ports, so the fingerprint never saw them. Config cache now correctly invalidates when Vx1 VLAN data changes. File: Sidebar-js.html)
  Previous: 2026-04-29 (Snake/IXIA audit flags wired into settings modal: auditFlagSnakeIpType and auditFlagIxiaIpType were declared and running checks but missing from _ALL_AUDIT_FLAGS, _getAuditFlag(), and _setAuditFlag() — checkboxes rendered but refreshAuditSettings/refreshAuditSettingsDots/enableAllAudits/disableAllAudits never touched them. Added to all three. File: Sidebar-js.html)
  Previous: 2026-04-29 (Audit live-issue classification fix — root cause: mixed-case links (wrong neighbor one end, no LLDP neighbor other end) had r.msg containing "no LLDP neighbor" so `noNeighbor=true` routed them to bridgeAuditFlagMissing branch; bridgePortStatus correctly marked those ports as 'mismatch' so audit count showed 4 but _auditIssues stayed empty → audit icon showed 0. Fix: (1) runValidation() UPDATE UI live section now iterates bridgePortStatus directly (same source as link coloring and count) instead of _lastBridgeResults; classification follows port status value ('mismatch'→bridgeAuditFlagMismatch, 'missing'→bridgeAuditFlagMissing), NOT r.msg content; _portToResult lookup built from _lastBridgeResults for detail messages; dedup by link pair via _bridgeAuditSeen Set. (2) _logBridgeIssuesToStatusLog same fix: uses bridgePortStatus[r.u]/[r.v] (hasMismatch) for classification instead of noNeighbor. (3) flagErrorOnPort now passes full detail message for link strip: 'LLDP mismatch: devU:ifU sees wrongDev:wrongIface / ...' instead of plain 'LLDP mismatch'. File: Sidebar-js.html)
  Previous: 2026-04-28 (Audit logging end-to-end redesign — state-based, not event-based. New global _auditIssues=[] replaces _loggedAuditErrors Set; rebuilt from scratch on every runValidation(). Design issues pushed to _auditIssues from validationErrors; live issues pushed from _lastBridgeResults/_lastBridgeOrphans. Audit tab now renders from _auditIssues via new renderAuditLog() — always current, no dedup complexity. _updateLogTabCounts uses _auditIssues.length for audit badge. _logBridgeIssuesToStatusLog simplified: [Audit-Live] setStatus calls are unconditional (first-time bridge check only, no dedup); logIssues param retained but ignored. runValidation UPDATE UI block replaced: no _loggedAuditErrors, no logIssues gate on summary — "Audit: N Issues Found" always accurate; renderAuditLog() called at end. clearStatusLog clears _auditIssues. Files: Sidebar-js.html)
  Previous: 2026-04-28 (Lo0/Lo1 configMap split + hover tooltips: generateSystemBlocks() now returns {lo0, system} instead of a plain string — lo0 holds the interface Loopback0 block, system holds router general+VRFs+VLANs. getDeviceConfig() assigns lo0 to configMap["000_LO0"] and system to configMap["001_SYSTEM"]. For MLAG VTEP devices (isVxlan && mlagState.isActive && hasP2p && gwVlans.size>0), Lo1 generated inline in getDeviceConfig() as configMap["000_LO1"]. generateVxlanBlock() no longer emits interface Loopback1 or the leading "!" — 055_VXLAN.full now starts cleanly with "interface Vxlan1". _openLoopbackConfig() now directly accesses data["000_LO0"]/data["000_LO1"] (fixes Lo0 click — root cause: 001_SYSTEM preamble blocked _extractInterfaceBlock). showLoopbackTooltip(e, deviceName, loNum) added with 350ms hover delay. Files: Code.gs, Sidebar-js.html)
  Previous: 2026-04-28 (Audit log invariant fixes — 3 changes: (1) "Audit: N Issues Found" summary now gated by logIssues — auto-revalidation suppresses summary when individual lines are also deduped (previously logged summary without detail, causing misleading Audit tab entries); "Audit Clean." always logged. (2) MLAG peer-link check routed through validationErrors[`_mlag_peer_${pairKey}`] synthetic key instead of direct setStatus — removes anti-pattern; outer guard !statusEl.innerText.includes("MLAG Peer-Link Missing") that blocked ALL [Audit - Design] messages removed. (3) Bridge panel _renderLldpResults sort order: sub-classifies mismatch into wrong-neighbor (order 0) vs no-neighbor (order 1); new svgNoNbr icon (minus-circle) distinguishes no-neighbor from wrong-neighbor. Taxonomy: [Audit-Live] → status bar + bridge panel + Audit tab; [Audit-Design] → status bar + Audit tab. Files: Sidebar-js.html)
  Previous: 2026-04-28 (Bridge/audit sync fix: _compareBridgeData() now stores _lastBridgeResults=results and _lastBridgeOrphans=orphans immediately before returning — guarantees bridgePortStatus and _lastBridgeResults are always populated together (both come from _compareBridgeData). Previously stored in _logBridgeIssuesToStatusLog, which could leave _lastBridgeResults=null if code was updated mid-session while bridgePortStatus had stale data. Storage removed from _logBridgeIssuesToStatusLog. Result: runValidation() [Audit-Live] replay always has detail matching the port count. Files: Sidebar-js.html)
  Previous: 2026-04-28 (GW formula fix for non-LEAF: getIpBlock() non-LEAF branch was using sheetIndex (device Arista counter) as the last octet — SPINE #6 was getting .6 instead of .1. Correct value is gwLastV4/gwLastV6 (parseInt(cfg.gw_v4_last/gw_v6_last)) — the operator-configured last octet, same field used by LEAF anycast. sheetIndex is arbitrary order-dependent counter, not an IP convention. Files: Code.gs, CLAUDE.md)
  Previous: 2026-04-28 (processDeviceConfig: _isEosDev guard moved above CASE 1 — covers both device strip (CASE 1) and link strips (CASE 2); non-EOS never turns any strip green. Files: Sidebar-js.html)
  Previous: 2026-04-28 (_selectiveConfigInvalidate fingerprint expanded — added vlan_, ip_type_, svi_vlan_, vrf_, desc_ to port-level fingerprint; previously changes to these fields left configCache stale and config showed old values after smart-poll refresh. File: Sidebar-js.html)
  Previous: 2026-04-28 (non-EOS strip green bug: (1) processDeviceConfig CASE 2 now guards with _isEosDev before turning strip green; (2) setSplitBtnState for all non-EOS strips passes actionsDisabled=true — prevents spurious ready-flash and button enable. Files: Sidebar-js.html)
  Previous: 2026-04-28 (_parseSviVlans: 1-4094 guard on incoming vlans array + explicitly requested VLAN IDs — silently drops out-of-range values before any `interface Vlan<N>` is generated. vlan_ NOT guarded (8888 valid for L3-et/po-int sub-interface). File: Code.gs)
  Previous: 2026-04-28 (Audit/bridge log sync — _lastBridgeResults/_lastBridgeOrphans globals store last bridge check output; _logBridgeIssuesToStatusLog gains logIssues param + _loggedAuditErrors dedup for [Audit-Live] msgs (same session-dedup as [Audit-Design]); runValidation() now replays [Audit-Live] via _logBridgeIssuesToStatusLog(_lastBridgeResults, _lastBridgeOrphans, null, logIssues) after [Audit-Design] loop — [Bridge] cmd failures excluded from replay (dsData=null); bridge check call sites pass logIssues=true (first-time results always logged). Result: explicit Audit press shows ALL enabled issues once; auto-revalidation suppresses duplicates; bridge check run still logs immediately. Files: Sidebar-js.html)
  Previous: 2026-04-28 (Audit log quality pass — 8 changes: (1) _inferLogType now matches '[audit' prefix (not '[audit]') so [Audit - Live] and [Audit - Design] msgs appear in Audit tab; (2) 'No neighbor:' renamed 'Missing LLDP neighbor:'; (3) VLAN Mismatch message includes dev:port + VLAN values; (4) Mode Incompatible includes dev:port + modes; (5) all node-level configErrors prefixed '${node.device}:${node.name}: ' at push time; (6) Duplicate GW lists device names instead of count; (7) Duplicate Interface uses fullId (dev:port) not just port; (8) VLAN L3 Conflict/Mixed Usage shows both conflicting portIds. Files: Sidebar-js.html)
  Previous: 2026-04-28 (_logBridgeIssuesToStatusLog: Extra cable (orphan) logs gated by bridgeAuditFlagMismatch — an extra physical cable is a topology mismatch; consistent with wrong-neighbor gating. Files: Sidebar-js.html)
  Previous: 2026-04-28 (_logBridgeIssuesToStatusLog: split mismatch log by type — real wrong-neighbor mismatches gated by bridgeAuditFlagMismatch → [Audit - Live] LLDP mismatch; no-neighbor links (msg contains 'no LLDP neighbor') gated by bridgeAuditFlagMissing → [Audit - Live] No neighbor. Both user-controllable via Bridge audit toggles. Files: Sidebar-js.html)
  Previous: 2026-04-28 (Restore [Audit - Live] LLDP mismatch and Extra cable to status log in _logBridgeIssuesToStatusLog — real wrong-neighbor mismatches must be logged; only SSH/cmd failures stay as [Bridge]. Files: Sidebar-js.html)
  Previous: 2026-04-28 (Bug fix _compareBridgeData: uMatch/vMatch used `bridgeU && expr` which evaluates to `undefined` (not `false`) when bridgeU is undefined (no LLDP neighbor on that port). `undefined !== false` so the mismatch branch was skipped and the link incorrectly showed as "partial" (SSH failed) instead of "mismatch" (no LLDP neighbor). Fix: `|| false` after each expression forces boolean — undefined||false=false — so missing-neighbor ports now correctly classify as mismatch. Files: Sidebar-js.html)
  Previous: 2026-04-28 (non-EOS strip divider fix extended to splitDevice — same pattern as splitLeft/Right: hide all .split-action-divider elements when all buttons hidden. Files: Sidebar-js.html)
  Previous: 2026-04-28 (_logBridgeIssuesToStatusLog now only logs [Bridge] Cmd failed — LLDP mismatch and Extra cable entries removed from status bar (bridge modal result list is the authoritative UI for those findings). Files: Sidebar-js.html)
  Previous: 2026-04-28 (Full-screen dialog minimize fix: mode-schema/reorder/view_custom containers have height:100vh !important which beats .modal-minimized height:auto !important (same specificity, last-write wins was the wrong rule). Fix: (1) higher-specificity CSS rule body.mode-schema #schemaContainer.modal-minimized overrides to height:auto; (2) toggleModalMinimize() now calls google.script.host.setHeight(hdr.offsetHeight+2) on minimize and setHeight(800) on restore for any full-screen-mode body class — shrinks the GAS dialog window itself so the blank rectangle disappears. Files: Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-04-28 (Log prefix taxonomy refined: [Bridge] Cmd failed for SSH/cmd failures (device unreachable — infrastructure); [Audit - Live] LLDP mismatch + Extra cable (topology audit findings); [Audit - Design] sheet validation errors. Cmd failed moved back to [Bridge]. Files: Sidebar-js.html)
  Previous: 2026-04-28 (REVERT schema-mode btn-modal-close hide: hiding [×] put [−] below the GAS dialog-chrome [×] instead of pairing them. GAS showModelessDialog chrome [×] is unavoidable OS-level UI. Correct: keep [−][×] paired in modal-header per design language; GAS chrome [×] is dialog frame, not our button. Files: Sidebar-css.html)
  Previous: 2026-04-28 (Bridge/Audit log unification: _logBridgeIssuesToStatusLog now emits [Audit] prefix (was [Bridge]) for LLDP mismatches, extra cables, and cmd failures — all appear in the Audit section of the status log. Audit log loop now filters LLDP messages from validationErrors (clean.startsWith('LLDP') → return) so per-port LLDP entries from flagErrorOnPort are never shown in status log — only used for visual port marking and error count badge. flagErrorOnPort LLDP messages reverted to generic ('LLDP mismatch', 'LLDP missing'). Files: Sidebar-js.html)
  Previous: 2026-04-28 (Schema mode (Manage Column & Formatting): hide .btn-modal-close in body.mode-schema via CSS — GAS showModelessDialog provides its own native close in the dialog title bar, making ours redundant (2 close buttons). _injectMinimizeButtons() still finds it in the DOM and inserts [−] before it, so only the minimize button is visible (correct position via margin-left:auto). Files: Sidebar-css.html)
  Previous: 2026-04-28 (non-EOS link strip: hide .split-action-divider elements when all buttons hidden — prevents stray | bars after label. Files: Sidebar-js.html)
  Previous: 2026-04-28 (buildConditionalRules() CF redesign: (1) A7 — added NOT(REGEXMATCH(int_,"^Vx")) guard so Vx1 rows don't amber-flag missing mode; (2) A8 — dropped ^l3-(et|po) mode restriction → vlan_ required for ALL modes (any mode + int_ filled + vlan_ empty → amber); (3) N/A-5 — changed xcvrNaFormula from mode=^l[23]-po to int_=^Vx — xcvr/speed/FEC fields depend on physical interface type (Et always applicable regardless of mode; Vx1 always grey); (4) N/A-Vx — new block greys sp_mode_, po_, ip_type_ for Vx1 rows; (5) W1 removed — et_speed=xcvr_speed is valid for native ports, not a mismatch indicator. Files: Code.gs)
  Previous: 2026-04-28 (renamed _pushLeftDev/_pushRightDev/_pushLeftPort/_pushRightPort → _leftDev/_rightDev/_leftPort/_rightPort; removed dead processInterfaceConfig. Files: Sidebar-js.html)
  Previous: 2026-04-28 (fetchData guard: "try again" → "retrying…" + auto-retry after 5s (smart poll alone won't re-fetch without a version change). Files: Sidebar-js.html)
  Previous: 2026-04-28 (fetchData guard: timeout 20s→30s (matches GAS execution limit); added _fetchGuardFired flag — late GAS response after guard fires is discarded (prevents silent render overwriting the error status). Files: Sidebar-js.html)
  Previous: 2026-04-28 (Prefetch status fixes: (1) "🚀 Requesting Topology Data" gated behind isFirstLoad — smart-poll/save/refresh messages no longer overwritten; (2) scheduleBackgroundPrefetch(forceRestart=true) now calls stopBackgroundPrefetch() if prefetch is mid-run — rebuilds queue with newly-invalidated devices instead of orphaning them. Files: Sidebar-js.html)
  Previous: 2026-04-28 (Background prefetch restart fix: scheduleBackgroundPrefetch(forceRestart) — skips re-run when _prefetchEverRan&&!forceRestart; _prefetchEverRan set in startBackgroundPrefetch(); _selectiveConfigInvalidate returns invalidated count; fetchData passes _invalidated!==0; save handlers pass true. Files: Sidebar-js.html)
  Previous: 2026-04-28 (ROOT CAUSE FIX strip-amber+View-"select a device or link first": selectedLineId cleared by redrawLinks()/filterTopology() without hiding strip — processDeviceConfig CASE 2 and openConfigFromButton read null selectedLineId. Fix: both now use _pushLeftDev/_pushRightDev (stable, set by selectLine, never cleared by render/filter). Files: Sidebar-js.html)
  Previous: 2026-04-28 (UserGuide.html: added "Audit Enforcement — Snake / IXIA" subsection at end of section 8 (Snake Test), just before section 9 (Per-VLAN VRF). Table documents auditFlagSnakeIpType (flags isSnakePrimary/isSnakeSecondary ports not p2p — needed for 200.oct2.oct3.1/24 host address, static ARP, and SNAKE VRF chain) and auditFlagIxiaIpType (flags TRAFFIC_SNAKE_EP1/EP2_L3 desc ports not p2p — gw generates ip address virtual on LEAF or wrong prefix on SPINE; p2p gives plain ip address for IXIA ARP). Files: UserGuide.html)
  Previous: 2026-04-28 (Background prefetch timing fix: _prefetchHold flag added to globals; _autoRunBridge() sets _prefetchHold=true at start, clears via _done() on completion or error; checkSteadyState() now checks isFetching||_prefetchHold in addition to D3/holistic alpha — prevents prefetch from starting while bridge scan (LLDP+devstatus) or any in-flight fetchData is still running. FILES: Sidebar-js.html)
  Previous: 2026-04-28 (Root cause fix for strip-not-green and View-button-disabled-forever: added _updateStripFetchError(deviceName, label) which sets selected device/link strips to red (#ef4444) with buttons re-enabled when fetchFullConfig fails. Called from three paths: (1) _fcGuard timeout — GAS framework failure leaves strip amber indefinitely; (2) withSuccessHandler res.error branch — server error leaves strip amber; (3) withFailureHandler — GAS script error leaves strip amber. Also added processDeviceConfig guard-removal from prior session: always calls setSplitBtnState in CASE 1/2 with no background-color guard; added null guard on allNodesData[fullId].details in for-loop. Files: Sidebar-js.html)
  Previous: 2026-04-28 (GW config for non-LEAF devices: getIpBlock() now enters the GW branch for all roles; SPINE/HARNESS get plain `ip address gw_v4_first.oct2.oct3.sheetIndex/mask` instead of being silently skipped. LEAF path (anycast/VARP) unchanged. Files: Code.gs, CLAUDE.md)
  Previous: 2026-04-28 (background prefetch counter fix: prefetchTotal = all Arista devices; prefetchQueue = uncached only; currentCount = prefetchTotal - prefetchQueue.length unchanged so counter reads "6 of 10" when 5 already cached, not "1 of 5". FILES: Sidebar-js.html)
  Previous: 2026-04-27 (processDeviceConfig CASE 2 anti-flicker guard changed from vBtn.disabled check to background-color check (matching CASE 1 pattern) — disabled state could be false in edge cases preventing auto-green. Now: splitLeft/splitRight turn green whenever background is not already green, regardless of button disabled state. Files: Sidebar-js.html)
  Previous: 2026-04-27 (GW config is LEAF-only: generateComplexL3Block() getIpBlock() branch `ipType.includes("gw")` now guards with `&& (!netSettings || netSettings.deviceRole === 'LEAF')` — SPINE/HARNESS/unset roles never generate ip address virtual / ip virtual-router address. callSettings = Object.assign({}, settings, { deviceRole }) injected in getDeviceConfig() and passed to all generateConfig() calls for both main port loop and snake secondary. View config on links now robust: openConfigFromButton('left'/'right') falls back to _extractInterfaceBlock(fullConfig,port) when currentLeftConfig is empty, and triggers fetchFullConfig if fullConfig also unavailable. Files: Code.gs, Sidebar-js.html)
  Previous: 2026-04-27 (Hide Copy/View/Push for non-EOS devices and link sides: selectDevice() now hides btnDevice+btnViewDevice alongside btnPushDevice when devData.type==='non-arista'; selectLine() now hides btnLeft+btnViewLeft and btnRight+btnViewRight alongside push buttons for non-EOS link ends. Files: Sidebar-js.html)
  Previous: 2026-04-27 (Fix "Select a device or link first" error on View Config after canvas deselect: canvas empty-click handler and Esc/global-reset handler both cleared selectedDeviceId/selectedLineId but did NOT hide split-btn elements — leaving View buttons enabled with no selection, so openConfigFromButton() fell through to CASE 3 guard. Fix: added querySelectorAll('.split-btn') hide in both paths (matching what selectLine/selectDevice already do). Files: Sidebar-js.html)
  Previous: 2026-04-27 (Fix group rects showing during search when member devices are hidden: filterTopology() now calls requestAnimationFrame(refreshGroupRects) in both the reset path (empty search) and the active-filter path, so member-based group rects whose all members are hidden by search are correctly removed. Files: Sidebar-js.html)
  Previous: 2026-04-27 (device_bridge.py v260427.3: _BLOCK_CMD_RE covers interface/router bgp/ospf/isis/vlan/address-family; _extract_eos_errors tracks current_block + last_block_in_errors — injects block context line before first sub-command error in a successful block (e.g. interface exists but speed fails), marks block represented when block command itself fails to avoid double-injection. JS _EOS_BLOCK_RE constant matches same set; _formatEosErrors + _formatEosErrorsHtml updated to use it. downloadBridgeScript() template synced to v260427.3. FILES: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-27 (EOS rejection ! grouping: _formatEosErrors(errs) plain-text helper inserts '!' line before each 'interface ' entry (except first); _formatEosErrorsHtml(errs) HTML version with styled <span> blocks. All 6 error-join sites updated: _p1EosErrs, _ptEosErrs (Phase 1 single/link push → _updatePushPreviewError); _v3EosErrs (Phase 3 verify HTML → _ptDetail); _pushEosErrs all-rejected path, _pushEosErrs partial path (Push All _setResultRow details); _vEosErrs (Push All Phase 3 verify → vError). _injectEosRejectionBanner now calls _formatEosErrorsHtml. _updatePushPreviewError div gets white-space:pre-wrap (pre-existing bug fix). FILES: Sidebar-js.html)
  Previous: 2026-04-27 (Push result label reword — all "Pushed" → "Committed" (EOS terminology); pushAllConfigs: 'EOS rejected — nothing committed'→'All rejected — device unchanged', 'Pushed — verify check failed'→'Committed — verify failed', 'Pushed (+N/-N) — some rejected'→'Committed (+N/-N) · N rejected' (count from _pushEosErrs.length), 'Pushed (+N/-N)'→'Committed (+N/-N)', 'N lines remaining after push'→'Committed — N lines still differ'; commitPushConfig modal: pt3 'no diff remaining'→'verified — in sync', 'EOS command rejection'→'EOS rejection on verify', title 'Push Succeeded — EOS Rejection on Verify'→'Committed — EOS rejection on verify', title 'Partial Push'→'Partial Commit', title 'Push Succeeded — Verify Failed'→'Committed — verify failed' (both verify-error and verify-catch paths). _setPushStatus messages updated to match. FILES: Sidebar-js.html)
  Previous: 2026-04-27 (generateAllModal push-active close warning: #genAllPushWarn div (hidden by default, amber banner) added below modal-header. closeGenerateAllModal() shows it for 3.5s and returns without closing instead of calling setStatus(). FILES: Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-27 (Push UX — confirm instead of disable: _commitInFlight boolean (module-level, near _pendingPush) set true at Phase 2 start, false in finally block. cancelPushConfig(): if _commitInFlight, shows confirm("Config commit is in progress … cannot undo … Close anyway?") — returns without action if user cancels. Removed closeXBtn.disabled = true/false from commitPushConfig()/closePushConfirmModal() — × stays enabled, confirm guards it. fetchData(): new guard at top (before isFetching=true) — if _commitInFlight || _pendingPush || _pushAllActive, shows confirm("Push All/push in progress — refresh will clear push status. Continue?") and returns if user cancels. FILES: Sidebar-js.html)
  Previous: 2026-04-27 (Push UX clarity: (1) pushConfirmModal × button gets id="btnPushConfirmClose" — disabled in commitPushConfig() during Phase 2 commit to prevent race between in-flight commit and abort; re-enabled in closePushConfirmModal(). (2) pushAllConfigs "EOS rejected commands" label → "EOS rejected — nothing committed" (empty diff = nothing staged = device unchanged). (3) Partial rejection (eos_errors + non-empty pushDiff): now shows "Pushed (+N / -N) — some rejected" in amber (#f59e0b) with both diff block and error details visible. (4) _pushAllActive boolean (module-level, near _pushAllStopped): set true on pushAllConfigs start, false on completion. closeGenerateAllModal() guards with _pushAllActive — shows status-warn and returns without closing if push in progress. FILES: Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-27 (pushAllConfigs diff summary: Phase 3 success row now shows committed diff block and label 'Pushed (+N / -N)' when pushDiff non-empty; 'Already in sync' (muted #64748b) when pushDiff empty — distinguishes actual push from no-op. pushDiff = res.diff.trim() captured after Phase 2 eos check and threaded into Phase 3 outcome. Files: Sidebar-js.html)
  Previous: 2026-04-27 (_extract_eos_errors command context: now captures PTY prompt echoes (lines with '#' and '(config') as prev_cmd; % error formatted as 'cmd → % err'. eAPI fallback stays plain '% err' (no echoes). _eosErrors() JS deduplicates with count suffix '(×N)'. _setResultRow details div gets white-space:pre-wrap. pushAllConfigs Phase 3 vError uses '\n' join. VERSION 260427.1→260427.2. 75 tests passing. Files: device_bridge.py, Sidebar-js.html, tests/test_device_bridge.py)
  Previous: 2026-04-27 (Push Targets list sorted by sheetIndex (device ID) numerically. Files: Sidebar-js.html)
  Previous: 2026-04-27 (Tests: added test_invalid_input_in_session_submode and test_timestamped_prompt_submode_not_a_stop_exec_is to TestExtractEosErrors — covers % Invalid input from interface sub-mode prompt ending in # (not a stop), and EOS clock-in-prompt format hostname.HH:MM:SS(config...)# (not a stop) vs hostname.HH:MM:SS# (IS a stop). 75 tests passing. Files: tests/test_device_bridge.py)
  Previous: 2026-04-27 (Push Targets device filter in generateAllModal: compact checkbox list of all EOS devices shown between genall-actions and results pane; all checked by default. openGenerateAllModal() calls _populateGenAllDeviceList() which builds sorted checkboxes from currentData.nodes (non-arista excluded). _updateGenAllSelCount() updates "N/total selected" counter. _genAllSelectAll(bool) toggles all. pushAllConfigs() filters deviceNames by checkbox state (no checkbox = include). Sidebar.html: #genallDeviceFilter section with #genAllDeviceList and #genAllSelCount. Sidebar-js.html: _populateGenAllDeviceList, _updateGenAllSelCount, _genAllSelectAll, openGenerateAllModal patch, pushAllConfigs deviceNames filter. VERSION 260427.30→260427.31. Files: Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-27 (_extract_eos_errors: new function in device_bridge.py (and embedded template in downloadBridgeScript()) that scans SSH/joined-eAPI output for EOS `%` rejection lines. Two stop conditions scope capture to config-command phase only: (1) `--- `/`+++ ` diff header; (2) bare exec-mode prompt — `line.endswith('#') and '(config' not in line`. Result returned as `eos_errors: [...]` via `**_asn_extra` in all push return dicts (SSH and eAPI paths). JS wires `_eosErrors(res)` helper into all 5 push call sites: pushDeviceConfig Phase 1 (abort session on empty diff + rejection), pushDeviceConfig Phase 1 banner (push proceeds but warns), commitPushConfig Phase 3 verify (rejection in verify shown as error), _pushConfigText (abort + banner like Phase 1), pushAllConfigs Phase 2 (skip Phase 3, show rejection), pushAllConfigs Phase 3 verify (vError instead of vDiff). VERSION 260426.70→260427.1. Files: device_bridge.py, Sidebar-js.html, tests/test_device_bridge.py)
  Previous: 2026-04-27 (Toolbar alignment: added min-height:24px to .tb-btns so the 18px mono-btn circle vertically centers within the same 24px baseline as all other toolbar controls (icon-btn/inputs). Files: Sidebar-css.html)
  Previous: 2026-04-27 (Version badge moved into tb-label inline with "APP" text — `APP v260427.22`; minimize button is now the sole element in tb-btns, removing visual ambiguity. Files: Sidebar.html)
  Previous: 2026-04-27 (Version badge in APP toolbar: `.tb-version-badge` span in APP tb-section shows `v<?= APP_VERSION ?>` (GAS scriptlet, renders at template evaluation time). Click copies version to clipboard. CSS: 10px JetBrains Mono, muted/hover, user-select:text. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-27 (EVPN gating uses parent-class tech-section-gated instead of per-chip tech-incompat: applyEvpnGating() now toggles tech-section-gated on evpnServiceSection (!evpnOn) and evpnVgwSection (!varpOk); CSS .tech-section-gated .tech-radio-label.tech-selected has specificity 0,3,0 so it beats tech-selected (0,2,0) — grey always wins. updateVarpMacVisibility() now checks vgwSection.classList.contains('tech-section-gated') before querying gwType — prevents MAC row showing when VGW section is gated. applyEvpnGating() calls updateVarpMacVisibility() at end; syncTechChipState() no longer called from applyEvpnGating(). Files: Sidebar-js.html)
  Previous: 2026-04-27 (runValidation() audit count/flag separation: all 28 checks now always count into auditCheckHadIssue regardless of enabled/disabled; flagging (flagErrorOnPort/configErrors/pairErrors/portErrorCount) only when flag enabled. Reset changed to full auditCheckHadIssue = {}. refreshAuditSettingsDots() badge cap raised 99+ → 999+. Files: Sidebar-js.html)
  Previous: 2026-04-27 (P2P chip groups + wrapping fix: P2P opts changed from flex-wrap:nowrap to flex-wrap:wrap gap:6px flex:1; IPv6+IPv6Unnum grouped as tech-chip-group (joined pill); IPv4+IPv6 and IPv4+IPv6Unnum grouped as second tech-chip-group. Files: Sidebar.html)
  Previous: 2026-04-27 (Tech modal EVPN gating grey-chip fix — SUPERSEDED by parent-class approach above. Files: Sidebar-js.html)
  Previous: 2026-04-27 (Audit Settings modal split into two sections: .audit-section-hdr.live (blue accent) before Bridge checks, .audit-section-hdr.design (gray accent) before Port-Channel onward; full-bleed margin:-16px; CSS added. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-27 (Audit per-check count badge always shows after first run: 0=cnt-clean (green pill), N>0 enabled=cnt-red, N>0 disabled=cnt-gray; hidden before first run. CSS: cnt-clean added (green tint). Files: Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-04-27 (Audit per-check rows: refreshAuditSettingsDots() injects auditCount_X span before auditDot_X (once, reused); count badge shown only when count>0 (cnt-red/cnt-gray); dot always empty dot-shape (ind-red:empty=red, ind-green=green, default=gray); toolbar badge hidden when 0. CSS: .audit-count-ind + .audit-issue-ind.ind-red:empty. Files: Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-04-27 (Network Tech Stack modal chip/card visual redesign: protocol options are now colored chips (light green #f0fdf4/#86efac available; dark green #16a34a/#15803d selected; grey #f1f5f9/#e2e8f0 incompatible). Hidden .chk-input (display:none) — label click toggles the input. syncTechChipState() iterates #techModal .tech-radio-label and toggles .tech-selected based on .chk-input.checked. applyTechCompat() calls syncTechChipState() at end; icon injection removed. _enableTechChk() calls syncTechChipState() after el.checked=true. Delegated change listener (_changeInit) on #techModal calls syncTechChipState() on every input change. IPv6+Unnumbered chips grouped as .tech-chip-group (joined pill: first-child left-radius, last-child right-radius, no left border) for BGP and OSPF. flex layout replaces grid for all opts divs. SERVICE and VGW labels no longer have flex:1. Files: Sidebar-css.html, Sidebar-js.html, Sidebar.html)
  Previous: 2026-04-27 (Audit toolbar badge always visible: shows portErrorCount when validation mode active + issues found, else 0; never hidden. Files: Sidebar-js.html)
  Previous: 2026-04-27 (Audit toolbar badge now shows portErrorCount: _updateAuditToolbarIcon(hasIssues, count) accepts optional count param; updates #auditBadge text+visibility alongside icon color change; call site in runValidation() passes portErrorCount. Badge hides when validation mode is off or count=0. Files: Sidebar-js.html)
  Previous: 2026-04-27 (Network Tech Stack modal — greyed option affordance: applyTechCompat() now injects a .tech-hint-icon span (SVG + circle) with margin-left:auto into each tech-incompat label so a visible indicator is always present; also sets row.title="Click to enable — will switch P2P to [mode]" dynamically using same priority as _enableTechChk; removed on compat restore. .tech-hint-icon CSS: margin-left:auto, color:#94a3b8, opacity:0.7; hover: primary blue + opacity:1. Hint text updated: "Greyed options require a different P2P mode — click any to switch automatically." Files: Sidebar-js.html, Sidebar-css.html, Sidebar.html)
  Previous: 2026-04-27 (Fix group rects showing for error-free groups in audit-only view: (1) applyAuditOnlyView now calls refreshGroupRects after DOM changes; (2) _computeGroupBounds skips display:none nodes (any visibility filter); (3) renderGroupRectsInto returns early when computed=null so empty-member groups are not rendered. Files: Sidebar-js.html)
  Previous: 2026-04-27 (Network Tech Stack modal alignment fixes — proto-underlay-label min-width 36px→76px+flex-shrink:0 so VXLAN/EVPN outer labels share same column width; VXLAN proto-underlay-opts gets flex:1 to fill remaining row width; SERVICE/VGW sub-sections restructured to flex rows with 80px fixed sub-label span wrapping text+ⓘ button and flex:1 on each option label — Per-VLAN/Bundle and Anycast GW/VARP now column-align; MAC span gets width:80px to align with VGW label above; .tech-incompat changed from opacity:0.4 to color:#94a3b8 on label text + opacity:0.45 on input only — checkboxes remain readable; hover restores to primary blue + opacity:0.85 on input. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-27 (Network Tech Stack modal — vertical column alignment via CSS grid on BGP/OSPF opts (repeat(4,1fr)) and VXLAN/EVPN opts (repeat(2,1fr)); clickable greyed options via .tech-incompat CSS class (opacity:0.4, hover 0.7+blue) replacing disabled/pointer-events:none; _enableTechChk(id) auto-switches P2P radio to minimum compatible mode then checks the box; click-to-enable capture listener in openTechModal guarded by _clickInit flag. Files: Sidebar.html, Sidebar-css.html, Sidebar-js.html)
  Previous: 2026-04-27 (Audit issue counts in Audit Checks modal: auditCheckHadIssue changed from boolean→count (incremented per occurrence); all 29 assignments changed to (||0)+1. audit-status-dot replaced by audit-issue-ind — dot when empty (no issues / not run), count badge when N>0; red+enabled, gray+disabled. refreshAuditSettingsDots() sets textContent=count and class. Sidebar.html: 24 span class renames. Sidebar-css.html: new .audit-issue-ind styles with :empty/:not(:empty) modes. Files: Sidebar-js.html, Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-27 (Network Tech Stack modal gap consistency — all three bordered section boxes margin-bottom 14px→18px; EVPN SERVICE+VGW sub-row gap 8px→6px 8px to match proto-underlay-opts. Files: Sidebar.html)
  Previous: 2026-04-26 (Fix two pre-existing issues: (1) fetchData _guard — 20s timeout added before getTopologyData GAS call; clearTimeout in both success+failure handlers (gas.md Rule 20); (2) bump all 7 "last synced" comments on 5 duplicate function pairs from 2026-04-20→2026-04-26 (verified identical in code review). Files: Sidebar-js.html, Code.gs)
  Previous: 2026-04-26 (Network Tech Stack modal — P2P row on single line (flex-wrap:nowrap on opts div, removed align-items:flex-start + padding-top:3px); .tech-radio-label gets min-width:80px for vertical column alignment across rows; .proto-underlay-opts gap reduced from 6px 20px to 6px 8px. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-26 (Replace emoji/Unicode with SVG icons (Rule 7): loading spinners 🔄◌→SVG arc, search icon 🔍→SVG magnifier in 6 btn.innerHTML locations; strip emoji from 3 showGlobalLoading/setStatus text strings; convert commented-out console.log to dbg() at line ~8876. Files: Sidebar-js.html)
  Previous: 2026-04-26 (Network Tech Stack badges unified: .proto-plane-badge--data and --ctrl both changed to neutral gray (background rgba(148,163,184,0.15), color #64748b). Files: Sidebar-css.html)
  Previous: 2026-04-26 (Fix 5 review failures: (1) .hc-label + .dev-vis-vlan-clear font-size 9px→10px; (2) .modal-body-scroll CSS rule added (overflow-y:auto; flex:1; min-height:0); (3) processDeviceBatch slow-path GAS _guard added (60s timeout); (4) 3 hand-written btn-modal-minimize removed from bridgeModal/pushConfirmModal/generateAllModal — injector handles them. Files: Sidebar-css.html, Sidebar-js.html, Sidebar.html)
  Previous: 2026-04-26 (Network Tech Stack modal redesign — rolled back plane dividers; GW+P2P redesigned as proto-underlay-row table (label col + flex-wrap opts col) under single INTERFACE section header, matching BGP/OSPF visual style; VXLAN label gets blue DATA badge, EVPN label gets purple CTRL badge; SERVICE+VGW sub-groups reverted to inline horizontal rows (label + info btn + options all on one line); .proto-underlay-opts reverted to flex-wrap horizontal; .tech-plane-divider* CSS removed; .proto-plane-badge + --data/--ctrl variants added. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-26 (Fix stale /health docstring version: device_bridge.py line 23 and Sidebar-js.html embedded template line 9363 both said "260426.23"/"260424.43" — corrected to "260426.70" to match VERSION. Docstring is documentation only; runtime /health uses VERSION variable. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-26 (Network Tech Stack modal — all option groups now vertical: GW/P2P section containers changed to flex-direction:column; .proto-underlay-opts changed to flex-direction:column gap:3px; .proto-underlay-row changed to align-items:flex-start with label padding-top:3px; EVPN SERVICE+VGW sub-groups restructured into header row + vertical options column. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-26 (UserGuide: document Loopback+Vx1 pinned rows — renamed "VTEP Device Cards" section to "Loopback & VTEP Pinned Rows"; Lo0 (all Arista, id.id.id.id, BGP+VTEP source for standalone), Lo1 (MLAG VTEP only, min.min.max.max, vxlan mlag source-interface), LO badge blue; Vx1 row now documented as clickable (view/copy/push interface Vxlan1 block); fixed stale "read-only" description. Also: _openLoopbackConfig comment updated Lo0/Lo1/Lo10→Lo0/Lo1. Files: UserGuide.html, Sidebar-js.html)
  Previous: 2026-04-26 (DevView Lo1 row fix: Lo1 now shown only for MLAG VTEP devices (node.vtepVlans && allDevicesData[node.id].mlagPeer). Standalone VTEPs (node.vtepVlans but no mlagPeer) show Lo0 only — they have no Loopback1 in generated config. Files: Sidebar-js.html)
  Previous: 2026-04-26 (MLAG VXLAN loopback pattern change: Lo0+Lo1+Lo10 → Lo0+Lo1. Lo0 serves both BGP/router-id AND vxlan source-interface Loopback0. Lo1 is shared MLAG pair VTEP IP (min.min.max.max, description VTEP_MLAG_SHARED, vxlan mlag source-interface Loopback1). Lo10 removed entirely. DevView: Lo1+Lo10 rows → Lo1 only for VTEP devices (node.vtepVlans). Flood list for remote MLAG pairs still uses min.min.max.max (now Lo1 IP). Files: Code.gs, Sidebar-js.html, CLAUDE.md)
  Previous: 2026-04-26 (View Config: highlight 'no' and 'default' lines in red — added cfg-cleanup class (color:#dc2626; font-weight:600) to Sidebar-css.html; added /^\s*(?:no|default)\s/i rule to highlightEosConfig() in Sidebar-js.html, placed before interface/router rules so it takes precedence at all indent levels. Files: Sidebar-css.html, Sidebar-js.html)
  Previous: 2026-04-26 (Remove _prepend_section_cleaners from downloadBridgeScript() embedded template: Code.gs generateVxlanBlock() already emits 'default vxlan vlan 1-4094 vni' + 'default vxlan flood vtep' — having both caused double-injection. Embedded template _push_config now passes config_text directly (matches actual device_bridge.py). Also removed _VXLAN_IFACE_RE regex. VERSION 260426.80. Files: Sidebar-js.html)
  Previous: 2026-04-26 (UserGuide: fix stale column order in Sheet Views — Mode/Vlan preset views listed po_ before sp_mode_ (old order); corrected to sp_mode_ before po_ to match current DEFAULT_SCHEMA_ARRAY. Files: UserGuide.html)
  Previous: 2026-04-26 (Lo0/Lo1/Lo10 loopback rows in DevView: _openLoopbackConfig(deviceName, loNum) generic function (replaces _openLo0Config) — portFilter="Lo<N>"; _extractInterfaceBlock finds interface Loopback<N> via _expandIfaceName (Lo(\d)→Loopback$1 added alongside Vx(\d)→Vxlan$1). DevView: pinned Lo rows rendered before Vx1 row — Lo0 for all Arista devices, Lo1+Lo10 for VTEP devices (node.vtepVlans). Blue style (rgba 59,130,246), "LO" badge. Click opens config modal with interface-only push. Non-Arista devices skipped via allDevicesData[node.id].type==='non-arista' check. Files: Sidebar-js.html)
  Previous: 2026-04-26 (Linear group rects missing on load bug fix: #linear-grect-svg was only created in switchLayout('linear') (user clicking the button), never in toggleViewMode() (initial load, fetchData refresh). Group rects were invisible until the user manually clicked Linear. Fix: extracted _initLinearGroupRectSvg() helper (rAF-deferred SVG creation + renderGroupRectsInto call); called from both switchLayout('linear') and toggleViewMode() isDetail+linear branch. switchLayout inline block replaced with _initLinearGroupRectSvg() call. Files: Sidebar-js.html)
  Previous: 2026-04-26 (UserGuide: document device_bridge.py push behaviour — "What Gets Pushed" section added under Config Push: Layer 1 = generated config is single source of truth (View Config = what is pushed, Vxlan cleanup embedded); Layer 2 = two push-time device-state additions: orphan interface cleanup + BGP ASN change; both visible in dry-run diff before commit. Files: UserGuide.html)
  Previous: 2026-04-26 (Vx1 config view/copy/push: Vx1 row in DevView is now clickable (cursor:pointer) — click calls _openVx1Config(deviceName) which sets _configViewContext={deviceName,portFilter:"Vx1"}, calls _setCfgModalPushBtns('interface'), reads configCache[device].data['055_VXLAN'].full and opens config modal. Push uses pushDeviceConfig with portFilter="Vx1" → _extractInterfaceBlock finds interface Vxlan1 via _expandIfaceName which now maps Vx(\d)→Vxlan$1. No cabling/audit changes. Files: Sidebar-js.html)
  Previous: 2026-04-26 (Move Vxlan cleanup into generated config — VERSION 260426.23→260426.70: 'default vxlan vlan 1-4094 vni' + 'default vxlan flood vtep' now emitted directly in generateVxlanBlock() in Code.gs as the first sub-commands under interface Vxlan1 (before vxlan udp-port 4789). Removed _prepend_section_cleaners() and _VXLAN_IFACE_RE from device_bridge.py — bridge no longer modifies config during push; generated config is the single source of truth. Removed 11 TestPrependSectionCleaners tests; 62 pass. Files: Code.gs, device_bridge.py, tests/test_device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-26 (Fix user-select on <code> in ASN banner: parent div has user-select:none but <code> showing 'no router bgp <old>' must be copyable — added user-select:text;-webkit-user-select:text to <code> inline style in _injectPushAsnChangeWarning(). Also: downloadBridgeScript() embedded template had 3 stale bugs — (1) old _SECTION_CLEANERS list replaced with _VXLAN_IFACE_RE approach, (2) 'if not dry_run:' guard removed from _push_config BGP block, (3) _find_bgp_asn_change returns ([], None) tuple not []; all 4 return paths now include **_asn_extra. VERSION 260426.67. Files: Sidebar-js.html)
  Previous: 2026-04-26 (Center New Group button in TOPO EDIT panel: added justify-content:center to #holistic-controls .hc-btns — single button was left-aligning when nodeSizeGroup hidden. Files: Sidebar-css.html)
  Previous: 2026-04-26 (Fix + - visible in all views: .segmented-group has display:flex !important so style.display='none' was overridden; added .segmented-group.is-hidden { display:none !important } CSS rule; updateTopoButtons now uses classList.toggle('is-hidden') for nodeSizeGroup. Files: Sidebar-css.html, Sidebar-js.html)
  Previous: 2026-04-26 (Fix stray | in TOPO EDIT toolbar: split nodeSizeMinus/Plus into separate #nodeSizeGroup segmented-group; updateTopoButtons hides group not individual buttons. Files: Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-26 (Close group rect modal on view switch: closeGroupRectModal() added to toggleViewMode() and switchLayout(). Files: Sidebar-js.html)
  Previous: 2026-04-26 (render() calls requestAnimationFrame(refreshGroupRects) at end — group rects persist after data refresh/cable change. Files: Sidebar-js.html)
  Previous: 2026-04-26 (Linear view group rect support: New Group button in linear mode; newGroupRect() guard allows layoutMode=linear; toggleViewMode cleanup removes #linear-grect-svg; refreshGroupRects() handles #linear-grect-svg layer. Files: Sidebar-js.html)
  Previous: 2026-04-26 (BGP ASN detection fix: _find_bgp_asn_change() was using 'show ip bgp summary | json' which returns vrfs.<vrf>.as=0 when BGP has no established peers — silently failing to detect the mismatch. Replaced with 'show running-config | include router bgp' plain-text SSH (bypasses _run_cmds which forces | json) — always returns 'router bgp <asn>' from committed running config regardless of peer state. VERSION 260426.23. Files: device_bridge.py)
  Previous: 2026-04-26 (Vxlan cleanup simplified — VERSION 260426.22→260426.23: unconditionally inject 'default vxlan vlan 1-4094 vni' + 'default vxlan flood vtep' as first sub-commands in every Vxlan block. EOS no-ops these when nothing is configured. Removes per-VLAN parsing (_VXLAN_FLOOD_RE/_VXLAN_VLAN_VNI_RE removed). Tests simplified to 4 cases. 73 pass. Files: device_bridge.py, Sidebar-js.html, tests/test_device_bridge.py)
  Previous: 2026-04-26 (Vxlan cleanup: no→default — VERSION 260426.21→260426.22: 'default vxlan flood vtep' and 'default vxlan vlan X vni' are valid EOS sub-commands and more idiomatic than 'no' (reset to factory default vs remove). Tests updated. 73 pass. Files: device_bridge.py, Sidebar-js.html, tests/test_device_bridge.py)
  Previous: 2026-04-26 (Surgical Vxlan cleanup — VERSION 260426.20→260426.21: replaced _SECTION_CLEANERS + 'default interface Vxlan1' with targeted sub-command injection inside Vxlan blocks. 'default interface Vxlan1' was too aggressive — also cleared vxlan source-interface, vxlan mlag source-interface, etc. New approach injects 'no vxlan flood vtep' (if any flood vtep lines present) and 'no vxlan vlan X vni' (per mapping) as the first sub-commands inside the interface Vxlan1 block, leaving all non-additive settings untouched. _SECTION_CLEANERS removed; _VXLAN_IFACE_RE/_VXLAN_FLOOD_RE/_VXLAN_VLAN_VNI_RE added. Tests updated: 5 new surgical tests replace old test_default_vxlan1 — 73 pass. Files: device_bridge.py, Sidebar-js.html, tests/test_device_bridge.py)
  Previous: 2026-04-26 (BGP AS mismatch detection in push preview: removed `if not dry_run:` guard around `_find_bgp_asn_change()` in device_bridge.py — now runs for ALL push modes including dry_run. Effect: (1) diff shown in push preview modal includes `no router bgp <old_asn>` when ASN changed; (2) `asn_changed` is returned in dry_run response; (3) `_injectPushAsnChangeWarning()` now fires from the `_pushConfigText()` dry_run path, showing yellow "BGP AS mismatch: device running AS X, config uses AS Y — no router bgp X included in diff" banner before user confirms push. VERSION 260426.19→260426.20. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-26 (Fix stray | in TOPO EDIT toolbar: split size buttons into separate #nodeSizeGroup segmented-group; updateTopoButtons hides/shows nodeSizeGroup instead of individual buttons — CSS :not(:last-child) border-right was firing on groupRectBtn because display:none siblings still count as DOM children. Files: Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-26 (Close group rect modal on view switch: closeGroupRectModal() added to toggleViewMode() and switchLayout() — prevents stale editingGroupRectId/pendingGroupRectGeom persisting across view switches and corrupting subsequent Save operations. Files: Sidebar-js.html)
  Previous: 2026-04-26 (render() calls requestAnimationFrame(refreshGroupRects) at end — group rects stay visible after data refresh/cable change in linear/flex without a view switch. Files: Sidebar-js.html)
  Previous: 2026-04-26 (BGP duplicate neighbor fix: generateBGP() emitted one neighbor entry per physical port-channel member (each member has its own sheet row, all pointing to the same logical interface/peer IP). Added seenGlobalNeighbors Set (global VRF) and seenVrfNeighbors Set (per VRF) to deduplicate by v4:<ip>, v6:<ip>, iface:<intf>, ov4:<ip>, ov6:<ip> — only the first occurrence is emitted. Peer group definitions (section 3) already had dedup via definedGroups Set. VRF neighbor section (section 6) also fixed. Files: Code.gs)
  Previous: 2026-04-26 (Linear view group rect support: New Group button now shown in linear mode (updateTopoButtons condition includes 'linear'); newGroupRect() guard allows layoutMode==='linear'; toggleViewMode cleanup removes #linear-grect-svg alongside #flex-grect-svg; refreshGroupRects() refreshes #linear-grect-svg layer. Files: Sidebar-js.html)
  Previous: 2026-04-26 (Duplicate close button fix: Sheet Manager footer "Close"→"Cancel" (edit/confirm modal — Cancel is correct, not a second close); Audit Settings footer "Close" removed entirely (action-only modal — Enable All/Disable All + header × is sufficient). Files: Sidebar.html)
  Previous: 2026-04-26 (UserGuide.html section 7 Technology Stack updated: replaced stale "Protocol grid (4 columns)" bullet with PROTOCOL UNDERLAY (BGP: IPv4/IPv6/Unnumbered/RFC5549, OSPF: v2-v3/Unnumbered) and PROTOCOL OVERLAY (VXLAN IPv4/IPv6 VTEP; EVPN with SERVICE Per-VLAN/Bundle and VGW Anycast GW/VARP including Virtual Router MAC field). Gateway description updated to clarify mechanism is set in VGW section. Toolbar "Technology Stack" description updated. Files: UserGuide.html)
  Previous: 2026-04-26 (Fix .info-box--dim .ip-ex-key width: 68px→100px — Auto Config example lives inside info-box--dim, so 68px override fired on GW section keys causing "GW Anycast vip" text to bleed past its element box (92px text > 68px element, both < 110px grid column so no clip, but technically wrong). Files: Sidebar-css.html)
  Previous: 2026-04-26 (Fix flex group rect persisting on view switch: #flex-grect-svg is appended to canvas-surface, not svg-layer — toggleViewMode cleanup only did svgLayer.innerHTML="" so the flex SVG survived Flex→Simple/Linear switches. Fix: d3.select('#flex-grect-svg').remove() added to toggleViewMode cleanup block. Files: Sidebar-js.html)
  Previous: 2026-04-26 (Group rect simulation timing fixes: (1) Linear→Flex: DOM style.left/top still held linear positions when renderGroupRectsInto fired — fix: apply nodesData initial positions to DOM elements before creating flex-grect-svg; (2) both views: nodes move after simulation starts but refreshGroupRects only fired on drag end — fix: d3Simulation.on("end.grects") and holisticSimulation.on("end") both call refreshGroupRects so member bounds snap correct after simulation settles. Files: Sidebar-js.html)
  Previous: 2026-04-26 (_computeGroupBounds/_getTopologyDevices root bug fix: both functions checked layoutMode to pick coordinate system, but layoutMode stays 'flexible' when switching Flex→Simple (toggleViewMode sets currentViewMode but not layoutMode). Fix: use _isHolisticView() = currentViewMode==='holistic' as the branch condition instead of layoutMode. Files: Sidebar-js.html)
  Previous: 2026-04-26 (_computeGroupBounds fix: holistic view nodes extend below circle due to pill labels (role/model/tags at NODE_R+8, up to 3 pills ×20px); now uses n.y+r+84 as bottom bound per node instead of n.y+r. Padding normalized to 30px for both flex and holistic views. Files: Sidebar-js.html)
  Previous: 2026-04-26 (Device-membership group rects (Option B): group rects now store `members: [deviceId]` array; bounds computed live from node positions on every render (_computeGroupBounds). Member rects: no drag/resize handles, body cursor:default, bounds auto-follow nodes. Legacy rects (no members): stored x/y/w/h, drag/resize as before. Modal adds Members checklist populated by _getTopologyDevices(). Button changed from "Draw Group" (toggleGroupRectMode crosshair) to "New Group" (newGroupRect() opens modal directly). Draw functions removed: beginGroupRectDraw, updateGroupRectDraw, endGroupRectDraw, mousedown trigger. refreshGroupRects() called on flex drag end so member rect bounds update after node move. Files: Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-26 (Revert compactSheet — removed compactSheet(), compactSheetFromMenu(), menu item, and tests; premature before add-link form is built. Kept only: finalRowCount buffer +5→+2 so Force Sync leaves 2 trailing blank rows instead of 5. Files: Code.gs, Tests.gs)
  Previous: 2026-04-26 (Schema column reorder: po moved after sp_mode — DEFAULT_SCHEMA_ARRAY order is now int → sp_mode → po → vlan… so po_ columns appear immediately after Mode, making it easier to fill in po_ fields once a po mode is selected. Affects new sheets and any sheet after Force Sync / Reset Schema. Files: Code.gs)
  Previous: 2026-04-26 (Link code bug fixes — 4 bugs: (1) redrawLinks() isNonEos check used wrong field allNodesData[link.u]?.details?.device_type (field doesn't exist on port data) — fixed to allDevicesData[devName].type via link.u.split(':')[0]; non-EOS links now correctly render amber. (2) hoverPort() used globalLinks.find(l=>l.id===lid) O(n) scan — replaced with linkMap[lid] O(1). (3) clearInteractionSVG() had no null guard before svg.innerHTML — added `if (svg)`. (4) drawBatch() had useless canvasCtx.beginPath()+canvasCtx.stroke() around stroke(path2d) loop — stroke(path2d) is immediate and doesn't affect the current path; the beginPath()+stroke() pair was a no-op; both removed. Files: Sidebar-js.html)
  Previous: 2026-04-26 (Topology view: (1) Icon reorder LINEAR|SIMPLE|FLEX — Simple and Flex sit adjacent since they share group rects; LINEAR remains default. (2) Group rects shared between Simple and Flexible views — same holistic_group_rects localStorage key; Flexible gets #flex-grect-svg SVG overlay (z-index:1, behind node cards z-index:20) created in startFlexibleLayout/destroyed in stopFlexibleLayout; _activeGroupRectSvg() helper selects correct SVG; toggleGroupRectMode and mousedown draw handler both allow flexible; holistic-controls panel shown in both simple+flexible but node-size buttons hidden in flexible; refreshGroupRects() refreshes both SVG layers. Files: Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-26 (Auto Config example GW section restructured: GW IPv4/IPv6 now show three flat rows — "GW IPv4 phy" (per-device last+id), "GW VARP vip" (ip virtual-router address, same on all), "GW Anycast vip" (ip address virtual, same on all); same pattern for IPv6. "GW IPv4"/"GW IPv6" rendered as section sub-headers spanning all 3 grid columns. ip-example-peer-grid key column widened 88px→110px; .ip-ex-key width 76px→100px to fit "GW Anycast vip". Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-26 (Ghost link fix — stale SVG interaction cleanup in redrawLinks(): after filter/hide, if hoveredLink.coords is null, clears interaction SVG and resets hoveredLink+cursor; if selectedLineId references a link with null coords, clears interaction SVG and resets selectedLineId+savedSelectionId. Prevents ghost SVG overlay lines persisting when devices/ports are hidden by DevVis or search. Files: Sidebar-js.html)
  Previous: 2026-04-26 (Auto Config example: "Anycast" label renamed to "Anycast GW" to match Technology modal terminology. Files: Sidebar.html)
  Previous: 2026-04-26 (Auto Config modal example section updated: added Lo1/Lo10 MLAG VTEP rows (Lo1=id.id.id.id unique, Lo10=min.min.max.max shared); GW section restructured to clearly label Anycast (ip address virtual, same on all) vs VARP (phy: last+id per device; vip: ip virtual-router address, same on all); removed misleading "(standalone)" label. Added collapse toggle (btnIpExampleToggle/ipExampleGrid) with chevron SVG; toggleIpExample() added to Sidebar-js.html. Files: Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-26 (Floating legend full overhaul: Roles corrected to match ROLE_COLORS.mid (Spine #7c3aed, Leaf #047857, Harness #1d4ed8, Transit #b45309, DUT #991b1b, IXIA #0891b2, Spirent #be185d, Non-EOS gray square border-radius:0); removed incorrect "L2" role entry (L2 is a port ip_type, not a device role). Status renamed "Port State" with 4 op-states (op-up/op-down/op-errdisabled/op-disabled) replacing old 2-state Connected/Disconnected. Added Links section with MLAG Peer swatch (#8b5cf6). Added VLAN Paths section (floatingVlanLegendGroup/floatingVlanItems, hidden until trace active) populated by updateVlanLegend(). Added .l-item .l-link CSS (20×3px horizontal swatch for link-type and VLAN items). Legacy #vlan-legend external overlay kept. Files: Sidebar.html, Sidebar-css.html, Sidebar-js.html)
  Previous: 2026-04-26 (Gating visual: applyEvpnGating() and applyTechCompat() no longer set style.opacity on containers/rows — only style.pointerEvents blocks interaction. Input controls grey via .chk-input:disabled{opacity:0.35} CSS; label text stays fully readable. Files: Sidebar-js.html)
  Previous: 2026-04-26 (DevVis hidden-device link fix: redrawLinks() now sets link.coords=null for any link whose endpoint is absent from posCache (hidden device or hidden port). Previously link.coords was only written on success and kept stale values — hoverPort's `if (link && link.coords)` guard passed on stale coords, drawing ghost interaction SVG lines and highlighting ports on hidden devices. With null clearing, hoverPort correctly skips hidden-device links; tooltip still works on visible device ports. Files: Sidebar-js.html)
  Previous: 2026-04-26 (Rename -P → -p for push timeout — VERSION 260426.18→260426.19: sshpass uses -p as an internal subprocess flag (not sys.argv), so no conflict. Improved -h help text: -t described as "Query timeout", -p described as "Push timeout" with explicit note that default auto-scales with -t but never drops below 300s (5 min). Updated startup banner and CLI comment. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-26 (PROTOCOL OVERLAY restructured to match UNDERLAY row style: VXLAN row (label+options inline) then EVPN row (label left, flex-column right: IPv4/IPv6 → SERVICE group with ⓘ+Per-VLAN+Bundle inline → VGW group with ⓘ+Anycast+VARP inline + MAC sub-row inside VGW). rowVarpMac is now a flex row inside evpnVgwSection — updateVarpMacVisibility() changed from display:'' to display:'flex' when shown. Files: Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-26 (DevVis visibility behavior: × close calls _devVisRestoreAll() (show all devices+links); − minimize preserves topology state, only collapses panel UI. toggleDevVisMinimize() is pure UI collapse — no state reset. Files: Sidebar-js.html)
  Previous: 2026-04-26 (Bug fixes — VERSION 260426.16→260426.18: (1) _find_ta_orphans: raw.get() moved to (raw or {}).get() — _ssh_cmds never raises, result can be None on internal failure. (2) _find_bgp_asn_change: summary.get() moved to (summary or {}).get() — same root cause. (3) open_only comment corrected: _ssh_stdin with force_tty=True always appends 2× exit for PTY cleanup; comment now accurately describes that we stop at show session-config diffs so diff is fully flushed before shell exits. 70 tests pass. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-26 (Tech modal PROTOCOL OVERLAY changed from 2-column side-by-side to vertical stack — DATA (VXLAN) on top, CONTROL (EVPN) below; outer wrapper dropped grid, CONTROL border-left → border-top. Files: Sidebar.html)
  Previous: 2026-04-26 (DevVis close/minimize now restore full topology: _devVisRestoreAll() helper (clears hiddenDevices+selectedInDevVis, force-sets all .node display:'', redraws links) called by both toggleDevVisPanelOpen() on close and toggleDevVisMinimize() on minimize. On minimize, also calls populateDevVisPanel() on re-expand to refresh eye-icon states. Previously applyDeviceVisibility() skipped .dimmed nodes, leaving hidden devices invisible after close. Files: Sidebar-js.html)
  Previous: 2026-04-26 (Tech modal PROTOCOL section restructured: replaced 4-column BGP|OSPF|VXLAN|EVPN grid with two new sections — PROTOCOL UNDERLAY (BGP + OSPF as horizontal option rows using proto-underlay-row/label/opts CSS classes) and PROTOCOL OVERLAY (DATA/VXLAN | CONTROL/EVPN two-column grid). Virtual Router MAC input (rowVarpMac/inputVarpMac) moved inline into CONTROL column below VARP row. chk-input CSS overhauled: appearance:none + custom SVG checkmark background-image so checkboxes and radio buttons render identically (blue square with white checkmark when checked). All element IDs unchanged — JS (applyTechCompat, applyEvpnGating, updateVarpMacVisibility) requires no changes. New CSS classes: proto-underlay-row, proto-underlay-label, proto-underlay-opts. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-26 (Push improvements — VERSION 260426.9→260426.16: (1) _SECTION_CLEANERS: 'no interface Vxlan1' → 'default interface Vxlan1' (resets to factory state in-place, cleaner than remove+recreate). (2) _find_bgp_asn_change() now returns (cmds, asn_info) tuple where asn_info={"from":old,"to":new} or None — caller unpacks both. (3) All 4 _push_device() return paths include asn_changed field when ASN mismatch detected. (4) _injectPushAsnChangeWarning(asnChanged) added — yellow banner injected into push confirm modal showing "BGP ASN changing: X → Y — BGP will reconverge after commit". Both pushDeviceConfig and pushAllConfigs inject it after _showPushConfirmModal(); in pushAllConfigs ASN banner injected before id-shift banner so id-shift stays on top (afterbegin ordering). (5) test_default_vxlan1 added; class docstring updated. 70 tests pass. Files: device_bridge.py, Sidebar-js.html, tests/test_device_bridge.py)
  Previous: 2026-04-26 (VGW moved inside EVPN column below SERVICE: evpnVgwSection sub-header + ⓘ + Anycast GW + VARP rows now inside tech-proto-col; standalone VGW section+radio card removed; infoVgw popup updated and repositioned below grid after infoEvpnService; applyEvpnGating() updated — evpnVgwSection gated on evpnOn||_techHasMlag; varpOk computed early and shared. End-to-end review: no additional bugs found. Files: Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-26 (Light-mode theming: #configBody switched from dark (#1e293b bg, #cbd5e1 text) to white (#ffffff bg, #1e293b text) with all EOS syntax-highlight colors recalibrated for light bg. Push modal diff <pre> blocks changed from dark (#0f172a bg) to light (#f8fafc bg, #e2e8f0 border) with + green #16a34a / - red #dc2626 / @ gray #64748b / default #1e293b. Tooltip stuck bug fixed: document mousemove handler now forcibly calls cancelTooltip()+hideTooltip() when activeTooltipPortId is set but e.target.closest('.port-row') is null — catches cases where a floating panel slides over a port row without triggering mouseleave. Files: Sidebar-css.html, Sidebar-js.html)
  Previous: 2026-04-26 (_SECTION_CLEANERS reverted to Vxlan1-only — VERSION 260426.5→260426.9: removed router bgp, router ospf/ospf3, and mlag configuration section cleaners; wiping those sections on every push causes unnecessary BGP reconvergence and MLAG flaps; only Vxlan1 needs pre-wipe (flood vtep is additive). Tests reverted: test_cleanup_bgp/ospf/mlag → test_passthrough_bgp/ospf/mlag + test_passthrough_multiple_sections (assert no cleanup prepended). 69 tests pass. Files: device_bridge.py, Sidebar-js.html, tests/test_device_bridge.py)
  Previous: 2026-04-26 (ip virtual-router mac-address now gated on role+EVPN/MLAG: generateGlobalBlock() guard = !mlagIsActive && isLeaf && (evpn_ipv4||evpn_ipv6) && GW; generateMlagConfig() guard = isLeaf && GW (MLAG sufficient, EVPN not required). isLeaf=deviceRole==='LEAF' at call site. detectMlagState/generateMlagConfig/generateGlobalBlock all take new isLeaf param. Tech modal: applyEvpnGating() gates Anycast GW on EVPN, VARP on EVPN||MLAG, SERVICE rows on EVPN; _techHasMlag from settings.hasMlag. Files: Code.gs, Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-26 (Minimize buttons for all modals + push modal footer fix: (1) _injectMinimizeButtons() now also finds headers via '.modal-header, .modal-keep-on-min' — handles configModal (config-header-modern) and reorderModal (reorder-header) which use custom header classes; (2) CSS minimize rule updated: .modal-minimized > *:not(.modal-header):not(.modal-keep-on-min) { display:none!important } — modal-keep-on-min exempts custom headers from being hidden; (3) config-header-modern and reorder-header gain class modal-keep-on-min in Sidebar.html; (4) _showPushConfirmModal() hides .modal-actions footer when alreadyInSync=true (per Rule 21 view-only = no footer); closePushConfirmModal() restores footer display. Files: Sidebar.html, Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-04-26 (Idempotent push improvements — VERSION 260425.66→260426.5: (1) BGP ASN detection bug fix: `summary.get("as")` was always None — EOS nests AS under `vrfs.<vrf>.as` not top-level; fixed to `next((v.get("as") for v in vrfs.values() if v.get("as")), None)`. (2) _SECTION_CLEANERS expanded: added router bgp (wipes stale BGP neighbors), router ospf/ospf3 with optional vrf suffix (wipes stale areas), mlag configuration (wipes stale peer config) — all alongside existing Vxlan1 cleaner. (3) Tests updated: 4 passthrough tests renamed to test_cleanup_* and assert cleanup lines are prepended; 3 new tests for ospf3 and OSPF vrf variants. 71 tests pass. Files: device_bridge.py, Sidebar-js.html, tests/test_device_bridge.py)
  Previous: 2026-04-26 (Tech modal EVPN/VGW gating: applyEvpnGating() added — Anycast GW requires EVPN; VARP requires EVPN or MLAG; EVPN SERVICE rows gated on EVPN selection; auto-switches VGW radio when selection becomes invalid. _techHasMlag cached from settings.hasMlag (added to getNetworkSettings() return). IDs added to Sidebar.html: evpnServiceSection, rowEvpnSvcPerVlan, rowEvpnSvcBundle, labelGwAnycast, labelGwVarp. Files: Sidebar.html, Sidebar-js.html, Code.gs)
  Previous: 2026-04-26 (L2 switchport cleanup: added `no switchport trunk native vlan` to generateAttributesBlock() L2 reset block (after `default switchport trunk allowed vlan`, before `default switchport access vlan`). `default switchport trunk allowed vlan` does NOT reset native VLAN — without this, removing nv<N> from the sheet left the old native VLAN on the device. Now native VLAN is always cleared to default (VLAN 1) before any new value is applied. Files: Code.gs)
  Previous: 2026-04-26 (EVPN service model moved inside EVPN column of protocol grid — SERVICE sub-header + ⓘ button + Per-VLAN/Bundle radios now live inside the EVPN tech-proto-col; infoEvpnService popup remains below grid at full width; standalone EVPN SERVICE MODEL label + radio card removed. Files: Sidebar.html)
  Previous: 2026-04-25 (Startup banner: Timeout line now shows both query timeout and push timeout separately ("Xs (queries: -t) | Push: Ys (-P)"); Endpoints line now lists all 5 endpoints (/health /lldp /devstatus /pushconfig /reconcile); Options line includes -P PUSH_TIMEOUT. VERSION 260425.65→260425.66. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-25 (Added -P PUSH_TIMEOUT flag to -h help text in device_bridge.py and embedded template. -P sets the configure-session push timeout independently of -t (read queries). Default: max(t*4, 300)s. Recommended: -t 30 -P 600 for large configs instead of inflating -t. VERSION 260425.57→260425.65. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-25 (IPv6 address cleanup — comprehensive: `no ipv6 address` prepended before all `ipv6 address` on Ethernet P2P (snake ::1/64 and regular ::sheetIndex), GW SVIs VARP paths (MLAG and standalone), non-EVPN standalone SVIs, and vx1 VARP SVIs. `default ipv6 address virtual` prepended before all `ipv6 address virtual` on GW SVIs Anycast paths (MLAG and standalone — vx1 Anycast was already done). Prevents IPv6 address accumulation when VLAN/IP/device-ID changes. Files: Code.gs)
  Previous: 2026-04-25 (IPv6 loopback cleanup: `no ipv6 address` now prepended before `ipv6 address` on Lo0 (always when IPv6 P2P enabled), Lo1, Lo10 (MLAG VXLAN). IPv4 `ip address` replaces in-place on EOS; IPv6 is additive — without cleanup, device ID shifts leave old IPv6 addresses on loopbacks alongside new ones. `default ipv6 address virtual` on vx1 Anycast SVIs was already handled. Files: Code.gs)
  Previous: 2026-04-25 (Auth expiry pre-check: do_POST now calls _check_ssh_agent() at the start of every POST request (key-based SSH only — skipped for sshpass/askpass). Returns {"auth_expired":True,"message":"..."} immediately before spawning any device threads, saving 30–120s of SSH timeouts per device when arista-ssh cert has expired. Sidebar: _handleBridgeAuthExpired(msg) helper shows bridgeAuthWarn banner + toolbar auth-expired icon + status error. Wired into: runBridgeCheck (throw+allSettled early exit), runReconcile, pushDeviceConfig (Phase 1), pushAllConfigs (per-device retry loop), orphan cleanup, deleteAndUnconfigure, iface push. VERSION 260425.50→260425.57. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-25 (DevVis dummy item fix: .dev-vis-ip-dummy visibility:hidden → display:none — removes blank grid cells from DevVis ip-type/int-mode/svi filter grids. Files: Sidebar-css.html)
  Previous: 2026-04-25 (CF A6 xcvr_speed+missing xcvr_type=RED; CF A8 missing vlan on l3-et/l3-po=AMBER; Audit Q missing vlan on l3-et/l3-po; gray↔audit sync fully closed; SheetVis layout fix pt2: vlan-section display:flex flex-direction:column; vlan-strip flex:1 overflow-y:auto (removed max-height:80px) — strip now fills full vlan-section height with no blank gap. Files: SheetAssistPanel.html)
  Previous: 2026-04-25 (SheetVis layout fix: panel-body flex:0 0 auto (shrink-wrap, not grow); device-list and col-items restored to max-height:84px (4-row cap, scroll); vlan-section flex:1 min-height:0 (fills remaining dialog height). Files: SheetAssistPanel.html)
  Previous: 2026-04-25 (_ssh_stdin force_tty=True: allocates PTY (-tt) so EOS assigns named VTY instead of UnknownTty. PTY doesn't propagate stdin EOF, so two explicit 'exit' commands are appended automatically — first exits any configure-session context (session stays PENDING), second closes EOS exec shell. Extra exits at exec level are harmless. All three call sites updated: _abort_stale_sessions, _push_config, _finalize_session. Synced to embedded template. VERSION 260425.17→260425.50. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-25 (Audit checks M/N/O/P added; CF rule A4 now only flags 2+ VLANs on l3; A6 removed — xcvr+Po is not an error; SheetVis blank space fix: .device-list and .col-items had max-height:84px capping lists to 4 rows and leaving blank space in panel-body — replaced with min-height:0 so flex:1 fills full available height. Files: SheetAssistPanel.html)
  Previous: 2026-04-25 (status msg: _regenerateAllConfigs completion now reads "All N configs ready — push each device to apply new IDs." Files: Sidebar-js.html)
  Previous: 2026-04-25 (SheetAssistPanel blank space fixes: (1) #navToast changed from flex child to position:absolute within .status-bar — removes invisible padding from flex layout, eliminating blank space on right of status bar. (2) .ip-item-dummy changed from visibility:hidden to display:none — removes blank grid cells in ip-type and svi filter sections. Files: SheetAssistPanel.html)
  Previous: 2026-04-25 (_regenerateAllConfigs bug fixes: (1) configCache structure fix — getDeviceConfig returns {config:configMap,error:...}; cache must be stored as {data:result.config} not result directly. (2) Push modal closed immediately on 'Regenerate All' — diff shown before regen was for old config; cancelPushConfig() called first so user re-initiates push with fresh diff after regen. Status message updated. Files: Sidebar-js.html)
  Previous: 2026-04-25 (Device ID shift detection + graceful config handling: (1) device_bridge.py: re-enabled Vxlan1 section cleaner (_SECTION_CLEANERS) — prepends 'no interface Vxlan1' before new Vxlan1 block on every push, clearing stale flood vtep IPs. VERSION bumped to 260425.17. (2) Code.gs: three new DocProps functions — getDeviceIdSnapshot() returns {name:sheetIndex} map or null; saveDeviceIdSnapshot() saves current IDs and returns map; checkDeviceIdShift() compares snapshot vs current IDs, returns {shifted:[{name,oldId,newId}], isFirstRun}. DEVICE_ID_SNAPSHOT stored in DocumentProperties. (3) Sidebar-js.html: _lastIdShiftResult module variable caches last shift check. On topology load: checkDeviceIdShift() called after fetchData() completes — status-warn shown if IDs shifted. On push: _injectPushIdShiftWarning() prepends yellow banner to push modal body with shifted device names + 'Regenerate All Configs' / 'Push Anyway' buttons. _regenerateAllConfigs() iterates all Arista devices, calls getDeviceConfig() for each with progress status, then calls saveDeviceIdSnapshot() to clear the warning. On first push: saveDeviceIdSnapshot() called after Phase 2 commit to establish baseline. (4) Tests.gs: test_deviceIdSnapshot() added — tests isFirstRun, save/get roundtrip, no-shift after save, shift detection with injected stale ID, new device not reported as shifted. Registered in runAllTests(). Files: device_bridge.py, Code.gs, Sidebar-js.html, Tests.gs, CLAUDE.md)
  Previous: 2026-04-25 (buildConditionalRules overhaul: restructured with 6 priority tiers (high→low): (1) AUDIT RED #fca5a5/#991b1b — value present in N/A field (A1:po filled+ET mode; A2:svi_vlan filled+l3; A3:ip_type filled+l2+no-svi; A4:vlan filled+pure-l3; A5:vrf filled+l2+no-svi; A6:transceiver cols filled+Po mode); (2) MISSING AMBER #fef3c7/#92400e — sp_mode_ empty when int_ filled; (3) INACTIVE GRAY #f1f5f9/#cbd5e1 — int_ empty → all device cols via single multi-range rule using SCHEMA_KEYS safe detection; (4) N/A GRAY #e2e8f0/#cbd5e1 — field N/A for mode (svi/l3, po/ET, ip_type/l2-no-svi, vlan/pure-l3, xcvr-cols/Po, vrf/l2-no-svi — 6 sub-rules); (5) QUALITY WARNINGS — existing xcvr amber+pale-orange; (6) MEMBER PORT — existing text dim. Added indices: vlanIdx, vrfIdx, encodingIdx. No auto-delete: onEdit never clears cell values. Requires Save & Sync. Files: Code.gs)
  Previous: 2026-04-25 (varp_mac no-default + validation: (1) openTechModal load: varpMacEl.value now falls back to '' (not '001c.7300.0099') — field shows empty when MAC was never configured. (2) getCurrentTechState: varp_mac fallback changed to '' so unset MAC is stored as empty string. (3) validateTechSettings Rule 5: if GW is enabled (chkGwIpv4||chkGwIpv6) and varp_mac is empty, save is blocked with error "L3 Gateway requires a Virtual Router MAC". No EOS default exists — 001c.7300.0099 is a convention from examples only. CLAUDE.md updated. Files: Sidebar-js.html, CLAUDE.md)
  Previous: 2026-04-25 (SheetVis layout polish: (1) navToast stays visible until GAS returns — showNavToast no longer auto-dismisses; hideNavToast() clears it; called as first statement in withSuccessHandler and withFailureHandler of _navigateVlan. (2) device-list and col-items capped at 4 rows: max-height:84px (4×21px row) on both — overflow-y:auto already present. (3) vlan-strip scrollable: max-height:80px; overflow-y:auto; align-items:flex-start so wrapped rows don't stretch. Files: SheetAssistPanel.html)
  Previous: 2026-04-25 (ip virtual-router mac-address GW guard: generateGlobalBlock() now only emits ip virtual-router mac-address when !mlagIsActive && (gw_ipv4 || gw_ipv6) — skipped for pure L2/routing-only standalone devices with no GW configured. Tests.gs: tests 4+6 updated to include gw_ipv4:true; new test 8 verifies no MAC emitted when GW disabled. CLAUDE.md updated. Files: Code.gs, Tests.gs, CLAUDE.md)
  Previous: 2026-04-25 (SheetVis navToast device name at click: _navigateVlan looks up vlanSummary client-side for vid's devices list; picks devices[occIdx % devices.length] to show "Jumping to VLAN <vid> on <device>…" immediately — no server round-trip. Falls back to "Jumping to VLAN <vid>…" if devices empty. Removed withSuccessHandler toast update. Files: SheetAssistPanel.html)
  Previous: 2026-04-25 (cancelPushConfig in-modal feedback: modal stays open with title "Aborting session on <device>…" and buttons disabled while abort fetch is in flight — closes only after fetch resolves. No-session path closes immediately with "Push cancelled" status. APP_VERSION 260425.29. File: Sidebar-js.html)
  Previous: 2026-04-25 (GW mechanism + MAC corrections: (1) useAnycastGW/useVarpGW: removed EVPN guard — ip address virtual and ip virtual-router address both work without EVPN; flags now gw_l3_type!=='varp' and gw_l3_type==='varp' respectively. (2) MLAG ip virtual-router mac-address: generateMlagConfig() now uses settings.varp_mac (same configured MAC as standalone) instead of auto-derived 0011.2233.XXXX — MAC must be identical on all switches. (3) Sidebar.html: rowGwL3Hint updated ("Works with or without EVPN..."); radio label drops "EVPN" suffix; rowVarpMac info-box corrected ("same configured MAC on all devices"). (4) Tests.gs: added non-EVPN anycast test (1b). (5) CLAUDE.md: useAnycastGW/useVarpGW and MAC invariants updated. Files: Code.gs, Sidebar.html, Tests.gs, CLAUDE.md)
  Previous: 2026-04-25 (_inferLogType regex fixes: (1) audit: now catches bare "Audit …"/"Audit:" prefix (Audit Mode, Audit Clean., Audit: N Issues) via /^audit[\s:]/ in addition to explicit [Audit] prefix. (2) config regex extended: "prefs"→"pref" (catches "Preferences Saved", "IP prefs saved"); added "refresh" (catches "Manual refresh — reloading topology"). bridge and push unchanged. Files: Sidebar-js.html)
  Previous: 2026-04-25 (SheetVis navToast two-phase with device name: _navigateVlan now fires showNavToast("Jumping to VLAN <vid>…") immediately at click (before GAS call), then updates to "Jumping to VLAN <vid> on <device>…" in withSuccessHandler when r.device is available. Toast CSS: no dark pill background — unstyled inline text (#64748b) matching status bar style. Files: SheetAssistPanel.html)
  Previous: 2026-04-25 (MLAG VXLAN split source IP: generateVxlanBlock() MLAG branch changed from shared-Lo1 (legacy) to split-source pattern (modern). Now emits Loopback1 with unique per-device IP (id.id.id.id, VTEP_UNIQUE) as vxlan source-interface, and Loopback10 with shared pair IP (min.min.max.max, VTEP_MLAG_SHARED) as vxlan mlag source-interface Loopback10. Old single Lo1 with shared IP removed. vxlan virtual-router encapsulation mac-address mlag-system-id retained. Flood list logic unchanged (remote MLAG pairs still identified by min.min.max.max). CLAUDE.md updated. Files: Code.gs, CLAUDE.md)
  Previous: 2026-04-25 (Status log improvements: (1) Audit per-line logging: all runValidation() calls now log each individual issue as its own [Audit] entry. Session dedup via _loggedAuditErrors Set prevents re-logging same error on auto-revalidate after edits; logIssues=true (explicit audit button) forces re-log. clearStatusLog() clears _loggedAuditErrors. (2) Configurable max lines: STATUS_LOG_MAX changed from const→let, default 5000. setStatusLogMax(n) trims log + updates #slgMaxCurrent label. Sidebar.html drawer footer: .slg-footer with Max lines dropdown (1k/2.5k/5k/10k/25k) + current label. (3) Hook fix: topoassist-deploy-tracker.sh now sets nvm PATH before clasp push + adds --force flag; fixes "Cannot read access_token" error in hook context. (4) .slg-entry.status-warn color added to CSS (#d97706). Files: Sidebar-js.html, Sidebar.html, Sidebar-css.html, hooks/topoassist-deploy-tracker.sh)
  Previous: 2026-04-25 (SheetVis navToast style + timing: removed dark pill background (was rgba(15,23,42,0.88)/light text); now unstyled inline text matching status bar (#64748b, no bg, no border-radius, no padding). _navigateVlan now calls showNavToast at move start (before GAS call) with "Jumping to VLAN <vid>…" instead of in withSuccessHandler — fires immediately. device navigate() was already correct. Files: SheetAssistPanel.html)
  Previous: 2026-04-25 (BGP ASN change auto-cleanup: _find_bgp_asn_change(ip, config_text) — queries device show ip bgp summary for current ASN; if config_text has router bgp <new_asn> and old_asn != new_asn, prepends 'no router bgp <old_asn>' to the configure session so the old block is removed first (EOS cannot change AS in-place). Called from _push_config whenever not dry_run; best-effort (returns [] on any failure). Synced to embedded template. VERSION 260425.14→260425.16. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-25 (Revert -tt PTY in _ssh_stdin: force_tty=True caused _ssh_stdin to hang/timeout (300s) because PTY mode doesn't propagate stdin EOF — SSH never exits. Reverted _ssh_stdin to _ssh_base() (-T, no PTY). UnknownTty in EOS show users is cosmetic/acceptable. _ANSI_RE.sub() is now a no-op but harmless. VERSION 260425.13→260425.14. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-25 (SSH PTY + auth fixes: (1) _ssh_base(force_tty=False) — added force_tty param; _ssh_stdin calls with force_tty=True to use -tt (force PTY) so EOS assigns a named VTY instead of 'UnknownTty' in 'show users'/syslog; show/JSON calls keep -T (no PTY). (2) _ANSI_RE = re.compile(r'\x1b(?:\[[0-9;]*[A-Za-z]|[^[])')  — module-level ANSI escape stripper; applied in _ssh_stdin to clean PTY control codes before diff parsing. (3) /health always reports health["auth"] = dict(_ssh_auth) whenever METHOD=="ssh" — was only reporting for key-based (sshpass/askpass modes never showed auth status in sidebar). All three changes applied to device_bridge.py AND embedded template in Sidebar-js.html. VERSION 260425.9→260425.13. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-25 (Status log type filter tabs: _inferLogType(msg) infers type at push time (audit/bridge/config/push/'') added to each statusLog entry. _statusLogType global drives renderStatusLog() — 'error' tab filters by cls, others by e.type. _updateLogTabCounts() updates tab button labels with live counts (e.g. "[Audit] 3"). setStatusLogType(type) switches active tab + re-renders. clearStatusLog() resets type to ''. HTML: .slg-tabs row with 6 buttons (All/Errors/[Audit]/[Bridge]/[Config]/[Push]) between slg-header and slg-list. CSS: .slg-tab + .slg-tab.active (blue) + error active (red). Files: Sidebar-js.html, Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-25 (Anycast GW ip virtual-router mac-address fix: generateGlobalBlock() condition changed from gw_l3_type==='varp' && !mlagIsActive to just !mlagIsActive — both VARP and anycast require ip virtual-router mac-address on standalone devices; MLAG block already emits it for MLAG-paired devices. updateVarpMacVisibility() now shows Virtual Router MAC input for both gateway modes (was VARP-only). rowGwL3Hint text corrected (removed false "No ip virtual-router mac-address needed" claim). Tests.gs: anycast standalone test flipped to assert MAC IS present; added anycast MLAG test asserting MAC not in global block. CLAUDE.md updated. Files: Code.gs, Sidebar.html, Sidebar-js.html, Tests.gs, CLAUDE.md)
  Previous: 2026-04-25 (Push overhaul: (1) Removed no shutdown from generateConfig (Et/PO) and generateComplexL3Block (SVIs, sub-interfaces, l3-et/po-int) in Code.gs — EOS default, generates noise. MLAG Vlan4093/4094 no shutdown kept. (2) Auto-clean orphan #TA interfaces during full-device push: bridge _find_ta_orphans() queries show interfaces description, finds #TA-tagged interfaces not in config_text, prepends default interface/no interface cleanup cmds; _push_config(all_ifaces=True) triggers this; JS passes all_ifaces:true when portFilter=null. Skips Loopback/Management/Vxlan/Vlan4093-4094. Best-effort — failure never blocks push. VERSION 260425.9. Files: Code.gs, device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-25 (SheetVis VLAN nav polish: (1) Code.gs navigateToVlanOccurrence: occurrences.push now includes device:devName; return value adds device:target.device (was {total,idx}, now {total,idx,device}). (2) SheetAssistPanel.html: .status-bar is now a flex row (space-between); #navToast is a flex child (no longer position:fixed) — toast appears right-aligned beside status text. <span id="statusText"> added inside statusBar; setStatus() targets statusText. _navigateVlan: toast fires in withSuccessHandler with device name ("Jumping to <device>…") instead of pre-flight "Jumping to VLAN <vid>…". 30s VLAN strip auto-poll: _vlanFp fingerprints vid:rowCount:flags; _pollVlanSummary is a no-op while in flight (_vlanPollActive guard); setInterval(30s) started after initial load. Files: Code.gs, SheetAssistPanel.html)
  Previous: 2026-04-25 (Tech modal anycast hint reword: rowGwL3Hint text changed from "Anycast GW requires EVPN enabled. VARP works with or without EVPN and requires a shared virtual MAC address." to "Emits ip address virtual on SVIs/sub-interfaces. No ip virtual-router mac-address needed — EVPN advertises the gateway MAC automatically. Requires EVPN enabled." — clarifies that ip virtual-router mac-address is NOT required for anycast mode. Files: Sidebar.html)
  Previous: 2026-04-25 (Cabling report Scenario C (mixed breakout) column order fix: build position-aware qLeftPart/qRightPart/bLeftPart/bRightPart so left half is always xcvr+speed and right half is always speed+xcvr — consistent with Scenario A/B. Old bPart=[speed,xcvr] landed on left position, inverting the order relative to all other cable lines. Files: Sidebar-js.html)
  Previous: 2026-04-25 (Two-phase push overhaul: _push_config open_only path no longer sends 'exit' as final SSH command — SSH stdin EOF closes the session naturally, leaving EOS session PENDING. 'exit' caused the SSH stream to close before 'show session-config diffs' output was fully flushed, producing an empty diff. _extract_session_diff stop condition now includes s.endswith('#') to detect bare EOS prompt at EOF. eAPI open_only path uses results[-1] (last cmd is show diffs). _abort_stale_sessions runs before open_only and commit pushes. _pushSessionGuard auto-aborts after 5 min of inactivity. _pendingPush guard added to pushAllConfigs. pushDeviceConfig has cancel-while-in-flight guard. VERSION bumped to 260425.1. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-24 (Vx1 port card row: col-et shows only "Vx1" (no VLAN suffix); hover over the row shows a tooltip with the generated interface Vxlan1 config via showVx1Tooltip(e, deviceName). showVx1Tooltip uses configCache[dev].data["055_VXLAN"].full; shows "Loading…" immediately then re-renders on fetchFullConfig callback. Uses activeTooltipPortId="vx1:<dev>" sentinel so mouseleave cancels correctly. Files: Sidebar-js.html)
  Previous: 2026-04-24 (Vx1 VLAN range bug fix: (1) Code.gs getTopologyData: replaced parseInt(v.trim()) with expandVlanString(_vx1Vlans).forEach(v=>...) when building vtepVlansByDevice — parseInt("1-4000")=1 was silently truncating ranges; expandVlanString correctly expands to {1..4000} → compressVlanRanges gives "1-4000". (2) Sidebar-js.html buildVlanStrip + buildVlanSummary Pass 1b: replaced parseVlanInput(node.vtepVlans) with node.vtepVlans.split(',') — parseVlanInput would re-expand "1-4000" to 4000 individual chips/rows; comma-split preserves range tokens as single chip/row ("1-4000" → one chip-vtep). Files: Code.gs, Sidebar-js.html)
  Previous: 2026-04-24 (DevVis + VLAN Summary Modal — Vx1/VTEP VLAN support: (1) buildVlanStrip: added hasVtep:false to vlanInfo init; new pass reads currentData.nodes[].vtepVlans (compressed ranges), expands via parseVlanInput(), marks hasVtep=true; chip priority chip-vtep>chip-svi>chip-trunk>chip-native; tooltip shows "· VTEP"; .dev-vis-vlan-chip.chip-vtep added to Sidebar-css.html (teal #ccfbf1/#0f766e/#5eead4). (2) buildVlanSummary Pass 1b: scans currentData.nodes.vtepVlans, expands ranges, adds vtepDevices:[node.id] annotation; VLANs new to map get type:'VTEP'; existing entries get vtepDevices appended. (3) renderVlanSummaryTable: VTEP type shown in teal #0d9488; vtepDevices shown as "[Vx1: leaf1, leaf2]" teal annotation in ports column. Files: Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-04-24 (Push mechanism gap fixes + template sync: (1) Bug1 cancel-while-in-flight: pushDeviceConfig now checks modal visibility after Phase 1 fetch returns — if user cancelled while SSH was in-flight, aborts the PENDING EOS session silently and returns (mirrors _pushConfigText guard); (2) Bug2 phase tracker label: "Phase 1: Dry-run / diff ready" → "Phase 1: Config staged / session open" — correctly reflects open_session semantics; (3) Bug3 pushAllConfigs guard: added _pendingPush check at entry — Push All now warns and returns if a single-device push is awaiting confirm (prevents _abort_stale_sessions killing the open session); (4) Bug4 eAPI finalize detection: _finalize_session eAPI path now calls show configuration sessions first and raises RuntimeError if session_name absent — prevents silent false-positive commit on expired sessions; (5) Template _SECTION_CLEANERS sync: embedded template had old 8-entry list; cleared to empty [] matching device_bridge.py. VERSION 260424.43. Files: Sidebar-js.html, device_bridge.py)
  Previous: 2026-04-24 (Vx1 port card row: col-et shows "Vx1  VL <list>" (AP VLANs as muted italic suffix in interface column); col-mode shows "N/A"; col-po empty. Files: Sidebar-js.html)
  Previous: 2026-04-24 (canonicalizeInterface audit: _buildExpectedPortsPerDevice() now wraps po_ in canonicalizeInterface() before adding to expected set — po_ is raw user input and may be Port-Channel10 instead of Po10. port from allNodesData key is already canonical (processed in getTopologyData()). CLAUDE.md updated with global rule: never inline Ethernet→Et substitutions, always use canonicalizeInterface() / _norm_iface(). Files: Sidebar-js.html, CLAUDE.md)
  Previous: 2026-04-24 (Vx1 port card row: col-mode now shows "N/A" (italic, muted) instead of vtepVlans; vtepVlans moved to col-po (repurposed — Vx1 never has a PO) with teal color and "AP VLANs: <list>" tooltip. sp_mode_ is NOT used for Vx1 config generation. Files: Sidebar-js.html)
  Previous: 2026-04-24 (Push session auto-abort guard: _pushSessionGuard (5-minute setTimeout) starts after Phase 1 opens an EOS configure session — if user doesn't commit/cancel within 5 min, guard fires: aborts session via /pushconfig/finalize, closes modal, restores push button, shows "Push session timed out" status. _clearPushSessionGuard() helper clears it; called at start of commitPushConfig(), cancelPushConfig(), and closePushConfirmModal() to prevent double-abort. APP_VERSION 260424.39. File: Sidebar-js.html)
  Previous: 2026-04-24 (Four-part #TA ownership + reconcile improvements: (1) Sub-interface descriptions: generateComplexL3Block() sub-int branch now emits "description -> PeerDev-PeerPort #TA" (was bare "#TA"). (2) _SECTION_CLEANERS emptied: push no longer prepends "default interface / no router bgp / no router ospf / no mlag configuration" before sections — generated config is self-sufficient. Tests updated from 13 old cleaner tests to 7 pass-through tests. (3) BGP #TA: all neighbor description lines in generateBGP() (global + VRF) and generateBGPEvpnOverlay() now include "#TA" suffix so reconcile can track/delete stale BGP neighbors. (4) Orphan cleanup: _norm_iface(name) extracted to module level in device_bridge.py (Ethernet→Et, Port-Channel→Po, lowercase) — _reconcile_device now matches long-form EOS JSON names to abbreviated topology names; 9 tests added. _buildExpectedPortsPerDevice() now includes sub-interfaces (l3-*-sub-int → port.vlan) and SVIs (l2-* with svi_vlan_). unconfigureOrphan() sends correct EOS cleanup: "no interface" for sub-ints/SVIs, "default interface" for physical/PO. VERSION 260424.36→260424.37. Files: Code.gs, device_bridge.py, Sidebar-js.html, tests/test_device_bridge.py)
  Previous: 2026-04-24 (Push two-phase session: Phase 1 uses open_session:true → bridge opens configure session, pushes config, gets diff, leaves session PENDING on EOS (exit instead of abort), returns session_name. Phase 2 commit uses /pushconfig/finalize (new endpoint) with {action:'commit', session_name} — re-enters named session and commits without re-pushing any config. Cancel calls /pushconfig/finalize with abort + shows "session aborted" status. Phase 3 verify unchanged (dry_run). device_bridge.py: _push_config(open_only=False) param; _finalize_session(ip, session_name, action) new method; /pushconfig/finalize HTTP endpoint; _extract_session_diff stops on #exit too. VERSION 260424.36. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-24 (DevVis panel Rule 6 fix: added user-select:none/-webkit-user-select:none to .dev-vis-item, .dev-vis-name, .dev-vis-count, .dev-vis-ip-label; added cursor:default to .dev-vis-ip-label. Fixes phantom I-beam cursor blinking near search input. Files: Sidebar-css.html)
  Previous: 2026-04-24 (SheetVis VLAN chip — Vx1/VTEP VLAN support: getSheetVlanSummary now reads int_<device> columns (devIntCol map) to detect Vx1 rows via canonicalizeInterface(intRaw)==='Vx1'. Sets hasVtep:true on all VLANs appearing on Vx1 rows (both trunk tokens and native). hasVtep returned in summary object. chip-vtep CSS (teal: #ccfbf1 bg / #0f766e text / #5eead4 border) — matches DevVis Vx1 row color. buildSapVlanStrip priority: hasVtep → chip-vtep > hasSvi → chip-svi > trunk → chip-trunk > chip-native. Files: Code.gs, SheetAssistPanel.html)
  Previous: 2026-04-24 (SheetVis nav toast: #navToast (position:fixed, bottom:40px, right:8px, dark pill, opacity transition 0.15s, 1.4s auto-dismiss) fires on any navigation — device click calls showNavToast("Jumping to <name>…"), VLAN chip calls showNavToast("Jumping to VLAN <vid>…"). showNavToast(msg) uses clearTimeout(_navToastTimer) to reset on rapid clicks. Toast is separate from status bar: status bar stays persistent ("VLAN 10 · row 1/3"), toast is transient action feedback. Files: SheetAssistPanel.html)
  Previous: 2026-04-24 (SheetVis VLAN chip UI overhaul: (1) .vlan-count badge: inline-block pill (vertical-align:middle, border-radius:6px, padding 1px 4px, rgba(0,0,0,0.2) bg) instead of superscript. (2) Persistent status: _refreshVlanStatus() shows "VLAN 10 · row 1/3" whenever a chip is selected, stays until cleared (no setTimeout); sapVlanTotal{} caches total per vid so status shows immediately with cached value before server round-trip. (3) Multi-select shows "N VLANs selected". (4) clearSapVlanFilter resets sapVlanTotal and calls setStatus(''). Files: SheetAssistPanel.html)
  Previous: 2026-04-24 (SheetVis VLAN chip count badge: .vlan-count restyled from invisible (8px, opacity:0.65) to bold 9px superscript with rgba(0,0,0,0.15) background badge (clearly visible on all chip colors). Tooltip now shows "N rows · dev1, dev2" instead of device names only. Files: SheetAssistPanel.html)
  Previous: 2026-04-24 (_compressVniLines: upgraded from one-line-per-run to single comma-separated line — e.g. VLANs {10,11,12,20} → " vxlan vlan 10-12,20 vni 10010-10012,10020". vlanParts[] and vniParts[] built separately then joined with ','. Returns 1-element array (or [] for empty). Tests.gs: "two runs", "non-consecutive singles", "unsorted input" expected values updated to comma-separated single-line format. Files: Code.gs, Tests.gs)
  Previous: 2026-04-24 (Bridge auto-reconnect default: changed bridgeAutoReconnect localStorage check from === 'true' to !== 'false' so checkbox is enabled by default (absent key = on). Files: Sidebar-js.html)
  Previous: 2026-04-24 (VXLAN VNI mapping: consecutive VLANs now emit a single range line instead of one line per VLAN (e.g. "vxlan vlan 10-20 vni 10010-10020"). _compressVniLines(vlansIterable, vniBase) extracted as pure helper before generateVxlanBlock() — takes a Set/iterable, filters 1-4094, sorts, collapses consecutive runs. generateVxlanBlock() calls it via forEach. Tests.gs: test_compressVniLines() added (8 cases: single, full-range, two-runs, non-consecutive, unsorted, out-of-range, custom base, empty); registered in runAllTests(). Files: Code.gs, Tests.gs)
  Previous: 2026-04-24 (SheetVis VLAN chip overhaul: (1) navigateToVlanOccurrence(vid,occIdx) replaces scrollToVlanCell in Code.gs — scans all vlan_ columns, finds matching rows by token (parseVlanWithNative), navigates to int_ column of Nth occurrence (hidden vlan_ column safe), returns {total,idx}. (2) getSheetVlanSummary now returns rowCount (trunk+native row counts). (3) Chip shows rowCount superscript badge (.vlan-count). (4) Single click same chip cycles to next occurrence instead of clearing; status shows "VLAN X · row N/total". (5) sapVlanOccIdx tracks occurrence index per vid; cleared on clearSapVlanFilter(). Range tokens (1-100) stored/matched as-is — no expansion. Files: Code.gs, SheetAssistPanel.html)
  Previous: 2026-04-24 (Bridge: toolbar icon red when down (was yellow); auto-reconnect every 60s — checkbox "Auto-reconnect on disconnect" added below "Enable on topology bring-up" in bridge settings card (chkBridgeAutoReconnect, saved to localStorage bridgeAutoReconnect). setBridgeAutoReconnect() added. _syncBridgeEnableCheckbox() syncs both checkboxes. _checkBridgeHealth() catch schedules 60s _bridgeReconnectTimer if pref is set; success path clears it. _bridgeReconnectTimer global added. Files: Sidebar-css.html, Sidebar-js.html, Sidebar.html)
  Previous: 2026-04-24 (SheetVis VLAN header: added .vlan-hint span ("shift+click multi") beside "vlans" label — muted italic 9px, overrides uppercase/bold of header. Navigation status restored: setStatus('Jumping to {device}…') before scrollToVlanCell(), always cleared in both withSuccessHandler and withFailureHandler (no serverBusy guard). Files: SheetAssistPanel.html)
  Previous: 2026-04-24 (Rollback: bridge URL configurability removed — SSH tunnel required to reach bus-home defeats the purpose; BRIDGE_URL reverted to const 'http://localhost:8765', saveBridgeUrl() removed, URL input field removed from Sidebar.html bridge settings card, _checkBridgeHealth() catch block reverted to static "python3 device_bridge.py" text. Files: Sidebar-js.html, Sidebar.html)
  Previous: 2026-04-24 (SheetVis VLAN chip complete rework: (1) scrollToDeviceColumn() was removed from Code.gs long ago — all prior navigation calls were silently failing. Added scrollToVlanCell(deviceName) to Code.gs: finds vlan_{device} column header in row 2, sets active range to that cell. (2) Single click replaces filter with just clicked VLAN (toggle-clear if already sole); Shift+click adds/removes (multi-select). (3) On activation always calls scrollToVlanCell(entry.devices[0]) silently. onclick now passes event: toggleSapVlanChip(event,'vid'). Files: Code.gs, SheetAssistPanel.html)
  Previous: 2026-04-24 (Bridge URL configurable — run bridge on bus-home: BRIDGE_URL changed from const 'http://localhost:8765' to let, reads from localStorage key 'bridgeUrl' (default: 'http://localhost:8765'). Bridge settings card in Sidebar.html gets a URL input field (oninput → saveBridgeUrl()). saveBridgeUrl() updates BRIDGE_URL + localStorage + triggers _checkBridgeHealth(). openBridgeModal() populates the input on open. _checkBridgeHealth() catch block dynamically builds bridgeCmd text: localhost → "python3 device_bridge.py"; remote → "python3 device_bridge.py -b \"\"". Files: Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-24 (SheetVis VLAN chip: on activation, toggleSapVlanChip() now calls navigate(entry.devices[0]) to scroll the sheet to the first device carrying that VLAN. No navigation on deactivation. File: SheetAssistPanel.html)
  Previous: 2026-04-24 (SheetVis VLAN chip fix: toggleSapVlanChip() was receiving vid as a JS number literal (unquoted in onclick), but vlanSummary[i].vid is a string (JS object key). Strict indexOf mismatch caused all devices to disappear on chip click. Fixed by quoting vid in onclick: toggleSapVlanChip(\'vid\'). SheetVis placeholder updated to "Search devices, columns & VLANs…". Files: SheetAssistPanel.html)
  Previous: 2026-04-24 (DevVis + SheetVis search UX: DevVis search row moved above filter sections (between sel-row and ip_type section); placeholder updated to "Search devices & VLANs…". Both filterDevVisList() and filterAll() extended to also hide VLAN chips whose vid doesn't contain the query string — search now covers devices, columns (SheetVis), and VLAN chips in both panels. Files: Sidebar.html, Sidebar-js.html, SheetAssistPanel.html)
  Previous: 2026-04-24 (device_bridge.py VERSION 260423.35 — SSH error clarity: added _clean_ssh_err(err_text) helper that strips SSH noise lines (pseudo-terminal, permanently-added, debug:) before surfacing errors. Applied to _push_device_ssh RuntimeError ([:200]) and _ssh_stdin verbose logger ([:160]). Belt-and-suspenders over -T+LogLevel=ERROR: if noise somehow appears, real error is first. Template in downloadBridgeScript() synced. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-23 (device_bridge.py VERSION 260423.34 — two SSH fixes: (1) PTY/noise suppression: added -T flag + LogLevel=ERROR to all three _ssh_base() branches; eliminates "Pseudo-terminal will not be allocated" and "Warning: Permanently added" from SSH stderr. (2) Push timeout scaling: added PUSH_TIMEOUT config var (CLI: -P, env: BRIDGE_PUSH_TIMEOUT, default: max(TIMEOUT*4,300)); _ssh_stdin now uses PUSH_TIMEOUT when len(cmds)>5 so large configure sessions (28k+ commands) don't time out mid-push while read queries keep using the smaller TIMEOUT. Both changes applied to device_bridge.py AND the embedded template in downloadBridgeScript() in Sidebar-js.html. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-24 (Audit toolbar icon three-state color: (1) Sidebar-js.html — _updateAuditToolbarIcon(hasIssues) added; wired into runValidation() after setStatus line (portErrorCount > 0 drives hasIssues); toggleValidationMode() calls _updateAuditToolbarIcon(false) when disabling audit; applyValidationState() stripped of inline style overrides — class-based only. (2) Sidebar-css.html — 3 CSS rules added mirroring bridge pattern: #validateBtn.audit-issues{color:#ef4444}, .audit-clean{color:#22c55e}, .audit-disabled{opacity:0.45}. (3) UserGuide.html — Audit Engine intro updated to describe icon color states: dimmed=disabled, green=enabled+clean, red=enabled+issues. Files: Sidebar-js.html, Sidebar-css.html, UserGuide.html)
  Previous: 2026-04-24 (Bridge modal Audit Flags removed — consolidated into Audit Checks modal: (1) Sidebar.html — removed separator + "Audit flags" section with 3 checkboxes (chkBridgeAuditMismatch/Missing/InternalVlan); (2) Sidebar-js.html — removed 3 sync lines from openBridgeModal(); removed toggleBridgeAuditMismatch/Missing/InternalVlan() functions (now dead — callers were those checkboxes only); added applyLldpIndicators() call in toggleAuditCheck() when name==='bridgeAuditFlagMissing' to preserve gray↔red dot recolor side-effect; (3) UserGuide.html — "Audit Flags" h3 replaced with "Bridge/LLDP Audit Checks" note pointing to Audit Checks modal; stale references to "Flag internal VLAN conflicts" and "Flag missing neighbors" in Device Bridge modal updated. Files: Sidebar.html, Sidebar-js.html, UserGuide.html)
  Previous: 2026-04-24 (holistic view: devVisPanel auto-closes and devVisPanelBtn dims (opacity 0.3, pointer-events:none) when switching to holistic mode — panel not applicable without port cards; restores on switch back. updateTopoButtons() step 3d added. File: Sidebar-js.html)
  Previous: 2026-04-24 (Audit icon single/double-click — UserGuide: Audit Engine intro paragraph updated to describe single-click→settings modal, double-click→toggle audit mode. File: UserGuide.html)
  Previous: 2026-04-24 (Audit icon single/double-click: validateBtn onclick changed from toggleValidationMode() → onAuditIconClick(); gear button (btnAuditSettings) removed from toolbar. Single click → openAuditSettingsModal() (220ms debounce); double click → toggleValidationMode(). _auditClickTimer added alongside _bridgeClickTimer. Mirrors existing onBridgeIconClick() pattern exactly. Files: Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-24 (Audit Settings Modal — UserGuide: "Audit Check Settings" section added under Audit Engine. Documents all 24 checks in a 3-column table (Category / Check / Description), gear icon, dot-color legend, and Enable All/Disable All. VERSION 260424.3→260424.4. File: UserGuide.html)
  Previous: 2026-04-24 (Audit Settings Modal: 24 individual audit check toggles organized into 7 categories (Port-Channel 4, Interface 3, VLAN 5, IP/Gateway 5, Mode/Speed 2, MLAG 2, Bridge/LLDP 3). Each check has a status dot: gray=disabled/not-run, green=enabled+clean, red=enabled+issues. State in localStorage (default all enabled). JS: _ALL_AUDIT_FLAGS[], _getAuditFlag(), _setAuditFlag(), openAuditSettingsModal(), closeAuditSettingsModal(), refreshAuditSettings(), refreshAuditSettingsDots(), toggleAuditCheck(), enableAllAudits(), disableAllAudits(); auditHasRun bool + auditCheckHadIssue{} object reset per runValidation() call. Gear button (btnAuditSettings) added to audit segmented-group in toolbar. Modal registered in modalOrder, closeFuncs, closeAllModals(). CSS: .audit-cat-hdr/.audit-chk-row/.audit-chk-label/.audit-status-dot/.dot-green/.dot-red. VERSION 260423.38→260424.2. Files: Sidebar.html, Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-04-24 (SheetAssistPanel VLAN chip strip: new "vlans" section added below device list. CSS (.vlan-section/.vlan-chip/.vlan-clear/.hidden-vlan), HTML (#vlanSection), and JS (vlanSummary[], sapVlanFilter[], buildSapVlanStrip(), toggleSapVlanChip(vid), clearSapVlanFilter(), applySapVlanFilter()) all added. load() fires getSheetVlanSummary() after init. toggleMinimize() hides/shows #vlanSection; height bumped 550→590. Filter is client-side OR-logic: matching devices shown, non-matching get .hidden-vlan. Files: SheetAssistPanel.html)
  Previous: 2026-04-23 (ipConfigModal MLAG PEER LINK: restored full inner label "Base (lower gets .base, higher .base+1)"; fixed first-column truncation by setting --ip-label-w:110px on the ip-row (was inheriting 80px default, clipping "Peer Link IP" to "Peer Link..."). File: Sidebar.html)
  Previous: 2026-04-23 (VLAN chip strip in devVisPanel: new "vlans" section added between svi_vlan and search row. Chips color-coded green=SVI, blue=trunk, grey=native-only; clickable to filter port rows by VLAN (OR logic, hidden-vlan CSS class). Clear button when filter active. Resets on non-silent topology reload. New: buildVlanStrip(), toggleVlanChip(vid), clearVlanFilter(), applyVlanFilter(); topoVlanFilter=[]; _DEV_VIS_MIN_IDS updated. Files: Sidebar.html, Sidebar-css.html, Sidebar-js.html)
  Previous: 2026-04-23 (ipConfigModal MLAG PEER LINK: inner label shortened from "Base (lower gets .base, higher .base+1)" to "Base (.base / .base+1)" — long text was pushing ip_mlag_base input off-screen. File: Sidebar.html)
  Previous: 2026-04-23 (Auto Config / push confirm UX fix: push confirm modal commit button now hidden (display:none) during diff-loading state — only appears once diff content is ready. Sidebar-js.html _showPushConfirmModal loading branch. Files: Sidebar-js.html)
  Previous: 2026-04-23 (setStatus gap fixes: (1) deleteAndUnconfigure "delete anyway" error path now calls setStatus('{device}:{port} deleted — topology refresh…') before deleteInterface GAS call; (2) _onAddDiscoveredDone partial/full success messages now append ' — topology refresh…' suffix. File: Sidebar-js.html)
  Previous: 2026-04-23 (Audit check L — SVI VLAN Mismatch: Sidebar-js.html CHECK GROUP 2 now flags when svi_vlan_ contains a VLAN ID not present in vlan_. _parseSviVlans() silently drops such VLANs during config gen (no SVI generated); this check surfaces the mismatch. Logic: parseVlanInput(vlan_) + native build allowedSviVlans set; svi_vlan_ tokens (plain ID and nv<N> shorthand) checked against it; missing IDs → configErrors.push("SVI VLAN Mismatch: ..."). Applies only to l2-* modes; svi_vlan=all is exempt. UserGuide.html: "SVI VLAN Mismatch" entry added in Audit Engine section. FILES: Sidebar-js.html, UserGuide.html)
  Previous: 2026-04-23 (Sheet View menu: added "L2/L3: IP" preset — viewIpTypeL2L3() calls applyCustomView(['int','po','sp_mode','vlan','svi_vlan','ip_type']). ip_type filter activity detection upgraded from int_-only to L2/L3 columns (int + po + sp_mode + vlan + svi_vlan) so L2-only rows with po_/vlan_ but no int_ are not missed. devPoCol and devVlanCol maps added to refreshSheetRowVisibility single-pass header scan. Rolled back standalone L2/L3 ip_type_active SheetAssistPanel filter (was redundant). Files: Code.gs, SheetAssistPanel.html)
  Previous: 2026-04-23 (setStatus before fetchFullConfig call sites: IP Config save now calls setStatus('IP prefs saved — {device} config refresh…') before fetchFullConfig; Tech Settings save calls setStatus('Tech settings changed — {device} config refresh…') before fetchFullConfig; Global Config save calls setStatus('Global config changed — {device} config refresh…') before fetchFullConfig. fetchFullConfig internal status now always shows "Fetching {device}…" regardless of customSuccessMsg (removed wrong "Tech Settings Saved & Fetching…" branch). File: Sidebar-js.html)
  Previous: 2026-04-23 (int_ onEdit toast removed — audit-only validation: Code.gs onEdit PHASE 0 now only validates po_ columns with a toast; int_ columns accept any non-empty value on edit — unsupported interface type (non-Et/Vx) is flagged in Audit mode only via validIntRegex=/^(Et|Vx)\d+/i in Sidebar-js.html CHECK GROUP 2. Rationale: users type interface names iteratively; onEdit toast mid-entry is disruptive. UserGuide.html onEdit description updated to reflect audit-only int_ checking. FILES: Code.gs, UserGuide.html)
  Previous: 2026-04-23 (Reconcile feature: (1) device_bridge.py — /reconcile POST endpoint added (elif before else/pushconfig); _reconcile_device() SSHes `show interfaces description`, filters lines containing #TA, skips Loopback*/Management*/Vxlan*/Vlan4093/4094, diffs against expected_ports list, returns {ok, ta_total, matched, orphans:[{name, description, linkStatus, protocol}]}; VERSION 260422.20→260423.22. (2) Sidebar.html — Reconcile button (btnRunReconcile) added to bridge modal footer; bridgeReconcileResults div added to bridge modal body; btnDeleteUnconfigure (btn-danger-mono, margin-right:auto) added to editModal footer alongside btnDelete. (3) Sidebar-css.html — .bridge-recon-dev/.bridge-recon-dev-hdr/.bridge-recon-counts/.bridge-recon-ok-msg/.bridge-orphan-list/.bridge-orphan-row/.bridge-orphan-info/.bridge-orphan-name/.bridge-orphan-desc/.bridge-link-badge(.badge-up/.badge-down/.badge-unk)/.btn-unconfigure styles added. (4) Sidebar-js.html — _esc() HTML-escape helper; _buildExpectedPortsPerDevice() builds {dev:[ports+POs]} from allNodesData; runReconcile() posts to /reconcile, calls _renderReconcileResults(); _renderReconcileResults() builds orphan rows with Unconfigure buttons; unconfigureOrphan(dev,ip,portName,btn) pushes `no description` via /pushconfig; deleteAndUnconfigure() deletes from sheet+unconfigures on device; showEdit() shows btnDeleteUnconfigure when bridge is up (replacing btnDelete), btnDelete when bridge is down; openConnectModal/openAddLinkModal hide both delete buttons; _checkBridgeHealth() enables/disables btnRunReconcile alongside btnRunBridge. Files: device_bridge.py, Sidebar.html, Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-04-23 (int_ strict type validation restored — Et and Vx only: (1) Code.gs onEdit PHASE 0: int_ regex changed to /^(Et|Vx)\d+/i — only Ethernet and Vxlan/VTEP are valid Arista cable-endpoint interface types; toast "only Et or Vx allowed" on mismatch; (2) Sidebar-js.html audit CHECK GROUP 2: validIntRegex restored as /^(Et|Vx)\d+/i; new check A "Unsupported Interface" fires when port name doesn't match — Po/Lo/Ma/Tu/Twe/Gi/Te/Hu/Fa all flagged. Rationale: Arista uses Et for all physical Ethernet regardless of speed; Po belongs in po_ column; virtual interfaces are not cable endpoints. VERSION 260423.20→260423.21. Files: Code.gs, Sidebar-js.html)
  Previous: 2026-04-23 (ROLLBACK: removed incorrect l2/l3 row-visibility filter added to SheetAssistPanel.html and Code.gs — was a standalone "l2 / l3" filter section toggling row visibility; user's actual request is different. Files: SheetAssistPanel.html, Code.gs)
  Previous: 2026-04-23 (setStatus on remaining refresh triggers + schema/force-sync now call fetchData: (1) Device Manager save: handleSuccess now builds +added/-removed device name list and calls setStatus("Device Manager saved: +X, −Y — topology refresh…") before fetchData(true); (2) Schema Manager onComplete: after structural or dropdown sync, calls setStatus("Schema columns/dropdowns updated — topology refresh…") + fetchData(true) — rebuildSheet does not bump DATA_VERSION so poll cannot catch this; label-only changes skip fetchData; (3) Force Sync success: calls setStatus("Force Sync complete — topology refresh…") + fetchData(true) before the 1.5s button-reset timeout — same DATA_VERSION gap applies. "Snapshot restore" has no dedicated feature; session-start cache restore already covered. FILES: Sidebar-js.html)
  Previous: 2026-04-23 (Remove unsupported interface rejection from int_: (1) Code.gs onEdit PHASE 0: removed isValidInt check and toast warning for int_ columns — int_ now accepts any non-empty value with no prefix restriction; po_ format check retained; (2) Sidebar-js.html audit: removed validIntRegex constant and "Invalid Interface Name" check (was CHECK GROUP 2 check A) — no audit error is raised for non-standard interface name prefixes. UserGuide updated: Format validation description updated to remove int_ mention; "Invalid Interface Name" audit entry removed. VERSION 260423.17→260423.18. Files: Code.gs, Sidebar-js.html, UserGuide.html)
  Previous: 2026-04-23 (#TA description marker on all TopoAssist-managed interfaces: every interface generated by generateConfig/generateComplexL3Block/generateVxlanBlock now includes a `#TA` suffix in its description, enabling device-side reconciliation via `show interfaces description | grep "#TA"`. Rules: (1) Physical/Et user desc → `<text> #TA`; (2) auto peer → `-> Leaf2-Et1 #TA`; (3) MLAG Peer Link → `-> Leaf2-Et1-MLAG-PEER-LINK #TA`; (4/6/8) child of Po → `-> Leaf2-Et1-Po10 #TA`; (5/7) regular/MLAG PO → `-> Leaf2-Po10 #TA`/`-> Leaf2 & Leaf3-Po10 #TA`; (9) PO Peer Link → `-> Leaf2-Po10-MLAG-PEER-LINK #TA`; (10) no desc/peer → `#TA`; (11) PO no group → `#TA`; (12) sub-int → `description #TA`+encapsulation; (13) non-EVPN SVI → `description #TA`; (14) EVPN GW SVI → `ANYCAST_GW_vrf_vlan #TA`; (15) Vx1 SVI → `ANYCAST_GW_vrf_v #TA`. Skipped (no #TA): Loopback*, Vxlan1, MLAG Vlan4093/4094. Reconcile flow: `show int desc | grep "#TA"` → diff against sheet → orphans → `default interface`. VERSION 260423.17. Files: Code.gs)
  Previous: 2026-04-23 (ipConfigModal fixes: (1) .ip-modal-body now has flex:1; min-height:0 so body scrolls within the modal's 85vh max-height instead of being clipped by overflow:hidden; (2) modal width increased from 620px to min(700px, calc(100vw-40px)) for more content breathing room. VERSION 260423.15→260423.16. Files: Sidebar-css.html, Sidebar.html)
  Previous: 2026-04-23 (setStatus on all refresh triggers: every fetchData/fetchFullConfig call site now shows a user-visible reason before triggering the refresh. fetchData sites: (1) poll DATA_VERSION change → "Sheet changed — refreshing topology…"; (2) deletePort success → "{device}:{port} deleted — topology refresh…"; (3) saveEdit CONNECT success → "{devA}:{portA} ↔ {devB}:{portB} — topology refresh…"; (4) saveEdit ADD success → same format; (5) saveEdit topology-change edit → "{local} ↔ {newPeer} — topology refresh…"; (6) saveEdit attribute-only edit → "{device}:{port} & {peer} — topology refresh…"; (7) Esc×2 manual refresh → "Manual refresh — reloading topology…". fetchFullConfig already self-reports via "Fetching {device}…" for the active device. VERSION 260423.13→260423.15. Files: Sidebar-js.html)
  Previous: 2026-04-23 (DevView Vx1 pinned row: VTEP devices now show Vx1 as a read-only first row in the device card port list. Server-side: getTopologyData() collects AP VLANs from all Vx1 rows per device (before the early-return) into vtepVlansByDevice map; compressVlanRanges() formats the Set; vtepVlans string added to each node object in the topology payload. Client-side: render() in Sidebar-js.html prepends a non-clickable Vx1 row (teal accent border-left, VTEP pill, VLAN list in mode column) before pList.forEach — no allNodesData entry, no onclick, no connectivity dot, cursor:default. VERSION 260423.11→260423.13. Files: Code.gs, Sidebar-js.html)
  Previous: 2026-04-23 (toggleModalMinimize overlay fix: on minimize, saves editOverlay visibility in modal.dataset.hadOverlay and hides it (was leaving dim backdrop blocking background); on un-minimize, restores overlay only if hadOverlay='1'; applies to all modals via the shared function — generateAllModal/configModal/editModal/pushConfirmModal all benefit. Files: Sidebar-js.html)
  Previous: 2026-04-23 (Three audit/validation fixes: (1) Duplicate GW check fixed — section F of runValidation() now uses svi_vlan_ to determine which VLANs actually have gateways instead of all trunk VLANs from vlan_; vlan interfaces extract VLAN from port name, trunk ports filter allVlans by svi_vlan_ (all/list/nv<N>); (2) Duplicate Interface audit fixed — CHECK GROUP 5 now stores port-level errors in validationErrors[portId] alongside link errors from flagErrorOnPort, so disconnected duplicate ports are caught; audit filter devsWithErrors and device click aggregation both updated to check port-keyed entries; (3) Topology refresh reason — poll success handler now calls setStatus("Sheet changed — refreshing topology…") before fetchData(false) so the user sees why a refresh triggered. VERSION 260423.10→260423.11. Files: Sidebar-js.html)
  Previous: 2026-04-23 (BGP maximum-routes 0: added 'neighbor X maximum-routes 0' to all three peer group definitions (generateBGP underlay groups, generateBGPEvpnOverlay pgOvV4, pgOvV6) — EOS default is 12000 which was silently re-applied after we removed the explicit lines; setting 0 = unlimited. VERSION 260423.9→260423.10. Files: Code.gs)
  Previous: 2026-04-23 (addLinkPair + addSnakePair idempotency: both now scan all data rows before writing and return {error:"Link already exists:…"} if the same port pair already exists — prevents duplicate sheet rows on repeated "Add Link" clicks. addLinkPair checks int_devA=portA AND int_devB=portB; addSnakePair checks both directions (portA↔portB and portB↔portA). Data is read in a single getDataRange().getValues() call. VERSION 260423.8→260423.9. Files: Code.gs)
  Previous: 2026-04-23 (Four EOS config correctness fixes: (1) Vx1 excluded from topology/cabling/audit — getTopologyData nodesOnRow loop now returns early on pName==="Vx1" (forEach), preventing it from entering processRowLinks, forming cable links, landing in allNodesData, or triggering audit checks like "Incomplete SVI: IP Type blank"; (2) generateVxlanBlock VLAN guard changed from v>1&&v<4094 to v>=1&&v<=4094 — VLAN 1 and 4094 now included in vxlan vlan X vni Y; (3) BGP: removed all 'neighbor X maximum-routes 12000' lines from generateBGP and generateBGPEvpnOverlay; changed 'maximum-paths 64' → 'maximum-paths 64 ecmp 64' in generateBGP and 'maximum-paths 4' → 'maximum-paths 64 ecmp 64' in generateBGPEvpnOverlay; (4) P2P IPv6 unnumbered: removed invalid 'ipv6 unnumbered Loopback0' line from both snake and regular P2P paths — only 'ipv6 enable' is valid EOS CLI (generates link-local for RFC5549/BGP). VERSION 260423.5→260423.8. Files: Code.gs)
  Previous: 2026-04-23 (Modal scroll + DevView minimize fixes: (1) techModal body div: added overflow-y:auto; flex:1; min-height:0 inline — was clipping content silently instead of scrolling (all other modal bodies already had scroll); (2) toggleDevVisMinimize(): added JS height-pinning on minimize — panel.style.height=header.offsetHeight+'px' + panel.style.overflow='hidden'; cleared on restore; CSS display:none on children alone doesn't collapse flex panel height in GAS iframes. VERSION 260423.2→260423.3. Files: Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-23 (device_bridge.py: stale session cleanup join timeout raised from min(5,TIMEOUT) to TIMEOUT — sluggish devices after large pushes need full TIMEOUT to SSH in and abort stale sessions; prevents hitting 5-session pending limit on retry. VERSION 260422.19→260422.20. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-22 (Modal minimize universal + background fixes: (1) DevView .dev-vis-panel: bg changed bg-card→bg-modal, border changed border-light→border, shadow strengthened to 0 4px 16px -2px rgba(0,0,0,0.22) — fixes invisible dark-mode border (--border-light=#1e293b = same as bg-card); (2) Bridge-specific minimize CSS override (.modal-floating.modal-minimized #bridgeModalBody) removed — generic .modal-minimized > *:not(.modal-header){display:none!important} handles all modals; (3) Bridge modal minimize button: was btn-mono + Unicode − + toggleBridgeMinimize() (broken — referenced removed #btnBridgeMinimize id) → now btn-modal-minimize + SVG line icon + toggleModalMinimize('bridgeModal'); (4) toggleBridgeMinimize() reduced to one-liner legacy alias for toggleModalMinimize('bridgeModal'); (5) _showPushConfirmModal(): hides editOverlay at start (was causing "black background" when push modal opened while config modal editOverlay was still visible); (6) _injectMinimizeButtons(): new auto-inject function called from initApp() after syncToolbarTooltips() — queries all .modal-std/.modal-floating, skips modals without id/header/close-btn or already having .btn-modal-minimize, inserts SVG minimize button before .btn-modal-close; handles all 16 modals in one shot. VERSION 260422.36 (no bump — no GAS logic change). Files: Sidebar.html, Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-04-22 (Bridge modal UX: (1) BRIDGE section separated above Audit Flags in settings card — chkBridgeEnabled now in its own "Bridge / AUTO" row with bridge-settings-sep divider above Audit Flags section; (2) Devices section collapsible: bridge-dev-hdr clickable header with chevron SVG; toggleBridgeDevSection() toggles bridgeDevTable display and rotates chevron; preference saved in localStorage['bridgeDevCollapsed']; _syncBridgeDevCollapse() restores state on openBridgeModal(); CSS: .bridge-dev-hdr, .bridge-dev-toggle, #svgBridgeDevChevron.collapsed rules added. VERSION 260422.35→260422.36. Files: Sidebar.html, Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-04-22 (Bridge settings card restructure: removed standalone chkBridgeEnabled label + bridge-settings-sep + bridge-audit-always static badge from Sidebar.html; replaced with unified audit-flags grid: LLDP "Flag mismatched neighbour" (chkBridgeAuditMismatch, default on), LLDP "Flag missing neighbors", VLAN "Flag internal VLAN conflicts", BRIDGE "Enable on topology bring-up" (chkBridgeEnabled moved to bottom of grid); added bridgeAuditFlagMismatch localStorage state + toggleBridgeAuditMismatch() in Sidebar-js.html; openBridgeModal() now syncs chkBridgeAuditMismatch; runValidation() gates mismatch flagging on bridgeAuditFlagMismatch (was always-on); removed .bridge-audit-always CSS rule from Sidebar-css.html. VERSION 260422.34→260422.35. Files: Sidebar.html, Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-04-22 (VARP physical-IP bug fixes: (1) generateComplexL3Block standalone VARP path: physical IP now uses gwLastV4+sheetIndex (was gwLast, same as virtual — EOS requires distinct IPs); mirrors MLAG VARP pattern; (2) vx1 block VARP path: now emits ip address (gwLast+deviceSheetIndex) before ip virtual-router address (gwLast) — previously only emitted virtual-router address with no physical IP, which is invalid in EOS; vx1PhySuffixV4/V6 vars added; (3) Tests.gs VARP test: added assertions verifying physical ends in .2 (gwLast+sheetIndex=2) and virtual-router ends in .1 (gwLast=1). VERSION 260422.33→260422.34. Files: Code.gs, Tests.gs)
  Previous: 2026-04-22 (Bridge results UX + vx1VlanSet bug fix: (1) generateConfig() now has optional 6th param vx1VlanSet (defaults to new Set()) — fixes "vx1VlanSet is not defined" ReferenceError when called from getTopologyData (config tooltips); callers inside getDeviceConfig now pass the real Set; (2) discovered links section moved above results list in Sidebar.html; (3) _renderLldpResults: partial=orange ? icon (svgPart stroke #d97706) + "SSH failed — " prefix; unknown=dashed gray circle (svgUnk stroke-dasharray 3 3) + "Not queried" fixed label; unknown rows not clickable; (4) addSelectedDiscovered: status before/after — "Adding N link(s)..." / "N added" / "N added · M failed"; _onAddDiscoveredDone(btn,added,errors). VERSION 260422.31→260422.33. Files: Code.gs, CLAUDE.md, Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-22 (Per-VLAN VRF EVPN/VXLAN feature: (1) getNetworkSettings/saveNetworkSettings: added evpn_service ('per-vlan'|'vlan-aware-bundle'), gw_l3_type ('anycast'|'varp'), varp_mac (default '001c.7300.0099') string fields with getString() helper; (2) processRowLinks: Vx1 now exempt from connected:false (logical port, not physical); (3) vx1 SVI block: description changed to ANYCAST_GW_<vrf>_<vlan> when VRF set; gw_l3_type drives ip address virtual (anycast) vs ip virtual-router address (VARP); gw_ipv6 gates IPv6 commands; vx1VlanSet built from all vx1 VLAN claims; (4) generateComplexL3Block: new 5th param vx1VlanSet; SVIs in vx1VlanSet skipped (vx1 takes precedence); GW branch: EVPN anycast→ip address virtual, EVPN VARP→ip address+ip virtual-router address, non-EVPN→legacy ip address; ANYCAST_GW description added for EVPN-active GW SVIs; useAnycastGW/useVarpGW flags; (5) generateGlobalBlock: new mlagIsActive 3rd param; adds ip virtual-router mac-address for VARP standalone only (MLAG block handles its own); (6) generateBGP + generateBGPEvpnOverlay: vlan-aware-bundle mode generates single vlan-aware-bundle EVPN_VLAN_AWARE_BUNDLE block with all VLANs, RT=asnBase:1; per-vlan mode unchanged; (7) Tech modal: EVPN SERVICE MODEL section (Per-VLAN/VLAN-aware bundle radios) + L3 GATEWAY MECHANISM section (anycast/VARP radios) + VARP MAC input (shown only for VARP); openTechModal/saveTechSettings/getCurrentTechState updated; updateVarpMacVisibility() function added. (8) Tests.gs: added test_generateGlobalBlock (6 cases: non-EVPN, EVPN device, gw_ipv6, VARP standalone, VARP MLAG, anycast) and test_generateComplexL3BlockGwType (5 cases: EVPN anycast, EVPN VARP, non-EVPN legacy, VRF description, vx1VlanSet exclusion); both registered in runAllTests(). Files: Code.gs, Sidebar.html, Sidebar-js.html, Tests.gs)
  Previous: 2026-04-22 (Bridge+Audit status log enrichment + bridge modal fix: (1) openBridgeModal no longer hides bridgeResults on open if results already populated — previous run results stay visible when user clicks bridge icon; (2) _logBridgeIssuesToStatusLog(results,orphans,dsData) helper logs [Bridge] LLDP mismatch / extra cable / cmd-failed entries to status log after every bridge check (both auto and manual); (3) runValidation(logIssues) — new param; when logIssues=true, appends per-issue [Audit] entries (deduped) to status log before the "Audit: N Issues Found" summary; (4) toggleValidationMode calls runValidation(true) so toggling audit on logs individual issues. VERSION 260422.30→260422.31. Files: Sidebar-js.html)
  Previous: 2026-04-22 (cmdErrors partial-failure visibility: _ssh_cmds now returns (results, cmd_errors) tuple instead of raising — callers decide; _check_lldp re-raises (partial LLDP unusable); _check_devstatus keeps partial data and adds cmdErrors:{cmd:errStr} to response; _eapi_cmds wrapped to return ({}, ) tuple for consistency; bridge UI: orange dot overlay for devices with ok:true but cmdErrors present (green→orange upgrade after devstatus pass), dot title lists failed cmd+reason; summary message appends "· N cmd failures" in orange when any partial failures; Sidebar-css.html adds .bridge-dev-dot.dot-orange { background: #f97316 }. VERSION 260422.18→260422.19. Files: device_bridge.py, Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-04-22 (_ssh_cmds: run commands sequentially not in parallel threads — multiple concurrent SSH connections to same device hit EOS ip ssh maximum-sessions limit causing "Connection closed by UNKNOWN port 65535" (exit 255); device-level parallelism from _run_parallel provides speed, per-command parallelism within a device is unnecessary. VERSION 260422.17→260422.18. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-22 (SSH_ASKPASS shim for macOS empty-password auth without sshpass: on startup if sshpass absent, creates temp #!/bin/sh\necho script, sets SSH_ASKPASS+SSH_ASKPASS_REQUIRE=force+DISPLAY=:0 in _SSH_ENV; _ssh_base() gains askpass branch (BatchMode=no, PasswordAuthentication=yes, NumberOfPasswordPrompts=1); all subprocess SSH calls pass env=_SSH_ENV; /health skips arista-ssh check when _ASKPASS_SCRIPT set; banner shows "empty-password (SSH_ASKPASS)"; temp file cleaned on exit via atexit. Template fully synced: _check_ssh_agent now uses arista-ssh check-auth, _ssh_base() added, _ssh_cmds/_ssh_stdin use _ssh_base()+env. VERSION 260422.16→260422.17. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-22 (SSH stderr capture in verbose mode: stderr_mode=PIPE when VERBOSE, else DEVNULL; CalledProcessError now prints last stderr line as [error text] suffix on "→ failed (exit 255)" line so actual SSH error reason is visible. VERSION 260422.15→260422.16. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-22 (Fix false "session expired" on macbook: arista-ssh check-auth is a live token check — when it says ok, auth IS valid. Exit 255 also fires for unreachable hosts/network drops/ProxyJump failures, not just auth expiry. Removed _ssh_auth counter override from /health; /health now trusts _check_ssh_agent() directly; on auth ok, resets _ssh_auth so it doesn't accumulate stale failures. VERSION 260422.14→260422.15. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-22 (_check_ssh_agent() rewritten to use "arista-ssh check-auth" instead of ssh-add -l — cleaner and directly verifies arista-ssh credential validity; handles: binary not found, exit 0=ok, non-zero=expired, timeout. VERSION 260422.13→260422.14. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-22 (Bridge result rows clickable + auth message + checkbox rename: (1) _renderLldpResults — non-skip rows get onclick="_selectBridgeLink(u,v)" + hover cursor (CSS already done); _selectBridgeLink(u,v) added near _setBridgeOrphanBadge — finds link in globalLinks by endpoints (either direction) then calls selectLine(link.id); (2) all auth expiry messages now "bus-home session expired — run: arista-ssh login"; (3) bridge auto-check checkbox renamed "Enabled when topology loads for first time"; template VERSION 260422.12→260422.13. Files: Sidebar-js.html)
  Previous: 2026-04-22 (SSH auth exit-255 dynamic detection: arista-ssh-agent certs can expire while still appearing in ssh-add -l; EOS rejects them → SSH exits 255. Added _ssh_auth dict + _SSH_AUTH_THRESHOLD=3; _ssh_cmds.fetch() increments _ssh_auth["failures"] on exit 255, resets on success; /health two-layer check: static _check_ssh_agent() first, then _ssh_auth override if dynamic counter triggered. Template synced. VERSION 260422.11→260422.12. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-22 (Bridge audit flags UI redesign: (1) bridge-audit-flags-grid CSS (30px cat label + 1fr checkbox, gap 5x6); bridge-audit-cat style (10px muted uppercase); (2) Sidebar.html settings card restructured — "Audit flags" header, LLDP row (chkBridgeAuditMissing) + VLAN row (chkBridgeAuditInternalVlan) in the grid; (3) bridgeAuditFlagMissing default changed to ON (!=='false'); bridgeAuditFlagInternalVlan added (default ON); toggleBridgeAuditInternalVlan() added; both checkboxes synced on openBridgeModal(); validation block K now gated on bridgeAuditFlagInternalVlan; UserGuide updated. Files: Sidebar.html, Sidebar-js.html, Sidebar-css.html, UserGuide.html)
  Previous: 2026-04-22 (SSH agent auth expiry detection: _check_ssh_agent() in device_bridge.py checks SSH_AUTH_SOCK + ssh-add -l (exit 0=ok, 1=expired, 2=not running); included as "auth" field in /health only when METHOD="ssh" and no sshpass (key-based auth). Sidebar: bridge-auth-expired CSS (orange); bridgeAuthWarn div in bridge modal; _silentBridgeCheck + _checkBridgeHealth read data.auth; _updateBridgeToolbarIcon gains auth-expired state (suppresses auto-Bridge). Template synced: _check_ssh_agent + _SSHPASS_BIN added, do_GET updated, VERSION→260422.11. Files: device_bridge.py, Sidebar-js.html, Sidebar-css.html, Sidebar.html)
  Previous: 2026-04-22 (DevView 2 more fixes: (1) Minimize now uses _DEV_VIS_MIN_IDS array of element IDs — directly sets inline display:none on each child (devVisSelRow, devVisIpSection, devVisIntModeSection, devVisSviSection, devVisSearchRow, devVisList) and also sets header.style.borderBottom/borderRadius inline. Most reliable approach — inline style beats any CSS. (2) clearVlanTrace() line 5102 was unconditionally setting node.style.display="" ignoring hiddenDevices — fixed to if(!hiddenDevices.has(node.dataset.id)) node.style.display="". This was the Esc+` clearing DevView filter bug. File: Sidebar-js.html)
  Previous: 2026-04-22 (DevView minimize: CSS display:none approach was not collapsing panel height in GAS iframe. Replaced with JS-driven approach in toggleDevVisMinimize(): on minimize, read header.offsetHeight and set panel.style.height = that value + panel.style.overflow = 'hidden'; on restore, clear both inline styles. CSS is-minimized rules remain as a secondary guard. File: Sidebar-js.html)
  Previous: 2026-04-22 (Copy-paste auto-refresh fix: getTopologyData() cache accuracy guard — GAS execution takes 2-6s; a user paste can bump DATA_VERSION during that window causing stale data to be cached under the post-paste version. Fix: snapshot versionAfterCleanup immediately after the cleanup bump; at cache-store time compare against the current DATA_VERSION; if they differ, cache and return versionAfterCleanup instead of the newer version — client re-polls, detects the gap, and triggers a correct fresh fetch. Also removed two stale console.log debug prints from getTopologyData(). Files: Code.gs)
  Previous: 2026-04-21 (DevView 3 bug fixes: (1) closing panel now clears hiddenDevices + selectedInDevVis and calls applyDeviceVisibility() — all devices become visible again on close; (2) minimize CSS consolidated into single rule covering dev-vis-sel-row/ip-section/search-row/list; added .dev-vis-panel.is-minimized .dev-vis-header { border-bottom:none; border-radius:8px } to remove double-border artifact; duplicate separate sel-row rule removed; (3) Bug 3 (filterTopology unconditionally showing hidden devices on search clear) was already fixed (lines 3497/3514 already check hiddenDevices). Also fixed: minimize button was Unicode − → SVG rect. Files: Sidebar-js.html, Sidebar-css.html, Sidebar.html)
  Previous: 2026-04-22 (device_bridge.py: sshpass made optional — detect at startup via shutil.which(); if not found (e.g. macOS), fall back to key-based SSH (BatchMode=yes); _ssh_base() helper centralizes auth mode; startup banner shows auth mode. VERSION 260422.9→260422.10. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-22 (device card column header: renamed "Br" (title="Bridge") → "LL" (title="LLDP") in node-cols-header innerHTML. Files: Sidebar-js.html)
  Previous: 2026-04-22 (verbose SSH logging (-v flag): _ssh_cmds logs "[show] ip: cmd" before + "→ ok/timeout/failed/error" after per command; _ssh_stdin logs "[stdin] ip: <first-real-cmd> (N cmds)" before + "→ ok (N lines)/timeout/failed: <auth-err>" after. Template _ssh_cmds+_ssh_stdin fully refreshed to match source. VERSION 260422.8→260422.9. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-22 (device_bridge.py connection + concurrency fixes: (1) _run_parallel held lock during full SSH call (up to 15s), serializing all device threads — fixed to release lock before check_fn(ip), only re-acquire for dict write; (2) _ssh_stdin Popen now used as context manager (with Popen) — explicit pipe close + wait on both success and timeout paths. VERSION 260422.7→260422.8. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-22 (device_bridge.py robustness fixes from log review: (1) _ssh_cmds.fetch() catches exceptions into errors[] and re-raises after join — suppresses unhandled thread tracebacks while preserving exception type (TimeoutExpired/CalledProcessError) for _run_parallel; (2) _json() catches BrokenPipeError/ConnectionResetError — client closing connection mid-response no longer prints traceback. VERSION 260422.6→260422.7. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-22 (LLDP key normalization refactor: extracted inline dict-rename in _check_lldp into pure module-level helper _normalize_lldp_neighbors(raw) — renames EOS native "lldpNeighborInfo"→"bridgeNeighborInfo" so client _compareBridgeData uses one key across all transports. 5 unit tests added (TestNormalizeLldpNeighbors): key rename, data preservation, multiple interfaces, empty input, passthrough for interfaces lacking the key. VERSION 260422.5→260422.6. Files: device_bridge.py, Sidebar-js.html, tests/test_device_bridge.py)
  Previous: 2026-04-21 (device_bridge.py push join_budget fix: budget formula was TIMEOUT*(RETRIES+1)+DELAY*RETRIES+5=58s but each _push_config attempt also blocks for _abort_stale_sessions cleanup thread join (up to min(5,TIMEOUT)=5s) before _ssh_stdin, making actual worst-case 68s. Fixed to (TIMEOUT+_abort_overhead)*(PUSH_RETRIES+1)+PUSH_RETRY_DELAY*PUSH_RETRIES+5=73s. Also added lock around the post-join None→error conversion to prevent race with lingering daemon threads. VERSION 260422.4→260422.5. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-22 (LLDP match fix: EOS native JSON (ssh/eapi) uses "lldpNeighborInfo" per interface but client _compareBridgeData reads "bridgeNeighborInfo" — caused all ports to show missing/no-visibility. _check_lldp now normalizes lldpNeighborInfo→bridgeNeighborInfo in output; _oc_lldp_to_eos in device_bridge.py also fixed to emit bridgeNeighborInfo (template was already correct). Tests updated. VERSION 260422.3→260422.4. Files: device_bridge.py, Sidebar-js.html, tests/test_device_bridge.py)
  Previous: 2026-04-22 (device_bridge.py crash fix: _ssh_stdin now catches TimeoutExpired, kills proc, and re-raises cleanly; _push_config cleanup thread replaced bare lambda with _safe_abort() that swallows all exceptions — eliminates noisy thread crash when device is unreachable during stale-session cleanup. VERSION 260422.1→260422.2. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-22 (device_bridge.py _ssh_stdin: fix [Errno 7] Argument list too long when pushing large configs (50K+ lines) via jump host. Old approach base64-encoded the config and embedded it in argv — exceeded Linux ARG_MAX (~2MB). New approach uses ProxyCommand=ssh -W %h:%p JUMP_HOST so stdin is piped directly from local; sshpass handles device password as before. VERSION bumped 260421.9→260422.1 in device_bridge.py and downloadBridgeScript() template in Sidebar-js.html. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-21 (Unified drag: single _makeDraggable(elOrId, opts) replaces 3 separate implementations. opts: headerSel, saveKey (localStorage), noReset, toolbarGap. Auto-applied to .modal-std:not(.modal-large) in initApp(). bridge: saveKey+noReset+toolbarGap. devVis: headerSel(.dev-vis-header)+noReset+toolbarGap. modal-std auto-resets to centered on hide via MutationObserver. CSS cursor:move on .modal-draggable .modal-header/.dev-vis-header. Files: Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-04-21 (SVG/Unicode icon audit fixes: (1) device_bridge.py /health docstring VERSION "260421.1"→"260421.9"; (2) drag-hint ⬆→SVG arrow in Sidebar.html; (3) audit header ✕/⚠→ERR/WARN; (4) push-progress _ptSet ✓/✗/⚠→plain text; (5) btn feedback ✓Applied/✔Copied→Applied/Copied in Sidebar-js.html. Files: device_bridge.py, Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-21 (Rename "Debug Logging" → "Debug log in F12" in Schema Manager checkbox label and UserGuide. Files: Sidebar.html, UserGuide.html)
  Previous: 2026-04-21 (Draggable modals: pushConfirmModal + generateAllModal headers are now draggable. _makeDraggable(modalId) attaches mousedown/mousemove/mouseup to .modal-header — converts transform-centered position to explicit top/left on first drag, clamps to viewport. Guard: header.dataset.draggable='1' prevents double-attach. _resetModalPosition() clears top/left/transform on close. .modal-draggable class added; CSS: cursor:move on .modal-draggable .modal-header. Minimize button still works. Removed redundant "Push all device configs…" genall-desc span. Files: Sidebar.html, Sidebar-css.html, Sidebar-js.html)
  Previous: 2026-04-21 (Push All + Config Push modals: (1) phase colors in pushAllConfigs rows — fetching/pushing→#60a5fa blue, retry→#f59e0b amber, verifying→#a78bfa purple; (2) amber warning "Commits directly — no per-device diff review" below Push All button; (3) minimize button (.btn-modal-minimize) on pushConfirmModal and generateAllModal headers — collapses to header via .modal-minimized CSS class; toggleModalMinimize() toggles class + swaps dash/chevron icon. Files: Sidebar.html, Sidebar-css.html, Sidebar-js.html)
  Previous: 2026-04-21 (Holistic canvas controls (hc-btns): wrapped groupRectBtn/nodeSizeMinusBtn/nodeSizePlusBtn in segmented-group — consistent with toolbar panel 1 style. Files: Sidebar.html)
  Previous: 2026-04-21 (Toolbar LAYOUT/CABLE segmented-group: wrapped mono-label+mono-input pairs in segmented-group; .segmented-group .mono-input border:none height:24px bg:transparent; .seg-inner-sep left-border divider between W/G and O/T pairs; segmented-group gains align-items:center gap:2px. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-21 (device_bridge.py: CLI flags -u/-b/-t replace env var overrides; _arg() helper parses sys.argv; -h shows combined example (-u root -b jumphost -t 30 -v); startup banner shows "Options: -u -b -t -v"; env vars still work as fallback; VERSION bumped 260421.7→260421.9. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-21 (Toolbar panel 1 border style: wrapped all icon-btn groups (CABLING, CONFIG, VIEW, AUDIT, THEME, HELP) in segmented-group divs — same grouped border as panel 2; replaced audit-group-box with segmented-group; added .segmented-group .icon-btn border/divider CSS; removed audit-group-box CSS. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-21 (device_bridge.py: -h/--help flag — prints usage, all options (-v, -h), env var overrides (BRIDGE_SSH_USER, BRIDGE_JUMP_HOST, BRIDGE_TIMEOUT) with defaults and export examples, then exits; VERSION bumped 260421.6→260421.7. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-21 (device_bridge.py: -v verbose flag — VERBOSE global gates all push logs (SSH connected, session action, retry/error/timeout per device); log_message (HTTP request lines) also gated; startup banner shows Verbose ON/OFF hint; VERSION bumped 260421.5→260421.6. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-21 (Shortcuts Help: strict search — added shortcut-ctx description to s key "Exact match — 'leaf01' won't match 'leaf01a'; port text matched as whole token"; added strict example row in Examples column "leaf01 → Strict: exact name only". File: Sidebar.html)
  Previous: 2026-04-21 (device_bridge.py: push logging — print SSH connected + session action + diff line count to terminal on each push; VERSION bumped 260421.4→260421.5. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-21 (Push modal performance: skip stale-session cleanup on dry-run (preview) — cleanup only needed on commit pushes; saves one full SSH round-trip per preview; loading text changed from "Fetching config…" to "Computing diff…" since config is always cached when Push is reachable; VERSION bumped 260421.3→260421.4. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-21 (Shortcuts Help modal: added .kbd-plus connector — [Esc]+[Esc] and [Shift]+[Enter] now show explicit + between keys to clarify combined-key operations; .kbd-plus CSS added to Sidebar-css.html. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-21 (device_bridge.py: BRIDGE_TIMEOUT env-var override — TIMEOUT = int(os.environ.get("BRIDGE_TIMEOUT", "15")); VERSION bumped 260421.2→260421.3; Overrides startup line updated to include BRIDGE_TIMEOUT; template in downloadBridgeScript() synced. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-21 (push confirm modal: (1) loading phase text now updates — "Fetching config…" during _fetchConfigForPush, then "Computing diff…" before dry-run fetch; _pushConfigText (config modal push) shows "Computing diff…" directly (no config-fetch phase); id="pushLoadingText" on loading span; _setPushLoadingText(text) helper added near _updatePushPreviewError. (2) removed both auto-close timeouts — alreadyInSync modal stays open; push-successful modal stays open; user must close manually. File: Sidebar-js.html)
  Previous: 2026-04-21 (check-deploy.sh: fix VERSION sed parse — device_bridge.py uses multi-space alignment around = so pattern now uses [[:space:]]* instead of literal space. File: .claude/check-deploy.sh)
  Previous: 2026-04-21 (Help modal text + CSS polish: VLAN trace ctx→"Type v<VLAN ID> to trace"; Esc+Esc→"Hard reset — force reload from sheet"; Bridge ctx expanded; Config Search ctx→"Find bar inside View Config modal"; Enter/Shift+Enter→"Next/Previous result"; "Card Interactions"→"Device Cards"; dbl-click→"Double-click device header"; drag→"Drag right edge"; 14px bottom buffer added; .action-ex justify-self:center+text-align:center. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-21 (generate/push toolbar icon flashes while active; stop buttons red when active: added id=btnGenerateAllToolbar; _setGenToolbarActive(on) helper toggles is-active class; bridge-scan animation reused; genall-btn.stop-active CSS red; runGenerateAll/pushAllConfigs wire is-active on start + off at all end paths; _resetStopPushBtn now also resets className. Files: Sidebar.html, Sidebar-css.html, Sidebar-js.html)
  Previous: 2026-04-21 (dev-vis panel: search input height pinned to 20px — browser UA was inflating to ~26px; now matches ip_type filter row height. File: Sidebar-css.html)
  Previous: 2026-04-21 (Help modal polish: (1) always-active notice moved from footer to top (.help-notice banner); (2) Search sub-label "Shortcuts"→"Global"; (3) "Config Find Bar"→"Config Search" with ctx "Find bar inside Global Config Template modal"; (4) .shortcut-row align-items:center + :has(.shortcut-ctx) override to start; (5) .kbd-group justify-content:space-between+width:100% so 2-key combos spread to both edges; (6) .help-footer-note→.help-notice CSS. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-21 (Per-VLAN VRF audit: extracted inline VRF loop from auditSchemaVsSheet() into pure helper _auditVrfIssues(rows, headers, aristaDevices, rowOffset); auditSchemaVsSheet() now calls _auditVrfIssues(data.slice(2), headers, aristaDevices, 3); added test_auditVrfIssues() in Tests.gs with 22 cases covering all 6 conditions; registered in runAllTests(). Files: Code.gs, Tests.gs)
  Previous: 2026-04-21 (UserGuide: added 3 missing audit sections — (1) "Interface Operational State (St Column)" H3 after Topology Card Indicators: connected/notconnect/errdisabled/disabled states, EOS context, ssh/eapi requirement; (2) "Schema Audit (Check Sync)" H3 after Schema Manager: missing/extra columns, per-VLAN VRF issues, read-only note; (3) "Live Sheet Validation" H3 before Checkpoints: onEdit column-A guard + format validation + cell backgrounds, validateAndCleanData field rules, runFullSheetCleanup normalisation. File: UserGuide.html)
  Previous: 2026-04-21 (Esc LIFO fix: pushConfirmModal/schemaContainer/viewCustomContainer added to modalOrder + closeFuncs in global keydown handler — Esc now correctly closes these instead of triggering canvas reset. File: Sidebar-js.html)
  Previous: 2026-04-21 (Help modal layout: full-width SEARCH section (shortcuts + autocomplete left | examples right via .help-search-inner 2-col grid + .help-sub-label) then 2-col REST below (Reset&Clear+General left | Config Find Bar+Card Interactions right). Removed .help-examples-grid usage. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-21 (Help modal: gesture badges (dbl-click, drag) switched from .search-ex (purple) to .action-ex (dark var(--text-main)) — gestures are not typed queries; .action-ex added to Sidebar-css.html. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-21 (Per-VLAN VRF: removed dead variable vrfWarns from Sidebar-js.html showAuditResults(); added per-VLAN VRF section (§9) and Per-VLAN VRF Issues audit bullet to UserGuide.html. Files: Sidebar-js.html, UserGuide.html)
  Previous: 2026-04-21 (Modal button standard (Rule 21): all 16 modal headers now use .btn-modal-close SVG × button instead of text "Close"; added CSS for .btn-modal-close (28×28px, transparent bg, hover tint); fixed: globalCfgModal/groupRectModal/reorderModal missing header close, bridgeModal/pushConfirmModal/editModal duplicate close removed from header or footer, editModal footer added right-align class. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-21 (Help modal: removed × clear-search entry from Reset & Clear section — button is self-evident in the UI. File: Sidebar.html)
  Previous: 2026-04-21 (test-device_bridge.md: expected pytest count updated 56→64. File: .claude/commands/test-device_bridge.md)
  Previous: 2026-04-21 (Per-VLAN VRF: vrf_ now accepts comma-separated list positionally matched to VLANs. _parseVrfList(vrfVal)→string[] and _resolveVrfAtIndex(list,idx)→string|null helpers added. getIpBlock(v, resolvedVrf) accepts optional per-VLAN VRF override. SVI loop, sub-int loop, Vx1/ANYCAST SVI all zip vrfList with VLAN index. auditSchemaVsSheet() now returns vrfIssues[]{sev,msg} covering: wrong mode, range+multi-VRF, count mismatch (sub-int vs vlan_, trunk vs svi_vlan_ or vlan_ when svi=all), no SVIs+multi-VRF. showAuditResults() renders vrfIssues section. Tests added for _parseVrfList and _resolveVrfAtIndex. Files: Code.gs, Tests.gs, Sidebar-js.html)
  Previous: 2026-04-21 (Help modal polish: width→880px; title→"Shortcuts Help"; header→standard white (removed dark #334155); × SVG→<code class="search-ex">×</code> text badge (matches dbl-click/drag style). Files: Sidebar.html)
  Previous: 2026-04-21 (toggleStatusLog: inp.focus() → inp.focus({preventScroll:true}) — prevents GAS iframe scroll on drawer open which was triggering toolbar ResizeObserver → adjustTop() → viewport.style.top shift → topology flap. File: Sidebar-js.html)
  Previous: 2026-04-21 (secondary toolbar final naming: center section label → "STATUS LIVE LOGS" (live #status text); right section label → "STATUS LOGS"; drawer-toggle button text → "HISTORY". File: Sidebar.html)
  Previous: 2026-04-21 (Help modal header: title color:white explicit (was inheriting var(--text-main) from CSS overriding parent color:white); Close button styled white/transparent-border; title simplified to "Keyboard Shortcuts". File: Sidebar.html)
  Previous: 2026-04-21 (Help modal overhaul: width 460→720px (min(720px,100vw-40px)); 2-column layout (Navigation+Reset+General left | Autocomplete+Config Find Bar+Card Interactions right); Search Examples moved to full-width 2-col grid below; kbd-group flex-wrap:nowrap so multi-key combos stay on one line; key column 96→110px; ⌨ unicode → SVG keyboard icon; new sections: Search Autocomplete (↑/↓/Enter/Esc), Config Find Bar (Enter/Shift+Enter/Esc); CSS: .help-two-col, .help-col, .help-examples-grid added. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-21 (STATUS LOGS center section: wrapped #status in a proper tb-section with tb-label "STATUS LOGS" ribbon above it — matches the same column layout as TOPO/INT SORT/SEARCH sections. tb-btns justify-content:center so live text stays centered. File: Sidebar.html)
  Previous: 2026-04-21 (Sheet View menu: removed "+" prefix from Transceiver, Speed, Port Channel, Mode, Vlan, SVI items. File: Code.gs)
  Previous: 2026-04-21 (STATUS ribbon: tb-label → "Status Live Logs"; button text restored to "LOG HISTORY". File: Sidebar.html)
  Previous: 2026-04-21 (CABLE toolbar: divider-v removed from inside tb-btns; replaced with tb-sep as sibling after the CABLE tb-section — now full-height like all other section separators. File: Sidebar.html)
  Previous: 2026-04-21 (Sheet View submenu flattened with descriptive labels — no nested submenus: Show All | sep | L1: Interface | L1: + Transceiver | L1: + Speed | sep | L1.5: + Port Channel | sep | L2/L3: + Mode | L2/L3: + Vlan | L2/L3: + SVI | sep | Custom View. File: Code.gs)
  Previous: 2026-04-21 (Sheet View submenu restructured into nested submenus: Show All (flat) | L1 → Int/Xcvr/Speed | L1.5 → Port Channel | L2 & L3 → Mode/Vlan/SVI | Custom View. Added viewSvi() = applyCustomView(['int','po','sp_mode','vlan','svi_vlan']). File: Code.gs)
  Previous: 2026-04-21 (toolbar polish: LOGS section → STATUS; button "LOG HISTORY" → "Status Live Logs"; #configSearch height 32→28px to match .btn-sm buttons in config modal header. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-21 (CABLE toolbar: divider-v moved to after T: input (end of section). File: Sidebar.html)
  Previous: 2026-04-21 (Loading overlay safety net — comprehensive: all GAS-call sites that show showGlobalLoading() now have a _guard timeout that auto-calls hideGlobalLoading()+setStatus(error) if neither handler fires. Timeouts: 15s for modal open/save ops (openTechModal, openIpConfigModal, saveIpConfig, saveTechSettings); 60s for _runForceSyncNow (also clears syncPollTimer); 20s for fetchFullConfig (flushes fetchQueue callbacks including hideGlobalLoading). Safe sites with existing guards: saveSchemaChanges (30s schemaLockTimer). Pattern rule: every showGlobalLoading() call must be paired with a _guard = setTimeout(hideGlobalLoading+setStatus, N); both withSuccessHandler and withFailureHandler must clearTimeout(_guard) as their first act. File: Sidebar-js.html)
  Previous: 2026-04-21 (CABLE toolbar: revert vertical stack; add divider-v between O: and T: inputs — same separator style as used elsewhere in toolbar. File: Sidebar.html)
  Previous: 2026-04-21 (Menu: removed Unicode icon prefixes from all Sheet Manager submenu items and Device Manager item. File: Code.gs)
  Previous: 2026-04-21 (Device Manager dialog height 900 → 1000px. File: Code.gs)
  Previous: 2026-04-21 (Menu: "Topology Manager" submenu (single item) flattened — replaced with direct .addItem('▥ Device Manager', 'showDeviceManagerUi'). Device Manager dialog height 800 → 900px. File: Code.gs)
  Previous: 2026-04-21 (GAS call timeout safety net: openTechModal() and openIpConfigModal() now set a 15s _guard timer that calls hideGlobalLoading()+setStatus(error) if neither success nor failure handler fires — prevents permanent loading overlay when GAS call hangs at framework level (auth expiry, quota). Both handlers clearTimeout(_guard) first. hideGlobalLoading() moved to first statement in openIpConfigModal() success handler (was last, after DOM ops). File: Sidebar-js.html)
  Previous: 2026-04-21 (VLAN Summary modal: vlanSummaryFilter height set to 24px (box-sizing:border-box, padding:0 10px) to match icon-btn and sort button heights. File: Sidebar.html)
  Previous: 2026-04-21 (STATUS LOGS secondary row redesign: #status moved to flex:1 center div (justify-content:center) between left-group and right-group; LOG HISTORY button changed from .slg-view-btn (blue accent) to icon-toggle sort-toggle inside segmented-group matching SHEET/MODE formatting; .slg-view-btn CSS removed from Sidebar-css.html as no longer used; right-group is now auto-width with leading tb-sep + LOGS label. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-21 (CABLE toolbar section: O: and T: inputs now stacked vertically (flex-direction:column) instead of side by side — each row is a flex div with label+input. File: Sidebar.html)
  Previous: 2026-04-21 (Device Manager dialog width: 1150 → 1000px. File: Code.gs)
  Previous: 2026-04-21 (STATUS LOGS toolbar: right-group gets padding-left:8px for breathing room from separator; .slg-view-btn gets margin-left:auto to push button to far right away from status text; "Log" button label renamed to "Log History". Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-21 (AUDIT toolbar: tb-sep now has cursor:default + user-select:none (fixes I-beam on section dividers); btnBridgeToolbar moved inside audit-group-box — all 3 audit icons (Bridge, Audit, AuditOnly) now always grouped in one bordered box; btnAuditOnly changed from display:none/flex toggling to disabled/enabled — always visible, grayed out (opacity 0.3) when audit is off. Files: Sidebar.html, Sidebar-css.html, Sidebar-js.html)
  Previous: 2026-04-21 (audit modal: internal VLAN conflicts now shown — _scanInternalVlanConflicts() scans allNodesData vs deviceInternalVlans (bridge data); showAuditResults() prepends orange ⚠ section (section 0) when conflicts found; success condition and totalIssues badge count both include internalConflicts.length. Only fires when bridge data is loaded. File: Sidebar-js.html)
  Previous: 2026-04-21 (DevVis panel auto-size: width changed from fixed 230px to min-width:230px + width:max-content + max-width:min(480px,calc(100vw-40px)) — grows to fit longest device name. Device list max-height changed from fixed 300px to calc(100vh-280px) — shows all devices, scrolls only when list exceeds viewport. .dev-vis-name already had white-space:nowrap so names drive width naturally. Drag clamp already uses offsetWidth/offsetHeight so no JS change needed. File: Sidebar-css.html)
  Previous: 2026-04-21 (audit-group-box: added 1px border-left divider between audit btn and btnAuditOnly via .audit-group-box .icon-btn + .icon-btn rule — no HTML change needed. File: Sidebar-css.html)
  Previous: 2026-04-21 (STATUS LOGS row: replaced clickable mono-status div with flex row — #status (flex:1, no longer clickable) + .slg-view-btn button (blue accent, log-lines SVG icon + "Log" label, always visible, calls toggleStatusLog()). mono-status: removed cursor:pointer, width:100%; replaced with flex:1+min-width:0+overflow:hidden+text-overflow:ellipsis. .slg-view-btn CSS added in Sidebar-css.html. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-21 (All Device Config modal: Stop button moved above description in each action card — order is now: action btn → Stop → desc. File: Sidebar.html)
  Previous: 2026-04-21 (icon-btn border: transparent→var(--border) so all toolbar icons have a permanent 1px border at rest, matching audit-group-box style; divider-v + split-action-divider: added cursor:default + user-select:none to prevent I-beam text cursor on hover. Files: Sidebar-css.html)
  Previous: 2026-04-21 (Status ribbon renamed "STATUS" → "STATUS LOGS"; drawer title "Status Log" → "Status Logs"; bug fix: setStatus("View Reset") had no timeout so it stuck permanently as the last message — replaced with setStatus(getReadyStatus()) which shows device/link counts. Files: Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-21 (toolbar/search polish: renamed EDIT section → CABLING; added tb-sep before it (first in right-group); mono-search::placeholder muted+italic (#94a3b8, italic); strict-btn height 22→26px to match search box. s shortcut already wired. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-21 (Global Config Template modal: removed redundant Close button from header (footer Cancel is the only close); added find bar (gcfg-find-bar) between header and textarea — search input, match count, Prev/Next chevron SVG buttons. JS: _gcfgMatches[], _gcfgMatchIdx, globalCfgSearchUpdate(), _gcfgJumpTo(), globalCfgSearchNext(), globalCfgSearchPrev(), globalCfgSearchKey() (Enter=next, Shift+Enter=prev, Escape=clear). openGlobalCfgModal() resets find state on open. CSS: .gcfg-find-bar, .gcfg-find-input, .gcfg-find-count, .gcfg-find-btn. Files: Sidebar.html, Sidebar-css.html, Sidebar-js.html)
  Previous: 2026-04-21 (All Device Config modal: Stop Generate + Stop Push buttons added below each action button in genall-action-card. Stop Generate: gray/disabled by default; activates to "Stop Generate" during runGenerateAll(); on success transforms to "Copy All" (btn-success-mono, calls copyAllConfigs); sets _generateAllAborted flag on click which is checked in success/failure handlers to ignore stale GAS result. Stop Push: gray/disabled by default; activates to "Stop Push" during pushAllConfigs(); sets _pushAllStopped flag; after _semAcquire(), if flag set → mark row "skipped" (gray), release semaphore, skip device; in-flight pushes complete normally. Both reset via _resetStopGenerateBtn()/_resetStopPushBtn() on open/complete/abort. btnCopyAllConfigs footer removed from HTML. .genall-btn:disabled { opacity: 0.4; cursor: not-allowed } added to CSS. Files: Sidebar.html, Sidebar-css.html, Sidebar-js.html)
  Previous: 2026-04-21 (holistic view: INT SORT section also dimmed — added id="tbSectionSort"; updateViewButtons() 3b loop now covers Layout+Cable+Sort. Files: Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-21 (VLAN conflict logic extracted to _vlanHasConflict(info): single helper used by both _applyVlanFilterSort and renderVlanSummaryTable — prevents future logic drift between filter and display. File: Sidebar-js.html)
  Previous: 2026-04-21 (VLAN Summary conflict filter bug: _applyVlanFilterSort "Conflicts only" filter still used old any-device logic (ports.length>0 && internalOn.length>0); fixed to same-device Set intersection matching renderVlanSummaryTable. File: Sidebar-js.html)
  Previous: 2026-04-21 (holistic view: LAYOUT + CABLE toolbar sections dimmed (opacity 0.35, pointer-events none) in holistic mode — added id="tbSectionLayout" and id="tbSectionCable" to tb-section divs; updateViewButtons() extended with step 3b loop to set/clear these. EDIT section already handled. Files: Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-21 (lo_base fallback fix: generateBGP() evpn_ipv6 branch and generateBGPEvpnOverlay() both had fallback IPv6 loopback `${pid}:${pid}:${pid}::${pid}` that ignored lo_base; fixed by computing pidLoId=pid+loBase in each — consistent with the populated loopbackV6 field in bgpNeighbors. File: Code.gs)
  Previous: 2026-04-21 (Dev Vis int_mode labels: renamed from 4-char run-together (l2ea, l3es etc.) to dash-separated 5-char (l2-ea, l3-es etc.) for readability. Added .dev-vis-ip-list .dev-vis-name { white-space: nowrap; overflow: hidden } to prevent wrap at panel column width. Files: Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-04-21 (Placeholder text standardisation across Sidebar.html: (1) ASCII ... → Unicode … in searchInp; (2) configSearch simplified Filter (e.g. '...')... → Filter…; (3) vlanSummaryFilter: "Filter by VLAN ID or device…" → "Filter by VLAN or device…"; (4) groupRectLabelInp: dropped trailing ...; (5) newDeviceInput: "DNS Name (e.g. ...)" → "e.g. Leaf[1-4] or Spine1" — no redundant label prefix; (6) newFieldKey: "Field key (e.g. rack_id)" → "e.g. rack_id". Standard: search boxes use Verb…; example fields use bare e.g. X with no wrapping parens and no trailing ellipsis. File: Sidebar.html)
  Previous: 2026-04-21 (All Device Config modal redesign: renamed from "All Device Config - Generate & Push" → "All Device Config" (header + toolbar button title); Generate/Push All buttons moved from modal-actions footer to a dedicated action section at top (genall-actions flex row); each action card has full-width button + one-line genall-desc below; results area (generateAllLeft/Right) remains below as scrollable section; Copy All stays in footer; openGenerateAllModal sets btn label "Generate All"; runGenerateAll success sets "Re-Generate All". CSS: .genall-actions, .genall-action-card, .genall-btn, .genall-desc added to Sidebar-css.html. Files: Sidebar.html, Sidebar-css.html, Sidebar-js.html)
  Previous: 2026-04-21 (VLAN Summary conflict fix: hasConflict was true whenever any device had the VLAN internally AND any device had it on a port — cross-device false positive. Fixed: conflict only when the SAME device has the VLAN both user-configured (in ports[]) and EOS-internal (in internalOn[]). Uses Set intersection on device names. File: Sidebar-js.html)
  Previous: 2026-04-21 (mlag_peer_base configurable offset: added ip_mlag_base input (default 1) to IP Config modal under new "MLAG PEER LINK" section (between LOOPBACK and P2P). Code.gs: mlag_peer_base default in getIpPreferences(); generateMlagConfig() computes mlagBase=parseInt(mlag_peer_base)||1, localIpBit=isLower?mlagBase:mlagBase+1, peerIpBit=isLower?mlagBase+1:mlagBase — so lower Device ID gets .base, higher gets .base+1 (default .1/.2). Auto-derived info box updated: shows ".base / .base+1" formula instead of hardcoded ".0/.1". Sidebar-js.html: load/save/reset ip_mlag_base. Files: Code.gs, Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-21 (Help modal + drag-to-resize persistence: (1) Help modal "Card Interactions" section added — dbl-click header=collapse/expand, drag=resize; "General" section gains Bridge icon dbl-click=toggle auto-check entry; (2) drag-to-resize now persists: initResizer passes nodeId to initDrag; stopDrag saves width to localStorage 'nb_card_width' keyed by nodeId; card render restores saved width after initResizer; (3) UserGuide: Device Bridge toolbar entry notes dbl-click to toggle auto-check; Tips section adds "Card Resize" bullet. Files: Sidebar.html, Sidebar-js.html, UserGuide.html)
  Previous: 2026-04-21 (Search improvements: (1) autocomplete now groups results under "Devices" / "Ports" section headers; (2) matched substring highlighted with <mark class="search-hl"> (yellow) in autocomplete labels; (3) port autocomplete items show which field matched (desc/vrf/vlan/mode/ip/po) as a dim italic context line when match is not in port name itself; (4) filterTopology status now shows "N devices · M ports" instead of "Showing N matches"; (5) "/" keyboard shortcut now wired (was documented but not implemented); (6) type-port CSS badge added (was missing — port items had unstyled badge); (7) highlightMatch()/_escHtml() helpers added. Files: Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-04-21 (lo_base end-to-end fix: added loopbackV6 to bgpNeighbors in processP2pNeighbor+generateMlagConfig; generateBGP+generateBGPEvpnOverlay read loopbackV6 for IPv6 overlay peers; generateOSPF+generateOSPFv3 gained ipPrefs param (router-id now uses loId); generateVxlanBlock gained ipPrefs param — loBase applied to MLAG VTEP id1/id2, standalone VTEP myLoId, flood-list MLAG id1/id2, flood-list standalone remoteLoId; all call sites updated. File: Code.gs)
  Previous: 2026-04-21 (VLAN Summary: sort buttons (ID/Type/VRF/Ports) + Conflicts-only toggle in subheader. Module-level _vlanSortKey/'id' and _vlanConflictsOnly/false reset on open. _applyVlanFilterSort() centralizes text filter + conflicts filter + sort; filterVlanSummary() calls it. Count shows "N VLANs · conflicts only" when active. Conflict = port has both user-configured ports AND EOS-internal VLANs on same device. Files: Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-21 (Fix "ipPrefs is not defined" in config engine: loBase code landed in generateSystemBlocks() which had no ipPrefs param; added ipPrefs as 5th param and updated the single call site to pass it. File: Code.gs)
  Previous: 2026-04-21 (IP Config modal: rename "Loopback Base" label → "Loopback"; snake-inputs CSS: remove muted color rules from ip-label and input-wrapper label so columns match other sections — keep only italic input value + muted ::placeholder. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-21 (Full toolbar ribbon: primary left group labeled APP [－] | LAYOUT [W G] | CABLE [O T]; Refresh moved from left group to CONFIG section (first button); height reductions: icon-btn 28→24px, mono-input 26→22px, row padding 6→3px top/bottom, tb-section gap 3→1px, min-height 38→30px. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-21 (Loopback base offset: added ip_lo_base input (default 0) to IP Config modal LOOPBACK section. Code.gs: lo_base default in getIpPreferences(); generateConfig() computes loId=deviceId+loBase for Loopback0 ip/ipv6 and router-id ipv4/ipv6; processP2pNeighbor() uses peerLoId for loopbackV4; generateMlagConfig() gains ipPrefs param, uses mlagLoId for partner loopbackV4. EXAMPLE grid: Loopback0 + Router-ID rows added at top. Loopback/Router-ID removed from auto-derived section. Sidebar-js.html: load/save/reset ip_lo_base. Files: Code.gs, Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-21 (SheetAssistPanel int_mode filter: show full val names (l2-et-access etc.) — removed label shorthand from INT_MODE_FILTER_ITEMS, renderIntModeFilterList() now renders item.val. Rollback: Sidebar-js.html _INT_MODE_ITEMS restored to abbreviated labels (l2ea etc.) — Dev Vis panel is compact so short labels are appropriate there. File: SheetAssistPanel.html, Sidebar-js.html)
  Previous: 2026-04-21 (Toolbar ribbon redesign: primary right-group split into labeled .tb-section wrappers — EDIT/CONFIG/VIEW/AUDIT/THEME/Utility; sort moved from primary center-group to secondary SORT section; holistic controls (#holistic-controls) moved to canvas floating panel; SECTION 27 added. Files: Sidebar.html, Sidebar-css.html, Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-21 (Int mode filter: show full val names (l2-et-access etc.) instead of abbreviated labels (l2ea etc.) — removed label property from _INT_MODE_ITEMS, renderIntModeList() now renders item.val directly. File: Sidebar-js.html)
  Previous: 2026-04-21 (IP Config modal EXAMPLE grid: devices 1,2 → 3,4 (headers, P2P last-octets, GW MLAG phy last-octets 2,3→4,5, ASN 65001,65002→65003,65004). Snake Test inputs: added class="snake-inputs" wrapper; new .snake-inputs CSS in Sidebar-css.html mirrors .info-box--dim input rules (muted label/placeholder, italic, no box border). Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-21 (device_bridge.py push retry on SSH failure: VERSION 260421.1→260421.2; added PUSH_RETRIES=2 + PUSH_RETRY_DELAY=4s constants; run_push() now retries up to 2× on subprocess.TimeoutExpired or RuntimeError containing "connection refused"/"ssh failed" — handles device warm-restart after config reset; join budget updated to TIMEOUT*(PUSH_RETRIES+1)+PUSH_RETRY_DELAY*PUSH_RETRIES+5=58s; lock now only wraps the dict write, not the push itself. VERSION synced in Sidebar-js.html template. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-21 (Rename "Generate All Configs" → "All Device Config - Generate & Push": toolbar button title + modal header. Files: Sidebar.html)
  Previous: 2026-04-21 (Config push modal — immediate load: _showPushConfirmModal now accepts diff=null to show a loading state ("Fetching diff…", Commit disabled) immediately when Push is clicked; _pushConfigText calls it before the dry-run fetch so the modal appears instantly; on return the modal is updated in-place (diff shown + Commit enabled, or error shown). Cancellation guard added: if user clicks Cancel while fetch is in-flight, modal visibility is checked before updating. New helper _updatePushPreviewError() updates the open modal with title/body/Close button on fetch errors. Success auto-close extended: 2500ms → 5000ms. File: Sidebar-js.html)
  Previous: 2026-04-21 (IP Config modal: split EXAMPLE title to 2 lines (VLAN example + color legend separately); added info-box--keep-colors to auto-derived section so f-dyn color spans render correctly. File: Sidebar.html)
  Previous: 2026-04-21 (Snake Test section: removed incorrect info-box--dim wrapper from editable input fields — dim styling is for static reference text only, not form inputs. File: Sidebar.html)
  Previous: 2026-04-21 (Toolbar right-group full rearrangement into 4 logical groups: Edit [editModeBtn] | Config [Tech, AutoCfg, GlobalCfg, GenAll] | View [VLAN, DevVis, Photo, Cabling] | Audit [Bridge, Config Audit box] | Appearance [B, Moon] | Help. Bridge moved from View→Audit (validates physical wiring vs sheet). GenAll moved from Export→Config. Appearance moved to end before Help. File: Sidebar.html)
  Previous: 2026-04-21 (Holistic canvas controls moved from toolbar to floating panel: groupRectBtn + nodeSizeMinusBtn + nodeSizePlusBtn removed from right-group toolbar; added as #holistic-controls div inside #viewport (position:absolute bottom-right 16px, z-index:2500, display:none by default). JS _updateViewModeButtons() now shows/hides #holistic-controls flex panel instead of opacity-toggling individual buttons. Files: Sidebar.html, Sidebar-css.html, Sidebar-js.html)
  Previous: 2026-04-21 (Review blockers fixed: (1) device_bridge.py /health docstring VERSION "260420.3"→"260421.1"; (2) tests/test_device_bridge.py _IVLANS fixture used wrong key "vlans"→"internalVlans" (EOS format), fixing 3 failing tests; (3) Sidebar-css.html: added font-family:'JetBrains Mono' to .bridge-cmd-code; added generic code,.inline-code CSS rule (11px, bg-body bg, light border, border-radius:3px, user-select:text). Files: device_bridge.py, tests/test_device_bridge.py, Sidebar-css.html)
  Previous: 2026-04-21 (check-deploy.sh: fixed VERSION grep — was matching APP_VERSION on line 4 instead of embedded Python VERSION on line 8442; fixed sed to handle both quote styles. Removed 4 stale-named project commands (review.md, review-full.md, check-gas-userguide.md, check-deploy-gas.md) — correct global versions exist as topoassist-review-code-*.md. Updated test-device_bridge.md expected count: 34→56. Files: .claude/check-deploy.sh, .claude/commands/test-device_bridge.md)
  Previous: 2026-04-21 (Toolbar edit group reorder: editModeBtn moved to rightmost position — order is now groupRectBtn, nodeSizeMinusBtn, nodeSizePlusBtn, editModeBtn. File: Sidebar.html)
  Previous: 2026-04-21 (Global UI rules 7d/7e/7f added: font scale extended to 5 levels (10/11/12/13/14px); textarea state taxonomy; inline code style. Input state taxonomy CSS (.has-error, :disabled, :read-only, .input-affix) added to Sidebar-css.html. Files: Sidebar-css.html + global ui.md)
  Previous: 2026-04-21 (Edit Mode button greyed + pointer-events:none in holistic view; auto-exits edit mode when switching to holistic if active. Files: Sidebar-js.html)
  Previous: 2026-04-21 (Fix holistic view model pills missing on direct load — getDeviceMetadata() is async so deviceMetadata was empty when renderHolistic() first ran; fix: re-call renderHolistic(currentData) in the getDeviceMetadata success handler when currentViewMode==='holistic'. Files: Sidebar-js.html)
  Previous: 2026-04-21 (Input state taxonomy CSS: added .input-wrapper.has-error + .error-msg, input:disabled, input:read-only, .input-affix patterns to Sidebar-css.html; global rule 7c added to ui.md + Reference_Card.md. Files: Sidebar-css.html)
  Previous: 2026-04-21 (info-box--dim input styling: typed value now italic + font-weight:400 (readable but annotation-style); ::placeholder already #94a3b8 italic. Global rule updated in ui.md + memory. Files: Sidebar-css.html)
  Previous: 2026-04-21 (Fix ReferenceError: maxSavedX is not defined in holistic view — stale `maxSavedX -= shiftX` in auto-shift block was dead code left from old maxSavedX+PAD node-seeding approach; removed. Files: Sidebar-js.html)
  Previous: 2026-04-21 (Snake Test info-box dim UX: added ::placeholder rule — font-style/color don't cascade into input ::placeholder, so .info-box--dim .input-wrapper input::placeholder { color:#94a3b8; font-style:italic } added explicitly. Files: Sidebar-css.html)
  Previous: 2026-04-21 (Snake Test info-box dim UX: (1) removed directional "Reverse/Forward routes" sub-labels from EP1/EP2 Subnet input-wrappers — replaced with "comma-separated CIDRs"; (2) added CSS rules so ip-labels + input-wrapper labels inside info-box--dim render as #94a3b8 (dim muted) and inputs use var(--bg-body) background, making snake test fields visually annotation-style rather than primary-data-entry. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-21 (Snake Test multi-subnet support: EP1/EP2 Subnet fields now accept comma-separated CIDRs (e.g. "10.1.1.0/24, 10.2.2.2/32, 10.3.0.0/16"); _toRoute() replaced by _toRoutes() returning an array; every fwdRoute/revRoute push replaced with forEach loop so one ip route is emitted per subnet per VRF hop; UI inputs widened to 240px with updated placeholders and labels. Files: Code.gs, Sidebar.html, UserGuide.html)
  Previous: 2026-04-21 (Holistic view node sizing: (1) NODE_R default 42→52 (bigger nodes to fit wider pills); (2) holisticNodeR global (localStorage 'holistic_node_r', range 36–72, step 4) drives NODE_R in renderHolistic; (3) +/- node size buttons in toolbar (nodeSizePlusBtn/nodeSizeMinusBtn), grey when not in holistic view; (4) adjustHolisticNodeSize(delta)/_updateNodeSizeBtns() helpers; (5) Pill sizing: height 13→16px, font 8px→10px (min-font-size rule), char factor 4.8→6px, truncation 14→18 chars, spacing i*16→i*20; (6) Canvas-grow-on-edit-mode-toggle fix: _suppressCanvasResize flag — set true in toggleEditMode() before rAF(redrawLinks), cleared in redrawLinks STEP 3 to skip that resize call only. Files: Sidebar-js.html, Sidebar.html)
  Previous: 2026-04-21 (Snake Test EP1/EP2 Subnet: (1) field now accepts full CIDR (10.99.99.0/24) in addition to legacy 3-octet prefix (10.99.99 → auto-expanded via _toRoute() helper in Code.gs); removed hardcoded .0/24 suffix display from UI; widened subnet input to 140px; placeholder updated to "10.99.99.0/24"; (2) All Snake Test entry boxes wrapped in info-box info-box--dim container for visual annotation styling. Files: Sidebar.html, Code.gs, UserGuide.html)
  Previous: 2026-04-21 (info-box audit: .bridge-hint updated — added font-style:italic + color:#94a3b8 to match annotation style. Files: Sidebar-css.html)
  Previous: 2026-04-21 (info-box--dim style update: left-accent border only + italic text to visually distinguish annotation/hint text from actual values. Global rule added to ~/.claude/rules/ui.md rule 7b. Files: Sidebar-css.html)
  Previous: 2026-04-21 (Auto Config modal UX: (1) EP subnet — add .0/24 suffix display so user sees full /24 route generated; (2) EP Port MAC/IP label column width — CSS var --ip-label-w on .ip-label (default 80px) + Snake Test rows wrapped in div with --ip-label-w:100px so "EP1/2 Port MAC" shows fully; (3) Rename "Last Octet (GW)" → "Last Octet" on GW IPv4; (4) Mask label-w narrowed to 36px on all Mask input-wrappers (was inheriting 76–108px) to reduce dead space; (5) Rename "Prefix" → "First Hextet" on P2P+GW IPv6 rows. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-21 (UserGuide + README: update clasp login to use --no-localhost — works on SSH/headless machines; explains paste-back-URL flow. Files: UserGuide.html, README.md)
  Previous: 2026-04-21 (Fix internal VLAN detection: _parse_internal_vlans() used wrong key "vlans" — EOS "show vlan internal usage | json" actually returns {"internalVlans": {"4064": "Ethernet16/1", ...}} not {"vlans": {...}}. Updated key lookup in device_bridge.py and embedded template in Sidebar-js.html; fixed all TestParseInternalVlans tests to match real EOS format; bumped VERSION 260420.3→260421.1. Files: device_bridge.py, Sidebar-js.html, tests/test_device_bridge.py)
  Previous: 2026-04-21 (Orphan cable notifications: amber badge on bridge toolbar icon when extra cables found (not in sheet); badge clears on modal open; status bar shows amber "· N extra cables found" message; _setBridgeOrphanBadge() helper added; _renderLldpResults() calls it; UserGuide updated with "Discovered — Not in Sheet" section. Files: Sidebar.html, Sidebar-css.html, Sidebar-js.html, UserGuide.html)
  Previous: 2026-04-20 (View-switching cleanup: (1) remove redundant loadStateFromStorage() call in fetchData success handler — toggleViewMode already calls it internally; (2) renderHolistic: when isRestored+hasNewNodes, call zoomToFit at 600ms after gentle alpha(0.3) restart so new devices aren't off-screen; (3) bug #1 agent finding confirmed NOT a bug — saveCurrentStateToStorage intentionally reads stale currentViewMode to save the mode being left. Files: Sidebar-js.html)
  Previous: 2026-04-20 (renderHolistic: fix "full canvas as linear view" bug — unknown/new nodes were placed at maxSavedX+PAD (a horizontal line extending canvas to linear-view width). Replaced with centroid placement: compute centroid of saved-position nodes, seed unknown nodes there with ±NODE_R*2 jitter so D3 spreads them into available gaps rather than inflating canvas. Files: Sidebar-js.html)
  Previous: 2026-04-20 (Full rename: lldp* → bridge* identifiers in Sidebar.html and Sidebar-css.html — all CSS classes, HTML element IDs, and user-visible text renamed; protocol-specific LLDP strings kept. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-20 (Simple view (holistic): device model shown as first pill below node circle when set in device metadata. Model stored in nodesData.model from deviceMetadata[n.id].model; getPillsForNode() prepends model as first entry; model pills styled light-blue (MODEL_PILL: #e0f2fe/#0369a1/#bae6fd), truncated at 14 chars. Files: Sidebar-js.html, UserGuide.html)
  Previous: 2026-04-20 (Topology card model line fixes: (1) deviceMetadata now pre-loaded in fetchData() success handler via getDeviceMetadata() so model shows immediately on topology load without needing Device Manager open. (2) _refreshModelLines() helper extracted — updates all .dev-model-line elements with priority manual>bridge, strips DCS- prefix before comparing, adds model-mismatch amber class + tooltip when both exist and differ. applyDevStatusToCards() and saveMetadataAndFinish() call _refreshModelLines(). CSS: .dev-model-line.model-mismatch { color: #fbbf24 !important }. Files: Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-04-20 (Topology card model line: .dev-model-line added to each device card header below .dev-version-line; populated at render() from deviceMetadata, updated by applyDevStatusToCards() with priority manual > bridge platform > empty; verLine no longer shows platform; saveMetadataAndFinish refreshes model lines on save. CSS: .dev-model-line in Sidebar-css.html. Files: Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-04-20 (NotebookLM questions textarea: editable questions saved to UserProperties ('notebooklm_questions'); loaded on modal open via loadNotebookLMQuestions(); auto-saved on blur via saveNotebookLMQuestions(); askNotebookLM() prepends questions above cabling table. Server: getNotebookLMQuestions()/saveNotebookLMQuestions() in Code.gs; default 5-question set in NOTEBOOK_LM_QUESTIONS_DEFAULT const. Files: Code.gs, Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-20 (Tests.gs: added test_deviceMetadata() suite — 8 assertions covering JSON round-trip, null-prop fallback for getDeviceMetadata, and buildMetadataMap filter logic; registered in runAllTests(). Files: Tests.gs)
  Previous: 2026-04-20 (AI Validate: switched from Gemini API to NotebookLM copy+open — "Ask NotebookLM" button copies cabling markdown + validation prompt to clipboard and opens the lab NotebookLM notebook in a new tab; no API key required; removed Gemini server functions from Code.gs; replaced client Gemini functions with askNotebookLM() in Sidebar-js.html. Files: Code.gs, Sidebar.html, Sidebar-js.html, UserGuide.html)
  Previous: 2026-04-20 (Device Model + Rack fields: per-device Model and Rack metadata stored in DocumentProperties key DEVICE_METADATA ({devName:{model,rack}}). Code.gs: getDeviceMetadata(), saveDeviceMetadata(metaMap) added alongside getDeviceHostnames/saveDeviceHostnames; getExistingDevices() now loads DEVICE_METADATA and includes model/rack in each device object. Sidebar-js.html: deviceMetadata={} global; applyDeviceListFromServer carries d.model/d.rack into currentDeviceList and syncs deviceMetadata global; renderManagerList() adds .dev-meta-row sub-row per device with Model+Rack inputs (model placeholder auto-fills from devStatusMap[devId].platform when bridge has been run); updateDeviceMeta(index,field,value) and buildMetadataMap() helpers; save chain: saveMlagPeersAndFinish→saveMetadataAndFinish→saveDeviceMetadata→handleSuccess; openCablingModal() lazy-loads getDeviceMetadata() if global is empty; copyCablingCsv() adds Model A/Rack A/Model B/Rack B columns; _buildCablingMarkdownStr() adds same; downloadCablingReport() adds DUTs-to-be-used section at top (devName>>rack, only for devices with rack set) and shows model inline in per-device block headers. Sidebar-css.html: .dev-meta-row, .dev-meta-label, .dev-meta-inp styles added. Files: Code.gs, Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-04-20 (AI Cabling Validation: added Gemini API integration to cabling modal — "AI Validate" section with key entry (saved to UserProperties as 'gemini_api_key'), "Validate with Gemini" button, inline result panel (green/amber/red). Server: getGeminiApiKey(), saveGeminiApiKey(), validateCablingWithGemini() in Code.gs. Client: _buildCablingMarkdownStr() helper extracted from copyCablingMarkdown(); checkGeminiKeyStatus(), saveGeminiKey(), clearGeminiKey(), validateCablingGemini() in Sidebar-js.html. Files: Code.gs, Sidebar.html, Sidebar-js.html, UserGuide.html)
  Previous: 2026-04-20 (Snake VRF gate: SNAKE_* VRFs now only emitted when both primary AND secondary ports are valid (cabling complete) — matches the snakePairsForStatic gate; uncabled snake ports no longer produce orphan VRF instances. Files: Code.gs)
  Previous: 2026-04-20 (Sheet View device badge: replaced colCount numeric badge with EOS/non-EOS type badge — getSheetDeviceList() now returns isArista bool instead of colCount; SheetAssistPanel.html: .col-count replaced with .type-badge .eos/.non-eos CSS; renderDevices() uses d.isArista to render "EOS" (green) or "non-EOS" (slate) badge. Files: Code.gs, SheetAssistPanel.html)
  Previous: 2026-04-20 (Device Bridge check messaging: _autoRunLldp() completion message changed from "LLDP check complete" to "Device Bridge checks complete — N/M reached"; auth error detection via /auth|expired|permission denied|authentication failed|arista-ssh-agent/i shows amber color + count in message; green if all reached, red if some failed without auth cause; timeout extended 3s→4s. runLldpCheck(): now flashes bridge toolbar icon (.lldp-scanning) for the duration of the manual check (start→remove on completion, same as auto-run); shows summary status instead of hiding statusEl on success; partial endpoint failure appended to summary rather than replacing it. Files: Sidebar-js.html)
  Previous: 2026-04-20 (SVG icon audit: replace 8 remaining Unicode characters with SVG — Sidebar.html: legend toggle ▼→SVG chevron, Device Manager column headers ⇅👁️✎🗑️→inline SVGs; Sidebar-js.html: △/"⚠"/"✔" in innerText DOM writes → plain text (color already conveys state). Files: Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-20 (UserGuide.html: fix 3 stale menu paths — "Topology Manager > Open Visualizer" → "Show Topology", "Column Views" → "Sheet View", "Topology Manager > Global Config" → toolbar button with SVG icon. Files: UserGuide.html)
  Previous: 2026-04-20 (L3 Snake: add TTL reset config generation — 081_SNAKE_PBR (ACL + route-map + ip policy per interface) and 082_SNAKE_TRAFFIC_POLICY (traffic-policies block + input/output per interface); added tests; updated UserGuide. Files: Code.gs, Tests.gs, UserGuide.html)
  Previous: 2026-04-20 (Add README.md — installation (clasp + manual), Device Bridge setup, features summary. Files: README.md)
  Previous: 2026-04-20 (UserGuide.html: expand Installation — Option A clasp, Option B manual copy-paste (7-file table), Device Bridge section (download, requirements, config). Files: UserGuide.html)
  Previous: 2026-04-20 (UserGuide.html: add Installation section (5 steps: clone repo, create Sheet+script, clasp install/login, update .clasp.json scriptId, push + run onOpen). Files: UserGuide.html)
  Previous: 2026-04-20 (UserGuide.html: @page width 210mm→260mm for wider print output. Files: UserGuide.html)
  Previous: 2026-04-20 (UserGuide.html: @page { size: 210mm 5000mm } — single tall page for print, no page breaks. Files: UserGuide.html)
  Previous: 2026-04-20 (UserGuide.html: print CSS — replace per-element break rules with * { break-inside/before/after: avoid !important } to suppress all page breaks. Files: UserGuide.html)
  Previous: 2026-04-20 (UserGuide.html: fix two stale sections — Interface Naming adds Vx (VXLAN/VTEP) prefix; MLAG section rewritten as explicit-only via Device Manager, removes stale count≥4 heuristic text. Files: UserGuide.html)
  Previous: 2026-04-20 (UserGuide.html: replace 🌐 emoji with inline SVG globe icon (stroke style matching sidebar); topoassist-check-gas-userguide skill updated with YYMMDD.N version format note. Files: UserGuide.html, topoassist-check-gas-userguide.md)
  Previous: 2026-04-20 (UserGuide.html: update to latest — (1) Column Views section: 6 named presets (Cabling/Transceiver/Speed/Po/Mode/Vlan) matching Code.gs; Transceiver shows xcvr_type_, Speed adds et_speed_+xcvr_speed_. (2) Schema Manager: Force Sync orphan flow updated (yellow button + Delete & Sync / Cancel only, no Keep); schema prefix collision guard documented. (3) New "Sheet — Speed Colors" section: speed-tier palette for et_speed_/xcvr_speed_ with amber/pale-orange conditional rules (Rule 4: xcvr_speed==et_speed; Rule 5: empty xcvr_speed + xcvr_type mismatch). (4) New "Sheet — Transceiver Type Colors" section: xcvr_type_ color palette (OSFP/QSFP-DD/QSFP56/QSFP28+QSFP100+DSFP/QSFP+/SFP28+SFP25/SFP+/SFP 1G/BASE-T). Files: UserGuide.html)
  Previous: 2026-04-20 (UserGuide.html: replace hardcoded v4.5 heading with <?= APP_VERSION ?> template tag; openUserGuide() switched to createTemplateFromFile().evaluate() so version is injected dynamically. Files: UserGuide.html, Code.gs)
  Previous: 2026-04-20 (UserGuide.html: add Print/Save as PDF + Download HTML buttons at top; @media print hides bar, removes box-shadow, sets break-inside:avoid on step-boxes/cards/info-boxes. Files: UserGuide.html)
  Previous: 2026-04-20 (Auto Config modal: renamed P2P/GW section headers to "POINT-TO-POINT (P2P) INTERFACE" / "GATEWAY (GW) INTERFACE"; added --label-w CSS custom property to align inputs per section; fixed snake test ip-labels and inner labels to use correct port terminology. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-20 (Sheet View panel: balanced 50/50 split — both col-left and col-right use flex:1 min-width:0; dialog width narrowed 620→540px. Files: SheetAssistPanel.html, Code.gs)
  Previous: 2026-04-20 (Section 1 PROJECT OVERVIEW rewritten: full day-0 capability description — Cabling Report, Wrong Cabling Report, Topology Visualization, Auto Config Generation, Config Push, Auditing (11 checks), Topology Search, Device Bridge. Files: INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-20 (Add plain SFP (1G) color rule (#f8fafc) at end of xcvr_type list. Files: Code.gs)
  Previous: 2026-04-20 (Add SFP25 color rule (#e0f2fe, same as SFP28) to xcvr_type; add SFP25 to Rule 5 xcvr_speed hint formula (SFP28|SFP25 → expect 25g). Files: Code.gs)
  Previous: 2026-04-20 (Drop orphan Keep concept: Force Sync now shows confirm dialog (Delete & Sync / Cancel) — schema is source of truth, no preservation. Removed keepOrphanKeys chain, clientOrphanKeys param, globalAttributes findAttrKey hacks. Files: Sidebar-js.html, Code.gs)
  Previous: 2026-04-20 (Force Sync yellow-only on orphans: remove badge span from button, _updateForceSyncBadge now only toggles button color. Files: Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-20 (Fix xcvr_ deletion on Keep: pass client-detected orphan keys through call chain (_showOrphanConfirm → triggerSyncOnly → _runForceSyncNow → syncSchemaPreservingOrder 4th param clientOrphanKeys) so server never re-detects independently. Files: Code.gs, Sidebar-js.html)
  Previous: 2026-04-20 (Orphan dialog fixes: (1) _getOrphanAttrKeys skips removed-device columns (key=header when no device suffix matched — caused garbage headers + data loss on Keep); (2) getOrphanedColumnsInfo adds removedDevice flag; (3) _showOrphanConfirm splits keepable vs removed-device sections, modal uses flex+max-height:90vh so buttons never go off-screen. Files: Code.gs, Sidebar-js.html)
  Previous: 2026-04-20 (Force Sync button turns yellow when orphan columns detected; orphan confirm dialog list is now scrollable (max-height:220px) in a bordered box; modal widened 320→380px. Files: Sidebar-js.html)
  Previous: 2026-04-20 (Fix getOrphanedColumnsInfo: devices array holds objects; use d.name when building expectedHeaders and stripping suffix — previously k+d produced "int_[object Object]" causing all schema columns to appear as orphans. Files: Code.gs)
  Previous: 2026-04-20 (Conditional formatting Rule 5: pale-yellow xcvr_speed_ when empty AND xcvr_type native speed ≠ et_speed (breakout detected, fill in xcvr_speed for cable-group detection). QSFP28/QSFP100/DSFP→100g, QSFP56→200g, QSFP-DD/OSFP→400g/800g, QSFP+→40g, SFP28→25g, SFP+→10g/1g. Files: Code.gs)
  Previous: 2026-04-20 (Migrate all getExistingAttributes callers to _getOrphanAttrKeys: syncSchemaPreservingOrder + saveDeviceConfiguration now use header-based orphan detection (schema×devices expected set). getExistingAttributes marked @deprecated. Files: Code.gs)
  Previous: 2026-04-20 (xcvr_type color rules: add QSFP100 → green-100 #dcfce7. Files: Code.gs)
  Previous: 2026-04-20 (Fix getOrphanedColumnsInfo: switch from key-extraction (getExistingAttributes) to header-based detection (schema×devices expected set). Old approach missed xcvr_-style orphans silently deleted by rebuildSheet. Files: Code.gs)
  Previous: 2026-04-20 (Conditional formatting: fix dead 'speed' rule → 'et_speed'+'xcvr_speed' via shared _SPEED_COLOR_RULES; add Rule 4 — amber xcvr_speed_ when et_speed==xcvr_speed (lane-split attention). Files: Code.gs)
  Previous: 2026-04-20 (Auto Config modal: add f-id (teal) color for device IDs in example; widen ip-label 44→80px; keep-colors on example box; MLAG phy computed octets use ip-ex-result. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-20 (device_bridge.py: extract _build_devstatus_ssh() pure fn; add 8 tests covering partial SSH command failures (ver/ifs/ivlans each None, all None). VERSION 260420.2→260420.3. Files: device_bridge.py, Sidebar-js.html, tests/test_device_bridge.py)
  Previous: 2026-04-20 (Force Sync orphan dialog: replace confirm() with _showOrphanConfirm() — explicit "Keep Columns" / "Delete Columns" inline modal, data-loss warning highlighted. Files: Sidebar-js.html)
  Previous: 2026-04-20 (device_bridge.py: guard ver/ifs None in _check_devstatus — failed SSH thread for one command no longer crashes others. VERSION 260420.1→260420.2. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-19 (Auto Config modal: replace single-device example with two-peer CSS grid (Device 1 + Device 2 columns, P2P/GW/VNI/ASN rows); change .input-wrapper to flex-row so label is inline-left of input with explicit per-input widths. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-20 (device_bridge.py: fix 'show vlan internal' → 'show vlan internal usage'; switch VERSION format to YYMMDD.N (260420.1). Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-20 (Fix device_bridge.py devstatus showing ok:False: _parse_internal_vlans() crashed with AttributeError when 'show vlan internal' thread failed and returned None — guard added. VERSION 2.11→2.12. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-20 (Rename schema key xcvr → xcvr_type (column label: Xcvr Type). Eliminates prefix collision with xcvr_speed_ permanently. Migration: on next Force Sync, old xcvr_ column is detected as orphan — keep it to copy data, then delete. Files: Code.gs, Sidebar-js.html, Tests.gs)
  Previous: 2026-04-20 (P1 prevention — 3 layers: (1) Extract findAttrKey(header,schemaKeys) pure fn (longest-match wins) + replace inline logic in rebuildSheet + 9-assertion test suite in Tests.gs. (2) Prefix collision guard in saveSchemaConfig (server) and addNewField (client) — blocks authoring keys where one is a prefix of another. (3) Pre-write occupancy diff in rebuildSheet: count non-empty cells per column before/after from raw backupValues; throw+rollback if any schema column with data becomes empty. Files: Code.gs, Sidebar-js.html, Tests.gs)
  Previous: 2026-04-20 (Fix rebuildSheet() attrKey matching: pick longest matching schema key instead of last. Bug: xcvr_speed_gd435 matched both xcvr_speed_ and xcvr_; xcvr_ (shorter, later in array) overwrote correct key, silently wiping xcvr_speed_ column values on every Force Sync. Fix: add k.length > attrKey.length guard. Files: Code.gs)
  Previous: 2026-04-20 (Tests.gs: add QSFP100 no-xcvr_speed_ test — covers real-world case where xcvr_speed_ column is blank but xcvr_=QSFP100; xcvr_ prefix alone must trigger breakout grouping. 4 assertions. Files: Tests.gs)
  Previous: 2026-04-20 (syncSchemaPreservingOrder() now calls ensureDummyColumn() at start — _sys_ guaranteed before any rebuild work, including the no-devices early-exit path. Files: Code.gs)
  Previous: 2026-04-20 (Fix onStructuralChange() in Code.gs: toast was shown on ANY column deletion, not just when _sys_ was actually gone. Now checks sysGone before toasting.)
  Previous: 2026-04-20 (Bump APP_VERSION 4.4→4.5 (Code.gs, Sidebar-js.html). Update stamp format: "git commit: <hash>" instead of bare hash. All 7 GAS files re-stamped. Hook topoassist-stamp.sh updated to use new format. Files: Code.gs, Sidebar-js.html, all GAS files, topoassist-stamp.sh)
  Previous: 2026-04-19 (Sheet View panel: increase width 490→620px to accommodate full column names. Files: Code.gs)
  Previous: 2026-04-19 (Fix SyntaxError: APP_VERSION redeclared in Tests.gs — all .gs files share one global scope; const only in Code.gs. Tests.gs now has a comment instead. CLAUDE.md updated with constraint. Files: Tests.gs, CLAUDE.md)
  Previous: 2026-04-19 (Tests.gs: remove runAllTestsCli + doGet — clasp run/web app auth not viable without gcloud; tests run manually via runAllTests() in Apps Script editor. Files: Tests.gs)
  Previous: 2026-04-20 (Add version comment as first line of all GAS files: Code.gs and Tests.gs get "// TopoAssist v4.4"; Sidebar-js.html gets "<!-- TopoAssist v4.4 -->". HTML files already had it. All files now show version on line 1. Files: Code.gs, Tests.gs, Sidebar-js.html)
  Previous: 2026-04-19 (Versioning: unified APP_VERSION="4.4" across ALL files — Code.gs (canonical), Sidebar-js.html, Tests.gs (replaces TESTS_VERSION); HTML files carry matching comment. Bump all on every release. Files: Code.gs, Sidebar-js.html, Sidebar.html, Sidebar-css.html, UserGuide.html, SheetAssistPanel.html, Tests.gs)
  Previous: 2026-04-19 (Tests.gs: add TESTS_VERSION constant (1.1); emitted in Logger.log header and alert title on runAllTests(). Bump when tests change. Files: Tests.gs)
  Previous: 2026-04-19 (Fix chassis native SFP false-breakout: isBreakoutA/B now requires !!aggA/!!aggB in addition to slash check — prevents Et3/1-style chassis ports from being misidentified as QSFP-DD breakout lanes. Fix applied to Code.gs _buildCableGroupsForTest and Sidebar-js.html buildCableGroups (kept in sync). Also fixed Tests.gs snake /32 assertion — regex now instead of too-broad includes() check. Files: Code.gs, Sidebar-js.html, Tests.gs)
  Previous: 2026-04-20 (check-deploy: moved script to .claude/check-deploy.sh to avoid skill rendering stripping $1/$2/$3 from bash functions in markdown. check-deploy.md now just calls bash .claude/check-deploy.sh. Files: .claude/check-deploy.sh, .claude/commands/check-deploy.md)
  Previous: 2026-04-19 (check-deploy v3: adds commit history section (today/last week/last month) after the sync table; classify() labels each file [GAS]/[LOCAL]/[other]; uncommitted files shown with ⚠. Files: .claude/commands/check-deploy.md)
  Previous: 2026-04-19 (unified /check-deploy: merged sync-check into check-deploy — single command shows three-way git/local/GAS table + device_bridge.py VERSION check. Removed sync-check.md. Fixed TMPDIR variable conflict (renamed to PULL_DIR). Files: .claude/commands/check-deploy.md, removed .claude/commands/sync-check.md)
  Previous: 2026-04-19 (sync-check command: added .claude/commands/sync-check.md — pulls remote GAS into temp dir and diffs against local; reports OK/DIFFERS/REMOTE ONLY per file. Run via /sync-check.)
  Previous: 2026-04-19 (clasp deploy: documented login process (--no-localhost) + auto-push hook. Section 3 rewritten with clasp workflow, re-auth steps, script ID. topoassist-deploy-tracker.sh now runs clasp push automatically after GAS file edits; reports OK/FAILED with error. check-deploy.md updated to reflect clasp workflow. Files: INSTRUCTIONS_topoassist.txt, .claude/commands/check-deploy.md, ~/.claude/hooks/topoassist-deploy-tracker.sh)
  Previous: 2026-04-19 (Rename ip-info-box → info-box (generic): ip-info-box/ip-info-box--dim/ip-info-box--keep-colors → info-box/info-box--dim/info-box--keep-colors across Sidebar.html + Sidebar-css.html; class is now a universal pattern not tied to IP config feature. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-19 (Auto Config info boxes: dim all static reference boxes — auto-derived (info-box--dim) and formula key (info-box--dim info-box--keep-colors); CSS extended: .auto-fixed-label/.auto-fixed-formula → #94a3b8; .legend-label/.legend-octet → #94a3b8; keep-colors variant restores f-var/#2563eb + f-dyn/#d97706 for formula key color legend. Warning banners intentionally excluded. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-19 (Network Tech Stack modal label renames: "GATEWAY" → "GATEWAY (GW) INTERFACE"; "P2P INTERFACE" → "POINT-TO-POINT (P2P) INTERFACE"; removed "SVI · Sub-Int · L3 Port" italic hint span from gateway row. File: Sidebar.html)
  Previous: 2026-04-19 (Debug Logging UI toggle: added "Debug Logging" checkbox to Schema Manager footer (Sidebar.html left footer-group). toggleDebugLogging(on) sets/removes localStorage 'topoassist_debug' key; dbg() reads on every call so toggle is live — no reload needed. initSchemaManager() syncs checkbox to current localStorage state on open. Files: Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-19 (Auto Config example box: dim + compact — added ip-info-box--dim scoped CSS (muted #94a3b8 for all values, 10px font, tighter padding/gaps); VLAN example 1500→1520 (oct3=0→20, all 4 octets/hextets non-zero); f-var/f-dyn both #94a3b8 inside dim box. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-19 (VLAN Summary modal: show EOS-internal VLANs. buildVlanSummary() now does two passes — Pass 1: user-configured VLANs from allNodesData (unchanged); Pass 2: iterates deviceInternalVlans (devId→[vids]), adds INT-type entries for new VLANs, or tags internalOn[] on existing entries for conflicts. renderVlanSummaryTable(): INT type shown in purple, INT-only rows show "EOS Internal: devA, devB" in ports column, conflicted rows show red [CONFLICT] badge on VLAN ID. filterVlanSummary(): also searches internalOn device names. UserGuide updated with L2/L3/INT/CONFLICT type legend. Files: Sidebar-js.html, UserGuide.html)
  Previous: 2026-04-19 (Auto Config example box: split GW IPv4/IPv6 into standalone + MLAG sub-rows (↳ MLAG phy, ↳ MLAG vip) with separators between groups; added IPv6 MLAG coverage (ipv6 virtual-router address); added .ip-ex-sep (dashed divider), .ip-ex-sub .ip-ex-key (muted key style), .ip-ex-note .f-var/.f-dyn (color override inside notes). Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-19 (Toolbar tooltip auto-sync: added data-modal="<modalId>" to 7 toolbar icon-btn elements (techModal, ipConfigModal, globalCfgModal, vlanSummaryModal, cablingModal, generateAllModal, auditModal) in Sidebar.html. Added syncToolbarTooltips() in Sidebar-js.html called at initApp() start — reads each button's data-modal, finds the modal, reads .modal-header-title, sets button.title. Modal title is now single source of truth; Bridge button intentionally omitted (verbose custom tooltip). Files: Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-19 (Sync toolbar button tooltips to modal titles: "Export Cabling" → "Export Cabling Report", "Audit Logic" → "Configuration Audit". showModalLoading() already reads modal-header-title at runtime so loading messages are inherently in sync. File: Sidebar.html)
  Previous: 2026-04-19 (Fix toolbar button tooltip: "Enable Technology" → "Network Tech Stack" to match modal header. File: Sidebar.html)
  Previous: 2026-04-19 (Generate All modal split layout: modal widened 600→900px; generateAllBody replaced with generateAllLeft (generate results) + generateAllRight (push results, hidden until Push All clicked). openGenerateAllModal() resets right pane to hidden; runGenerateAll() writes to generateAllLeft; pushAllConfigs() reveals generateAllRight and writes push progress there with "Push Results" pane header. Both panes scroll independently. Files: Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-19 (Review fixes: Sidebar-js.html:2519 guard updated from includes("✗ MLAG Peer-Link Missing") to includes("MLAG Peer-Link Missing") — fixes bug introduced when we changed the ✗ prefix to [FAIL]; guard now correctly detects the message regardless of prefix. Sidebar-js.html:5864 schema audit success icon ✓ at 32px replaced with 40px inline SVG checkmark. Files: Sidebar-js.html)
  Previous: 2026-04-19 (Debug toggle: added dbg(...) helper at top of Sidebar-js.html — wraps console.log, gated by localStorage key 'topoassist_debug'==='1'. Enable: localStorage.setItem('topoassist_debug','1') in browser console + reload. Disable: localStorage.removeItem('topoassist_debug') + reload. Restored all key trace logs as dbg() calls: init mode, app init, cache restore, poll change detection, auto-refresh trigger, fast/slow path save, prefetch start/complete, config cache invalidation, manual refresh, topology data received, Auto-LLDP skipped/triggered/results/errors, schema audit triggered/response, Custom View schema+prefs fetch. Files: Sidebar-js.html)
  Previous: 2026-04-19 (Debug cleanup: removed all console.log trace statements (~30 locations) from Sidebar-js.html — init mode, cache restore, poll change detection, device manager save paths, sort handler, schema audit, background prefetch, config cache invalidation, manual refresh, fetchData receive, Auto-LLDP run/results/errors, Custom View Manager fetch chain. Kept all console.error/warn at real error boundaries (catch blocks, DOM guards, server failure handlers). Fixed Unicode ✗ in 5 validation error message strings (Po Mismatch/Asymmetry, MLAG Peer-Link/Required, Duplicate P2P VLAN) → [FAIL] prefix. Fixed Unicode in 7 console.error strings (removed ✗/⚠️ prefixes). Bumped canonicalizeInterface last synced date in Code.gs 2026-04-17→2026-04-19. Files: Sidebar-js.html, Code.gs)
  Previous: 2026-04-19 (SVG icon audit: replaced all Unicode icon characters (✓ ✕ ☰ ✎ × △) with inline SVG in Sidebar-js.html — 11 locations: confirm/cancel/delete device buttons, reorder handle, rename button, label chip remove, close label picker, schema handle, delete field button, Force Sync "Synced!" state, validation error status indicator. Also fixed /health docstring in device_bridge.py to show version 2.11 (was 2.10). Also: snake route revision — routes now emitted ONLY when ep2_subnet/ep1_subnet configured (fwdRoute=null/revRoute=null → no ip route statement); removed /32 host fallback entirely; when no subnets set, header shows "ARP skeleton only" message instead of route lines. Tests.gs T4 updated: no routes expected when subnets absent; prefsFull now includes ep1_subnet/ep2_subnet. Files: Sidebar-js.html, device_bridge.py, Code.gs, Tests.gs)
  Previous: 2026-04-19 (Snake route simplification: always exactly 2 static routes per VRF — one forward, one reverse. fwdRoute={ep2_subnet}.0/24 when ep2_subnet set, else 10.99.99.98/32. revRoute={ep1_subnet}.0/24 when ep1_subnet set, else 10.99.99.99/32. /32 is never emitted alongside /24 (redundant). fwdHost/revHost are header-only comments showing specific test host .98/.99 for traffic gen reference. Tests.gs: T7 + T_ep1_subnet + T_subnet_routes (renamed from T_4routes) updated for 2-route assertions. UserGuide.html: 080_SNAKE bullet + example config + field table updated. Files: Code.gs, Tests.gs, UserGuide.html)
  Previous: 2026-04-19 (Refactor: extract _parse_internal_vlans(ivlans) as module-level pure function in device_bridge.py and embedded template in Sidebar-js.html; _check_devstatus() now calls it instead of inline expression. test_device_bridge.py: TestParseInternalVlans — 6 cases: empty dict, vlans key present but empty, single VLAN, multiple VLANs returned sorted, string keys converted to int, extra top-level keys ignored. 50→56 pytest passing. Files: device_bridge.py, Sidebar-js.html, tests/test_device_bridge.py)
  Previous: 2026-04-19 (Audit Check K: Internal VLAN Conflict — if deviceInternalVlans has data for the port's device, expands vlan_ (vlans + native) via parseVlanInput and intersects with the internal VLAN list; pushes configError naming conflicting VLANs. Silently skipped when no bridge data available. UserGuide: added Internal VLAN Conflict bullet in Audit Engine section. Files: Sidebar-js.html, UserGuide.html)
  Previous: 2026-04-19 (Internal VLANs via device bridge: device_bridge.py _check_devstatus() now runs 'show vlan internal' as a third command (ssh/eapi path only), returns internalVlans:[N,...] sorted int list. VERSION 2.10→2.11 in device_bridge.py and Sidebar-js.html embedded template. Sidebar-js.html: deviceInternalVlans global (devId→[N,...]) populated at both devStatusMap sites (auto-LLDP and manual runBridgeCheck). filterTopology() device match extended: search term matching an internal VLAN number highlights the device. traceVlan() appends .legend-internal-note to #vlan-legend listing "VLAN N — EOS internal on: dev1, dev2"; legend made visible even when no port carries the VLAN. Sidebar-css.html: .legend-internal-note style (10px JBM, muted, border-top). UserGuide.html: new Internal VLANs subsection in Device Bridge section. Files: device_bridge.py, Sidebar-js.html, Sidebar-css.html, UserGuide.html)
  Previous: 2026-04-19 (Snake test: bidirectional VRF chain + EP1/EP2 rename. TRAFFIC_IN_L3→TRAFFIC_SNAKE_EP1_L3, TRAFFIC_OUT_L3→TRAFFIC_SNAKE_EP2_L3. generateSnakeStaticConfig() fully rewritten: PRIMARY port emits fwd route always + rev terminal (ep1_nh) or rev egress-vrf chain (prev.secondary); SECONDARY port emits fwd terminal (ep2_nh) or fwd egress-vrf chain (next.primary) + rev route always. getIpPreferences() defaults: removed ixia_mac/ixia_nh/ixia_subnet; added ep1_nh/ep1_mac/ep1_subnet/ep2_nh/ep2_mac/ep2_subnet (all ''; MACs optional — static ARP only when set; NHs soft-warn only). 080_SNAKE audit: dropped ixia config hard-check; added nhWarn soft warning when ep1_nh or ep2_nh empty. checkSnakeTrafficWarning() (Sidebar-js.html): shows soft yellow warning when snake pairs present + NH missing. Sidebar.html snake panel: 7 rows (Bridge MAC + EP1 NH/MAC/Subnet + EP2 NH/MAC/Subnet). Tests.gs test_generateSnakeStaticConfig: prefsBase/prefsFull updated; T4-T10 rewritten for bidirectional output; added T_indirect, T_ep1_subnet, T_no_nh. UserGuide.html Section 7: rewritten for bidirectional design. Files: Code.gs, Sidebar.html, Sidebar-js.html, Tests.gs, UserGuide.html)
  Previous: 2026-04-19 (Audit Check J: nv<N> on non-trunk port → configErrors.push in CHECK GROUP 2. Flags 'nv<N> in VLANs column is only valid on trunk ports' when parseVlanWithNative(d.vlan_).native is set and mode does not include 'trunk'. UserGuide.html: new audit bullet for native VLAN on non-trunk. Files: Sidebar-js.html, UserGuide.html)
  Previous: 2026-04-19 (svi_vlan_ nv<N> support: _parseSviVlans now recognises nv<N> token (extracts N via regex, adds to requested set). All 3 call sites now include native VLAN in the vlans array passed to _parseSviVlans (generateComplexL3Block uses vlansForSvi = vlans+[native]; Vx1 call site adds native to apVlans; processP2pNeighbor uses vlansForSvi). svi_vlan_='all' now includes native VLAN; svi_vlan_='nv100' creates SVI for VLAN 100. Tests.gs: 4 new _parseSviVlans cases. UserGuide.html: SVI VLANs description updated. Files: Code.gs, Tests.gs, UserGuide.html)
  Previous: 2026-04-19 (Bug fix: access port config was using raw d.vlan_ → switchport access vlan nv100 (invalid EOS). Fixed to use parseVlanWithNative(d.vlan_).vlans so nv<N> token is silently stripped before the access vlan command. File: Code.gs)
  Previous: 2026-04-19 (Remove n_vlan column from schema; encode native VLAN as nv<N> token inside vlan_ field. Added parseVlanWithNative(str)→{native,vlans} helper (DUPLICATED in Code.gs + Sidebar-js.html — last synced 2026-04-19). vlan_ validation regex updated to accept nv\d+ token. All n_vlan_ references replaced: trunk config (parseVlanWithNative(d.vlan_)), allVlans collection, processP2pNeighbor IP calc, gateway VLAN collection, VLAN audit, VLAN summary map, search text, getVlanForSublayer. Tests: test_parseVlanWithNative added (11 cases). UserGuide.html: nv<N> syntax documented in Sheet Conditional Dimming section. Files: Code.gs, Sidebar-js.html, Tests.gs, UserGuide.html)
  Previous: 2026-04-19 (Sheet View panel improvements: preset buttons auto-apply immediately via submitCustomView(); active preset indicator (.active CSS class on clicked button, cleared by toggleAllView(), set on panel open by detectActivePreset()); removed false "only mandatory column" dialog from submitCustomView(); VIEW_PRESETS hoisted to module-level const; loading spinner: emoji ⏳ → inline SVG; data-preset attribute on each preset button. Files: Sidebar.html, Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-04-19 (Tests.gs: add 2 missing _buildCableGroupsForTest coverage gaps — (1) modular chassis native SFP (2-level slash, no aggX) → verifies 2 separate Scenario B groups with raw Et{slot}/{port} names, no breakout grouping; (2) modular chassis QSFP breakout (3-level Et5/22/1..4, aggX=100g-4) → verifies collapse to 1 group keyed by Et5/22/1 anchor and phyA=Et5/22/1. All 6 decision-tree branches now covered. Files: Tests.gs)
  Previous: 2026-04-19 (getPhysicalPortParent: switch from parts.pop() → parts[last]='1' (normalize lane to /1). Rationale: parts.pop() gave Et14 which is not a valid EOS port name; Et14/1 IS valid and is the lane-1 anchor for the physical QSFP transceiver. All breakout lanes of the same QSFP now collapse to the same lane-1 key (Et14/4→Et14/1, Et5/22/3→Et5/22/1). Native ports (Et14/1 already lane 1) are unchanged. Updated in both Sidebar-js.html and Code.gs (keep in sync). Tests.gs: updated all phyA/phyB and groupKey expected values to /1 form (Scenario A: Et14/1 <-> Et48/1, Scenario C: Et14/1, Cases 3/4, 1.6t-8). CLAUDE.md: updated constraint (was "never parts[last]='1'", now "never parts.pop()"). Files: Sidebar-js.html, Code.gs, Tests.gs, CLAUDE.md)
  Previous: 2026-04-19 (Cabling BASE-T display fix: sfp-1000baset in xcvr_speed_ is a transceiver-type descriptor, not a plain speed string. downloadCablingReport: added sL/sR BASE-T normalization → sL='1g' and fills xL='BASE-T' if xcvr_ column was empty (avoids "BASE-T sfp-1000baset" in report). copyCablingCsv + copyCablingMarkdown: cleanSpeed() maps any BASE-T xcvr_speed_ value → '1g'. Code.gs getPhysicalPortParent docstring: removed misleading pizza-box native port examples (Et14/4→Et14, Et48/1→Et48) and replaced with correct modular chassis examples (Et5/22/1→Et5/22). Files: Sidebar-js.html, Code.gs)
  Previous: 2026-04-19 (Sheet View presets: replaced 5 old presets (Cabling/Po/Mode/Vlan/Xcvr) with 6 named presets — Cabling(int), Transceiver(int+xcvr), Speed(int+xcvr+et_speed+xcvr_speed), Po(int+po), Mode(int+po+sp_mode), Vlan(int+po+sp_mode+vlan). Renamed Code.gs functions: viewIntOnly→viewCabling, viewIntAndXcvr→viewTransceiver, viewIntAndPo→viewPo, viewIntAndMode→viewMode, viewIntAndVlan→viewVlan; added viewSpeed(). Menu labels updated. Sidebar.html preset buttons updated (6 buttons, xcvr→transceiver, +speed). loadViewPreset() in Sidebar-js.html updated. Files: Code.gs, Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-19 (Bug fix: viewShowAll() now resets all three row-level filters before refreshing — previously it only reset device visibility and column type visibility but left ip_type / int_mode / svi row filters intact, so rows hidden by those filters stayed hidden after "Show All Columns/Devices". Fix: saveSheetViewIpFilter(['p2p','gw','blank']), saveSheetViewIntModeFilter([]), saveSheetViewSviFilter(['active','blank']) added before applyCustomView(). File: Code.gs)
  Previous: 2026-04-19 (Fix 1.6t-8 transceiver regex: old pattern /^(\d+)g-(\d+)$/i used 'g' suffix only — missed '1.6t-8' (Tbps) and decimal prefix. Fixed regex to /^(\d+(?:\.\d+)?)[gt]-(\d+)$/i in 5 locations: aggA/aggB in buildCableGroups() and _buildCableGroupsForTest (Code.gs), lA/lB in _breakoutSides() (Sidebar-js.html + Code.gs), lL/lR in downloadCablingReport(). Ratio check also fixed: parseInt() gave 1 for '1.6t-8' — replaced with _gbps() helper (Math.round(parseFloat(m[1]) * (t-suffix ? 1000 : 1))) in _breakoutSides and downloadCablingReport. Tests.gs: added 3 _breakoutSides cases (1.6t-8 QSFP-A, 1.6t-8 QSFP-B, 1.6t-8 Scenario A) and 1 _buildCableGroupsForTest case (8-lane Scenario C with leaf1:Et14/1..8 xcvr=1.6t-8, server1:Et1..8 xcvr=200g). Files: Sidebar-js.html, Code.gs, Tests.gs)
  Previous: 2026-04-19 (Tests.gs: add test_generateSnakeStaticConfig() — 10 groups / ~30 cases: empty/null pairs, missing bridge_mac, VLAN 200 single pair, VLAN 1500 subnet formula, single pair full IXIA config, custom ixia_subnet→dest, 2-pair egress-vrf chain, 3-pair egress-vrf chain, custom p2p_v4_first base. Added to runAllTests(). UserGuide.html Section 7 rewritten: TRAFFIC_IN/OUT port setup (desc_=TRAFFIC_IN_L3/TRAFFIC_OUT_L3), Auto Config Snake Test fields (IXIA MAC, Traffic Port P2P IP, IXIA Subnet + warning banner), full end-to-end flow (TRAFFIC_IN→snake chain→TRAFFIC_OUT), updated 080_SNAKE example with IXIA ARP+route on last secondary, bidirectional limitation note. Files: Tests.gs, UserGuide.html)
  Previous: 2026-04-19 (Tests.gs: remove misleading getPhysicalPortParent test cases for Et14/1, Et14/4, Et48/1 → on pizza-box switches these are native hardcoded interface names, NOT breakout lanes; aggX guard (xcvr_speed_ must match Xg-N) prevents stripping in production; only modular chassis 3-level case (Et5/22/1→Et5/22) is a realistic stripping scenario. Files: Tests.gs)
  Previous: 2026-04-19 (Tests.gs: replace Et5/22 test with Et5/22/1 (modular breakout lane → Et5/22); Et5/22 as a 2-level port never reaches getPhysicalPortParent in practice because aggX guard blocks it — modular chassis breakout lanes are always 3-level (Et5/22/1..N). Files: Tests.gs)
  Previous: 2026-04-19 (Tests.gs: fix misleading label for Et5/22 test (22 IS numeric, label said non-numeric); add non-numeric last-part case (Et5/abc → unchanged); add Case 3 (both slash, only A has aggregate xcvr_speed_), Case 4 (both slash, only B has aggregate xcvr_speed_), and Scenario C B-is-QSFP variant — all 6 groupKey paths in _buildCableGroupsForTest now have explicit test coverage. Files: Tests.gs)
  Previous: 2026-04-18 (IXIA NH placeholder: updated placeholder in Sidebar.html for ip_ixia_nh from '10.99.99.2' to '200.15.0.2' (VLAN 1500 example: oct2=15 oct3=0 → subnet 200.15.0.0/24 → traffic generator P2P IP = 200.15.0.2). Label changed from 'IXIA Next-Hop IP' to 'Traffic Port P2P IP' to clarify it must be the traffic generator's interface IP on the direct P2P link to TRAFFIC_OUT — not an arbitrary IP. Files: Sidebar.html)
  Previous: 2026-04-19 (Cabling: fix getPhysicalPortParent bug (was returning Et14/1 instead of Et14 — used parts[last]="1" instead of parts.pop()); fix compressPortList comment (it joins, not compresses); duplicate getPhysicalPortParent, compressPortList, _breakoutSides, _buildCableGroupsForTest to Code.gs for server-side unit testing; add full cabling test suite to Tests.gs covering Scenario A/B/C, snake self-loop, non-Arista, missing node. Files: Code.gs, Sidebar-js.html, Tests.gs)
  Previous: 2026-04-19 (Rename IXIA→TRAFFIC throughout snake chain: IXIA_IN_L3→TRAFFIC_IN_L3, IXIA_OUT_L3→TRAFFIC_OUT_L3; all variable names (foundIxiaIn→foundTrafficIn, snakeIxiaFlags→snakeTrafficFlags, _snakeIxiaFlags→_snakeTrafficFlags, checkSnakeIxiaWarning→checkSnakeTrafficWarning, snakeIxiaWarning→snakeTrafficWarning div ID). Generalizes from IXIA-specific to any traffic generator. Files: Code.gs, Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-19 (TRAFFIC_IN/OUT auto-detection via description field: ports with desc_='IXIA_IN_L3' auto-assigned vrf SNAKE_{firstPrimary}; 'IXIA_OUT_L3' auto-assigned vrf SNAKE_{lastSecondary}. VRF priority in getIpBlock(): ixiaRole('in'/'out') > isSnakePrimary/Secondary > vrf_ column. snakePairsForStatic pre-computed before main rows loop in getDeviceConfig() (was after) so firstPrimary/lastSecondary are available when processing IXIA rows; duplicate snakeIntColIdx declaration and old snake pairs block removed. foundIxiaIn/foundIxiaOut tracked; 080_SNAKE blockStatus flags 'IXIA_IN_L3 desc'/'IXIA_OUT_L3 desc' missing when snake pairs exist. getTopologyData() scans allCollectedNodes for those desc_ values, returns snakeIxiaFlags:{hasIn,hasOut} in result. Sidebar-js.html: _snakeIxiaFlags global; extracted from data in both topology receive paths; checkSnakeIxiaWarning() now shows per-item messages (missing fields by name + missing desc instructions). Files: Code.gs, Sidebar-js.html)
  Previous: 2026-04-19 (Snake IXIA config: added 3 new Auto Config fields — IXIA MAC, IXIA NH, IXIA Subnet (stored as ixia_mac/ixia_nh/ixia_subnet in UserProperties). getIpPreferences() defaults all 3 to ''. generateSnakeStaticConfig() simplified to 2 params (snakePairs, ipPrefs) — bridgeMac/ixiaMac/ixiaNh all read from ipPrefs; dest derived from ixia_subnet (e.g. 10.99.99.98/32 from prefix 10.99.99) instead of hardcoded. Last-secondary block now emits arp+route toward IXIA_OUT if ixia_mac+ixia_nh set, else comment. Call site blockStatus lists both Bridge MAC and IXIA config if missing. Sidebar.html: 3 new ip-row inputs + snakeIxiaWarning banner (yellow, shown when snake links detected but any IXIA field empty). Sidebar-js.html: load/save/reset for 3 fields; checkSnakeIxiaWarning() added; oninput on IXIA fields calls checkIpDirty+checkSnakeIxiaWarning; warning checked on modal open and after reset. Architecture: IXIA_IN/OUT are regular P2P rows — user sets vrf_ to SNAKE_{firstPrimary}/SNAKE_{lastSecondary} manually. Files: Code.gs, Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-19 (Fix cabling export — snake loopback cables: revert skip-entirely approach; instead suppress breakout detection when nodeA.device===nodeB.device (isSelfLoop). A breakout primary port (e.g. Et14/4) used as snake primary was triggering phantom breakout groups; forcing isBreakoutA/B=false on self-loops makes snake cables always render as Scenario B (standard single cable row) with correct port names. Files: Sidebar-js.html)
  Previous: 2026-04-18 (Fix cabling export regression: buildCableGroups() now skips links with type='snake'. Snake self-loop links were added to globalLinks in the snake test feature (commit 1b591e8) but buildCableGroups had no type filter — snake links created phantom same-device "cables" in the cabling report and corrupted breakout/split-cable grouping. Fix: `if (link.type === 'snake') return` guard at top of forEach. Files: Sidebar-js.html)
  Previous: 2026-04-18 (Bridge MAC auto-fill: openIpConfig() load handler now auto-populates ip_bridge_mac from _detectedBridgeMac if the saved pref is empty — device bridge check runs → _detectedBridgeMac cached → user opens Auto Config → field pre-filled if not yet saved. Also completes bridge MAC auto-detection chain: device_bridge.py _check_devstatus() returns bridgeMac (systemMacAddress from EOS); Sidebar-js.html embedded template updated (VERSION 2.9→2.10, bridgeMac field); _detectedBridgeMac global declared; _autoDetectBridgeMac(dsData) scans devStatusMap for unique bridge MAC (null if ambiguous); called in both auto-LLDP and runBridgeCheck paths after devStatusMap is set. Files: Sidebar-js.html, device_bridge.py)
  Previous: 2026-04-18 (Hardcoded UI label audit: eliminated all stale string literals that could break on modal/button rename. (1) updateDirtyButton() — removed 3rd defaultText param; now caches original label in btn.dataset.cleanLabel on first call and derives all states from it. (2) updateSaveButtonVisuals() and updateDeviceSaveButton() — same data-cleanLabel pattern. (3) openGlobalCfgModal() — caches btn.textContent into btn.dataset.cleanLabel on first open instead of hardcoding 'Save & Apply'. (4) saveGlobalCfgFromSidebar() failure handler — restores from btn.dataset.cleanLabel || 'Save & Apply'. (5) showModalLoading() helper — reads .modal-header-title from DOM so loading messages never go stale. Files: Sidebar-js.html)
  Previous: 2026-04-18 (UserGuide snake section rewritten: comprehensive Section 7 with mode table (L2 trunk / L3 routed / L3 sub-int), physical setup, sheet setup, generated config example across all 4 sections (001_SYSTEM VRFs, 050_Et* interfaces, 080_SNAKE ARP+routes), VRF chain explanation, manual config checklist (bridge MAC + EOS command, loopback cables, IXIA traffic to 10.99.99.98/32). Files: UserGuide.html)
  Previous: 2026-04-18 (Add showModalLoading(modalId) helper: reads .modal-header-title from the DOM so loading messages never go stale on modal rename; replaced showGlobalLoading("Loading Auto Config...") and showGlobalLoading("Fetching Tech Stack...") with showModalLoading("ipConfigModal") and showModalLoading("techModal"). Files: Sidebar-js.html)
  Previous: 2026-04-18 (Snake L2+SVI guard: SVI block in generateComplexL3Block() now suppressed for BOTH isSnakePrimary and isSnakeSecondary — prevents orphaned SVI on primary snake port for unsupported L2+SVI snake config. Supported snake modes: l2-et-trunk (no SVI), l3-et-int, l3-et-sub-int. Files: Code.gs)
  Previous: 2026-04-18 (Snake VRF chain full implementation: (1) getIpPreferences() + saveIpPreferences() now include bridge_mac key (stored in UserProperties). (2) generateSystemBlocks() skips ipv6 unicast-routing for SNAKE_* VRFs (L3 IPv4 chain only). (3) getIpBlock() in generateComplexL3Block(): snake ports use vrf SNAKE_{portName} instead of sheet vrf_ column; IP scheme changed from /32 with sheetIndex→ /24 with .1 as host (both primary and secondary get {base}.oct2.oct3.1/24 in their respective VRFs). (4) L2+SVI secondary duplication fix: mode.startsWith("l2-") SVI branch guarded by !d.isSnakeSecondary. (5) getDeviceConfig() main rows loop now sets details.isSnakePrimary=true when linkInfo.isSelfLoop. (6) collectDeviceData() scans snake_int_ column and adds SNAKE_{port} entries to vrfs Set for L3 snake rows. (7) getDeviceConfig(): new secondary snake port loop generates interface config for snake_int_ ports (with isSnakeSecondary=true); new 080_SNAKE configMap section calls generateSnakeStaticConfig(). (8) New generateSnakeStaticConfig(snakePairs, ipPrefs, bridgeMac): generates static ARP (arp vrf SNAKE_* ip .2 bridge-mac arpa) + ip route chain (primary→own subnet .2; secondary→egress-vrf to next primary; last secondary has no route). Snake test destination: 10.99.99.98/32. (9) Auto Config modal: new Snake Test section with Bridge MAC input (Sidebar.html). Sidebar-js.html: load/save/reset bridge_mac. Files: Code.gs, Sidebar.html, Sidebar-js.html, UserGuide.html)
  Previous: 2026-04-18 (Auto Config modal redesign: width 520px→620px; layout reordered — Example (top, linear rows) → Auto-derived → Formula key → Config inputs; removed 3-col ip-example-grid; new .ip-example-linear/.ip-ex-row/.ip-ex-key/.ip-ex-val/.ip-ex-note/.ip-ex-result CSS classes for linear stacked example; GW IPv6 example row added; configurable formula key box now stands alone without embedded example. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-18 (Snake L3 IP revised: dropped port-number/regex approach; primary=sheetIndex/32, secondary=sheetIndex+1/32. Unique VLAN per snake row (existing audit rule) guarantees unique oct2.oct3, so sheetIndex-based last octet never conflicts across snake pairs. Simpler, consistent with regular P2P convention. Files: Code.gs)
  Previous: 2026-04-18 (Snake L3 IP: switched from sheetIndex-based lastOctet to port-number-derived /32 (IPv4) or /128 (IPv6). Formula: {base}.{vlan÷100}.{vlan%100}.{ethernetNumber}/32. Extracts first digit group from portName via regex. Eliminates same-VRF subnet conflict — two ports on same device sharing a /30 was rejected by EOS; /32 host addresses never conflict. No VRF change needed. Files: Code.gs)
  Previous: 2026-04-18 (Modal white-bg full audit: fixed inline background:var(--bg-card) overrides inside modal body divs — helpModal body, generateAllBody, vlanSummaryBody now use var(--bg-modal, #ffffff); vlanSummaryFilter header row uses var(--bg-secondary); VLAN table row striping from var(--bg-card)→var(--bg-modal, #ffffff); inline font-size:9px→10px in dynamic label chip span and label editor input/button. Files: Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-18 (calculateGlobalTopology() unified snake scan: removed separate STEP D; snake_int_ columns now collected in header mapping (allSnakeIntCols) and snake secondary ports appended to rowNodes in Step A; Step B detects nodeA.dev===nodeB.dev and sets isSelfLoop:true with same guard (skip if non-self-loop entry exists). Single-pass, no second loop over data. Files: Code.gs)
  Previous: 2026-04-18 (Global UI rule compliance: (1) replaced Unicode &#x26A0;&#xFE0F; warning icon with inline SVG in checkTechDirty() banner; (2) added --bg-modal CSS variable (dark: #1e293b in :root, white: #ffffff in [data-theme="light"]) — .modal-std and .modal-floating now use var(--bg-modal, #ffffff) per modal-default-white rule; (3) bumped info-title + section-header from 11px→10px (category marker level); (4) all font-size: 9px→10px (15 instances across badges, tags, role selects, label chips, version lines); (5) font-size: 10.5px→11px (col-mode, 2 instances). Files: Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-04-18 (snake_int added to DEFAULT_SCHEMA_ARRAY after xcvr — snake_int_DevName columns now pre-created alongside all other device columns; addSnakePair() simplified to require the column exists (no more dynamic append); buildModalColumn() and saveEdit() dynamicIds both filter snake_int so it never appears as a user-editable modal field. Files: Code.gs, Sidebar-js.html)
  Previous: 2026-04-18 (Snake test: added guard in globalLinkMap snake registration — skips if primary or secondary port already has a non-self-loop entry, preventing overwrite of regular peer links if a port mistakenly appears in both a regular row and a snake row. Files: Code.gs)
  Previous: 2026-04-18 (Snake test self-loop support: same-device connections (e.g. Leaf1:Et2 ↔ Leaf1:Et3) now stored as snake_int_DevName column appended to sheet on demand; addSnakePair() creates the row; connectExistingToNew() detects devA===devB and routes there; calculateTopologyData() scans snake_int_ columns and sets isSelfLoop:true in globalLinkMap; getTopologyData() flags isSnakePrimary on primary ports, adds snake secondary ports to allCollectedNodes for config gen, pushes type='snake' links to frontend; detectMlagState() skips processP2pNeighbor() for isSelfLoop entries; generateComplexL3Block() suppresses OSPF on snake ports and uses lastOctet=1 (primary) / lastOctet=2 (secondary) for P2P IP assignment; canvas: self-loop exits right from both ports with large arc (loopR=max(80,gapVal*2)), snake layer orange #f97316, lineWidth 3; saveEdit() ADD mode routes devA===devB to addSnakePair(). Files: Code.gs, Sidebar-js.html)
  Previous: 2026-04-18 (UI polish: normalized font scale across both modals — 10px category markers, 11px secondary labels/hints, 12px primary control text; removed 9px entirely; tech-proto-header 9px→10px, tech-proto-row 11px→12px, tech-radio-label 11px→12px; auto-fixed-label 10px→11px, auto-fixed-formula 11px→12px, input-wrapper label 10px→11px, input-wrapper input 11px→12px, ip-example 10px→11px; added hover states: tech-radio-label:hover color-shift, tech-proto-row:hover bg; tech modal 580px→640px; auto config 450px→520px; P2P radio now in same boxed panel as GW; added PROTOCOLS section label above grid; BGP Unnum label expanded to "Unnumbered"; validated error banner line-height added. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-18 (Tech modal redesign: GW on top (chkGwIpv4/chkGwIpv6 independent checkboxes), P2P expanded to 5-mode radio (ipv4/ipv6/ipv6unnum/ipv4ipv6/ipv4ipv6unnum), radio name p2pMode; 3 new flags INT_IPV4/GW_IPV4/GW_IPV6 → 16 total; generateComplexL3Block decoupled into p2pHasIpv4/p2pUseIpv6Unnum/p2pUseIpv6Explicit for P2P and gwHasIpv4/gwHasIpv6 for GW; generateSystemBlocks split into hasP2pIpv6 (Lo0 IPv6, router-id ipv6) + hasAnyIpv6 (VRF ipv6 unicast-routing); generateGlobalBlock gw_ipv6 added to ipv6 unicast-routing gate; generateVxlanBlock addVtepIpv6 now gated on vxlan_ipv6 only (not ||int_ipv6); 4 cross-validation rules in validateTechSettings(): IPv6 GW needs IPv6 routing protocol, VXLAN IPv4 needs IPv4 underlay, VXLAN IPv6 needs IPv6 underlay, EVPN needs BGP; TECH_COMPAT now 5 entries matching 5 P2P modes; P2P_MODE_FLAGS constant maps radio value→{int_ipv4,int_ipv6,int_ipv6_unnum}; error banner #techValidationErrors shown in modal; Save button disabled on validation failure. Files: Code.gs, Sidebar.html, Sidebar-js.html, UserGuide.html)
  Previous: 2026-04-18 (BGP ASN base now configurable: added bgp_asn_base field to getIpPreferences() (default '65000'); generateBGP() and generateBGPEvpnOverlay() accept ipPrefs as new last param, derive asnBase from it; both call sites updated; Auto Config modal gets BGP ASN section with ip_bgp_asn_base input; BGP ASN moved from fixed info box to configurable section with formula example. Files: Code.gs, Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-18 (Auto Config modal: renamed "IP + VNI Configuration" → "Auto Config" (title + toolbar button); added fixed-values info box showing Loopback/Router-ID, BGP ASN, MLAG peer-link formulas with examples; configurable formula key updated with per-device and MLAG GW examples; all input sections (P2P, GW, VNI) unchanged; added .auto-fixed-grid/.auto-fixed-label/.auto-fixed-val/.auto-fixed-formula/.auto-fixed-eg CSS. Files: Sidebar.html, Sidebar-css.html)
  Previous: 2026-04-18 (Tech modal: horizontal 4-column grid layout (560px wide); interface radio controls which protocol options are enabled — incompatible options are dimmed, disabled, and auto-unchecked when interface mode changes; applyTechCompat(mode) added with TECH_COMPAT matrix; radio onchange calls applyTechCompat+checkTechDirty; new CSS: tech-radio-label, tech-proto-col, tech-proto-header, tech-proto-row. Files: Sidebar.html, Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-04-18 (Bug fix: getTopologyData() was passing globalUnderlay string instead of full netSettings object to generateConfig() — all IP family gating in sidebar tooltip configs was broken; replaced globalUnderlay with globalNetSettings = getNetworkSettings(). Also removed stale /* REPLACE IN Code.gs */ comment above generateComplexL3Block(). File: Code.gs)
  Previous: 2026-04-18 (IP family flags — 13 new DocumentProperty flags replace old underlay string + vxlan/evpn booleans. Flags: INT_IPV6, INT_IPV6_UNNUM, BGP_IPV4, BGP_IPV6, BGP_IPV6_UNNUM, BGP_RFC5549, OSPF_IPV4, OSPF_IPV6, OSPF_IPV6_UNNUM, VXLAN_IPV4, VXLAN_IPV6, EVPN_IPV4, EVPN_IPV6. Tech modal rebuilt: 5-section layout (INTERFACE radio group + 4 protocol checkbox groups), 420px width. generateConfig() 5th param renamed underlayProtocol→netSettings (object, still exactly 5 params). generateBGP() now takes settings as 8th param; peer groups gated per flag (pgV4↔bgp_ipv4, pgV6↔bgp_ipv6, pgV6Int↔bgp_rfc5549); MLAG peers bypass IPv4/IPv6 gate (isMlag=true). generateOSPFv3() added (mirrors generateOSPF but uses router ospf3 1). generateComplexL3Block() handles IPv6 unnumbered (ipv6 enable + ipv6 unnumbered Loopback0) vs explicit IPv6, OSPF per-interface gated on ospf_ipv4/ospf_ipv6. generateGlobalBlock/generateSystemBlocks gate IPv6 routing commands on int_ipv6/int_ipv6_unnum. generateMlagConfig/detectMlagState: last param isOspf→settings; MLAG peer-link IPv6 gated on hasMlagIpv6. generateBGPEvpnOverlay: settings 6th param; V6 overlay new. generateVxlanBlock: settings 13th param; Loopback1 IPv6 gated on vxlan_ipv6||int_ipv6. Backward compat: !netSettings defaults to all-IPv6-on; evpn_ipv4 defaults true. Files: Code.gs, Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-18 (OSPF expanded: generateComplexL3Block() now emits ip ospf commands on ALL P2P interface modes (L2+SVI, sub-interfaces, L3 routed) — previously only l3-et-int/l3-po-int; !d.vrf_ guard removed from all branches — VRF interfaces now covered by per-VRF OSPF processes; generateOSPF() refactored to group P2P interfaces by VRF and emit one router ospf 1 block for DEFAULT VRF plus router ospf 1 vrf <name> blocks for each non-default VRF; router-id is shared across all VRF instances. Files: Code.gs)
  Previous: 2026-04-18 (Review fixes: getSheetViewSviFilter() migrates stale 'yes'→'active' in saved SHEET_VIEW_SVI_FILTER on read+persist; applyCustomView() dead ScriptProperty writes removed (SHEET_VIEW_MODE, CUSTOM_VIEW_PREFIXES — never read). File: Code.gs)
  Previous: 2026-04-18 (svi_→svi_vlan_ rename stragglers: SheetAssistPanel.html SVI_FILTER_ITEMS val 'yes'→'active' and initSviFilter default ['yes','blank']→['active','blank']; Sidebar.html dev-vis filter label 'svi'→'svi_vlan'. Files: SheetAssistPanel.html, Sidebar.html)
  Previous: 2026-04-17 (SVI alias: added 'active' to svi_vlan_ standardization aliases in validateAndCleanData() — typing 'active' in the svi_vlan_ sheet column is now accepted and auto-converted to 'all' (same as legacy 'yes'/'true'/'1'/'y'). File: Code.gs)
  Previous: 2026-04-17 (Custom View: getViewPreferences() migrates stale 'svi'→'svi_vlan' in saved CUSTOM_VIEW_PREFS on first read and persists the fix; KEY_MIGRATIONS map is the pattern for future renames. vlan preset now includes 'svi_vlan'. Files: Code.gs, Sidebar-js.html)
  Previous: 2026-04-17 (Fix stale 'yes' references from svi→svi_vlan rename: topoSviFilter default+reset ['yes','blank']→['active','blank'] in Sidebar-js.html (topology SVI filter was broken); Code.gs sheet-view SVI filter check sviVal==='yes'?'yes':'blank' → sviVal?'active':'blank' (always evaluated 'active' rows as 'blank'); Code.gs validateAndCleanData() fallback 'yes'→'all'. Files: Sidebar-js.html, Code.gs)
  Previous: 2026-04-17 (svi_vlan schema options changed from ['all'] to [] — removes dropdown validation so users can type 'all' or VLAN IDs (e.g. '10', '10,20') without the orange warning triangle; _parseSviVlans() already handles all formats correctly. File: Code.gs)
  Previous: 2026-04-17 (Force Sync pre-flight warning badge: schema panel open now fires getOrphanedColumnsInfo(null) in parallel; _updateForceSyncBadge(orphans) shows an amber badge with count on #btnForceSync when orphaned columns exist; badge clears/refreshes after sync completes; race condition closed: deleteOrphans now defaults to false (not null) so server-side ui.alert never fires from sidebar path. Files: Sidebar.html, Sidebar-js.html)
  Previous: 2026-04-17 (Fix push thread timeout → "something went wrong": _abort_stale_sessions was consuming up to TIMEOUT=15s inside _push_config, leaving the join(TIMEOUT+3=18s) budget insufficient for the main push SSH; when join expired before the thread set results[dev], JS got undefined for that device and fell through to the generic "something went wrong" fallback — while the push had actually committed in the background. Fix: (1) _push_config now runs _abort_stale_sessions in a daemon thread capped at min(5,TIMEOUT)s, preventing cleanup from eating the push budget; (2) /pushconfig handler pre-initializes results dict with None for every device before threads start — every device key is always present in the response; (3) join timeout raised to TIMEOUT*2+5=35s; (4) any None remaining after join converted to {"ok":False,"error":"Push timed out — verify config was applied manually"}. VERSION 2.8→2.9. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-17 (Rename svi_ → svi_vlan_: schema key svi→svi_vlan (column svi_<dev>→svi_vlan_<dev>); values yes→all or VLAN IDs (e.g. "10" or "10,20"); blank=no SVI; added _parseSviVlans(sviVlanVal, vlans) helper; standardization migrates legacy yes→all; SVI filter item 'yes'→'active'; isSviOn=non-blank; all 3 config generation sites updated (generateComplexL3Block, AP VLAN vx1, processP2pNeighbor); added test_parseSviVlans (11 cases) to Tests.gs. Files: Code.gs, Sidebar-js.html, Tests.gs, UserGuide.html)
  Previous: 2026-04-17 (UserGuide.html: updated Single-device push step 2 to describe the live phase tracker (Phase 1 ✓ / Phase 2 live retries / Phase 3 verify result); step 3 updated: "already in sync" modal now says auto-closes after 5 s. File: UserGuide.html)
  Previous: 2026-04-17 (Single-device push phase tracker: when Commit is clicked in the push confirm modal, body now switches from diff view to a 3-row phase tracker (Phase 1: Dry-run ✓ / Phase 2: Commit → live status / Phase 3: Verify → result); each row updates in real-time as phases progress; remaining diff shown in pt_detail below rows on partial push; both cancel buttons hidden on commit, Close shown when done. commitPushConfig() in Sidebar-js.html. File: Sidebar-js.html)
  Previous: 2026-04-17 (Already-in-sync modal: empty Phase 1 diff now opens push confirm modal with title "Already in Sync", body "Running config already matches — no push needed.", auto-closes after 2s; previously only showed silent status bar message. _showPushConfirmModal() got alreadyInSync=false 4th param; both pushDeviceConfig() and _pushConfigText() empty-diff branches updated. File: Sidebar-js.html)
  Previous: 2026-04-17 (Idempotent config push: added _prepend_section_cleaners() + _SECTION_CLEANERS to device_bridge.py and Sidebar-js.html embedded template; called in _push_config() before splitting config into lines; prepends "default interface <name>" before each physical/LAG/Loopback/Vxlan/Vlan interface section and "no router bgp/ospf"/"no mlag configuration" before each protocol section — ensures stale BGP neighbors, interface IPs, OSPF config are wiped before new config is applied; Management interfaces excluded to protect SSH; VERSION 2.7→2.8; added TestPrependSectionCleaners (17 cases) to tests/test_device_bridge.py — 50 total pass. Files: device_bridge.py, Sidebar-js.html, tests/test_device_bridge.py, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-17 (Section 24 item 10: fullscreen is not possible in GAS — hard dialog size cap + iframe sandbox blocks Fullscreen API; do not attempt again)
  Previous: 2026-04-17 (SVG icon compliance: replaced all Unicode/emoji icons with CSS shapes or inline SVG — tech-stack connectors (◈→clip-path diamond, └→CSS L-shape), status text emojis removed, minimize/restore/expand buttons now use SVG, ✓ Verified button uses SVG checkmark)
  Previous: 2026-04-17 (Rule 13: added test suite — tests/test_device_bridge.py (34 pytest cases, 6 pure functions) + Tests.gs (GAS harness, 4 pure functions, 34 cases); see TESTING section near end)
  Previous: 2026-04-17 (VERSION 2.6→2.7: SSH_USER and JUMP_HOST now env-var overridable in device_bridge.py; startup banner shows "Overrides:" hint; removed unused shlex from embedded template; Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-17 (lldp_bridge.py: JUMP_HOST restored to "bus-home" default, now overridable via LLDP_JUMP_HOST env var — export LLDP_JUMP_HOST="" to SSH directly without editing file. File: lldp_bridge.py)
  Previous: 2026-04-16 (Quality review fixes: (1) Code.gs getNetworkSettings/saveNetworkSettings: getUserProperties→getDocumentProperties — network settings are shared topology settings, must be per-sheet not per-user; (2) device_bridge.py: removed unused shlex import; (3) lldp_bridge.py: JUMP_HOST hardcoded "bus-home"→"" so fresh installs SSH directly; (4) Sidebar-js.html: 11 inline HTML escape chains replaced with existing escapeHtml() helper. Files: Code.gs, device_bridge.py, lldp_bridge.py, Sidebar-js.html)
  Previous: 2026-04-17 (CLAUDE.md fully populated: added IPv6 intentional non-EOS note, SHEETNAME() must-not-remove note, Save & Sync reminder for buildConditionalRules, check-userguide to slash command list; global ~/claude/CLAUDE.md created with 8 global rules: INSTRUCTIONS discipline, JetBrains Mono, UI/UX symmetry, no modal out of canvas, apply global rules immediately, user-select:none broadly, SVG icons only, user feedback for long actions)
  Previous: 2026-04-17 (Expanded CLAUDE.md with critical design constraints; added /check-constraints command; fixed stale Section 24 item 2 (MLAG STRICT COUNT=4 → MLAG IS EXPLICIT ONLY); fixed stale Section 26 MLAG LOGIC; added PreToolUse hook to settings.json)
  Previous: 2026-04-17 (UserGuide.html updates: (1) Device Manager step — Non-Arista description updated: removed stale name-based auto-detect note; added EOS/Non-EOS toggle badge explanation (click EOS badge to mark as Non-EOS without delete/re-add; IXIA/SPIRENT role auto-sets Non-EOS; changes need Save + Force Sync); (2) Config Push section fully rewritten: split into Single-device push (Phase 1 dry-run → diff modal → Commit → Phase 3 verify) and Push All (no Generate needed; per-device fetch cache-hit=instant; up to 3 concurrent; live status fetching→pushing→verifying; auto-retry up to 3×; already-in-sync skips modal); removed stale "Configs must be generated" requirement. File: UserGuide.html)
  Previous: 2026-04-17 (Version bump 2.5→2.6: _push_config() EOS % error detection shipped without a version bump; also fixed /health docstring in device_bridge.py which still said "2.4" while VERSION was "2.5"; both VERSION constant and /health docstring now "2.6" in device_bridge.py and embedded template in Sidebar-js.html. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-16 (Added .claude/commands/ with 3 slash commands: /version-bump, /check-deploy, /check-userguide; updated FILE STRUCTURE section to document all files including device_bridge.py, lldp_bridge.py, .claude/commands/, and CLAUDE.md)
  Previous: 2026-04-16 (End-to-end bug fix pass — 4 fixes in Sidebar-js.html: (1) selectLine() lines 4852/4861 — allNodesData[u].details.config and allNodesData[v].details.config changed to allNodesData[u]?.details?.config / allNodesData[v]?.details?.config — guards against undefined port key when configLoaded is stale after a silent refresh; (2) setDeviceRole() — added renderManagerList() after checkForChanges() so type badge immediately updates to Non-EOS static badge when role changes to IXIA/SPIRENT (was only updating on next open); (3) addDevicesToList() — added role:'' and mlagPeer:'' to new device object literal to match structure of server-loaded devices from applyDeviceListFromServer() — prevents field-absence inconsistency in checkForChanges() JSON comparison; (4) setDeviceMlagPeer() — added renderManagerList() after checkForChanges() so all peer dropdowns immediately re-render when a peer is selected — prevents stale mutual-exclusion state in other rows. File: Sidebar-js.html)
  Previous: 2026-04-16 (processDeviceConfig() ordering fix: moved allNodesData per-port population loop (was at end of function) to run BEFORE Case 1/Case 2 blocks; previously Case 2 read allNodesData[link.u].details.config before that value was written from new configData, so currentLeftConfig/currentRightConfig was always one fetch behind (undefined on first fetch, stale on subsequent); also added optional chaining on those reads (allNodesData[link.u]?.details?.config) to guard against stale link references. File: Sidebar-js.html)
  Previous: 2026-04-16 (Manual refresh cache clear: fetchData(force=true) now clears configCache={} entirely before the memory cleanup block; this ensures a user-initiated manual refresh always fetches fresh device configs from the server — previously force=true only bypassed the Edit Mode check but left configCache intact, so cached (potentially stale) configs would still be served; silent=true fetches (background re-checks) are unaffected; the full-clear runs before _selectiveConfigInvalidate() so the subsequent invalidation is a no-op on the already-empty cache. File: Sidebar-js.html)
  Previous: 2026-04-16 (Selective configCache invalidation: replaced blanket configCache={} in fetchData() !silent block with per-device fingerprint-based invalidation via _selectiveConfigInvalidate(oldDevs, newDevs); snapshot of allDevicesData is taken before the memory cleanup block so old data is available for comparison; _selectiveConfigInvalidate() runs in the success handler right before the fromCache/autoVerify branch and scheduleBackgroundPrefetch() so it covers both paths; fingerprint = role+mlagPeer+hostname+sorted port signatures (name, connected, sp_mode_, po_, xcvr_speed_, et_speed_, category) joined by null-byte separator; removed devices are evicted immediately; changed/new devices added to invalidate set; MLAG peers of invalidated devices are also invalidated (both old and new peer name); try/catch safety net falls back to configCache={} on unexpected error; configCache={} at lines 3955/4150/4212 (IP save, Force Sync, global push) are preserved as-is — those are correct full-clear events; on no-change topology refresh, 0 devices are invalidated and background prefetch queue is empty so no server calls needed. Function _selectiveConfigInvalidate() added near startBackgroundPrefetch/stopBackgroundPrefetch. File: Sidebar-js.html)
  Previous: 2026-04-16 (pushAllConfigs() Phase 0 fetch redesign: function no longer requires _generateAllConfigs or configCache to be pre-populated — device list built from currentData.nodes filtered to EOS (non-arista excluded); per-device pipeline inside rolling concurrency window (PUSH_CONCURRENCY=3) now has Phase 0: _fetchConfigForPush(name) which returns from configCache instantly (cache hit) or calls google.script.run.getDeviceConfig() (cache miss); row shows "fetching config…" during server fetch, "pushing…" once config is ready; if fetch fails → red row "config fetch failed / <error>"; Phase 2 commit and Phase 3 verify unchanged except variable renamed text→config; _generateAllConfigs is no longer read by pushAllConfigs() at all (Generate button still writes to sheet for reference); "Push All" button now works immediately from topology load — no Generate step required. File: Sidebar-js.html)
  Previous: 2026-04-16 (pushAllConfigs() config source + message sync: (1) pushAllConfigs() no longer requires _generateAllConfigs to be populated — falls back to configCache for EOS devices in currentData.nodes; if user opened any device config modal (which populates configCache), Push All works immediately without clicking Generate; fallback builds [{name, text}] using formatConfigText(configCache[n.id].data); "No configs generated yet" → "No configs available — open a device config or click Generate first"; (2) bulk Phase 2 failure fallback `|| 'error'` → `|| 'something went wrong'`; (3) commitPushConfig() outer catch block status message now includes label prefix — was "Push error: msg — is device_bridge.py running?" missing the device name; now "<b>label</b> — Push error: msg — is device_bridge.py running?" matching all other status messages. File: Sidebar-js.html)
  Previous: 2026-04-16 (Push error message accuracy: (1) Python _push_config() — when SSH runs but diff is empty, now scans output for EOS "%" error lines; if found, raises RuntimeError("Config session error: <line>") so res.ok=false instead of silently returning ok:True,diff:"" which caused "already in sync" to appear when the config session actually failed (e.g., authorization denied, invalid command); (2) Phase 1 JS error message format cleaned up — "Push preview failed (label): msg" replaced with "label: msg" directly; 'unknown error' fallback replaced with 'something went wrong'; (3) Phase 3 commitPushConfig() when vRes.ok=false (verify SSH fails after a successful commit) — titleEl text changed from "Push Failed, check diff" to "Push Succeeded — Verify Failed" (commit was committed, only the post-push verification check failed); body explains "Config was committed but the post-push verification check failed"; applies to both the vRes.ok=false branch and the catch(verifyErr) block; (4) Phase 2 'unknown error' fallback also changed to 'something went wrong'. File: Sidebar-js.html)
  Previous: 2026-04-16 (Non-EOS device guards — config fetch, copy view & push blocked at 7 points: (1) fetchFullConfig() returns early (callback still fires); (2) pushDeviceConfig() returns with "Push not available — X is a non-EOS device"; (3) _pushConfigText() same guard; (4) _setCfgModalPushBtns() hides both Push Interface + Push All buttons when _configViewContext.deviceName is non-EOS; (5) device copy strip — btnPushDevice hidden when non-EOS selected; (6) link split strip — btnPushLeft/btnPushRight hidden per-side when that dev is non-EOS; (7) pushAllConfigs() skips non-EOS devices with "Skipped — non-EOS device" row in gray. File: Sidebar-js.html)
  Previous: 2026-04-16 (commitPushConfig() retry feedback + success msg auto-clear: (1) `lastAttempt` tracked across Phase 2 retry loop; if >1, Phase 3 status shows "(committed after N retries)" and success message includes retry note; (2) "Push successful, no config diff" in #status now clears to getReadyStatus() after 5s — previously stayed forever; modal still auto-closes at 2500ms. File: Sidebar-js.html)
  Previous: 2026-04-16 (Phase 1 empty-diff short-circuit: both pushDeviceConfig() and _pushConfigText() now check if Phase 1 dry-run returns empty diff — if so, skip modal and commit entirely, re-enable button, show "already in sync — no push needed" in status bar; previously the confirm modal opened with Commit still enabled even when there was nothing to push. File: Sidebar-js.html)
  Previous: 2026-04-16 (_autoRunLldp() cabling-change gate: added _lastLldpLinkSig (null=never run) and _getLinkSig() (sorted globalLinks id/u:v joined by |); fetchData success path now only calls _autoRunLldp() when bridge is up AND _getLinkSig()!==_lastLldpLinkSig — skips on config-only fetchData where links unchanged; bridge down→up transition in _updateBridgeToolbarIcon fires unconditionally (fresh connect, always validate); _lastLldpLinkSig is stored at the start of _autoRunLldp() so the snapshot is taken at run time. File: Sidebar-js.html)
  Previous: 2026-04-16 (commitPushConfig() modal-stays-open on all failures: previously all Phase 2 errors (timeout, explicit failure, network/catch) called closePushConfirmModal() — modal closed abruptly; now all three paths show error inline in titleEl/bodyEl with red text + show cancelFtr "Close" button + hide commitBtn, never auto-close; timeout path also runs /devstatus reachability check (same as pushAllConfigs bulk) → "Push timed out — device reachable, verify config manually" or "No response — device unreachable or push timed out"; Phase 3 errors already kept modal open. File: Sidebar-js.html)
  Previous: 2026-04-16 (Push retry added to commitPushConfig(): Phase 2 commit now retries up to 3 times on server-side timeout (res===null), same logic as pushAllConfigs() bulk push; commit button shows "Retry 1/3…" etc during retries; all retries exhausted → "Push timed out: no response after 3 retries — is device reachable?"; this covers ALL single-device and interface pushes since they all go through commitPushConfig(). File: Sidebar-js.html)
  Previous: 2026-04-16 (Status message text updates: session-restore "Restored (Checking for updates...)" → "Topology restored from cache (🚀 Checking for updates..)"; cold-start "🚀 Requesting Topology Data..." → "🚀 Requesting Topology Data.. no local cache". File: Sidebar-js.html)
  Previous: 2026-04-16 (Two status bar fixes: (1) _autoRunLldp() status messages restored — "🔍 Running LLDP check…" (blue) on start and "LLDP check complete" (green, 3s) on completion; safe now because _topologyReady=true guarantees topology is loaded before LLDP fires; (2) session-restore background fetchData changed from fetchData(false,true) to fetchData(false,true,true) — silent=true so "Restored (Checking for updates...)" stays visible instead of being overwritten by "🚀 Requesting Topology Data…". File: Sidebar-js.html)
  Previous: 2026-04-16 (_topologyReady flag added: _autoRunLldp() was firing via _silentBridgeCheck() transition at 400ms using stale session-restored currentData — before fetchData() returned; fixed with _topologyReady=false at fetchData() start, _topologyReady=true in fetchData() success handler (before _silentBridgeCheck + _autoRunLldp calls), and guard at top of _autoRunLldp() — skips with "topology not ready yet" if false; bridge down→up transition via _silentBridgeCheck now only triggers LLDP after topology is fully loaded. File: Sidebar-js.html)
  Previous: 2026-04-16 (_autoRunLldp() status bar messages removed: "🔍 Running LLDP check…" and "LLDP check complete" were racing with topology load status messages (first auto-LLDP fires at ~400ms via _silentBridgeCheck() transition, before fetchData returns); now relies solely on toolbar icon pulse animation (.lldp-scanning) as visual indicator — no status bar text from auto-LLDP. File: Sidebar-js.html)
  Previous: 2026-04-16 (Auto-LLDP scanning indicator: #btnBridgeToolbar gets .lldp-scanning class (CSS pulse animation, 0.9s opacity 1→0.35→1) while _autoRunLldp() fetches; removed on completion or error via _done() helper; CSS @keyframes lldp-scan added near bridge-up/bridge-down states. Files: Sidebar-js.html, Sidebar-css.html)
  Previous: 2026-04-16 (_autoRunLldp() IP resolution: now uses same 3-tier chain as config push — (1) Ma0/Management0 IP from sheet, (2) bridge modal input field, (3) device name as DNS hostname fallback; previously bailed out entirely if no sheet IPs found; now always proceeds and resolves each device independently; logs per-device resolution source. File: Sidebar-js.html)
  Previous: 2026-04-16 (_autoRunLldp() logging + status feedback: function was completely silent; added console.log at entry, at each skip path, and on each fetch outcome; also shows status bar message "🔍 Running LLDP check…" (blue) when fetch starts, then "LLDP check complete" (green, clears after 3s) on completion. File: Sidebar-js.html)
  Previous: 2026-04-16 (BGP EVPN skip message fix: when isBgp+isEvpn+no BGP neighbors, message was "BGP Underlay Skipped: No P2P interfaces configured" despite key being 100_BGP_EVPN; now uses "BGP EVPN Skipped: No P2P interfaces configured" when isEvpn, "BGP Underlay Skipped: No P2P interfaces configured" when !isEvpn. File: Code.gs)
  Previous: 2026-04-16 (VXLAN skip condition split: the single else branch (isVxlan=false) now splits into two — (1) !isVxlanDevice → "No LEAF/SPINE role configured" blockStatus "Disabled (No Role)"; (2) isVxlanDevice but settings.vxlan!=='true' → "Feature Disabled in Tech Settings" blockStatus "Disabled"; previously both cases produced the same "No LEAF/SPINE role configured" message. File: Code.gs)
  Previous: 2026-04-16 (Config skip message rephrase — P2P: 4 "no P2P" messages updated — "Device has no P2P Uplinks (L2 Access Switch)" → "No P2P interfaces configured" for VXLAN, OSPF Underlay, BGP Underlay, BGP EVPN Overlay; removed the "(L2 Access Switch)" inference which was presumptuous. File: Code.gs)
  Previous: 2026-04-16 (Config skip message rephrase: 2 messages in Code.gs updated — VXLAN "Feature Disabled in Tech Settings" → "No LEAF/SPINE role configured" (VXLAN is role-gated); MLAG "No MLAG Pair detected (Standalone Mode)" → "No MLAG role configured (Standalone Mode)"; OSPF and BGP "Feature Disabled in Tech Settings" kept as-is — those are tech-settings-driven, not role-dependent. File: Code.gs)
  Previous: 2026-04-16 (Auto-LLDP on topology load: fetchData() success handler now calls _autoRunLldp() immediately after _silentBridgeCheck() if _bridgeUpState is already true — fixes the case where bridge was already up so the down→up transition never fired again on topology reload; _silentBridgeCheck() still handles the fresh-connect path (bridge unknown→up transition). File: Sidebar-js.html)
  Previous: 2026-04-16 (Push timeout surfacing: /health endpoint in device_bridge.py now returns "timeout": TIMEOUT field alongside status/version/port/method; Sidebar-js.html stores it in module-level _bridgeDeviceTimeout=15 (default); both _checkBridgeHealth() and _silentBridgeCheck() update _bridgeDeviceTimeout from data.timeout on success; bulk push modal header shows "${_bridgeDeviceTimeout}s/attempt · 3 retries" as a dim label next to the Pushing N/total… counter so users know exactly what the per-attempt limit is. Files: device_bridge.py, Sidebar-js.html)
  Previous: 2026-04-16 (Push retry: Phase 2 push loop now retries up to 3 times on server-side timeout (res===null); network/HTTP errors break immediately with no retry; UI shows "retry 1/3…", "retry 2/3…", "retry 3/3…" in the device row during retries; after all attempts exhausted with no result, existing /devstatus reachability check runs and shows "Push timed out — device reachable, verify config manually" or "No response — device unreachable". File: Sidebar-js.html)
  Previous: 2026-04-16 (Non-EOS toggle + Auto-LLDP on bridge connect: (1) Device Manager type badge is now a clickable toggle button for non-IXIA/SPIRENT devices — clicking flips type between EOS↔Non-EOS locally; IXIA/SPIRENT devices show a static badge (role-managed); setDeviceType(index) added near setDeviceRole(); saveRolesAndFinish() now builds manualNonArista list from currentDeviceList (devices toggled non-arista whose role is not IXIA/SPIRENT) and passes it to saveDeviceRoles(rolesMap, manualNonArista); Force Sync still required to collapse/expand columns; _nonAristaSyncNotice covers all non-EOS devices including manually toggled; removed leftover name-based heuristic from currentDeviceList init (name.toLowerCase().includes("non-arista")); (2) Code.gs saveDeviceRoles() now accepts optional manualNonArista param — merges with role-based (IXIA/SPIRENT) list via saveNonAristaList(); existing filter logic replaced with validManual filter; (3) .type-toggle-btn and .type-toggle-nonarista CSS added to Sidebar-css.html (JetBrains Mono, dashed border for EOS state, solid fill for Non-EOS state); (4) Auto-LLDP on fresh bridge connect: _bridgeUpState module-level var added; _updateBridgeToolbarIcon() detects down/idle→up transition (wasUp→_bridgeUpState) and calls _autoRunLldp(); _autoRunLldp() silently fetches /lldp and /devstatus using _getLldpMgmtIps() auto-detected management IPs; processes results via _compareLldpData/_renderLldpResults/applyLldpIndicators/_applyHostnameIndicators/applyDevStatusToCards; skips silently if no topology loaded or no management IPs found; subsequent health checks do not re-trigger (wasUp already true); resets on bridge going down. Files: Sidebar-js.html, Sidebar-css.html, Code.gs, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-16 (Dead code removal — uniform topology view: removed orphaned "MLAG DESTINATION CHECK" block from runValidation() Check Group 1 (Sidebar-js.html); block referenced link.po and link.dst which are never set by processRowLinks() — only {id,u,v,type} are set; the if(link.po) guard was always false so the block never executed; the intent (PO splits to different upstreams) is now covered by Check Group 8 which reads allNodesData[link.u].details.po_ correctly; also removed poDestinationMap={} declaration which was only used by the dead block; no behavior change. File: Sidebar-js.html)
  Previous: 2026-04-16 (mlagPartner→mlagPeer field name fix: backend (Code.gs getTopologyData) sends mlagPeer on node objects; Sidebar-js.html was reading mlagPartner in 3 places — Check Group 3 (line ~2227) and Check Group 8 (2 lines ~2433-2434) — so declared MLAG peers were always seen as undefined and peer-link integrity checks / MLAG-style PO audit never matched declared pairs; fixed all 3 usages to mlagPeer. File: Sidebar-js.html)
  Previous: 2026-04-16 (Check Group 8 bug fix: completed poUpstreamOrigName→poUpstreamOriginals rename — declaration was renamed in previous session but 3 usages still referenced old name (ReferenceError); fixed: (1) assignment now stores both original PO name and original-case upstream device: poUpstreamOriginals[mapKey]={poName:poOrig,upstream:upstreamDev}; (2) retrieval destructures: const{poName:poOrig,upstream:upstreamOrigDev}=poUpstreamOriginals[mapKey]; (3) error message uses upstreamOrigDev instead of lowercase-from-mapKey upstreamDev — device names now appear in correct case. File: Sidebar-js.html)
  Previous: 2026-04-16 (IXIA/SPIRENT Force Sync notice: saveRolesAndFinish() now detects devices with IXIA/SPIRENT role in the roles map before calling saveDeviceRoles(); stores names in module-level _nonAristaSyncNotice (declared alongside _silentBridgeTimer); fetchData success path (fresh-fetch, non-audit branch) checks _nonAristaSyncNotice — if set, shows "ℹ DevName: IXIA/SPIRENT role set — run Force Sync to collapse to 1 column" in statusEl instead of getReadyStatus(), then clears the variable; notice appears exactly once after the topology refresh that follows the save. Files: Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-16 (Non-Arista sync via saveDeviceRoles + audit Check Group 8: (1) saveDeviceRoles() now auto-syncs NON_ARISTA_DEVICES — IXIA/SPIRENT role devices are added automatically; devices whose role changes away from IXIA/SPIRENT are removed (preserving manually-registered non-Arista entries that have no IXIA/SPIRENT role); this lets rebuildSheet() and getExistingDevices() rely solely on getNonAristaList()+colCount===1 without inspecting roles directly; rebuildSheet() reverted — deviceRolesForRebuild and NON_ARISTA_ROLES removed, only registeredNonArista+colCount===1 used; getExistingDevices() reverted — devRoleUpper check removed, only registeredNonArista+colCount===1 used; (2) runValidation() Check Group 8 added (Sidebar-js.html): detects MLAG-style PO pattern (two Arista devices sharing the same PO name toward the same upstream device) where no explicit MLAG peer is declared in Device Manager; builds poUpstreamSources map keyed by "poName|upstreamDev" → Set of PO-owner devices; only considers links where exactly one side owns the PO (the MLAG member side); for each pair of devices sharing a PO toward the same upstream, checks allDevicesData[devA].mlagPartner===devB && allDevicesData[devB].mlagPartner===devA; if not declared → flags "✗ MLAG Peer Required: DevA and DevB both use PoX toward upstream — declare as MLAG peers in Device Manager" on all related links. Files: Code.gs, Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-16 (Role-based non-Arista + MLAG heuristic removal: (1) isAutoNonAristaDevice() deleted — name-pattern matching ("ixia","spirent","traffic","tgen") replaced with role-based detection; rebuildSheet() now reads deviceRolesForRebuild=getDeviceRoles() and NON_ARISTA_ROLES=Set(['IXIA','SPIRENT']) alongside registeredNonArista; type='non-arista' when role is IXIA or SPIRENT; all 3 redundant isAutoNonAristaDevice() call sites in devicesToProcess.forEach and formatting loop simplified to devObj.type==='non-arista'; getExistingDevices() also checks devRoleUpper===IXIA||SPIRENT alongside existing registeredNonArista+colCount===1 checks; (2) calculateGlobalTopology() MLAG structural heuristic removed entirely — Steps 3 (splitter-device MLAG pair detection) and 4 (peer-link detection based on mlagPairs) deleted; directLinksMap, recordDirectLink, poGlobalCount, mlagPairs, and related variables removed; function now returns mlagConfigPorts:new Set(), peerLinkPorts:new Set(), mlagPeerMap:{} — all three are populated exclusively by explicit DEVICE_MLAG_PEERS override in getDeviceConfig() and applyVisualFormatting(); poMap kept (override needs it to identify shared PO bundles between declared peers); Step B simplified to only write globalLinkMap (recordDirectLink calls removed). Files: Code.gs, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-16 (Role-based config bug fixes — 4 bugs: (1) calculateGlobalTopology(): removed "< 4" poGlobalCount threshold from MLAG heuristic — the old check required ≥4 sheet rows sharing a PO name, which broke labs with 1 physical link per MLAG member (poGlobalCount=2); structural check (splitter device with ≥2 distinct peers on same PO) is sufficient; (2) computeVtepNames(): SPINE removed from vtepNames — SPINEs are underlay routers, not VTEPs; adding them to the static flood list produced incorrect VXLAN config; now only LEAF role is added; (3) VXLAN skip message when LEAF/SPINE has P2P but no GW VLANs: replaced hardcoded "Device identified as SPINE" with dynamic "${deviceRole} has no Gateway VLANs configured" and blockStatus "Skipped (No GW VLANs)" — was misleading for LEAF-role devices without Vx1 rows; (4) applyVisualFormatting(): now applies explicit MLAG peer override (same logic as getDeviceConfig()) after calculateGlobalTopology() — previously MLAG cell borders in the sheet ignored DEVICE_MLAG_PEERS and only reflected the heuristic, so explicitly declared pairs were never highlighted. File: Code.gs, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-16 (Explicit MLAG peer field: replaces topology heuristic (PO≥4 count) for MLAG pair declaration. Storage: DEVICE_MLAG_PEERS in DocumentProperties; saveDeviceMlagPeers() enforces bidirectionality server-side (A→B implies B→A automatically). getDeviceConfig(): after calculateGlobalTopology(), if explicitMlagPeers non-empty → topo.mlagPeerMap replaced entirely; topo.peerLinkPorts rebuilt from globalLinkMap (direct links between declared peers); topo.mlagConfigPorts rebuilt from poMap (shared PO bundles toward common upstream). calculateGlobalTopology() now returns poMap in addition to existing fields. getExistingDevices() and getTopologyData() (both push points) include mlagPeer field. Device Manager: MLAG peer dropdown added between Role and Tags (lists all non-arista peers excluding self, — for none); setDeviceMlagPeer()/buildMlagPeersMap() added; save chain extended: saveLabels→saveRoles→saveMlagPeers→handleSuccess. CSS grid updated 10→11 cols (30px 40px 40px 40px 1fr 30px 70px 80px 90px 200px 30px); also updated light-mode variant (9→10 cols); .mlag-peer-select style added (purple focus border to distinguish from role). Sidebar.html header: MLAG column added between Role and Tags. Files: Code.gs, Sidebar-js.html, Sidebar.html, Sidebar-css.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-16 (Remove backward compat — fully role-based EVPN/VXLAN: (1) getDeviceConfig(): removed roleSet var; isEvpnDevice/isVxlanDevice now strict — only LEAF/SPINE=true, all others (HARNESS, unset) =false; comments updated; (2) computeVtepNames(): removed else-if fallback block (hasP2p+gwVlans heuristic); only LEAF/SPINE added to vtepNames; unset-role devices are never VTEPs; (3) generateBGP() isPeerEvpn(): removed pr==='' case — only pr==='LEAF'||'SPINE' returns true; (4) generateBGPEvpnOverlay() isPeerEvpn(): same — removed pr==='' case. Net effect: devices without a role set get no VXLAN, no EVPN, no OVERLAY sessions generated. Files: Code.gs, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-16 (Bug fix: generateBGPEvpnOverlay() now accepts peerRoles param and filters non-EVPN peers via isPeerEvpn() helper — HARNESS peers no longer get EVPN overlay sessions in OSPF+EVPN mode; call site updated to pass peerRoles. Files: Code.gs, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-16 (LABEL_COLORS → ROLE_COLORS: renamed globally in Sidebar-js.html (replace_all); colorSource in topology card rendering simplified to just nodeRole (removed dead label fallback — custom labels never match ROLE_COLORS); search badge fallback for labels simplified to '' for same reason. ROLE_COLORS is now exclusively role-driven; custom labels always render with neutral fallback style. Files: Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-16 (Role/label split: DEVICE_LABEL_OPTIONS=[] (empty) — labels are now always user-defined custom free-text; predefined values moved to role. DEVICE_ROLE_OPTIONS=['LEAF','SPINE','HARNESS','TRANSIT','DUT','IXIA','SPIRENT'] constant added. Role dropdown in Device Manager uses DEVICE_ROLE_OPTIONS (expands from 3 to 7 options). buildLabelEditorHTML() simplified: predefined chip section removed; all current labels shown as custom chips with × delete; no DEVICE_LABEL_OPTIONS.forEach loop. LABEL_COLORS unchanged — still used for role-based topology card coloring. Files: Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-16 (Device role system: separate role field (LEAF/SPINE/HARNESS/none) added alongside labels. Role drives VXLAN/EVPN config generation; labels remain cosmetic. Storage: DEVICE_ROLES in DocumentProperties (getDeviceRoles/saveDeviceRoles). getExistingDevices() returns role per device. getTopologyData() includes role in both device push points (colIndex loop + final nodes map). In getDeviceConfig(): deviceRole extracted from targetDeviceObj; isVxlan/isEvpn now gated — role LEAF/SPINE → VXLAN+EVPN enabled; role HARNESS → both skipped; role unset → legacy heuristic (gwVlans). peerRoles map built from allDevices and passed to generateBGP(). generateBGP() accepts deviceRole+peerRoles; isPeerEvpn() helper — OVERLAY peer groups and loopback neighbor entries only generated toward LEAF/SPINE peers (HARNESS peers get underlay only). generateGlobalBlock(isEvpnDevice) — service routing protocols model multi-agent only added when isEvpnDevice=true (LEAF/SPINE); HARNESS skips it. computeVtepNames() is role-based: LEAF/SPINE added directly; unset role falls back to hasP2p+gwVlans heuristic; HARNESS never a VTEP. Device Manager: role dropdown (—/LEAF/SPINE/HARNESS) added as new column between Type and Labels; grid updated 8-col→9-col in both CSS rules; setDeviceRole(index,value) handler; buildRolesMap(); save chain extended: saveLabels→saveRoles→handleSuccess. Topology display: pillsToShow = [role, ...labels.filter(!=role)] — role first, dedup if same value; card coloring now uses colorSource=role||first-colored-label (single color, no gradient); dataset.roles includes role+labels for search; SVG pills use same getPillsForNode() dedup logic. Search: matches role field in addition to labels; badge prefers role. applyDeviceListFromServer() includes role field. Files: Code.gs, Sidebar-js.html, Sidebar.html, Sidebar-css.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-16 (Stability review fixes — 4 bugs: (1) closePushConfirmModal: Phase 3 hides cancelHdr/cancelFtr via display:none but reset only restored textContent not display — next push modal had invisible Cancel buttons; fix: added style.display='' reset alongside textContent reset for both buttons; (2) _showPushConfirmModal: didn't clear _pushAutoCloseTimeout at entry — if user triggered a second push within 2.5s of a successful push, the old auto-close timer would fire and close the new modal unexpectedly; fix: added clearTimeout(_pushAutoCloseTimeout) at top of function; (3) closeConfigModal: called closePushConfirmModal() which only hides the modal and resets its UI but does NOT clear _pendingPush or restore push button state — if user dismissed config modal while push confirm was showing, next pushDeviceConfig() call hit the _pendingPush guard and silently returned (push button appeared to do nothing); fix: changed call to cancelPushConfig() which also clears _pendingPush and restores button state; (4) pushAllConfigs "pushing…" row update: querySelector('span:last-child') result was not null-checked before .textContent assignment — if DOM structure diverged, would throw TypeError; fix: added null guard. Files: Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-16 (Single device push verify fixes in commitPushConfig: (1) vData[deviceName] || {} fallback now replaced with explicit null check — if server returns no entry for device, vRes is set to {ok:false, error:'No response from bridge for verify step'} instead of {} which previously produced misleading 'unknown error'; (2) vRes.error and verifyErr.message now HTML-escaped into vErrSafe before use in both innerHTML and _setPushStatus — prevents XSS from server-sourced error strings containing < > & characters. Files: Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-16 (Push All verify error details: _setResultRow() now accepts optional 6th param `details` (plain text); when verify fails (vDiff===null), the captured vError is passed as details and rendered as a red #f87171 monospace block below the row — same visual pattern as diff blocks; vError is now captured from all verify failure cases: network error (catch e.message), no server entry ('No response from bridge for verify step'), and vRes.ok===false (vRes.error). Files: Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-16 (Push All row states: added "pushing…" intermediate state — after _semAcquire() grants a slot, the placeholder row's status span is updated to "pushing…" so users see pending→pushing→verifying→result progression. Files: Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-16 (Push All rolling concurrency window: pushAllConfigs() now uses an async semaphore (PUSH_CONCURRENCY=3) so at most 3 device pipelines run simultaneously; _semAcquire()/_semRelease() implemented as a promise queue (no polling); entire per-device body wrapped in try/finally to guarantee _semRelease() even on early return (push-fail path) or thrown exception — without finally, early returns would leak semaphore slots and deadlock remaining devices. Files: Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-16 (Push All Phase 3 + "no response" diagnosis: pushAllConfigs() now has a two-phase push: Phase 2 commits, Phase 3 does dry_run verify to confirm nothing remains; rows show pending→verifying→final; okCount only incremented when Phase 3 returns empty diff; "no result" (server thread timeout, data[name] undefined) now triggers /devstatus connectivity check — if device reachable: "Push timed out — device reachable, verify config manually" (amber), if unreachable: "No response — device unreachable or push timed out" (red); extracted _setResultRow() and _updateSummary() helpers inside pushAllConfigs for DRY row HTML; summary counter updated only after all phases complete per device; row labels: green "Push successful, no config diff" / amber "X lines remaining after push" (with diff) / amber "Pushed — verify check failed" / red error; ipMap now stores plain ip string (not {ip, config}) since config comes from configs.map parameter. File: Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-16 (Stability pass v2.4→2.5 — 15 bugs fixed across device_bridge.py, Sidebar-js.html, Sidebar.html: (1) XSS: device names now HTML-escaped in pushAllConfigs placeholder rows AND result rows before interpolation into innerHTML; (2) _showPushConfirmModal: modal.style.display 'block'→'flex' — push confirm modal now renders correctly as flex column; (3) #cfgPushDivider in Sidebar.html: had duplicate style attributes (first style="height:18px;margin:0 4px" won, second style="display:none" was silently ignored by HTML parser) — merged into single style attribute with display:none included; (4) Phase 3 auto-close race: setTimeout return value now stored in _pushAutoCloseTimeout module var; closePushConfirmModal() calls clearTimeout(_pushAutoCloseTimeout) before hiding modal — manual close before 2.5s no longer leaves a stale timer that re-triggers close; (5) runLldpCheck Promise.all→Promise.allSettled: if /devstatus fails the LLDP results were silently discarded and button stayed disabled; now uses allSettled — LLDP and devstatus are independent, partial results render correctly, per-endpoint error shown in statusEl without blocking the other; (6) closeConfigModal now calls closePushConfirmModal() first — restores push modal button states and cancels auto-close timer if config modal is dismissed while push is in progress; (7) openLldpModal localStorage position validation: JSON.parse wrapped in try-catch, position values validated as non-negative strings before applying — corrupted localStorage no longer throws and positions modal off-screen; (8) _silentBridgeCheck debounce: multiple rapid calls (topology load + toggle + icon click) collapsed into one 400ms-delayed check via _silentBridgeTimer; (9) JUMP_HOST b64 quoting: f"echo {b64}" → f"echo '{b64}'" in both device_bridge.py and Sidebar-js.html template — base64 strings without quotes could be word-split by shell on JUMP_HOST; (10) MAX_WORKERS=10 concurrency cap: added module constant; _run_parallel and /pushconfig handler both use threading.Semaphore(MAX_WORKERS) inside thread fn — limits concurrent SSH to 10 even if 50+ devices; applied to both device_bridge.py and Sidebar-js.html template; (11) _pendingPushAutoClose and _silentBridgeTimer added as module-level vars; VERSION bumped 2.4→2.5 in both device_bridge.py and template. Files: device_bridge.py, Sidebar.html, Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-16 (Timestamps + "one less device" root cause: (1) Generate result header now shows HH:MM:SS timestamp (muted, 11px) alongside the "✓ N device(s) → written to X tab" line; (2) Push complete summary now shows HH:MM:SS timestamp alongside "Push complete: N/total successful"; both use new Date().toTimeString().slice(0,8) for 24h local time; (3) Root cause of "one less device" confirmed: old pushAllConfigs excluded devices with no management IP and no lldpIp_ modal input — bri459 (and any device missing from sheet Ma0 data) fell into noIp and was silently skipped; fixed by hostname fallback in previous commit — device_bridge.py reports per-device SSH failure if hostname doesn't resolve. Files: Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-16 (Generate All push overhaul + Phase 3 title fixes: (1) Push All button always visible: removed display:none from btnPushAllConfigs in Sidebar.html; removed btnPushAllConfigs show/hide logic from openGenerateAllModal() and runGenerateAll() — button is always present, clicking before generate shows "No configs generated yet — click Generate first"; (2) IP resolution fix: pushAllConfigs now resolves IPs same as pushDeviceConfig — management IP from sheet → lldpIp_ modal input → device name as hostname fallback (DNS); removed "No IPs found" hard error — all devices are attempted, device_bridge.py reports per-device connection failures; (3) Incremental per-device push results: replaced single bulk /pushconfig fetch (waited for all devices, showed nothing until all done) with parallel individual fetches (one per device); UI shows pending placeholder for every device immediately; each row updates in place as its response arrives; summary counter increments live "Pushing N/total…" → "Push complete: N/total successful"; body resets to results view on push start so accumulated generate output is replaced; (4) Phase 3 modal title fixes: partial (rem>0) title changed from "Partial Push" → "Partial Push Successful, check diff"; verify-failed (vRes.ok=false) title changed from "Verify Failed" → "Push Failed, check diff"; verify-error (catch) title also "Push Failed, check diff"; (5) Push All result message: "no changes" label changed to "Push successful, no config diff" to match single-device push messaging. Files: Sidebar.html, Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-16 (Three config modal/push fixes: (1) Search box static: #configSearch had width:220px expanding to width:320px on focus with transition:all — the 100px expansion shifted all action buttons right on every focus; fix: removed width:320px from :focus rule, changed transition to border-color+box-shadow only, added flex-shrink:0 to prevent compression — search stays at fixed 220px always; (2) Phase 3 result visible from push modal (outside flow): commitPushConfig was calling closePushConfirmModal() immediately after Phase 2 commit, so Phase 3 ran silently with no visible output (only #status updated, which is hidden behind config modal or not prominent enough); fix: keep push confirm modal open during Phase 3 — hide Commit+Cancel buttons, update title to "Verifying…" and body to "Running Phase 3 verification…"; on Phase 3 success auto-close modal after 2.5s; on partial push render remaining diff inline in modal body and show Close button; on verify error show error inline and show Close button; removed the separate _showPushConfirmModal call for remaining-diff case; (3) Message: "push verified, no diff remaining" changed to "Push successful, no config diff" in both _setPushStatus call and modal body text. Files: Sidebar-css.html, Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-16 (Config modal open-by-default bug: #configModal CSS rule had display:flex which overrode .modal-std's display:none — modal was visible on every page load; fix: removed display and flex-direction from #configModal rule (both already handled by .modal-std and JS), kept only overflow:hidden which was the only property actually needed. Files: Sidebar-css.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-16 (Four fixes — version 2.3→2.4: (1) Config modal header restructure: replaced single flex-wrap row with two dedicated rows — .config-header-top (title + Close button, justify space-between) and .config-header-actions (search + Copy/Push buttons, nowrap overflow-x:auto); removed flex-wrap from .config-header-modern which caused buttons to collide with config content; added #configModal { display:flex; flex-direction:column; overflow:hidden } so only #configBody scrolls; Close button moved from tools row to top row; (2) Phase 3 visibility in config modal: added #cfgPushStatus .cfg-push-status-bar div at bottom of configModal; added _setPushStatus(html) JS helper that writes to both #status (sidebar) and #cfgPushStatus (shown only when configModal is open); commitPushConfig now calls _setPushStatus() for all phase status updates so Phase 3 verifying/verified/partial/error is visible even when config modal is covering the sidebar; closeConfigModal() hides and clears #cfgPushStatus; (3) Pending session accumulation fix (v2.4): 'top' command does not reliably navigate to session root in EOS configure sessions — abort never ran, sessions accumulated as pending until EOS 5-session limit hit; fix: replaced 'top' with 'end' (exit to exec) + re-enter session (puts us at session root, safe because unique session name is never 'completed'); added _ssh_stdin(ip,*cmds) helper that encapsulates jump-host vs direct SSH piped-stdin logic; added _abort_stale_sessions(ip) that runs 'show configuration sessions', parses pending topoassist_* entries, aborts each — called automatically before every _push_config as pre-cleanup; (4) Explicit error for EOS session limit: if 'maximum number of pending sessions' appears in SSH output, raises RuntimeError with actionable message instead of silently returning empty diff; all four changes applied to both device_bridge.py and the embedded template in downloadBridgeScript() in Sidebar-js.html; user must re-download bridge script. Files: device_bridge.py, Sidebar.html, Sidebar-css.html, Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-16 (Version bump 2.2→2.3: reflects dynamic session name + top + terminal length 0 fix shipped in same session; VERSION constant and /health docstring updated in both device_bridge.py and the embedded template in Sidebar-js.html. Files: device_bridge.py, Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-16 (Dynamic session name + top + terminal length 0 fix: root cause of "No Changes — running config already matches" was EOS error "% Cannot enter session topoassist (already completed)" — once a named session is committed, EOS marks it permanently "completed" and rejects any attempt to re-enter it with that exact name; the stale-session cleanup block [abort, re-enter] silently failed because the session name was already used; fix: (1) dynamic session name topoassist_{int(time.time())} — guarantees a fresh unused name on every push call, making cleanup unnecessary; (2) replaced exit_to_root=["end",re-enter] with single "top" command — EOS "top" returns from any sub-mode depth to session root while staying inside the session (unlike "end" which exits to exec mode requiring re-entry, which would fail for the same completed-session reason); (3) added "terminal length 0" as first SSH command — disables --More-- pagination so full diff output reaches SSH stdout without truncation; all three changes applied to both device_bridge.py AND the embedded template in downloadBridgeScript() in Sidebar-js.html; added import time to both; user must re-download the bridge script. Files: device_bridge.py, Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (Device Bridge UI redesign + auto-check default on: (1) auto-check now defaults to enabled — changed all localStorage bridgeEnabled checks from === 'true' to !== 'false' so new users get integration active without manual opt-in (toggleBridgeEnabled, _syncBridgeEnableCheckbox, _silentBridgeCheck, _updateBridgeToolbarIcon); (2) UI redesign — replaced separate lldp-bridge-enable-row (plain toggle) + lldp-audit-row (card with 3 items crammed on one line) with a single .lldp-settings-card containing: auto-check checkbox, .lldp-settings-sep divider, .lldp-audit-header (section label + always-flagged badge), flag-missing checkbox — cleaner visual grouping; section header renamed "Host / IP" → "Devices", hint text shortened; CSS: removed .lldp-audit-row, added .lldp-settings-card/.lldp-settings-sep/.lldp-audit-header. Files: Sidebar.html, Sidebar-css.html, Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (Config diff template fix: the previous "exit×5→end" fix was applied to device_bridge.py but the embedded template in downloadBridgeScript() in Sidebar-js.html was only given a version/docstring bump — the actual exit_to_root line and _extract_session_diff start condition were left as the old broken code; fixed both in Sidebar-js.html template: (1) exit_to_root = ["exit"]*5+[...] → ["end", f"configure session {session}"] so downloaded scripts no longer have SSH sessions closed prematurely; (2) if s.startswith("--- "): → also startswith("+++ ") to handle EOS versions that emit +++ before ---. Files: Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (Version bump 2.1→2.2: bumped VERSION constant and docstring header in both device_bridge.py and the embedded downloadBridgeScript() copy in Sidebar-js.html to reflect the exit×5→end fix and _extract_session_diff hardening shipped in the same session. Files: device_bridge.py, Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (Fix diff always empty — exit×5 closed SSH session + _extract_session_diff hardening: (1) exit_to_root fix — the old exit_to_root = ["exit"]*5 + ["configure session topoassist"] was broken because after the config lines we're in a sub-mode (1-2 exits to session root), then 1 exit to exec mode, then the EXTRA exits attempt to exit exec mode — on EOS SSH piped stdin this closes the SSH connection, so "configure session topoassist", "show session-config diffs", and "abort/commit" never ran; the diff was always empty because it was never captured; fix: exit_to_root = ["end", f"configure session {session}"] — EOS "end" (equivalent of Ctrl-Z) jumps from any sub-mode depth directly to exec mode in ONE command without closing the SSH session and without aborting the staged configure session; then "configure session topoassist" re-enters at session root and show/commit run correctly; (2) _extract_session_diff hardened — previously only triggered capture when a line starts with "--- " (the first diff header line); some EOS versions may output "+++ " before "--- " flipping the order; fix: if not started: trigger on s.startswith("--- ") OR s.startswith("+++ "); stop condition '#commit' in s or '#abort' in s is safe because config lines may contain the word "commit" but the '#' prefix (from EOS echoing the command with shell prompt "Arista(config-s-topoas)#commit") distinguishes it from config content; added comprehensive docstring to the function explaining all design decisions for future regression prevention. Files: device_bridge.py, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (Push modal z-index + config header overflow fixes: (1) #pushConfirmModal z-index raised to 21000 !important — both pushConfirmModal (line 493) and configModal (line 688) are modal-std (z-index:20000), but configModal is later in DOM so it covered the push confirm modal when pushing from view config, making the diff invisible; fix: explicit #pushConfirmModal rule at 21000 ensures it always renders above configModal; (2) config-header-modern height:50px changed to min-height:50px + flex-wrap:wrap + gap:6px + padding:8px 15px — when Push Interface and Push All buttons are shown via _setCfgModalPushBtns('device'), the header's 6+ buttons plus search input overflowed the fixed 50px height causing text to be clipped by modal's overflow:hidden; flex-wrap:wrap lets buttons flow to a second row and min-height lets the header grow. Files: Sidebar-css.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (Device Bridge floating panel + enable state: (1) Bridge button moved from Export group to Modals group (after Device Visibility Panel, before divider) — id="btnBridgeToolbar", onclick="onBridgeIconClick()"; (2) lldpModal class changed modal-std→modal-floating — position fixed top-right (60px/20px), z-index:16000 (same as Dev Vis Panel, below modal-std 20000 so other modals stack above it), draggable by header, no editOverlay backdrop; (3) Modal header: id="lldpModalHeader" for drag handle, minimize btn (id="btnLldpMinimize") before Close; (4) Enable toggle checkbox (id="chkBridgeEnabled") in modal below status bar — persists to localStorage "bridgeEnabled"; (5) _bridgeClickTimer global for debounce; (6) onBridgeIconClick(): single-click >220ms → openLldpModal(), double-click <220ms → toggleBridgeEnabled(); (7) toggleBridgeEnabled(): flips localStorage, syncs checkbox, updates icon, triggers _silentBridgeCheck() if enabling; (8) setBridgeEnabled(val): from checkbox onchange; (9) _syncBridgeEnableCheckbox(): keeps modal checkbox in sync; (10) _silentBridgeCheck(): fetch /health, update toolbar icon only — no modal DOM, no-op if disabled; (11) _updateBridgeToolbarIcon(state): bridge-up (green #22c55e) / bridge-down (yellow #f59e0b) / bridge-disabled (opacity 0.45) on #btnBridgeToolbar; (12) _checkBridgeHealth() now also calls _updateBridgeToolbarIcon; (13) openLldpModal(): no editOverlay, restores drag pos from localStorage "bridgeModalPos", calls _syncBridgeEnableCheckbox; (14) closeLldpModal(): no editOverlay; (15) toggleLldpMinimize(): modal-minimized class hides #lldpModalBody + .modal-actions, btn text −↔□; (16) _initLldpModalDrag(): mousedown→mousemove→mouseup on header, clamps to viewport (respects toolbar), saves pos to localStorage "bridgeModalPos"; called from initApp(); (17) initApp() calls _updateBridgeToolbarIcon('idle') + _silentBridgeCheck() for startup restore; (18) fetchData() success: _silentBridgeCheck() after scheduleBackgroundPrefetch(); (19) CSS: .modal-floating + .modal-minimized rules, #btnBridgeToolbar state classes, .lldp-bridge-enable-row. Files: Sidebar.html, Sidebar-js.html, Sidebar-css.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (Push modal bug fixes: (1) readOnly wording — _showPushConfirmModal now uses context-aware wording when readOnly=true: "N lines still differ after push" instead of "N lines will change", and "Config verified — no diff remaining." instead of "No changes — running config already matches…"; readOnly=false paths unchanged; (2) removed dead code — _showPushSuccessFallback() was unreachable after Phase 3 error paths were updated to show inline error messages; function deleted entirely; (3) race condition guard — both pushDeviceConfig() and _pushConfigText() now return early if _pendingPush is already set (prevents concurrent pushes if user clicks Push Interface + Push All before first dry-run returns, which would overwrite _pendingPush). Files: Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (Verify phase diff/fail display: when post-commit verification (Phase 3 dry_run) shows remaining diff lines (rem>0), now calls _showPushConfirmModal(label+" — remaining diff", vRes.diff, true) to display the actual diff in readOnly modal alongside amber status text; when verify request returns ok:false, shows red "verify failed: <error>" in #status and restores button (was fallback to _showPushSuccessFallback()); when verify fetch throws (bridge disconnected etc.), shows red "verify error: <message>" in #status and restores button (catch variable renamed _ → verifyErr to use message); _showPushSuccessFallback() still exists for Phase 2 (commit) own catch but is no longer called from Phase 3 verify paths. readOnly modal: title="Verification Result", Commit hidden, Cancel→Close, shows remaining diff with colored +/- lines. Files: Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (Post-commit verification: commitPushConfig() now has a 3-phase flow — Phase 2: commit (POST /pushconfig no dry_run); Phase 3: verify (POST /pushconfig dry_run:true with same ip+config immediately after commit); empty diff → btn flashes "✓ Verified" + status shows green "push verified, no diff remaining"; non-empty diff → btn flashes "⚠ Partial" + status shows amber "N lines still differ after push"; if verify call fails (bridge disconnected etc.) falls back to _showPushSuccessFallback() which shows basic "✓ Pushed — N lines changed"; status shows "committed — verifying…" between phases; btn timeout 3500ms for Verified, 4000ms for Partial, 2500ms for fallback. Added _showPushSuccessFallback(btn,st,label,origText,diff) helper for fallback path. Files: Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (Push buttons in config view modal: added btnCfgPushIface ("Push Interface", hidden by default) and btnCfgPushAll ("Push All", hidden by default) to configModal header toolbar alongside existing Copy Interface / Copy All buttons, separated by a divider-v; _configViewContext global {deviceName, portFilter} tracks what the modal is showing; openConfigFromButton() now sets _configViewContext and calls _setCfgModalPushBtns(viewType) — 'interface' view: shows Push Interface only (portFilter set); 'device' view: shows both Push Interface + Push All; _setCfgModalPushBtns() resets button text/disabled state; closeConfigModal() clears _configViewContext and hides/resets both push buttons; cfgModalPushInterface(): if portFilter → calls pushDeviceConfig(deviceName,'btnCfgPushIface',portFilter) for single-interface push; if no portFilter (device view) → calls _extractAllInterfaceBlocks(currentRawConfig) to get all interface sections joined by \n!\n, then _pushConfigText(deviceName,ifConfig,label,'btnCfgPushIface'); cfgModalPushAll(): calls pushDeviceConfig(deviceName,'btnCfgPushAll',null) for full config push; _extractAllInterfaceBlocks(configText): splits by '\n!\n', filters sections whose first line starts with 'interface ', joins result; _pushConfigText(deviceName,config,label,btnId): lower-level push function that takes config text directly (no fetch), resolves IP via _getLldpMgmtIps()+modal input+deviceName fallback, does dry_run preview→_pendingPush→_showPushConfirmModal same as pushDeviceConfig. Files: Sidebar.html, Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (Single-interface push from link strip: when push is clicked from splitLeft/splitRight (link selection), only the selected interface's config block is pushed instead of the full device config; added _pushLeftPort/_pushRightPort globals (null = device-level push, set to port1/port2 on link selection, reset to null on single-port selection); pushLinkSide(side) now passes port to pushDeviceConfig(); pushDeviceConfig(deviceName,btnId,portFilter=null) — when portFilter is set, calls _extractInterfaceBlock(fullConfig,portFilter) to extract just that interface section from the \n!\n-separated formatted config text; if interface not found, shows error in #status; label = portFilter ? "DEVICE:PORT" : "DEVICE" used in modal subtitle and status messages; _pendingPush now includes label field; commitPushConfig() uses label for status display; _extractInterfaceBlock(configText,portName) helper: expands abbrev via _expandIfaceName, splits by "\n!\n", finds section whose first line matches "interface <name>"; device-level push (splitDevice) unchanged — portFilter=null pushes full config. Also: UnknownTty in EOS syslog is expected — SSH piped stdin has no TTY so EOS logs "UnknownTty"; commit still applies correctly. Files: Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (Config push 3 critical fixes: (1) Sub-mode context fix — 'show session-config diffs' and 'commit' only work from session root (config-s-topoas)#, not from inside interface/bgp/ospf sub-modes; after config lines we are still inside the last sub-mode so both commands silently failed; fix: append ["exit"]*5 + ["configure session topoassist"] after config lines — 5 exits bubble up to exec mode (session preserved), re-entering the session lands at session root; then show/commit run correctly; (2) _extract_session_diff stop condition fix — old code checked stripped.lower().startswith("commit") but EOS echoes the commit command as "Prompt(config-s-topoas)#commit" so startswith("commit") never matched; new logic: started=False until s.startswith("--- "), then appends all lines and breaks when "#commit" or "#abort" appears in line (echoed command); (3) IP resolution fallback — pushDeviceConfig() was erroring "no IP" when Device Bridge modal was never opened because lldpIp_* inputs only exist in DOM after _populateLldpDevTable() runs; fix: if no IP found from allNodesData or modal inputs, fall back to deviceName as hostname (same default the bridge modal uses, relies on DNS). All embedded downloadBridgeScript() copies synced. Files: device_bridge.py, Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (Push button disabled state fix: setSplitBtnState() now also queries and controls .push-action button (same as .copy-action and .view-action); previously push button stayed enabled while config was loading (orange/fetching state) because it was not included in the helper; now all three action buttons (Copy/View/Push) are disabled together when actionsDisabled=true and enabled together when config is ready. Files: Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (Config push 3 fixes: (1) Exit-code 255 fix — EOS closes SSH with status=255 after configure session commit even on success (proven by syslog showing CONFIG_SESSION_ENTERED/EXITED/CONFIG_I then xinetd EXIT status=255); removed incorrect "if proc.returncode not in (0,None): raise CalledProcessError" from _push_config; now captures both out AND err, only raises if output is empty AND stderr contains auth/connectivity keywords ("permission denied","authentication failed","no route to host","connection refused","connection timed out","host key verification failed"); (2) dry_run support — _push_config(ip,config_text,dry_run=False): when dry_run=True sends "abort" instead of "commit" so config session is discarded after showing diff (preview without applying); /pushconfig handler reads dry_run=bool(body.get("dry_run",False)) and passes to _push_config; returns {"ok":True,"diff":...,"dry_run":bool}; CalledProcessError no longer raised (removed from handler too); (3) Push confirmation modal — pushDeviceConfig() is now 2-phase: Phase 1 calls /pushconfig with dry_run:true → _showPushConfirmModal(deviceName,diff) renders diff pre (+ lines green #4ade80, - lines red #f87171, @ lines muted) in #pushConfirmModal; button stays disabled while modal open; Phase 2 commitPushConfig() calls /pushconfig without dry_run → closes modal, flashes "✓ Pushed", updates #status; cancelPushConfig() re-enables button and closes modal; _pendingPush global holds {deviceName,ip,config,btnId,origText}; closePushConfirmModal() resets #btnCommitPush; #pushConfirmModal added to Sidebar.html (460px, z-index:2000, header+subtitle+body+Cancel+Commit footer); (4) Orange button fix — removed color:#f59e0b and amber hover from .push-action in Sidebar-css.html; button now inherits white from .split-action; all embedded downloadBridgeScript() copies synced. Files: device_bridge.py, Sidebar.html, Sidebar-js.html, Sidebar-css.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (Config Push relocated: removed Push Config btn + #cfgPushResults from Device Bridge modal; Push moved to copy strip — added push-action btn (amber #f59e0b, id=btnPushDevice/btnPushLeft/btnPushRight) after View in splitDevice/splitLeft/splitRight; added #btnPushAllConfigs (btn-mono hidden) to Generate All modal footer alongside Copy All; Sidebar-js: added _pushLeftDev/_pushRightDev globals; set _pushLeftDev=dev1/_pushRightDev=dev2 at link selection, _pushLeftDev=device/_pushRightDev=null at single port; removed btnPushConfig enable/disable from _checkBridgeHealth; replaced pushConfig()+_renderPushResults() with: pushDeviceConfig(deviceName,btnId) — IP via _getLldpMgmtIps()+lldpIp_ fallback, fetches config via _fetchConfigForPush, POST /pushconfig, shows "✓ Pushed / N lines changed" in #status, 2.5s button flash; pushLinkSide(side) — reads _pushLeftDev/_pushRightDev; pushAllConfigs() — reads window._generateAllConfigs, builds ipMap via _getLldpMgmtIps()+lldpIp_ fallback, POST /pushconfig, calls _renderGenAllPushResults; _renderGenAllPushResults(body,data,noIp) — appends inline HTML to generateAllBody with per-device ok/fail + diff pre block + skipped-no-IP list; runGenerateAll() stores window._generateAllConfigs=configs and shows #btnPushAllConfigs; openGenerateAllModal() and runGenerateAll() both hide #btnPushAllConfigs on reset; Sidebar-css: .push-action color #f59e0b + amber hover. Files: Sidebar.html, Sidebar-js.html, Sidebar-css.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (Discovered links feature: _compareLldpData now returns {results, orphans} instead of plain array; orphan detection iterates lldpMap after comparison loop — filters to entries where both localDev and remDev are in allDevicesData, deduplicates A↔B/B↔A via sorted pairKey, skips pairs already in globalLinks (normalized via _normLldpIface); runLldpCheck destructures {results, orphans} and passes orphans to _renderLldpResults; _renderLldpResults(results, orphans) adds discovered section — if orphans.length>0 shows #lldpDiscovered with checkbox rows (.lldp-disc-row label elements, class lldp-disc-check, data-pair=encodeURIComponent(JSON.stringify(o))), badge (purple #ede9fe/#6d28d9); addSelectedDiscovered() reads .lldp-disc-check:checked, JSON.parse(decodeURIComponent(cb.dataset.pair)), calls google.script.run.addLinkPair(localDev,localIf,remDev,remIf,{},{}) for each, _onAddDiscoveredDone() calls fetchData(true) on success or alert on errors; Sidebar.html: #lldpDiscovered div (hidden) with #lldpDiscHdr, #lldpDiscoveredList.lldp-disc-list, #btnAddDiscovered btn-mono inside #lldpResults; Sidebar-css.html: .lldp-disc-list flex col gap 2px, .lldp-disc-row purple bg #f5f3ff hover #ede9fe JetBrains 11px, .lldp-disc-check accent #7c3aed, .lldp-disc-actions flex justify-end. Files: Sidebar-js.html, Sidebar.html, Sidebar-css.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (Config Push feature: device_bridge.py v2.1 — added shlex import; _extract_session_diff() module-level helper parses 'show session-config diffs' from SSH stdout (captures lines between '---'/'+++' and 'commit'); _eapi_push(ip, *cmds) sends eAPI runCmds with format="text" for configure session; _push_config(ip, config_text) — SSH: pipe cmds via stdin (direct: Popen+communicate, jump: echo shlex.quote | inner ssh), eAPI: _eapi_push + results[-2] is diff; rest/gnmi raise NotImplementedError; /pushconfig handler in do_POST uses inline parallel runner where ip_map values are {ip, config} dicts (not plain IPs); Sidebar.html: added #cfgPushResults div (hidden) + #cfgPushBadge + #cfgPushResultList inside modal body; added id="btnPushConfig" btn-mono button (disabled until bridge healthy) in modal footer; Sidebar-js.html: _fetchConfigForPush(deviceName) Promise wrapper around google.script.run.getDeviceConfig (uses configCache first); async pushConfig() — Phase 1 fetch configs via Promise.all(_fetchConfigForPush), Phase 2 POST /pushconfig, calls _renderPushResults; _renderPushResults(data,ipEntries) builds per-device rows with dot+name+status+colored diff pre block (diff-add/diff-del/diff-hdr spans), badge "N/M pushed", "No changes" msg when diff empty; _checkBridgeHealth() now also enables/disables #btnPushConfig; embedded downloadBridgeScript() synced: VERSION 2.1, shlex import, /pushconfig docstring, do_POST updated, _extract_session_diff + _eapi_push + _push_config added; Sidebar-css.html: .cfg-push-result-row, .cfg-push-row-ok/fail, .cfg-push-dev-name, .cfg-push-status, .cfg-push-diff-wrap, .cfg-push-diff (monospace pre, max-height 150px), .diff-add/#16a34a, .diff-del/#dc2626, .diff-hdr/muted, .cfg-push-no-change; UserGuide.html: added Config Push h3 subsection with requirements step-box, 5-step workflow, transport note. Files: device_bridge.py, Sidebar.html, Sidebar-js.html, Sidebar-css.html, UserGuide.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (UserGuide.html Device Bridge section: added Device Bridge toolbar entry (shield+lines SVG icon) to toolbar legend table; added complete <h2>Device Bridge</h2> section before Tips & Notes covering — Setup (Step 1 Download Bridge btn, Step 2 configure METHOD/JUMP_HOST/SSH_USER and run python3 device_bridge.py), Transport options info-box (ssh/eapi/rest/gnmi details), Running a Check (spinner + live phase updates), Device List status dots legend (SSH dot green/red/gray + hostname dot green/amber), amber hostname mismatch warning box, LLDP Results legend table (5 statuses: match/mismatch/partial/unknown/skip), Topology Card Indicators (EOS version line, LL dot legend, St dot with op-status legend), LLDP Audit Integration. Files: UserGuide.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (Hostname verification: device_bridge.py /devstatus now returns "hostname" from show version | json; .lldp-dev-row grid updated 1fr 140px 18px → 1fr 140px 18px 18px; lldpHn_${safeId} span added to each modal device row (.lldp-hn-dot); _applyHostnameIndicators(devices) compares _normSysName(ds.hostname) vs _normSysName(n.id) — hn-match (green) or hn-mismatch (amber+glow) with descriptive title; called after devStatusMap set in Promise.all handler; hostname dot reset on each run alongside SSH dot; CSS: .lldp-hn-dot, .hn-match, .hn-mismatch added; embedded downloadBridgeScript() copy synced with hostname field. Files: device_bridge.py, Sidebar-js.html, Sidebar-css.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 ("Run Checks" rename + SSH parallel fix: button renamed "Run Checks" (was "Run Check") in Sidebar.html; btn.textContent reset updated from 'Run LLDP Check' → 'Run Checks' (both success + error paths); error message updated lldp_bridge.py → device_bridge.py; device_bridge.py _ssh_cmds now runs multiple commands in parallel threads (fetch() closure per cmd, join(timeout=TIMEOUT)) instead of sequential for-loop — halves /devstatus time for SSH since show version + show interfaces status now run simultaneously per device; single-command case skips thread overhead; embedded copy in downloadBridgeScript() also updated. Files: Sidebar.html, Sidebar-js.html, device_bridge.py, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (Device Bridge progress feedback + audit row rewording: (1) .lldp-run-status gets CSS spinner (::before rotating border-top-color circle, @keyframes lldp-spin 0.75s); runLldpCheck() shows "Querying N devices…" with live phase updates — individual .then() on fetchLldp/fetchDev set lldpDone/devDone flags and call updateStatus() which appends "(LLDP done)" / "(LLDP + status done)" as each parallel fetch completes; (2) audit row: added .lldp-audit-section span ("LLDP Audit" uppercase label); "Mismatch → Audit always" → "Mismatches always flagged"; "Audit missing neighbors" → "Flag missing neighbors"; tooltip text clarified. CSS: .lldp-audit-section added. Files: Sidebar.html, Sidebar-js.html, Sidebar-css.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (device_bridge.py v2.0: replaces lldp_bridge.py; adds 4-transport support — METHOD="ssh"|"eapi"|"rest"|"gnmi"; ssh+eapi run EOS show cmds returning identical JSON; rest+gnmi use OpenConfig YANG with _oc_lldp_to_eos/_oc_version/_oc_platform/_oc_iface_status normalizers; _gnmi_val() extracts value from pygnmi response; _ssh_cmds/_eapi_cmds/_rest_get/_gnmi_get transports; _run_cmds() dispatcher for ssh/eapi; gNMI requires pip install pygnmi (conditional import HAS_GNMI); startup banner shows active transport + transport-specific config; /health now includes "method" field; lldp_bridge.py superseded but kept for reference; downloadBridgeScript() updated to embed device_bridge.py v2.0 and download as device_bridge.py; bridge cmd in modal updated to python3 device_bridge.py. Files: device_bridge.py (new), Sidebar.html, Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (Device Bridge rename + download: "LLDP Verification" modal/toolbar button renamed to "Device Bridge" (subtitle updated); Run button renamed "Run Check"; downloadBridgeScript() added — embeds full lldp_bridge.py as JS template literal, triggers browser download via Blob URL; "Download Bridge" btn-mono added to modal footer; lldp_bridge.py docstring + startup banner updated to "Device Bridge"; IMPORTANT: when lldp_bridge.py changes, update the embedded copy in downloadBridgeScript() in Sidebar-js.html. Files: Sidebar.html, Sidebar-js.html, lldp_bridge.py, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (lldp_bridge.py: _check_devstatus also strips " (engineering build)" and any parenthetical suffix via .split(" (")[0] — EOS engineering builds append this to the JSON version field. File: lldp_bridge.py, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (lldp_bridge.py: VERSION="1.2" constant; startup banner now shows "TopoAssist LLDP Bridge  v1.2"; /health uses VERSION constant; _check_devstatus strips "Software image version: " prefix from EOS version string (engineering builds return full text in JSON version field). File: lldp_bridge.py, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (LLDP bridge /devstatus + device card enhancements: lldp_bridge.py v1.2 adds /devstatus endpoint (show version | json + show interfaces status | json per device); runLldpCheck() now uses Promise.all([/lldp, /devstatus]) to fetch both in parallel; devStatusMap global stores per-device {ok,version,platform,interfaces}; applyDevStatusToCards() updates .dev-version-line divs with "EOS 4.x.y · 7050SX3" and overrides port St dots with live op-status (op-up=green, op-down=amber, op-errdisabled=red, op-disabled=gray); _expandIfaceName() maps Et1→Ethernet1, Ma1→Management1, Po1→Port-Channel1; .dev-version-line appended to node-header after labelRow; applyDevStatusToCards() called after render() + after LLDP check; CSS: .dev-version-line (9px mono, rgba white 0.7), .dot.op-up/op-down/op-errdisabled/op-disabled with !important overrides. Files: lldp_bridge.py, Sidebar-js.html, Sidebar-css.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (LLDP missing-dot color + label rename: "Flag missing neighbors" → "Audit missing neighbors"; missing dot is gray (lldp-port-missing-quiet, #94a3b8) when lldpAuditFlagMissing=false, red (lldp-port-missing) when true; _lldpEffectiveClass(status) helper computes the correct CSS class for missing; applyLldpIndicators() and port row render both use it; toggleLldpAuditMissing() now calls applyLldpIndicators() so dots recolor immediately on toggle without needing re-run. Files: Sidebar.html, Sidebar-css.html, Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (LLDP audit integration: mismatches always flagged in Audit; toggle "Flag missing neighbors" in LLDP modal (checkbox #chkLldpAuditMissing, persisted in localStorage lldpAuditFlagMissing, default off) for early-stage labs where dark ports are expected; toggleLldpAuditMissing() persists + calls runValidation() if audit is live; runValidation() has new LLDP section at end — iterates lldpPortStatus, calls flagErrorOnPort() for mismatch always + missing when flag enabled; after LLDP check completes runValidation() is re-called if isValidationMode; openLldpModal() syncs checkbox from lldpAuditFlagMissing; CSS: lldp-audit-row, lldp-audit-always (green badge), lldp-chk-label added. Files: Sidebar.html, Sidebar-css.html, Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (LLDP topology card indicators: new LL column added to port rows (before St) showing per-port LLDP status — green=match, yellow=mismatch, red=no neighbor; grid-template-columns updated 62px 1fr 30px 40px 16px 25px; col-lldp span + lldp-port-dot CSS (.lldp-port-match/.mismatch/.missing); global lldpPortStatus {} populated by _compareLldpData (reset on each check; okU/okV ports get match/mismatch/missing based on uMatch/vMatch + lldpU/lldpV null check); applyLldpIndicators() clears all .col-lldp then re-applies dots by row ID; called from runLldpCheck() after _renderLldpResults and from render() after applyTopoSviFilter so indicators survive topology redraws. Files: Sidebar-css.html, Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (lldp_bridge.py: added JUMP_HOST config var (default "bus-home"); when set, each device query runs ssh {JUMP_HOST} 'sshpass -p "" ssh -o StrictHostKeyChecking=no -o ConnectTimeout=8 admin@{ip} "show lldp neighbors detail | json"' so the bridge runs on local Mac (no sshpass needed locally, no tunnel needed) and the jump server does the lab SSH; when JUMP_HOST="" falls back to direct sshpass mode; startup banner now shows Jump host + Mode lines. File: lldp_bridge.py, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (LLDP SSH + matching fixes: (1) lldp_bridge.py: replaced BatchMode=yes with sshpass -p "" (PasswordAuthentication=yes, PubkeyAuthentication=no) — lab admin accounts use empty keyboard-interactive password which BatchMode blocks; (2) Sidebar-js.html _compareLldpData: added _normSysName() helper that strips FQDN domain and takes last dash-segment (DUT-L-Leaf1-VG-cal423.sjc.aristanetworks.com → cal423) for correct device matching; (3) fixed interfaceId quotes bug — switched to interfaceId_v2 (clean) with interfaceId fallback + quote-strip: interfaceId = '"Ethernet1"' (embedded quotes) caused _normLldpIface() to not match. Files: lldp_bridge.py, Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (_compareLldpData: fixed misleading "not queried" message in partial case — now distinguishes "SSH failed" (device in lldpData but ok:false) from "not queried" (device absent from lldpData); uses label() helper per-device and joins both into msg. File: Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (LLDP modal: host field now defaults to device name (DNS) instead of blank when no Ma0 IP found — autoIps[n.id] || n.id; section header "Management IPs" → "Host / IP" with hint "defaults to device name via DNS"; placeholder "mgmt IP" → "host / IP". Files: Sidebar.html, Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (lldp_bridge.py: removed redundant f-string prefix on "ConnectTimeout=8" — cosmetic only. File: lldp_bridge.py)
  Previous: 2026-04-15 (LLDP Verification switched to local bridge approach: lldp_bridge.py (stdlib-only Python HTTP server on localhost:8765, SSH admin/no-password, parallel threads); modal updated — protocol radio removed, bridge status bar added (dot+msg+Recheck button + lldp-bridge-cmd yellow box showing start command); LLDP_BRIDGE_URL const added; openLldpModal now disables Run btn until bridge health confirmed; _checkBridgeHealth() fetches /health, enables/disables Run; runLldpCheck now uses fetch() to bridge /lldp POST instead of google.script.run; dot title changed 'eAPI OK'→'SSH OK'; on fetch error calls _checkBridgeHealth() to recheck bridge; CSS: lldp-bridge-bar, lldp-bridge-msg, lldp-recheck-btn, lldp-bridge-cmd, lldp-cmd-code added. Files: lldp_bridge.py (new), Sidebar.html, Sidebar-js.html, Sidebar-css.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (LLDP Verification: new toolbar button (shield+lines SVG, Export group, after cabling) + #lldpModal (480px); server fn checkLldpNeighbors(ipMap,protocol) uses UrlFetchApp eAPI POST (admin: basic auth, validateHttpsCertificates:false); client: openLldpModal/closeLldpModal, _getLldpMgmtIps (auto-detect Ma0 IPs from allNodesData), _populateLldpDevTable, runLldpCheck, _normLldpIface, _compareLldpData (Et-only, statuses: match/mismatch/partial/unknown/skip), _renderLldpResults (SVG icons, sorted by severity); lldpModal added to modalOrder+closeFuncs+closeAllModals; CSS section 13 added. Files: Code.gs, Sidebar.html, Sidebar-js.html, Sidebar-css.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (Global config modal: openGlobalCfgModal() now resets btn.textContent to 'Save & Apply' on open — after a successful save the text was stuck as 'Saving...' on subsequent opens because the success handler only disabled the button before closing the modal. File: Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (Cabling sort fix: _sortCableGroups now anchors on devA (left side, always alphabetically-first per buildCableGroups) instead of min(sheetIndexMap[devA],devB) — the min-sheet-index anchor was wrong when sheet order differs from alphabetical order (e.g. SPINE1 before LEAF1 in sheet but LEAF1 is devA). Sort is now: primary = sheetIndexMap[devA], secondary = phyA natural sort. File: Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (Cabling sort: added _sortCableGroups(cableGroups, sheetIndexMap) helper used by copyCablingCsv and copyCablingMarkdown; replaces inline sort. Two-level sort: primary = min(sheetIndexMap[devA], sheetIndexMap[devB]) — same anchor device ordering as before; secondary = anchor device's phyPort natural sort (Et1<Et2<Et10) so cables for the same device are in interface order. Also sorts g.links within each breakout group by portA then portB (natural) so lane sub-ports appear in order (Et14/1, Et14/2, Et14/3, Et14/4). File: Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (Topology col vis removed from devVisPanel entirely — topology has no sheet columns, col vis belongs in Sheet View only: HTML (devVisColSelRow + devVisColList divs removed from Sidebar.html), JS (removed initTopoColVis, renderTopoColList, toggleTopoColSel, _updateTopoColSelBtns, selectAllTopoColVis, showSelectedTopoColVis, hideSelectedTopoColVis, _toggleTopoColVis, _commitTopoColVis + state vars topoColSchema/topoVisibleColKeys/selectedInColVis/topoColVisInited + initTopoColVis call in filterDevVisList), CSS (removed dev-vis-col-sel-row, dev-vis-col-list, dev-vis-col-item, dev-vis-col-chk rules), Code.gs (removed getColVisData()). OSPF VRF fixes: generateComplexL3Block() now guards ip ospf network/area/bfd per-interface commands with !d.vrf_ — VRF interfaces belong in BGP VRF sections, not global OSPF underlay; generateOSPF() now filters p2pInterfaces with !p.vrf — VRF P2P interfaces excluded from router ospf 1 no passive-interface list. Files: Code.gs, Sidebar.html, Sidebar-js.html, Sidebar-css.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-15 (Removed scrollToDeviceColumn() from Code.gs entirely; removed goToSheet() from Sidebar-js.html; removed → Sheet button from splitDevice toolbar; selectDevice() has no sheet navigation side-effect. Files: Sidebar.html, Sidebar-js.html, Code.gs, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-14 (IP & VNI Config: loading text "Loading IP Defaults..." → "Loading IP & VNI Config..."; example rebuilt as structured grid (.ip-example-grid 3-col: key/IPv4/IPv6) with title line "eg. VLAN 1500 → oct2=15, oct3=0" and rows P2P/GW/VNI; added .ip-example-title, .ip-example-grid, .ip-example-key, .ip-example-val CSS. Files: Sidebar-js.html, Sidebar.html, Sidebar-css.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-14 (IP & VNI Config modal: IPv4/IPv6 rows per section wrapped in .ip-section-rows (gap:4px) so they cluster tightly; VLAN 1500 example added in info-box (octs 15.0 · P2P 200.15.0.id · GW 100.15.0.1 · VNI 11500); .ip-example CSS added; .ip-legend-grid margin-bottom removed, row-gap 12px→8px. Files: Sidebar.html, Sidebar-css.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-14 (IP & VNI Config modal UX: button tooltip "IP Configuration" → "IP & VNI Config"; modal-large → modal-medium (760px→450px); ip-modal-body padding 25px→16px, gap 20px→12px. Files: Sidebar.html, Sidebar-css.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-14 (Rename: "VESPA VLANs" → "AP VLANs" throughout — vx1 rows now called AP VLAN GW; blockStatus "VESPA GW SVI" → "AP VLAN SVI"; vespaVlans variable → apVlans; comments updated. Files: Code.gs, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-14 (VESPA+MLAG supported; flood list fix: computeVtepNames() pre-scans all devices (hasP2p && gwVlans.size>0) and passes vtepNames Set to generateVxlanBlock; flood list now filters to VTEP leaves only, skipping spines. MLAG peer Vx1 bug fixed: collectDeviceData() now also applies explicit Vx1 gwVlans handling for MLAG peer rows (ip_type_ not reliable for VESPA). Files: Code.gs, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-14 (VESPA GW multi-row + VRF + VNI base: multiple vx1 rows now pre-collected before main interface loop (one per VRF/VLAN group); vrf_ included in SVI block; VNI base configurable via getIpPreferences (vni_base, default 10000) — applies to ALL VNIs (VTEP + VESPA); generateVxlanBlock gets vniBase param; IP Config modal renamed to "IP + VNI Configuration" with VNI Base field (ip_vni_base). Files: Code.gs, Sidebar.html, Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-14 (VESPA GW support: int_=vx1 rows skip physical interface config; vlan_ feeds allVlans+gwVlans unconditionally; if svi_=yes generates ANYCAST_GW SVIs with ip address virtual per VLAN using same oct2/oct3 IP formula; stored as configMap["050_Vx1"]. Files: Code.gs, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-14 (cabling physical rule: splitter cables are always 1-to-N (any QSFP/OSFP variant or OSFP): one aggregate connector → 2, 4, or 8 individual SFP ends. Two QSFP/OSFP connectors facing each other = ONE physical cable (laser lane split), never dual harnesses. Scenario A (both sides slash + aggX) = single cable → one CSV/Markdown row. Updated comments in copyCablingCsv/copyCablingMarkdown and INSTRUCTIONS. File: Sidebar-js.html)
  Previous: 2026-04-14 (buildCableGroups: when both sides have slash (isBreakoutA && isBreakoutB), check xcvr_speed_ aggregate format (Xg-N) to identify which side is the QSFP aggregate — if A has 100g-4 and B has 25g, group by phyPortA <-> devB (drops B port) so all 4 SFP lanes land in one group → correctly routes to Scenario C. Without this, both sides having slash always used phyPortA <-> phyPortB causing split into multiple Scenario A groups. File: Sidebar-js.html)
  Previous: 2026-04-14 (buildCableGroups: non-Arista devices (IXIA/Spirent) suppressed from breakout detection — their slash port notation (Et5/22) is card/port style, not QSFP-DD lane. isBreakoutA/B now false when device type is non-arista; phyPort kept as full port name (not stripped to Et5/1). Fixes IXIA Et5/22+Et5/26 being grouped as Scenario C mixed breakout. File: Sidebar-js.html)
  Previous: 2026-04-14 (Renamed int_speed → et_speed (label "Et Speed") everywhere: DEFAULT_SCHEMA_ARRAY, config gen, PortMapping lookup, audit Check E, search text, buildCableGroups fallback. Avoids int_ prefix collision with the interface key int_. Files: Code.gs, Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-14 (getExistingDevices: now reads row 1 alongside row 2; only treats int_X as a device column when row 1 at that column is non-empty — row 1 is populated only at the first column of each device group. Fixes et_speed_gd435 being misread as device "speed_gd435", which caused auditSchemaVsSheet to generate 13 phantom expected columns per device. File: Code.gs)
  Previous: 2026-04-14 (xcvr_speed dropdown: same options as et_speed — all standard speed values. File: Code.gs)
  Previous: 2026-04-14 (Orphan safety: removed migrateSpeedColumns(). getOrphanedColumnsInfo(optionalTargetSchema) added to Code.gs — returns [{key,hasData}] for each col in sheet but not in schema. syncSchemaPreservingOrder now accepts deleteOrphans param (null=ui.alert fallback, true=delete, false=keep). triggerSyncOnly() split into two steps: first calls getOrphanedColumnsInfo(null), shows client-side confirm() with data-loss warning if any orphan hasData, then calls _runForceSyncNow() with explicit deleteOrphans flag. saveSchemaChanges() structural path similarly calls getOrphanedColumnsInfo(cleanList) before sync. Files: Code.gs, Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-14 (Schema: renamed speed → et_speed (label "Int Speed") in DEFAULT_SCHEMA_ARRAY; added xcvr_speed (label "Xcvr Speed", no dropdown options) immediately after. Config gen: d.speed_ → d.et_speed_. PortMapping lookup: 'speed_'+refDev → 'et_speed_'+refDev. migrateSpeedColumns() added (renames speed_ headers → et_speed_, inserts empty xcvr_speed_ column next to it, resets SCHEMA_CONFIG_ARRAY DocumentProperty, shows alert). buildCableGroups: speedA/B now xcvr_speed_ || et_speed_ fallback. Audit Check E: d.details.et_speed_. Search text: d.et_speed_ + d.xcvr_speed_. downloadCablingReport isMixedBreakout: restored speed-first detection (100g-4 vs 25g, or ratio-match); port-slash as fallback; also refines leftIsQSFP from speed when aggregate speed is available. Tooltip: rows where both sides are blank (raw1 && raw2 both empty) are now skipped — suppresses noise for unconfigured attributes like xcvr_speed. Files: Code.gs, Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-14 (UI: removed SHOW ALL / HIDE ALL buttons from both Sheet View (SheetAssistPanel.html) and Topology View (Sidebar.html devVisPanel). Sheet View: Devices section now has only SEL ALL + SHOW + HIDE; Columns section replaced action-row with SEL ALL + SHOW + HIDE (selectedCols Set, toggleColSel, updateColSelBtns, selectAllCols, showSelectedCols, hideSelectedCols, _applyColView; renderColItems now adds .sel-chk checkbox; is-selected CSS on col-item). Topology View: added COLS sel-row (devVisColSelRow) + devVisColList grid below device sel-row; functions: initTopoColVis (lazy server call to getColVisData), renderTopoColList, toggleTopoColSel, _updateTopoColSelBtns, selectAllTopoColVis, showSelectedTopoColVis, hideSelectedTopoColVis, _toggleTopoColVis (individual click), _commitTopoColVis; CSS: dev-vis-col-sel-row, dev-vis-col-list (4-col grid), dev-vis-col-item, dev-vis-col-chk. Code.gs: added getColVisData() returning {schema, visibleKeys}. Files: Code.gs, Sidebar.html, Sidebar-js.html, Sidebar-css.html, SheetAssistPanel.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-14 (Cabling report mixed-breakout: replaced all speed-based isMixedBreakout detection with port-slash only (leftHasSlash !== rightHasSlash); sQSFPDisplay now multiplies per-lane speed by count (25g×4=100g-4) instead of just appending -count; output: "QSFP-DD 100g-4 ------ 25g N*SFP25(Breakout)". File: Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-14 (Cabling report mixed-breakout fix: downloadCablingReport() isMixedBreakout detection now falls back to port-slash notation (isBreakoutA/B) when speed alone can't differentiate (e.g. 25g↔25g on QSFP-DD breakout). Without this, Scenario A's withLanes() computed 25g×4=100g-4 and displayed fabricated speed. Now correctly routes to Scenario C (MIXED BREAKOUT) showing "QSFP-DD 25g-4 ------ 25g N*SFP25(Breakout)". File: Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-14 (Audit Check E speed mismatch: removed breakout-aware logic (_hasLane/_perLane/effA/effB); now does direct normSpA !== normSpB comparison; 'auto' and blank still skipped. File: Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-14 (Sheet view intModeFilter bug: getSheetViewIntModeFilter() returned legacy default ['l2','l3','et','po'] (group-key shorthand) but refreshSheetRowVisibility() does exact includes(sp) against full values like 'l3-et-int' — always false → P2P rows with non-blank sp_mode_ hidden. Fix: default now [] (skip filter = show all); also migrates any stored legacy format to []. File: Code.gs, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-14 (Cabling grouping fix: buildCableGroups() now uses port-slash notation (isBreakoutA/isBreakoutB) instead of hasLaneSpeed() to determine groupKey — so QSFP-DD breakout sub-ports (Et14/1-4) are grouped correctly even when both sides use 25g speed (not 100g-4); removed local hasLaneSpeed const from buildCableGroups. _breakoutSides() fallback fixed: was {a:isBreakoutA,b:isBreakoutB} (wrong semantics — slash side is QSFP aggregate, NOT the lane endpoint), now {a:false,b:true} when isBreakoutA&&!isBreakoutB and vice versa, else {a:false,b:false}. No change to audit speed check (25g↔25g already passes). File: Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-14 (Audit Check E — Speed Mismatch: added to Check Group 1 (link-based) inside isNonEosLink guard; compares speed_ on both sides of a link; skips blank and 'auto'; breakout-aware — 100g-4 ↔ 25g valid (100/4=25), 100g-4 ↔ 100g-4 compared as-is; msg: "Speed Mismatch: DevA (spA) ↔ DevB (spB)"; file: Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-14 (Config modal copy rework: rolled back body user-select:none (it blocked execCommand copy in WebKit); replaced single ⊞ Copy button with "Copy Interface" (btnCfgCopyInterface) + "Copy All" (btnCfgCopyAll) — neither closes the modal; copyAllConfig() copies currentRawConfig; copyInterfaceConfig() extracts interface blocks only; _copyToClipboard() helper uses navigator.clipboard.writeText with execCommand fallback (inline user-select:text on temp textarea); removed copyFilteredConfig(). Files: Sidebar-css.html, Sidebar.html, Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-14 (Multi-select batch hide/show: added to both Sheet View (SheetAssistPanel.html) and Topology View (devVisPanel). Sheet: checkbox per device-item, sel-row with SEL ALL/SHOW SEL/HIDE SEL buttons, selectedDevices Set, toggleDevSel/updateSelBtns/selectAllVis/hideSelected/showSelected(), batchToggleSheetDevices() in Code.gs; selection cleared on SHOW ALL/HIDE ALL. Topology: selectedInDevVis Set, dev-vis-chk checkbox + item click-to-select, dev-vis-sel-row in Sidebar.html, toggleDevVisSelect/_updateDevVisSelUI/selectAllDevVis/showSelectedDevVis/hideSelectedDevVis() in Sidebar-js.html, selection CSS in Sidebar-css.html; buttons show count "(N)"; selection cleared on SHOW ALL/HIDE ALL. Files: Code.gs, SheetAssistPanel.html, Sidebar.html, Sidebar-js.html, Sidebar-css.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-14 (Global config modal editor: always light mode — textarea bg #ffffff, text #1e293b, wrapper bg #f8fafc, border #cbd5e1; hardcoded (not CSS vars) so it stays readable in dark theme too; focus border #3b82f6 via onfocus/onblur; file: Sidebar.html)
  Previous: 2026-04-14 (Removed legacy showGlobalConfigEditor: deleted function from Code.gs and removed '▩ Global Config' menu item from Topology Manager submenu; Topology Manager now only has Device Manager; Global Config is exclusively via sidebar toolbar button; files: Code.gs, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-14 (Global Config sidebar modal: moved editor from standalone GAS showModalDialog into sidebar modal #globalCfgModal so save can clear configCache + refresh active device; toolbar button added (lines/magnifier SVG) between IP Config and VLAN Summary; openGlobalCfgModal()/closeGlobalCfgModal()/saveGlobalCfgFromSidebar() added to Sidebar-js.html; globalCfgModal registered in modalOrder, closeFuncs, and closeAllModals(); files: Sidebar.html, Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-13 (no-cursor rule: body gets user-select:none globally so no blinking text cursor appears on any non-input UI element (numbers, badges, labels, handles, rows, boxes); input/textarea/select override with user-select:text so editing still works; .schema-key-locked changed from user-select:all to user-select:none — it's locked, not copyable; files: Sidebar-css.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-13 (saveSchemaChanges() timeout scoping fix: syncPollTimer hoisted to function scope (let, null-initialized) so 30s schemaLockTimer timeout can clearInterval(syncPollTimer)+hideGlobalLoading() if server hangs mid-sync; previously the timeout could not reach syncPollTimer declared as const inside inner callback, leaving overlay stuck; file: Sidebar-js.html)
  Previous: 2026-04-13 (HD icons: replaced Unicode chars ◎ ▤ ✦ on btnTopoSimple/Linear/Flexible with crisp SVG icons (concentric circles, 3-line list, move/cross); removed font-size:18px from .icon-toggle CSS — was only needed to size the Unicode chars; files: Sidebar.html, Sidebar-css.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-13 (Progress feedback for Save & Sync and Device Save: saveSchemaChanges() now shows global loading overlay + polls SYNC_STATUS when isStructuralChange; saveDeviceChanges() now shows overlay + polls SYNC_STATUS during processDeviceBatch slow path; both follow same pattern as triggerSyncOnly(); files: Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-13 (Force Sync progress feedback: rebuildSheet now writes SYNC_STATUS to CacheService at each of 4 stages; syncSchemaPreservingOrder sets initial status; getSyncStatus() added; triggerSyncOnly() shows global loading overlay and polls getSyncStatus() every 600ms so user sees live phase updates instead of a frozen button; files: Code.gs, Sidebar-js.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-13 (Adaptive column widths: replaced COL_WIDTH_BY_ATTR fixed map with header-length formula — width=max(80,ceil(hdr.length*7)+16); auto-adapts to device name length; file: Code.gs, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-13 (Data safety fixes: restoreFromSnapshot() now backs up live sheet before clear + rolls back on error; resetToNewProject() now validates deviceName before clear + rolls back on error; both follow backup→validate→commit→rollback pattern; file: Code.gs, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-13 (rebuildSheet ghost-note fix: stage 4 clear chain now includes .clearNote() so the _sys_ note does not persist on the shifted device column after re-insert; without this, Force Sync left the "Managed by TopoAssist" note on both col A and col B; file: Code.gs)
  Previous: 2026-04-13 (Canvas boundary fixes: new global rule — no modal/HTML object may escape viewport; Sidebar-css.html: #tooltip min-width changed to min(750px,calc(100vw-20px)); .toolbar overflow visible→hidden + max-width added; .topo-legend + #vlan-legend + .dev-vis-panel max-width clamped to calc(100vw-40px); .search-container max-width:100%; .search-autocomplete-list overflow-x:hidden + max-width:100%; #configSearch:focus max-width:calc(100%-64px); .modal-large/.modal-medium use min() for width + max-width:calc(100vw-20px); INSTRUCTIONS Section 3B updated with canvas boundary rule; files: Sidebar-css.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-13 (End-to-end review fixes: BUG: detectMlagState() now takes isOspf as 9th param (was ReferenceError for MLAG devices); BUG: normalizeSpeed 800g condition fixed ("400"→"800"); BUG: clearVlanTrace() de-duplicated — vlanColorCache reset and updateVlanLegend({}) merged into single definition, stale wrapper removed; FONT: added font-family "JetBrains Mono" to .icon-btn, .icon-toggle, .sort-toggle, .btn-std, .mono-btn, .chk-label, .btn-clear, .input-wrapper label; DEAD: removed first escapeHtml definition, finalizeSaveUI, resetBtn, finalizeSave; IMP: removed unconditional duplicate setupCanvasInteraction() call in render(); files: Code.gs, Sidebar-js.html, Sidebar-css.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-12 (Global UI rules: added Section 3B documenting monospace and UI/UX symmetry requirements; fixed Sidebar-css.html: added font-family: "JetBrains Mono" to .btn-icon-edit and .btn-icon-delete; fixed .split-action padding from 4px 11px to 4px 10px to match .split-label; file: Sidebar-css.html, INSTRUCTIONS_topoassist.txt)
  Previous: 2026-04-12 (Force sync col A fix: post-rebuild block called only applyCustomView() — ensureDummyColumn() was never reached so _sys_ was permanently lost after each sync; added refreshSheetRowVisibility() call BEFORE applyCustomView() so _sys_ is inserted at col 1 first and device columns shift to col 2+ before column visibility is applied; also corrected INSTRUCTIONS.txt which wrongly stated applyCustomView re-creates _sys_; file: Code.gs, INSTRUCTIONS.txt) | (Config source-of-truth fix: getTopologyData() was calling generateConfig() without underlayProtocol, so OSPF per-interface commands were missing from initial tooltip config; added globalUnderlay = getNetworkSettings().underlay and passed it to generateConfig() call in getTopologyData(); file: Code.gs, INSTRUCTIONS.txt) | (Tooltip-config sync: processDeviceConfig() now strips "^\d+_" prefix from configMap keys before canonicalizeInterface() so "050_Et1"→"Et1" matches allNodesData["SPINE1:Et1"]; previously the prefix caused no match and tooltip right pane always showed "Loading..."; documented configMap key format and source-of-truth contract in Section 17; file: Sidebar-js.html, INSTRUCTIONS.txt) | (Column widths: replaced autoResizeColumns with fixed COL_WIDTH_BY_ATTR map (90–120px per attribute type) in rebuildSheet STAGE 6 — eliminates uneven auto-resize results; file: Code.gs) | Filter logic fixes: (1) initIpFilter fallback was [] instead of ['p2p','gw','blank'] — inconsistent with initIntModeFilter/initSviFilter, fixed; (2) removed dead validLinks ip_type OR-filter block in redrawLinks() — applyTopoIpTypeFilter() already adds hidden-ip-type to non-matching ports before posCache is built, so non-matching ports are absent from posCache and their links never enter validLinks; the OR block could never filter anything and its comment was misleading; cable exclusion is now handled solely by posCache; files: SheetAssistPanel.html, Sidebar-js.html, INSTRUCTIONS.txt) | (Bug fix: SheetAssistPanel filterAll(), applyBulkOptimistic(), and is-toggling cleanup in doShowAll/doHideAll all used querySelectorAll('.device-item') which grabbed ip_type/int_mode/svi filter items — search input hid filter buttons, bulk ops animated them; scoped all four to '#devList .device-item'; file: SheetAssistPanel.html, INSTRUCTIONS.txt) | (Added int_mode + svi filters to both Sheet View and Topology View: int_mode groups l2/l3/et/po matching sp_mode_ prefix/substring; svi matches svi_='yes' or blank; both are AND conditions alongside ip_type filter; filter rows use 4-column grid layout side-by-side; Sheet View stores in UserProperties SHEET_VIEW_INT_MODE_FILTER + SHEET_VIEW_SVI_FILTER, restored on load; Topology View in-memory topoIntModeFilter/topoSviFilter with hidden-int-mode/hidden-svi classes on port rows; dialog height 450→550; files: Code.gs, SheetAssistPanel.html, Sidebar.html, Sidebar-css.html, Sidebar-js.html, INSTRUCTIONS.txt) | (Topology View ip_type filter now hides port rows in device node boxes: applyTopoIpTypeFilter() adds/removes hidden-ip-type class on each port row based on ip_type_ vs topoIpFilter; hidden-ip-type added to isHidden check in redrawLinks() posCache building so excluded ports also exclude their cables; applyTopoIpTypeFilter() called from toggleTopoIpFilter() and after topology render; .port-row.hidden-ip-type { display: none !important } added to CSS; files: Sidebar-js.html, Sidebar-css.html, INSTRUCTIONS.txt) | (Sheet View ip_type filter redesigned to match Topology View list-style UI: replaced .filter-row toggle buttons with .ip-section/.ip-section-header/.ip-section-list; 3 real items p2p/gw/blank + dummy using .device-item/.eye-icon/.item-name/.is-hidden/.ip-filter-item classes; renderIpFilterList() replaces renderIpFilterButtons(); initIpFilter and toggleIpFilter call renderIpFilterList; toggleMinimize hides ipFilterSection; files: SheetAssistPanel.html, INSTRUCTIONS.txt) | (Topology View ip_type filter redesigned from 3 toggle buttons to device-list-style UI: devVisIpSection with devVisIpHeader label + devVisIpList; 3 real items p2p/gw/blank + 1 dummy item for symmetry; items use same dev-vis-item/dev-vis-eye/dev-vis-name/is-hidden classes as device list; renderIpTypeList() re-renders on every toggle and on fetchData reset; old button CSS/HTML removed; files: Sidebar.html, Sidebar-css.html, Sidebar-js.html, INSTRUCTIONS.txt) | (ip_type filter bug fixes: Sheet View now only checks ip_type_ for devices with non-blank int_ on each row — prevents blank-device columns from bypassing the filter; devIntCol/devIpTypeCol maps replace visIpTypeCols; Topology View now filters validLinks directly instead of rebuilding from globalLinks, and uses OR logic on both endpoints for mixed p2p/gw links; files: Code.gs, Sidebar-js.html, INSTRUCTIONS.txt) | (SheetAssistPanel layout made flex-based: html/body height:100% + display:flex column, panel-body uses flex:1/min-height:0 instead of fixed px height — future rows auto-adjust without manual height changes; setHeight removed from load(); Code.gs setHeight 420→450; files: Code.gs, SheetAssistPanel.html, INSTRUCTIONS.txt) | (Sheet View dialog height increased 420→450, panel-body height reduced 340→320 to accommodate ip_type filter row; files: Code.gs, SheetAssistPanel.html, INSTRUCTIONS.txt) | (ip_type filter default changed to all 3 selected (p2p/gw/blank); getSheetViewIpFilter returns [p2p,gw,blank] when unset; topoIpFilter initialized to all 3; initApp syncs button states; fetchData restores all 3 active on reload; files: Code.gs, Sidebar-js.html, INSTRUCTIONS.txt) | (ip_type filter added to Sheet View (SHEET_VIEW_IP_FILTER, UserProperties, getSheetViewIpFilter/saveSheetViewIpFilter/applySheetIpFilter, AND condition in refreshSheetRowVisibility) and Topology View (topoIpFilter in-memory, toggleTopoIpFilter, validLinks filtered in redrawLinks, reset on fetchData); 3 toggle buttons P2P/GW/BLANK in each panel; files: Code.gs, SheetAssistPanel.html, Sidebar.html, Sidebar-css.html, Sidebar-js.html, INSTRUCTIONS.txt) | (fix stale comment in onOpenInstallable: "Sheet Manager" → "Sheet View"; files: Code.gs, INSTRUCTIONS.txt) | (final naming: Sheet View panel/menu/storage, Topology View devVisPanel; files: Code.gs, SheetAssistPanel.html, Sidebar.html, INSTRUCTIONS.txt) | (renamed mechanisms: Sheet View→Sheet View (SHEET_VIEW_HIDDEN, getSheetViewHidden/saveSheetViewHidden), Topology View→Topology View (in-memory), Device Manager unchanged; files: Code.gs, INSTRUCTIONS.txt) | (three visibility mechanisms separated: Sheet View (SHEET_VIEW_HIDDEN, UserProperties, getSheetViewHidden/saveSheetViewHidden) fully independent from Device Manager (TOPOLOGY_HIDDEN_DEVICES, ScriptProperties, getHiddenDevices/saveDeviceVisibility); Topology View already in-memory; all Sheet View paths (toggleSheetDevice, showAll/hideAllSheetDevices, viewShowAll, applyCustomView, refreshSheetRowVisibility, getSheetDeviceList) switched to new Sheet View functions; stale comment in Sidebar-js.html fixed; files: Code.gs, Sidebar-js.html, INSTRUCTIONS.txt) | (saveDeviceVisibility: removed DATA_VERSION bump — hiding/showing device columns is a view preference, not a data change; bumping caused topology window poll to fire fetchData on every device hide/show from Sheet View; files: Code.gs, INSTRUCTIONS.txt) | (auto-open Sheet View moved from simple onOpen() to installable onOpenInstallable(): simple trigger runs with AuthMode.LIMITED and cannot call showModelessDialog(); onOpenInstallable() runs with full auth and is installed by ensureOnOpenTrigger(), called from showTopologyWindow() + showSheetAssistPanel() on first menu use; files: Code.gs, INSTRUCTIONS.txt) | (applyCustomView: replaced blanket showColumns(1,lastCol) reset + individual hideColumns loop with single-pass batched run algorithm — eliminates all-columns-expand flash; removed showSheetAssistPanel() call from applyCustomView — panel callers keep panel open, menu callers call it explicitly; files: Code.gs, INSTRUCTIONS.txt) | (onOpen auto-open: calls showSheetAssistPanel() on spreadsheet open if getViewPreferences().length < getSchemaConfig().length — active column filter detected; wrapped in try/catch; files: Code.gs, INSTRUCTIONS.txt) | (toggleSheetDevice unified with column toggle: now calls applyCustomView(getViewPreferences()) instead of targeted _applyDeviceCols(); _applyDeviceCols removed — it still had stale INT_ONLY/INT_AND_PO mode strings; files: Code.gs, INSTRUCTIONS.txt) | (setColumnVisibility() removed: all Sheet View menu items now call applyCustomView() with matching key arrays; showAllSheetDevices/hideAllSheetDevices/rebuildSheet also switched to applyCustomView(getViewPreferences()); files: Code.gs, INSTRUCTIONS.txt) | (global search: search bar moved above panel-body, filters both device-items and col-items; filterAll() replaces filterDevVisList(); col-items get data-label attr; toggleMinimize hides searchRow; files: SheetAssistPanel.html, INSTRUCTIONS.txt) | (Columns panel: replaced preset row with SHOW ALL/HIDE ALL matching device panel; removed loadViewPreset() and all preset CSS; files: SheetAssistPanel.html, INSTRUCTIONS.txt) | (Columns panel: replaced checkboxes+Apply button with eye icons + auto-apply on toggle; presets and All/None also auto-apply; col-item visual mirrors device-item (shared CSS classes, same toggling animation); visibleKeys[] tracks state; files: SheetAssistPanel.html, INSTRUCTIONS.txt) | (SheetAssistPanel merged with Custom View: two-column layout 490×420px — left=Devices, right=Columns with presets+checkboxes+Apply; menu "Custom View..." now calls showSheetAssistPanel(); files: SheetAssistPanel.html, Code.gs, INSTRUCTIONS.txt) | (trigger install moved from onOpen to menu-triggered functions: onOpen runs with AuthMode.LIMITED and cannot call ScriptApp.newTrigger() — ensureOnChangeTrigger() call removed from onOpen, added to showTopologyWindow() and showSheetAssistPanel() which run with full auth; trigger now installs automatically on first panel open; files: Code.gs, INSTRUCTIONS.txt) | (onChange col-A guard: handler named onStructuralChange(), ensureOnChangeTrigger() installs it as installable trigger; files: Code.gs, INSTRUCTIONS.txt) | (_sys_ col A gets gray background #e2e8f0: ensureDummyColumn() now always applies setBackground on rows 1..lastRow of col 1 after insert-or-detect, so the column is visually marked as reserved on every call; files: Code.gs, INSTRUCTIONS.txt) | (onChange() added: structural guard for col A deletion — fires on REMOVE_COLUMN, calls ensureDummyColumn() to re-insert _sys_ col, shows restore toast; onEdit cannot catch column deletion so onChange is required; files: Code.gs, INSTRUCTIONS.txt) | (stale __VIS__ comment fixed at Code.gs:3042 — onEdit col A guard comment now correctly says _sys_ instead of __VIS__; all __VIS__ references in comments have been updated to _sys_ throughout Code.gs) | (sheet hide does not affect topology: removed d.isVisible filter from getTopologyData() — rawDevices now uses allDevices directly so all devices always appear in topology regardless of sheet column visibility; hiddenDevicesList/hiddenDeviceSet also removed as they were only used for the filter; sheet visibility and topology visibility are now fully independent; files: Code.gs) | (__VIS__ fixed at column A: ensureDummyColumn() now always targets col 1 — checks if col 1 header is __VIS__ (re-hides and returns 1), otherwise calls sheet.insertColumnBefore(1) to place it there; device columns shift to col 2+ which aligns with setColumnVisibility's existing "skip col 1" guard so all device columns can now be hidden without complications; refreshSheetRowVisibility() restructured to call ensureDummyColumn() FIRST before reading headers/data, then re-reads lastCol and headers after the possible insert to ensure correct column indices; lastCol guard changed from < 1 to < 2 (need __VIS__ + at least one device column); files: Code.gs) | (holistic position preservation fix: saveCurrentStateToStorage() was replacing the entire localStorage state with only currently-visible nodes — hidden devices' positions were lost and not restored when the device was shown again; fix: load existing state from localStorage first, then merge/overwrite only currently-visible nodes, so hidden devices' positions survive across hide/restore cycles; files: Sidebar-js.html) | (setColumnVisibility ALL-mode __VIS__ fix: sheet.showColumns(1,lastCol) reset in setColumnVisibility un-hid __VIS__ in ALL mode since the ALL branch returned early without going through the batch-hide loop (non-ALL modes already hid __VIS__ correctly via batch loop since __VIS__ header matches no view prefix); fix: added refreshSheetRowVisibility() before the ALL-mode return so __VIS__ is re-hidden after viewShowAll(); files: Code.gs) | (ensureDummyColumn() always calls sheet.hideColumns() even when __VIS__ column already exists — prevents user from seeing it if manually unhidden; no force sync needed in refreshSheetRowVisibility since saveDeviceVisibility() already bumps DATA_VERSION; files: Code.gs) | (DevVisPanel.html renamed to SheetAssistPanel.html — old SheetAssistPanel.html (dead code) replaced; showDeviceVisPanel() renamed to showSheetAssistPanel() with 'DevVisPanel'→'SheetAssistPanel' filename ref and dialog title 'Device Visibility'→'Sheet View'; all 6 call sites in Code.gs updated via replace_all; files: Code.gs, SheetAssistPanel.html) | (Sheet View panel rename + NAV + blank row hiding: "⊞ Sheet Viewant" menu item removed from Sheet View submenu in Code.gs (SheetAssistPanel.html kept but no longer in menu); Device Visibility floating panel in sidebar renamed "Sheet View" (Sidebar.html dev-vis-title span); populateDevVisPanel() in Sidebar-js.html now separates eye-click (toggleDeviceVis — hides/shows node in topology canvas) from name-click (navigateDeviceInSheet — calls scrollToDeviceColumn via google.script.run to jump the sheet to that device's int_ column); added navigateDeviceInSheet(deviceId) function in Sidebar-js.html; .dev-vis-name in Sidebar-css.html gains cursor:pointer + hover underline + primary color (including for .is-hidden state); Code.gs: ensureDummyColumn(sheet) finds or creates a hidden __VIS__ column in row 2 of SHEET_DATA (appends to last column if missing, always hides it); refreshSheetRowVisibility() reads hiddenDevs set, finds visible int_ columns (excluding dummy col), scans all data rows (3+) to determine which have any non-empty value in a visible int_ column, writes "•" or "" to the dummy column, then batch-shows/hides rows via _batchRowOp(); _batchRowOp(sheet, rowNums, hide) batches consecutive row numbers for efficient hideRows()/showRows() calls; toggleSheetDevice() now calls refreshSheetRowVisibility() after column ops; showAllSheetDevices() and hideAllSheetDevices() each call refreshSheetRowVisibility() after setColumnVisibility(); files: Code.gs, Sidebar.html, Sidebar-js.html, Sidebar-css.html) | (Sheet Viewant UX overhaul: panel resized 340×560→360×580 (showSheetAssistUi in Code.gs); searchConnectionRows now returns speed/mode/vlan fields (looks up speed_refDev, sp_mode_refDev, vlan_refDev columns for devA||devB reference device); SheetAssistPanel.html fully rewritten — theme unified to all-dark (#0f172a body, #1e293b cards, #0a0f1a tab bar — was mixed light/dark); NAV: hidden devices shown at opacity 0.45 with "hidden" badge, Enter key jumps to first non-hidden result in filtered list; FILL: ⇄ swap button between device rows swaps both device select+port input, live onchange warning when DevA===DevB, "— unchanged —"→"— optional —" on speed/mode dropdowns, section labels added for required vs optional fields; BULK: filter input above row list (filters by device name substring), live "N / M selected" counter, selected rows use .selected CSS class (blue tint #0c2340), STAMP success flashes rows green for 900ms via .flash-ok class; STAT: table rows clickable (onclick→scrollToDeviceColumn), mini 28×3px utilization bar showing connected/total ratio in green alongside CONN count, amber ● dot before device name when orphan>0, totals footer row; FIND: Enter key on either device select triggers search, results show speed/mode/vlan as colored chips (.tag-speed blue, .tag-mode purple, .tag-vlan teal), "— any — matches any device" hint text; all tabs have .tab-hint description text explaining the advantage over direct sheet editing; files: Code.gs, SheetAssistPanel.html, INSTRUCTIONS.txt) | (Code.gs bulkUpdateRows: pre-resolves valid attrEntries outside the row loop to avoid redundant filtering per row; collects writes into a sorted array then applies them so adjacent columns can be reasoned about in order; updated counter now only increments when at least one write occurred (was always incrementing even for empty rows); files: Code.gs) | (SheetAssistPanel.html bug fix: toggleBulkRow double-flip — clicking a checkbox directly caused the browser to natively toggle checked, then onclick called toggleBulkRow(this.parentElement) which flipped it back, netting no change; fixed by splitting into two functions: toggleBulkRow(div) flips checkbox+syncs bg (called by div click), syncBulkRowBg(cb) only syncs bg (called by checkbox onclick after browser has already toggled it); files: SheetAssistPanel.html) | (Sheet Viewant Panel: new SheetAssistPanel.html (340×560px modeless dialog, dark theme, JetBrains Mono) opened via TopoAssist > Sheet View > ⊞ Sheet Viewant; 5 tabs: NAV (click device → scrollToDeviceColumn), FILL (port fill form → addLinkPair), BULK (multi-select rows → bulkUpdateRows stamps speed/mode/vlan/po), STAT (per-device port count table via getDevicePortSummary), FIND (device-pair row search via searchConnectionRows + activateRow); new GAS functions in Code.gs: showSheetAssistUi, getSheetAssistData, getSchemaOptions, getConnectionRows, bulkUpdateRows, getDevicePortSummary, searchConnectionRows, activateRow; menu entry added at top of Sheet View submenu with separator; files: Code.gs, SheetAssistPanel.html, INSTRUCTIONS.txt) | (Rename Device Name → DNS Name (UI labels only, internal variables unchanged): toolbar placeholder "DNS Name (e.g. Leaf[1-4] or Spine1)"; column header "DNS Name / Hostname"; ✎ tooltip "Edit DNS Name & Hostname"; sub-row editor label "DNS Name *"; error messages "DNS name: alphanumeric..." and "DNS name already exists"; internal code still uses d.name/dev.name/deviceName everywhere; files: Sidebar.html, Sidebar-js.html) | (Hostname discoverability: hostnameHtml in renderManagerList now always renders — when hostname is set shows .dev-hostname-text.dev-hostname-clickable (cursor:pointer, blue on hover, onclick=promptRenameDevice); when not set shows .dev-hostname-add span with text "+ set hostname" (onclick=promptRenameDevice, muted, blue on hover) so the option is always visible in the name cell without needing to know about ✎; CSS added: .dev-hostname-clickable (cursor:pointer, primary color on hover), .dev-hostname-add (10px muted monospace, opacity 0.5, full opacity + primary color on hover); files: Sidebar-js.html, Sidebar-css.html) | (Hostname audit & config generation: bug fix — hostname was missing from getTopologyData() devices.push() intermediate array (d.hostname never reached nodes payload); fixed by adding hostname: d.hostname||"" there; EOS hostname command added to config generation — eosHostname = targetDeviceObj.hostname||deviceName prepended as "hostname <eosHostname>\n!\n" at top of 000_GLOBAL section; device name still used for all topology lookups (deviceName+":" + pName), getColumnIndices, allDevices.find; getSheetDeviceList() now includes hostname field; all invariants confirmed correct: column suffixes, memory keys, link maps, data-id, hiddenDevices, labels/hostnames maps all keyed by short device name; files: Code.gs) | (Hostname shown in topology & DevVisPanel: node.hostname added to topology payload (Code.gs nodes map + getSheetDeviceList); topology card header shows hostname||id; D3 simple-view node text uses label=hostname||id for split/wrap logic; renderHolistic nodesData carries hostname; DevVisPanel render shows hostname||name as display label, data-hostname attribute added for search; filterDevVisList matches on both data-name and data-hostname; data-id/toggle() still use short device name as key — hostname is display-only; files: Code.gs, Sidebar-js.html, DevVisPanel.html) | (Hostname field added to Device Manager: devices now have an optional Hostname field separate from Device Name; Device Name = short key used as column-header suffix (int_cal423, sp_mode_cal423, etc.); Hostname = full EOS hostname (DUT-L-Leaf1-VG-cal423) written to sheet Row 1 and used in config generation — falls back to Device Name if not set; stored in DocumentProperties DEVICE_HOSTNAMES (DocumentProperties, not ScriptProperties) as {devName: hostname} map; getDeviceHostnames()/saveDeviceHostnames() added to Code.gs; getExistingDevices() now returns hostname field; rebuildSheet() outRow1 uses devObj.hostname||devName; rebuildSheet() backup phase builds hostnameToDevName reverse map from DocumentProperties+forcedOrderList so Row 1 hostname values normalize back to short device-name keys during backup — prevents data loss on rebuild; promptRenameDevice() replaced with sub-row editor (deviceEditRow li element inserted after clicked row) showing two inputs: Device Name (required, validated) + Hostname (optional); closeDeviceEditor()/applyDeviceEdit(index) handle the sub-row; buildHostnamesMap() added alongside buildLabelsMap(); save chain extended: saveLabelsAndFinish → saveHostnamesAndFinish → handleSuccess; structural change detection includes hostname; finalOrder includes hostname; CSS classes added: .dev-name-cell (column flex wrapper), .dev-hostname-text (muted sub-text below name), .device-edit-row/.device-edit-form/.device-edit-field (sub-row editor); Device Manager column header changed to "Device Name / Hostname"; files: Code.gs, Sidebar-js.html, Sidebar-css.html, Sidebar.html) | (Code.gs: DevVisPanel now works with Custom View — applyCustomView() saves SHEET_VIEW_MODE='CUSTOM' and CUSTOM_VIEW_PREFIXES (selectedKeys JSON) to Script Properties; re-hides hidden device columns after view reset (same i=1+ loop); calls showDeviceVisPanel() at end; setColumnVisibility() now handles mode='CUSTOM' by reading CUSTOM_VIEW_PREFIXES and checking startsWith per column; _applyDeviceCols() handles mode='CUSTOM' similarly so toggleSheetDevice/showAllSheetDevices/hideAllSheetDevices all work correctly in custom view; changes in Code.gs only) | (devVisPanel always opens maximized: toggleDevVisPanelOpen() now removes is-minimized class and resets devVisMinBtn text to '−' when isOpen=true, so panel always opens fully expanded regardless of previous minimize state; Sidebar-js.html) | (devVisPanel drag fix: drag now works from any part of the panel (not just header); mousedown listener moved from header to panel element; excluded from drag: button, input, .dev-vis-list (allows scroll); on first drag, right/bottom CSS is converted to left/top via anchorToLeftTop() so top-clamping is clean; minTop clamped to toolbar.getBoundingClientRect().bottom + 4px so panel cannot go above toolbar; maxLeft/maxTop clamped to viewport edges; document.body cursor set to grabbing during drag and cleared on mouseup; _dragInit guard moved from header to panel; CSS: cursor:grab on .dev-vis-panel.is-open, cursor:grabbing on :active, cursor:pointer on panel buttons/inputs, cursor:default on .dev-vis-list; removed cursor:grab from .dev-vis-header; changes in Sidebar-js.html, Sidebar-css.html) | (UI parity: topology sidebar devVisPanel restructured to match gsheet panel layout — ALL/NONE buttons moved from header into new .dev-vis-action-row div below header; renamed "ALL"→"👁 SHOW ALL" and "NONE"→"✕ HIDE ALL"; header now contains only title + minimize + close; dark-theme colored buttons: .dev-vis-show-all bg #14532d text #4ade80, .dev-vis-hide-all bg #431407 text #fb923c; .dev-vis-panel.is-minimized now also hides .dev-vis-action-row; .dev-vis-action-btn removed from use; DevVisPanel.html load() now calls google.script.host.setHeight(380) first to guarantee open maximized; changes in Sidebar.html, Sidebar-css.html, DevVisPanel.html) | (DevVisPanel.html: search filter added — search-row div with search-inp input between action-row and device-list; filterDevVisList() filters .device-item visibility by data-name substring match (case-insensitive); render() calls filterDevVisList() at end to restore filter after re-render; toggleMinimize() now also hides/shows searchRow; CSS: .search-row (bg #f8fafc, border-bottom #e2e8f0, padding 5px 8px), .search-inp (JetBrains Mono 10px, border #cbd5e1, focus border #64748b, placeholder #94a3b8); matches topology sidebar panel feature parity) | (DevVisPanel.html: light mode redesign — background #ffffff, header #f1f5f9, borders #e2e8f0; btn-min border #94a3b8 bg #e2e8f0 text #334155; btn-show-all bg #dcfce7 text #15803d; btn-hide-all bg #ffedd5 text #c2410c; device-item border #f1f5f9 hover #f8fafc; eye-icon visible #16a34a hidden #cbd5e1; dev-name #1e293b hidden #94a3b8; col-count bg #f1f5f9 text #64748b; showing-in-progress green rgba(22,163,74,0.07) eye #16a34a name #15803d; hiding-in-progress orange rgba(234,88,12,0.06) eye #ea580c name #c2410c; status-bar bg #f8fafc text #64748b) | (Code.gs: "hide all columns" error fix — both device-hiding loops in setColumnVisibility now start at i=1 instead of i=0 so column 1 (device name column) is never included in a hide batch; applies to ALL-mode inline block and non-ALL mode hidden-device block; prevents Exception when a device name suffix accidentally matches column 1 header or when all columns except col1 belong to devices) | (Code.gs: hideAllSheetDevices/showAllSheetDevices bug fix — both functions previously had `if (currentMode !== 'ALL') setColumnVisibility(currentMode)` guard so no column ops ran in default ALL mode; removed the guard so setColumnVisibility is always called; setColumnVisibility('ALL') itself had an early return BEFORE the hidden-device hiding block so even direct calls in ALL mode never hid device columns; fixed by adding inline hidden-device hiding in the ALL branch before returning — loads headers, iterates i=1+, hides matching device columns, then returns; showAllSheetDevices/hideAllSheetDevices now always call setColumnVisibility(currentMode) unconditionally) | (DevVisPanel.html: CSS specificity fix for is-toggling states — two distinct processing colors: hiding-in-progress uses orange (is-toggling.is-hidden: bg rgba(245,158,11,0.08), eye #f59e0b, name #fbbf24, col-count opacity 1) placed AFTER .is-hidden rules so it wins; showing-in-progress uses green (is-toggling:not(.is-hidden): bg rgba(74,222,128,0.08), eye #4ade80, name #4ade80); both pulse via dvp-pulse animation; same two-color logic applies to single-device toggle() and bulk doShowAll()/doHideAll(); btn-min enlarged (26×26, font 15px, border #475569, bg #1e293b, text #cbd5e1, disabled opacity 0.3) for visibility) | (DevVisPanel.html: SHOW ALL / HIDE ALL button redesign — panel-header now contains only title + btn-min (22×22 bordered); new action-row below header holds btn-show-all (green bg #14532d, text #4ade80, "👁 SHOW ALL") and btn-hide-all (dark-red bg #431407, text #fb923c, "✕ HIDE ALL") as full-width flex buttons; bulk operations now use optimistic UI — applyBulkOptimistic(hide) immediately flips all .device-item classes+eye SVGs+titles+is-toggling before server call; revertBulkOptimistic() restores each item from devices[] data on failure; setBulkDisabled(on) disables btnAll+btnNone+btnMin during call; doShowAll() shows status "Showing all devices…" and on success removes is-toggling from all items; doHideAll() shows "Hiding all devices…"; toggleMinimize() now also hides/shows actionRow so the SHOW ALL/HIDE ALL row collapses with the list; old setWorking() replaced by setBulkDisabled()) | (DevVisPanel.html: minimize button (− / +) collapses device list and status bar, calls google.script.host.setHeight(36) when minimized and 380 when restored; toggling state now uses amber/orange instead of black — .is-toggling sets amber background tint, eye-icon color #f59e0b, dev-name color #fbbf24 with no strikethrough, pulse animation stays at opacity 1→0.55 so color is always visible) | (DevVisPanel.html click feedback: optimistic update — toggle() immediately flips the clicked item's visual state (is-hidden class + eye SVG) before the server responds; adds is-toggling class which triggers dvp-pulse keyframe animation (opacity 0.55→0.2 loop) to show processing; on success removes is-toggling; on failure reverts class, SVG, title, and d.isVisible back to original; status bar shows "Updating [name]…" during call; data-name attribute added to each .device-item in render() for reliable querySelector lookup; changes only in DevVisPanel.html) | (devVisPanel UX: minimize button (− / +) collapses list+search row, .is-minimized hides .dev-vis-search-row and .dev-vis-list; search input filters device list live via filterDevVisList() called on oninput; click feedback: toggleDeviceVis() immediately adds .is-toggling (opacity 0.4, pointer-events:none) then defers actual work to requestAnimationFrame so browser paints the dim state first; targeted _updateDevVisItem() replaces full populateDevVisPanel() on single toggles — only patches one item's class+SVG, avoids full list re-render for 100-device lists; DEV_VIS_EYE_OPEN / DEV_VIS_EYE_CLOSED module-level constants replace per-call inline strings; populateDevVisPanel() calls filterDevVisList() at end to restore search filter after bulk ALL/NONE; CSS: .dev-vis-search-row, .dev-vis-search-inp, .dev-vis-panel.is-minimized rules, .dev-vis-item.is-toggling; changes in Sidebar.html, Sidebar-css.html, Sidebar-js.html) | (Modal audit & z-index fixes: devVisPanel z-index raised from 1100→16000 so it floats above editOverlay (15000) and stays accessible when any modal is open; editModal gained a header Close button (was missing — only had footer Cancel); groupRectModal duplicate header Cancel removed (footer Cancel+Save is the correct pattern); generateAllModal duplicate footer Close removed (header Close kept); vlanSummaryModal duplicate footer Close removed (header Close kept); reorderModal header ✕ + footer Close kept — valid UX for a Save/Close manager; all changes in Sidebar.html + Sidebar-css.html) | (Dev vis panel fixes: close button replaced ✕ text with SVG X (10px, bordered 18×18 box, red on hover) in Sidebar.html; #devVisPanelBtn.active CSS added (blue bg, white icon) so toolbar eye turns blue when panel is open; toggleSheetDevice() rewritten to targeted column ops — no more full showColumns(1,lastCol) reset; hide path: batch-hideColumns only for this device's columns; show path: _applyDeviceCols() reveals only columns matching current SHEET_VIEW_MODE so non-view columns stay hidden; _applyDeviceCols() helper added to Code.gs; eliminates expand-collapse visual glitch on per-device toggle) | (Sheet Device Visibility Panel: new DevVisPanel.html modeless dialog (240×380px, dark theme, JetBrains Mono) auto-opens when any column view preset is activated (viewIntOnly/viewIntAndPo/viewIntAndMode/viewIntAndVlan/viewIntAndXcvr each now call showDeviceVisPanel() after setColumnVisibility()); panel lists all devices with eye-toggle per device + ALL/NONE bulk buttons; toggling calls toggleSheetDevice(name, hidden) which updates TOPOLOGY_HIDDEN_DEVICES + re-applies current SHEET_VIEW_MODE via setColumnVisibility(); showAllSheetDevices()/hideAllSheetDevices() return updated device list; setColumnVisibility() now: saves mode to SHEET_VIEW_MODE script property, and after hiding view columns also hides all columns of hidden devices (endsWith('_'+devName) match); getSheetDeviceList() returns [{name, isVisible, colCount}]; since saveDeviceVisibility() bumps DATA_VERSION, topology sidebar auto-refreshes when sheet panel toggles a device; hiding is persisted via TOPOLOGY_HIDDEN_DEVICES — affects both sheet columns and topology data generation; new functions in Code.gs: showDeviceVisPanel, getSheetDeviceList, toggleSheetDevice, showAllSheetDevices, hideAllSheetDevices) | (Device Visibility Panel: added ✕ close button in panel header (.dev-vis-close-btn — red on hover); closes via toggleDevVisPanelOpen(); CSS in Sidebar-css.html, HTML in Sidebar.html) | (Device Visibility Panel: floating panel (bottom-right, draggable) with per-device eye-toggle hide/unhide; toolbar eye button next to VLAN modal opens/closes it; ALL/NONE bulk buttons; hidden state stored in hiddenDevices Set (session-only, not persisted); applyDeviceVisibility() hides nodes + triggers redrawLinks; filterTopology patched in 3 places to respect hiddenDevices when resetting node display; render() re-applies hiddenDevices after data refresh; panel auto-repopulates after render if any devices are hidden; drag implemented via mousedown on header; CSS class .dev-vis-panel/.dev-vis-item/.dev-vis-eye/.dev-vis-name/.dev-vis-count/.dev-vis-action-btn in Sidebar-css.html; panel HTML + toolbar button in Sidebar.html; JS functions toggleDevVisPanelOpen/populateDevVisPanel/toggleDeviceVis/showAllDevices/hideAllDevices/applyDeviceVisibility/initDevVisPanelDrag in Sidebar-js.html) | (Check Group 7 fix: Po child links collapsed to logical Po-pair key before VLAN duplicate check — same VLAN across all member links of a Po no longer triggers false positive) | (Cabling modal: Copy CSV + Copy Markdown moved under Topology Patching List as sub-buttons; "Copy as Table" section removed; Device Patching List unchanged) | (MLAG GW physical IP collision fix: phySuffix changed from sheetIndex±1 to gwLastV4+sheetIndex — eliminates peer-to-peer physical IP collision and physical/virtual-router collision simultaneously) | (Audit Check Group 7 added: duplicate P2P VLAN across sheet — same VLAN on multiple P2P links causes derived-IP subnet collision; flags all affected links in runValidation() in Sidebar-js.html) | (OSPF+MLAG fixes: generateMlagConfig gains isOspf param — Vlan4093 SVI gets ip ospf network point-to-point + ip ospf area 0.0.0.0 when OSPF underlay active; generateOSPF adds Vlan4093 to no-passive list when MLAG peer exists; generateBGPEvpnOverlay filters isMlag-only neighbors to prevent spurious loopback EVPN session to MLAG partner; generateOSPF guard changed to hasP2pForOspf — skips OSPF for MLAG-only devices with no P2P uplinks; generateConfig PO description fallback to d.remoteMlagPair when d.poGroup absent; removed dead seenNeighbors Set) | Renamed NetBench → TopoAssist: createMenu() call in Code.gs, menu-path references in UserGuide.html, CLAUDE.md header, INSTRUCTIONS.txt; tech modal redesigned as linear two-section layout: UNDERLAY section (BGP checkbox + OSPF checkbox, multi-select, replaces None/BGP/OSPF radio buttons) and OVERLAY section (VXLAN + EVPN checkboxes indented); underlay now supports combined 'bgp+ospf' value — stored as NET_UNDERLAY in UserProperties; isBgp/isOspf now derived via underlay.includes() so both can be true simultaneously; generateComplexL3Block() OSPF interface commands guard changed from === 'ospf' to underlayProtocol && underlayProtocol.includes('ospf') — null guard required because getTopologyData() Phase 5 calls generateConfig() without underlayProtocol (tooltip config generation has no settings context), so undefined must remain safe; Sidebar-js getCurrentTechState/saveTechSettings build underlay string from chkTechBgp+chkTechOspf checkboxes; openTechModal loads underlay via .includes() into two checkboxes; cascade logic updated: noUnderlay = neither BGP nor OSPF checked, auto-enables BGP when VXLAN/EVPN enabled with no underlay; CSS added .tech-section-label style; OSPF underlay added as alternative to BGP: NET_UNDERLAY property replaces NET_BGP ('bgp'|'ospf'|'none'); generateOSPF() generates router ospf 1 process block with passive-interface default + no passive-interface per P2P peer; generateBGPEvpnOverlay() generates stripped BGP EVPN overlay-only block for OSPF+EVPN case; generateComplexL3Block() adds ip ospf network point-to-point/area 0.0.0.0/neighbor bfd on l3-et-int/l3-po-int when underlayProtocol=ospf; tech modal UI changed from BGP checkbox to None/BGP/OSPF radio buttons; generateMlagConfig: VLAN 4093 now has trunk group MLAG_PEER (was missing, could leak onto downstream trunks); Vlan4093 now has no autostate (was missing, SVI could flap during failover breaking BGP peering); reload-delay order fixed to mlag 300 then non-mlag 330 (non-mlag must be higher so MLAG ports come up first); CLAUDE.md added: instructs AI to use Winnow MCP tools for all Arista/EOS questions before web search; runFullSheetCleanup: removed auto-cleanup C/D/E/F — MLAG+L3 conflict, L3 stale SVI, L2 stale ip_type_, ET stale PO now flagged by audit only, not auto-corrected, so user can rollback; added audit Check I: Stale IP Type on L2 port with no SVI; validateAndCleanData: removed Rules 1/2/3 (L3+SVI, L2+ip_type_, ET+Po) — audit flags these now; onEdit Phase B: removed all data-clearing cascades on mode/SVI column edits — kept only cell background greying;Restore wizard redesign; Generate All Configs moved to export group; Po audit checks; VLAN Summary Panel; Card Collapse; toolbar regrouped [Edit|Appear|Modals|Export|Audit|Help|Collapse] with dividers; audit-group-box outlined rect; ERR btn→alert-circle SVG icon, red fill when active; collapse toggle SVG ▴=collapse ▾=expand; IP modal P2P legend "ID"→"Device ID"; fix ERR btn not showing on initial load when audit default-on; card columns: header "IP"→"Cat", Et 62px, L2 shown in Cat col, header border-left aligned, sort btn "IP"→"CAT"; schema modal UX: modified-highlight dark-mode fix, SVG icons in footer btns, close btn-mono, "Gsheet Coloring"→"Apply Formatting", Reset Defaults confirm text clarified; New Project Reset: Sheet View menu item + showNewProjectDialog() + resetToNewProject(); Custom View: preset buttons Cabling/Po/Mode/VLAN/Xcvr + Apply stays open with ✓ Applied feedback; Device Manager: header close button, inline rename via input, inline delete confirm with pendingDeleteIndex; Global Config Editor: dark theme redesign, Cancel button, ✓ Saved feedback; bugfix: deletePort() wrong selector .btn-danger→getElementById(btnDelete); bugfix: toggleLegendDisplay() null guard on floatingLegend; fix blinking cursor before btnToggleCards: span→div with pointer-events:none; btnToggleCards disabled+opacity 0.3 in simple/holistic view via updateTopoButtons(); code quality: removed unused rowModified variable from runFullSheetCleanup(); added backup integrity check in rebuildSheet(); added null guard before getValues()[0] in applyCustomView(); saveDeviceVisibility() now uses atomic setProperties() to prevent race condition between TOPOLOGY_HIDDEN_DEVICES and DATA_VERSION writes; pre-built deviceColCounts map in rebuildSheet() to eliminate O(n²) forEach per device; empty catch blocks in rebuildSheet() now log via console.warn; updateMultipleInterfaces() lock uses lockAcquired flag so finally only releases if lock was held; extracted isAutoNonAristaDevice() helper replacing 4 inline duplicates and fixing inconsistent ixia-only check in formatting stage; saveDeviceConfiguration() device name comparison is now case-insensitive; getAttributeSchema() stale comment removed; SHEET_DATA init logs a warning when a custom sheet name is active; added safeCachePut() helper wrapping all 8 cache.put() calls in getTopologyData() with silent-failure logging; added JSDoc to rebuildSheet(), calculateGlobalTopology(), getTopologyData(); saveDeviceConfiguration() validates device names at input boundary (alphanumeric/hyphens/underscores, max 64 chars); Sidebar-js.html: added escapeHtml() utility; nameDisplay in renderManagerList() now escapes device names before innerHTML injection; getTopologyData() expanded from 4 to 6 phases: Phase 1/6 Sanitizing data (shows "Data sanitized ✓" or "Data clean ✓"), Phase 2/6 Applying visual styles (always shown, skips work when color disabled), Phase 3/6 Calculating topology (reports device count + row count after calc), Phase 4/6 Mapping connections (row progress every 50), Phase 5/6 Generating configs (port progress every 25), Phase 6/6 Caching result (reports node + link counts); fixed concurrent-execution phase flicker: runFullSheetCleanup() now accepts skipVersionBump param — when called from getTopologyData() it passes true, suppressing the early DATA_VERSION bump that was causing the poller to spawn a concurrent second execution mid-computation; DATA_VERSION is now bumped inside getTopologyData() only after the topology cache is fully stored, guaranteeing any poller-triggered second fetch gets an immediate cache hit instead of running all phases again; Phase 2 applyGlobalFormatting() runs on isColorEnabled||forceSync (both manual and poll-triggered refreshes); Phase 3 label renamed to "Calculating topology..." (dropped "& MLAG"); cache hit sets fromCache:true on the result; fetchData(force, autoVerify) now accepts autoVerify param; cold start uses fetchData(false, true) — tries GAS cache first for instant render, then auto-fires fetchData(true, false) to verify cache==actual; status shows "⚡ Loaded from cache — verifying..." during verify, "⚡ Loaded from cache" on subsequent cache hits, or getReadyStatus() on fresh fetch; fromCache status moved to after getReadyStatus() so it is no longer overwritten by the render cycle; added global isFetching flag — fetchData sets it true on entry and false in success/failure/catch handlers; smart poller checks isFetching before triggering fetchData so a poller-triggered fetch never spawns a concurrent GAS execution while one is already in flight — this eliminates the phase-flickering caused by localDataVersion="0" on first load triggering a second fetch 4 seconds into the initial fetch; fetchData(force, autoVerify, silent) now has a silent param — when true suppresses the "🚀 Requesting Topology Data..." status so the "⚡ Loaded from cache — verifying..." message stays visible during verify; verify call uses fetchData(true, false, true); getTopologyData() version bump and GAS cache store are now unconditional (removed !forceSync gate) so verify/forceSync fetches also write back to GAS cache — previously forceSync runs computed fresh data but never persisted it, causing every load to miss cache; status assignment moved to AFTER toggleViewMode()/render() call so "⚡ Loaded from cache — verifying..." is never overwritten by runValidation() writing "Audit Clean." — cache hits always set status last, fresh fetches in audit mode leave "Audit Clean." untouched, fresh fetches without audit set getReadyStatus(); session-storage restore path now calls fetchData(false, true) instead of fetchData(false) so autoVerify=true on every startup — without this, GAS cache hits on the restore path showed "⚡ Loaded from cache" only and never fired the verify fetch, so "Audit Clean." never appeared; downloadCablingReport() now computes totalCableGroups and totalIndividualLinks after buildCableGroups() and prints "Total Cables: N (M individual port connections incl. breakouts)" in the header of both Topology Patching List and Device Patching List — the breakout suffix is omitted when all groups are single-link; both files show the same total since it is derived from cableGroups count regardless of mode; memory cleanup (allNodesData/allDevicesData/globalPortElements/configCache) is now gated on !silent so silent/verify fetches keep the existing rendered topology visible while the background GAS call runs — previously the cleanup fired immediately wiping connections before the response arrived; handleCopy() now saves btn.innerText as originalLabel before setting "✔ Copied" and restores it on timeout instead of hardcoding "Copy" — fixes buttons reverting to just "Copy" instead of their original label (e.g. "Copy CSV", "Copy Markdown"); Sidebar.html Copy as Table section: each button now wrapped in a column div with a 10px sub-label beneath it; copyCablingCsv() and copyCablingMarkdown() columns expanded to Device A, Port A, Xcvr A, Speed A, Speed B, Xcvr B, Port B, Device B — extracted _breakoutSides(g) helper that mirrors Topology Patching List mixed-breakout detection: when one side has Xg-N speed matching count, that side is the QSFP aggregate and gets bo=false; the other (SFP lane) side gets bo=true; speed-sum check (SFP×count==QSFP) used as secondary detection; falls back to isBreakoutA/B port-slash heuristic only when speed is absent or equal; this fixes Et13/1 (QSFP-DD, 100g-4) incorrectly getting "(Breakout)" — now only the SFP25 side gets it; xcvrLabel(x, bo) appends "(Breakout)" based on computed bo from _breakoutSides(); cleanSpeed() strips "auto" to blank; CSV fields are double-quote escaped; both CSV and Markdown functions use _breakoutSides(); QSFP aggregate side now shows physical parent port (g.phyA/g.phyB) instead of individual lane port names — when isMulti && !bo, portA/portB resolves to phyA/phyB so all 4 breakout rows show "Et1" instead of "Et1/1", "Et1/2", "Et1/3", "Et1/4"; Copy as Table export-desc updated to match new column list — "Copy CSV" gets "For Excel / Google Sheets" and "Copy Markdown" gets "For GitHub / Docs"; description simplified to just "Columns: DeviceA, PortA, DeviceB, PortB, Speed.") | (Device Manager dialog widened: showDeviceManagerUi() setWidth(600)→setWidth(800) in Code.gs; .modal-large CSS width 650px→760px in Sidebar-css.html — at 800px dialog width 90vw gives ~720px effective so long hostnames no longer truncate in the DNS Name / Hostname sub-row editor; files: Code.gs, Sidebar-css.html) | (rebuildSheet int_ column width fix: autoResizeColumns sized int_devName column to full hostname width because hostname was already in Row 1 when resize ran; generic fix: hideRows(1) before autoResizeColumns so GAS only measures Row 2 headers + data rows, then showRows(1) to restore — column widths are now based on Row 2 content regardless of what Row 1 contains; outRow1 still pushes hostname (no change to data); files: Code.gs) | (Device Manager label column widened 150px→200px: grid-template-columns label slot increased so 3+ pills fit in 2 rows instead of 3; files: Sidebar-css.html) | (Device Manager custom label delete: buildLabelEditorHTML() now builds customChips for labels not in DEVICE_LABEL_OPTIONS — each rendered as an inline-flex span with the label text + an × button that calls toggleDeviceLabel(index,lbl) to remove it; customChips appended after predefined chips inside the flex wrap div so custom labels appear alongside predefined ones with a delete affordance; files: Sidebar-js.html) | (D3 node label hyphen-stripping fix: wrapping logic splits hostname on [-_.] then reassembles with "-" for parts on the same line but was starting new lines without a leading hyphen and without a trailing hyphen on the previous line — fix adds trailing "-" to the current line (only for lines 0 and 1, not the truncation line) when overflow causes a line break, so "LAB-DC1-Leaf1" wraps to "LAB-DC1-" / "Leaf1" instead of "LAB-DC1" / "Leaf1"; applies to both simple-view D3 force graph and holistic view; files: Sidebar-js.html) | (Device Manager placeholder examples genericized: DNS Name input placeholder changed from "e.g. Leaf1" to "e.g. leaf1"; Hostname input placeholder changed from "e.g. DUT-L-Leaf1-VG-cal423" to "e.g. LAB-DC1-Leaf1"; files: Sidebar-js.html) | (Hostname race-condition fix — topology card headers showed DNS name instead of hostname after saving: root cause was poller-triggered fetchData(false) firing between the DATA_VERSION bump (in processDeviceBatch/saveDeviceVisibility) and the later saveDeviceHostnames call, causing getTopologyData to cache nodes with hostname:"" before hostnames were persisted; that stale fetch held isFetching=true, blocking the forced fetchData(true) in handleSuccess; fix: (1) fast path now calls saveDeviceHostnames FIRST (before saveDeviceVisibility bumps DATA_VERSION) so hostnames are in DocumentProperties before any version change is visible to the poller; (2) slow path passes buildHostnamesMap() as third arg to processDeviceBatch, which saves to DEVICE_HOSTNAMES inside the same GAS execution before the DATA_VERSION bump — both saves are idempotent since saveDeviceHostnames is still called at end of chain via saveHostnamesAndFinish; files: Code.gs, Sidebar-js.html) | (viewShowAll now behaves like showAllSheetDevices: calls saveDeviceVisibility([]) + setColumnVisibility(ALL) + refreshSheetRowVisibility(); menu label renamed to Show All Columns/Devices; Column Sync check filters __VIS__ from actualHeaders to prevent false Extra: 1 warning; files: Code.gs, INSTRUCTIONS.txt) | (removed stale hiddenDeviceSet peer-port filter in getTopologyData(): the linkInfo/hiddenDeviceSet.has() guard was missed when decoupling topology from sheet visibility; since all devices always appear in topology now, the filter is obsolete and caused a runtime ReferenceError; files: Code.gs) | (SheetAssistPanel NAV fix: clicking device name now jumps to sheet column via scrollToDeviceColumn(); eye icon gets its own onclick with stopPropagation; dev-name span gets separate onclick→navigate(); added navigate() JS function; .dev-name cursor:pointer + hover underline; files: SheetAssistPanel.html) | (col A __VIS__ protection + hide-all guard: onEdit now checks startCol===1 before the startRow<3 guard — clears edit and shows toast "Column A is reserved for TopoAssist row visibility markers"; toggleSheetDevice guard checks if hiding deviceName would leave all devices hidden, throws friendly Error if so; hideAllSheetDevices always throws friendly Error since hiding all would leave only hidden __VIS__ causing GAS exception; files: Code.gs) | (onEdit col A row 2 fix: row 2 col 1 holds the __VIS__ header — clearing it would cause ensureDummyColumn to insertColumnBefore(1) on next call, duplicating col A; fix: if startRow===2, restore DUMMY_VIS_HEADER instead of clearContent; rows 1 and 3+ still clearContent; files: Code.gs) | (rebuildSheet post-sync visibility restore: rebuildSheet wipes __VIS__ and all column visibility; added setColumnVisibility(currentMode) call at end of stage 5 (before success toast) so __VIS__ is recreated via ensureDummyColumn, hidden device columns are re-hidden, and row markers are rewritten — wrapped in try/catch so formatting errors do not block the success toast; files: Code.gs) | (hide-all column fix: removed blocking guards from toggleSheetDevice and hideAllSheetDevices; instead catch the GAS hide-all error at the two actual throw sites — _applyDeviceCols (try/catch: if last device col being hidden, showColumns(1,1) to expose __VIS__ then retry hideColumns) and ensureDummyColumn (tryHideCol1 helper: if __VIS__ is last visible column, leave it visible — re-hidden next time a device is shown); result: HIDE ALL and individual hide-all both work, __VIS__ stays visible as placeholder when all devices are hidden; files: Code.gs) | (tryHideCol1 + _applyDeviceCols catch robustness: GAS exceptions from SpreadsheetApp may not set e.message — changed tryHideCol1 to blanket catch (only failure mode for hideColumns(1,1) is hide-all so blanket is safe); changed _applyDeviceCols catch from e.message check to String(e).toLowerCase().includes(hide all) which works regardless of error object structure; files: Code.gs) | (__VIS__ always visible: removed all hide-col-1 logic — ensureDummyColumn no longer calls hideColumns(1,1), tryHideCol1 helper removed; _applyDeviceCols try/catch removed (no longer needed); __VIS__ col A is always visible to users, eliminating all hide-all-columns errors; files: Code.gs) | (DUMMY_VIS_HEADER renamed __VIS__ → _sys_; ensureDummyColumn on insert now sets row 1 col 1 = "↕", cell note on header = "Managed by TopoAssist — do not edit.", column width = 36px; files: Code.gs) | (removed bullet markers from _sys_ col: refreshSheetRowVisibility no longer writes •/"" to col A — markers were purely cosmetic and never read back; row hide/show still works via toShow/toHide lists computed from int_ column data; col A now stays clean with only the ↕ label and _sys_ header; files: Code.gs) | (Bug fix: Code.gs ip_type normalization in refreshSheetRowVisibility() — was only mapping "" to "blank", left unexpected values uncategorized; fixed to use same normalization as Topology View: (ipRaw==="p2p"||ipRaw==="gw") ? ipRaw : "blank", so any non-p2p/non-gw value consistently maps to "blank" in both Sheet View and Topology View; file: Code.gs, INSTRUCTIONS.txt) | (Sheet View race condition fix: added serverBusy flag + setServerBusy(on) which sets flag, toggles body.server-busy CSS class, and disables bulk/minimize buttons; every mutating entry point guards with if(serverBusy)return — toggle, doShowAll, doHideAll, toggleColItem, applyColBulk, toggleIpFilter, toggleIntModeFilter, toggleSviFilter; CSS body.server-busy grays out filter items, non-toggling device eye icons, non-mandatory col items via pointer-events:none + reduced opacity; currently-toggling item animation unaffected; setServerBusy(false) called in both success and failure handlers; file: SheetAssistPanel.html, INSTRUCTIONS.txt) | (int_mode filter: switched from 4 groups l2/l3/et/po to 8 specific sp_mode_ values with exact matching; labels l2ea/l2et/l2pa/l2pt/l3ei/l3es/l3pi/l3ps with full value as tooltip; bug fix: blank sp_mode_ ports now always pass the filter in both Sheet View (Code.gs) and Topology View (Sidebar-js.html) — previously GW/NA ports with no sp_mode_ were hidden even when all modes selected; initIntModeFilter validates saved values and falls back to all 8 if stale old group values detected; files: Code.gs, SheetAssistPanel.html, Sidebar-js.html, INSTRUCTIONS.txt) | (navigate() status fix: success/failure handlers now guard with if(!serverBusy) before setStatus() — prevents navigate clearing live "Applying filter..." message when a filter server call is in-flight; file: SheetAssistPanel.html, INSTRUCTIONS.txt) | (Tooltip UX: added 350ms show delay via scheduleTooltip(e,portId,peerId)/cancelTooltip() — tooltipShowTimer fires showTooltip using lastMouseEvent (current position at fire time, tracked by global mousemove); port row mouseenter/mouseleave and canvas link hit/unhit/mouseleave all use schedule/cancel instead of immediate show/hide; refreshCallback inside showTooltip still calls showTooltip directly (no re-delay on config load refresh); positioning fixed: tooltip now vertically centered on cursor (y = clientY - h/2) with gap=18px to the right, flips left if near right edge, clamped 10px from all edges — cursor never inside tooltip bounds; files: Sidebar-js.html, INSTRUCTIONS.txt)
================================================================================

WHAT IS THIS FILE?
  This file contains everything an AI needs to recreate the TopoAssist project
  from scratch. It is kept in sync with every code change made to the project.

--------------------------------------------------------------------------------
WORKFLOW RULE: Before making ANY code change to this project, read the relevant
INSTRUCTIONS.txt sections first. After making changes, update INSTRUCTIONS.txt
immediately (the hook enforces this). If the code and INSTRUCTIONS.txt disagree,
DO NOT silently pick one — ask the user which is correct and confirm the intended
flow before proceeding or updating docs.

--------------------------------------------------------------------------------
SECTION 0A: SYSTEM INVARIANTS (Must hold after every operation)
--------------------------------------------------------------------------------

These invariants represent the highest-priority contracts in the system.
When any of these are violated, visible bugs result immediately.

1. _sys_ ALWAYS AT COL 1 OF SHEET_DATA
   Ensured by: refreshSheetRowVisibility() → ensureDummyColumn() inserts it.
   Post-rebuild rule: refreshSheetRowVisibility() MUST run BEFORE applyCustomView().
   If order is reversed, _sys_ is missing and applyCustomView() corrupts col 1.

2. generateConfig() ALWAYS CALLED WITH ALL 5 ARGS
   Signature: generateConfig(portName, d, ipPrefs, seenPos, netSettings)
   Both callers — getTopologyData() and getDeviceConfig() — must pass netSettings.
   netSettings is the full settings object (13 IP family flags). Omitting it silently
   drops all protocol-family-gated commands (OSPF per-interface, IPv6, etc.).

3. TOOLTIP CONFIG == CONFIGS SHEET CONFIG (same source of truth)
   Both paths call getDeviceConfig() → configMap[key].full.
   getTopologyData() pre-generates via generateConfig() with globalUnderlay.
   fetchFullConfig() → processDeviceConfig() maps the same configMap to allNodesData.
   Any divergence means one path is bypassing getDeviceConfig().

4. processDeviceConfig() STRIPS ^\d+_ PREFIX BEFORE canonicalizeInterface()
   configMap keys are prefixed: "050_Et1", "060_Po10", "000_BASE", etc.
   Must strip prefix first: portName.replace(/^\d+_/, "") → canonicalizeInterface()
   Without strip: "050_Et1" does not canonicalize to "Et1", allNodesData lookup fails,
   tooltip always shows "Loading..." (silent failure, no error thrown).

5. allNodesData KEYS ARE "DeviceName:PortName" (canonical, no prefix)
   Example: "SPINE1:Et1", "LEAF1:Po10" — never "SPINE1:050_Et1".
   canonicalizeInterface() normalizes "Ethernet1" → "Et1", "Port-Channel10" → "Po10".
   All code that reads/writes allNodesData must use this format.

6. canonicalizeInterface() IS DUPLICATED — KEEP BOTH IN SYNC
   Lives in Code.gs (server config gen) AND Sidebar-js.html (client topology render).
   Both copies carry "// DUPLICATED — last synced: <date>" comment.
   Update the date on BOTH copies whenever either is changed.

7. configCache[deviceName].data IS THE FULL configMap
   Format: { "050_Et1": { full: "...", ... }, "000_BASE": { full: "..." }, "000_GTPL": { full: "..." }, ... }
   It is NOT a per-port string. Code that reads it must key by prefixed port name.

8. allNodesData IS FULLY REPOPULATED ON EVERY render() CALL
   It is cleared at the start of render() and rebuilt from the server payload.
   Do not persist cross-render state in allNodesData — it will be lost.

9. selectedLineId IS A RENDERING VARIABLE — NEVER READ IN ASYNC CALLBACKS
   selectedLineId is cleared by two render-pipeline paths without hiding the strip UI:
     a. redrawLinks() — clears selectedLineId when link.coords=null (node hidden/zoom)
     b. filterTopology() — always clears selectedLineId on any search input change
   By the time a GAS withSuccessHandler or fetchQueue callback fires (seconds later),
   selectedLineId is often null even though the link strip is still visible.
   RULE: any async callback or click handler needing the selected link's device/port
   must use _pushLeftDev/_pushRightDev/_pushLeftPort/_pushRightPort — set by selectLine()
   and only changed by an explicit new selection. Never use selectedLineId for this.
   selectedDeviceId is stable (not cleared by render pipeline) — safe in async callbacks.
   Root cause of the "strip stays amber / View button gives 'select a device or link
   first'" bug: openConfigFromButton and processDeviceConfig CASE 2 both read
   selectedLineId at async time. Fixed 2026-04-28 to use _pushLeftDev/_pushRightDev.

--------------------------------------------------------------------------------
SECTION 0B: CRITICAL CALL CHAINS (Order matters — do not reorder)
--------------------------------------------------------------------------------

POST-REBUILD (rebuildSheet or syncSchemaPreservingOrder):
  rebuildSheet() stages 1→5
  → refreshSheetRowVisibility()   ← creates _sys_ at col 1 via ensureDummyColumn()
  → applyCustomView()             ← hides/shows columns; _sys_ must already exist

TOPOLOGY LOAD (getTopologyData):
  getTopologyData(forceSync)
  → runFullSheetCleanup()
  → applyGlobalFormatting()
  → calculateGlobalTopology()     ← returns { mlagConfigPorts, peerLinkPorts, mlagPeerMap, globalLinkMap }
  → [per-port] generateConfig(pName, details, ipPrefs, seenPos, globalNetSettings)
  → return payload                ← includes pre-generated config per port

CONFIG FETCH (right pane tooltip, on demand):
  fetchFullConfig(deviceName)
  → google.script.run.getDeviceConfig(deviceName)
  → processDeviceConfig(dev, res.config)
      strips ^\d+_ prefix from each key
      → canonicalizeInterface(shortPort)
      → allNodesData[dev+":"+shortPort].details.config = configMap[key].full

CONFIGS SHEET (Generate All Configs menu):
  getAllDeviceConfigs()
  → [per device] getDeviceConfig(dev)   ← same function as CONFIG FETCH path
  → formatConfigMap(configMap)          ← sort keys numerically, join .full with 
!

  → writeConfigsToSheet()

ROW VISIBILITY (show/hide rows by filter):
  refreshSheetRowVisibility()
  → ensureDummyColumn()                 ← inserts _sys_ at col 1 if missing
  → read headers row
  → show/hide rows based on filter prefs (SHEET_VIEW_*)

SCHEMA SYNC (force sync button):
  syncSchemaPreservingOrder()
  → orphan check (warn if data exists for removed schema keys)
  → rebuildSheet()
  → [post-rebuild chain above]

CLIENT INIT (sidebar load):
  initApp()
  → initTheme(), initToolbarCollapse()
  → renderIpTypeList(), renderIntModeList(), renderSviList()
  → hideGlobalLoading()
  → loadStateFromStorage(currentViewMode)   ← populates savedNodePositions
  → loadGroupRects()
  → try instant restore from sessionStorage
  → fetchData(false, true)                  ← auto-verify, always runs
  → bindGlobalSearchListeners(), initLayoutObserver()
  → startSmartPolling()

--------------------------------------------------------------------------------
SECTION 0C: TERMINOLOGY (READ BEFORE EVERY CHANGE)
--------------------------------------------------------------------------------

Two completely separate HTML dialogs open in different contexts. Never confuse them.

  "Sheet View" / "sheet view"
      File:    SheetAssistPanel.html
      Opens:   showSheetAssistPanel() — appears as a modeless panel INSIDE the
               Google Sheet (not the sidebar topology dialog)
      Title:   "Sheet View" (490×420 dialog)
      Purpose: Device visibility toggles (show/hide sheet rows per device) +
               Column view selector (hide/show sheet columns)
      Filter panels: ip_type, int_mode, svi_vlan filters — all SheetAssistPanel.html
      User terms: "sheet view", "sheet panel", "Sheet View"

  "Topology View" / "topology view"
      Files:   Sidebar.html + Sidebar-js.html + Sidebar-css.html
      Opens:   showSidebar() — appears as the TOPOLOGY SIDEBAR (1600×900 dialog)
      Purpose: Visual network topology (port cards, D3 holistic graph, edit mode,
               config push, validation, device/schema managers)
      Sub-modes (currentViewMode):
        'detailed'  — left port-card list + D3 canvas (the default)
        'holistic'  — D3 force-graph only (circle nodes, no port cards)
        'simple'    — same as holistic but the Dev Vis floating panel calls it
                      "Topology View" in its header
      Dev Vis panel: floating panel labeled "Topology View" inside the sidebar —
                     its int_mode filter uses SHORT labels (l2ea, l2et…) because
                     the panel is compact; full names live in SheetAssistPanel.
      User terms: "topology view", "sidebar", "canvas"

  RULE: If the user says "sheet view" → SheetAssistPanel.html.
        If the user says "topology view" or "sidebar" → Sidebar.html / Sidebar-js.html.
        If unsure, ASK before touching code.

── VIEW TAXONOMY — THREE DISTINCT VIEWS ────────────────────────────────────────

  When the user says "add X to [view]", match to one of these three — do NOT
  conflate them. All three are active simultaneously during a session.

  1. DevVis  (Device Visibility floating panel — inside Topology Sidebar)
       File:     Sidebar-js.html
       Toggle:   toggleDevVisMinimize()
       Purpose:  Per-device show/hide in the topology canvas (hides port cards +
                 D3 nodes). Also has an int_mode row filter with SHORT labels
                 (l2ea, l2et…) for compact display.
       Scope:    Topology canvas only — has no effect on the Google Sheet.

  2. SheetVis  (Sheet View modeless panel — inside Google Sheet)
       File:     SheetAssistPanel.html
       Opens:    showSheetAssistPanel()
       Purpose:  (a) Device visibility: hides/shows sheet ROW groups per device.
                 (b) Column selector: shows/hides sheet COLUMNS.
                 (c) Row filters: ip_type (p2p/gw/blank), int_mode, svi — all
                     operate on sheet row visibility via refreshSheetRowVisibility().
       Scope:    Google Sheet only — has no effect on the topology canvas.

  3. Sheet View menu  (TopoAssist → Sheet View submenu — GAS menu bar)
       File:     Code.gs  (onOpen addSubMenu + view functions)
       Calls:    applyCustomView([...keys]) + showSheetAssistPanel()
       Purpose:  Column-visibility PRESETS that show a named subset of columns.
                 Each item is a one-click "view mode" for the sheet.
       Current presets:
         L1: Interface         → ['int']
         L1: Transceiver       → ['int', 'xcvr_type', 'et_speed', 'xcvr_speed']
         L1: Speed             → ['int', 'xcvr_type', 'et_speed', 'xcvr_speed']
         L1.5: Port Channel    → ['int', 'po']
         L2/L3: Mode           → ['int', 'po', 'sp_mode']
         L2/L3: Vlan           → ['int', 'po', 'sp_mode', 'vlan']
         L2/L3: SVI            → ['int', 'po', 'sp_mode', 'vlan', 'svi_vlan']
         L2/L3: IP             → ['int', 'po', 'sp_mode', 'vlan', 'svi_vlan', 'ip_type']
         Custom View...        → opens SheetVis panel (view 2)
       Scope:    Google Sheet column visibility only.

  DISAMBIGUATION RULE:
    "add X filter/row filter"          → SheetVis (2) — row visibility
    "add X to the filter panel"        → SheetVis (2) — row filter section
    "add X view" / "add X preset"      → Sheet View menu (3) — applyCustomView preset
    "add X to the menu"                → Sheet View menu (3)
    "show/hide X in topology"          → DevVis (1)
    References to "L2/L3: SVI, Vlan, Mode" existing items → Sheet View menu (3)

--------------------------------------------------------------------------------
SECTION 1: PROJECT OVERVIEW
--------------------------------------------------------------------------------

── MANAGEMENT SUMMARY ──────────────────────────────────────────────────────────

TopoAssist is an Arista lab day-0 automation tool built into Google Sheets.

It lets engineers go from a blank rack to a fully configured, verified network
without touching the CLI manually:

  1. Document  — capture physical cabling, transceivers, and port assignments
                 in a structured sheet
  2. Visualize — see the live topology diagram and trace VLANs across the network
  3. Validate  — flag wrong cables, speed mismatches, and config errors before
                 deployment
  4. Generate  — produce device configs (interfaces, BGP, OSPF, VXLAN/EVPN,
                 MLAG) automatically from the sheet data
  5. Push      — deploy configs directly to Arista EOS devices with dry-run,
                 commit, and verify phases
  6. Audit     — confirm post-deploy that every device matches the intended design

Net result: lab bring-up that used to take days of manual CLI work is reduced to
filling in a spreadsheet and clicking a button. This reduces engineer time on
day-0 activities by a minimum of 2–5 days and delivers a working base setup.

────────────────────────────────────────────────────────────────────────────────

TopoAssist is a Google Apps Script (GAS) day-0 lab workbench embedded in a
Google Spreadsheet. It covers the full lifecycle of bringing up an Arista EOS
lab — from physical cabling verification through config generation, push, and
post-deploy auditing.

── CORE CAPABILITIES ───────────────────────────────────────────────────────────

  Cabling Report
    Export the full physical cabling topology from the sheet as a structured
    table (CSV / Markdown). Includes port, speed, transceiver type, breakout
    grouping, and cable-group compression (e.g. Et14/1-4 as a single QSFP-DD
    row). Snake self-loop cables are excluded automatically.

  Wrong Cabling Report / Cabling Audit
    Flag incorrect or mismatched cables via the Configuration Audit engine:
    - Speed mismatch between et_speed_ (EOS-reported) and xcvr_speed_ (expected)
    - xcvr_type ↔ et_speed mismatch (transceiver native speed ≠ actual speed)
    - Missing xcvr_speed_ when breakout is likely (Rule 5 orange highlight)
    - MLAG peer-link missing, asymmetric, or wrong port-channel
    Conditional formatting rules colour the sheet live so mismatches are visible
    without opening any modal.

  Topology Visualization
    Interactive canvas diagram rendered with D3.js inside the sidebar. Devices
    are nodes; cables are edges coloured by layer (underlay, overlay, MLAG,
    snake). Features: drag-to-reposition, zoom/pan, node search/highlight,
    VLAN trace (colour-highlights all ports carrying a VLAN), topology filter
    (by device name, VLAN, internal VLAN), PNG export, dark/light theme toggle.

  Auto Config Generation
    One-click per-device EOS configuration generation from the sheet data:
    - Interface config  — switchport (access/trunk), L3, sub-interface, SVI
    - Underlay          — BGP (eBGP, unnumbered, RFC-5549) / OSPF / OSPFv3
    - Overlay           — VXLAN (L2/L3 VNI), EVPN (IPv4/IPv6)
    - MLAG              — peer-link, MLAG domain, keepalive, SVI
    - Snake Test        — bidirectional L3 VRF chain with static ARP + routes
                          for traffic-generator connectivity (EP1/EP2 endpoints)
    IP addressing is formula-driven from configurable base octets (P2P /31s,
    GW SVIs, Loopback /32s). BGP ASN base is configurable. 16 network-family
    flags (INT_IPV4, INT_IPV6, INT_IPV6_UNNUM, GW_IPV4, GW_IPV6, BGP×4,
    OSPF×3, VXLAN×2, EVPN×2) let you mix/match protocol families per topology.
    Supports IXIA/Spirent traffic generators as non-Arista endpoints.

  Config Push
    Directly push generated configs to live Arista EOS devices via
    device_bridge.py (local Python bridge, SSH or eAPI):
    - 3-phase push: dry-run diff → commit → verify
    - Idempotent: section cleaners wipe stale BGP/OSPF/interface state first
    - Single-device push from topology node, or Generate All → Push All
    - Live phase tracker with elapsed timer; "already in sync" auto-closes
    - Abort-stale-sessions cleans orphan SSH sessions before each push

  Auditing
    Post-deploy configuration audit checks across all devices:
    A. Port-channel link count consistency
    B. MLAG peer-link symmetry (same PO on both peers)
    C. Duplicate P2P VLANs across device pairs
    D. Speed mismatch (et_speed_ ≠ xcvr_speed_ when both set)
    E. VLAN range validity (1–4094)
    F. VLAN conflicts across trunk ports on the same PO
    G. Native VLAN on non-trunk port
    H. MLAG peer-link missing / required
    I. Po mismatch / asymmetric link count
    J. nv<N> native-VLAN token on non-trunk port
    K. Internal VLAN conflict (EOS-reserved VLANs via device bridge data)
    M. Stale SVI: svi_vlan_ filled but mode is l3-* (SVI only valid on L2 modes)
    N. Stale VLANs: vlan_ has 2+ VLANs (comma or range) but mode is pure l3-* non-sub-int
       (single VLAN is allowed on l3-et/l3-po)
    O. Stale VRF: vrf_ filled but mode is l2-* with no SVI
    P. Incomplete Xcvr: xcvr_speed_ filled but xcvr_type_ is blank
    Q. Missing VLAN: mode is l3-et/l3-po but vlan_ is blank (required for IP derivation)

  Topology Search
    Instant filter across all devices, ports, VLANs, descriptions, and internal
    VLANs. Matching nodes are highlighted on the canvas; non-matching nodes are
    dimmed. VLAN trace mode colours all ports carrying a given VLAN (including
    EOS-internal VLANs surfaced via device bridge). Device bridge search also
    matches on internal VLAN numbers.

  Device Bridge (live device connectivity)
    device_bridge.py — local Python HTTP bridge (SSH + eAPI dual path):
    - LLDP neighbor pull → auto-populates cabling sheet from live topology
    - Device status check: EOS version, interface list, internal VLANs,
      bridge MAC (for snake test ARP config)
    - Config push (3-phase) and push-all workflows

── SCOPE ───────────────────────────────────────────────────────────────────────

  LAB TOOL ONLY — not for production.
  IPv6 syntax intentionally deviates from strict Arista EOS syntax to align
  with IPv4 for lab testing convenience.
  IXIA/Spirent traffic generators appear as non-Arista device rows in the sheet.

--------------------------------------------------------------------------------
SECTION 2: FILE STRUCTURE
--------------------------------------------------------------------------------

All files live in: ~/claude/projects/topoassist/

  Code.gs             — Server-side GAS. All backend logic:
                        Menu, column views, schema, sheet rebuild engine,
                        visual formatting engine, topology calculation,
                        config generation (BGP, MLAG, VXLAN/EVPN), snapshots,
                        device manager, IP preferences, global config editor.

  Sidebar.html        — HTML entry point for the modeless dialog (sidebar).
                        Includes CSS/JS via GAS include() helper.
                        Renders multiple UI modes: topology, manager, schema,
                        view_custom, all driven by template.initialMode.

  Sidebar-css.html    — All CSS for the sidebar UI. Must use JetBrains Mono
                        explicitly on EVERY element (buttons, inputs, labels,
                        status text). Elements do NOT inherit from body in GAS.

  Sidebar-js.html     — All client-side JavaScript. D3.js topology rendering,
                        SortableJS for drag-and-drop, html2canvas for PNG export,
                        edit mode, validation mode, config generation calls,
                        schema manager, device manager, search/filter, polling.

  UserGuide.html      — In-app documentation page (modal dialog). Covers:
                        Quick Start (4 steps), full toolbar reference (primary +
                        secondary rows) with actual inline SVGs copied from
                        Sidebar.html — each icon wrapped in .icon-cell (28x28
                        bordered div) for visual accuracy. Keyboard shortcuts,
                        visual color coding (connection colors, MLAG borders,
                        conditional dimming, device labels), sheet/schema
                        management, column visibility
                        views, checkpoints, config generation (MLAG, IP
                        addressing, auto-description rules, tech stack, global
                        config template), audit engine, and tips.

  SheetAssistPanel.html — Merged "Sheet View" modeless dialog (490×420px). Two-column
                        layout: LEFT = device visibility (Devices panel), RIGHT = column
                        view selector (Columns panel). Replaces both the old 240×380 sheet
                        assist panel and the separate Custom View dialog. Opened via
                        showSheetAssistPanel() or the "Custom View..." menu item.

  INSTRUCTIONS_topoassist.txt — This file. Updated on every code change.

  device_bridge.py    — Local Python HTTP bridge. Runs on bus-home. Handles
                        /health, /lldp, /devstatus, /pushconfig endpoints.
                        NOT deployed to GAS — run directly: python3 device_bridge.py

  .claude/commands/
    version-bump.md      — /version-bump: check VERSION sync between device_bridge.py
                           and the embedded template in Sidebar-js.html (downloadBridgeScript())
    check-deploy.md      — /check-deploy: list files modified this session, grouped by
                           deployment target (GAS upload vs local run)
    check-userguide.md   — /check-userguide: check if UserGuide.html needs updating
                           based on changes made this session
    check-constraints.md — /check-constraints: verify all Section 24 hard design constraints
                           (canonicalizeInterface sync, generateConfig param count,
                           hasKey usage, VERSION sync, no MLAG heuristic)

  CLAUDE.md           — Loaded at session start. Contains Winnow usage rules for
                        EOS/Arista questions.

--------------------------------------------------------------------------------
SECTION 3: GOOGLE APPS SCRIPT DEPLOYMENT
--------------------------------------------------------------------------------

DEPLOY METHOD (bus-home, 2026-04-19+):
  clasp is configured for direct push from bus-home. No manual copy-paste needed.

  Files:
    ~/claude/projects/topoassist/.clasp.json   — script ID + rootDir (committed)
    ~/claude/projects/topoassist/appsscript.json — GAS manifest (committed)

  Push all 7 GAS files:
    cd ~/claude/projects/topoassist && clasp push

  The PostToolUse hook auto-runs clasp push after every GAS file edit.

  Script ID: 1NL5x7CnPy2Rceet87qUTWlYxfqWaRC19pk5f1zpQR6k5FWf53BQ2JfDJ
  GAS Editor: https://script.google.com/u/0/home/projects/1NL5x7CnPy2Rceet87qUTWlYxfqWaRC19pk5f1zpQR6k5FWf53BQ2JfDJ/edit

CLASP AUTH (headless server — run once per machine):
  clasp login --no-localhost
  → Prints an OAuth URL. Open it in any browser.
  → After authorizing, Google redirects to localhost:8888/?code=... (browser shows "This site can't be reached").
  → Copy the full URL from the browser address bar and paste it back in the terminal.
  → Credentials saved to ~/.clasprc.json. No expiry — persists until revoked.

  Re-auth needed if: ~/.clasprc.json deleted, token revoked, or "Error: 401" on clasp push.

  Enable Apps Script API first (required): https://script.google.com/home/usersettings
    → Toggle "Google Apps Script API" ON.

INITIAL SETUP STEPS (new spreadsheet):
  1. Create a new Google Spreadsheet.
  2. Open Extensions > Apps Script.
  3. Create 5 files matching the names above (Code.gs already exists by default).
  4. Paste code from each file into the corresponding Apps Script file.
  5. Save. Reload the spreadsheet.
  6. Run onOpen() once to authorize permissions.
  7. The "TopoAssist" menu will appear in the spreadsheet.

PERMISSIONS REQUIRED:
  - SpreadsheetApp (read/write spreadsheet)
  - PropertiesService (script, document, user properties for state)
  - CacheService (user cache for topology caching)
  - HtmlService (sidebar/dialog rendering)
  - SpreadsheetApp.getUi() (menus, prompts, alerts, toasts)

EXTERNAL LIBRARIES (CDN, loaded in Sidebar.html):
  - D3.js v7.8.5          — Topology visualization
  - SortableJS v1.15.0    — Drag-and-drop device ordering in manager
  - html2canvas v1.4.1    — PNG export of topology
  - JetBrains Mono (Google Fonts) — Monospace font for all UI

--------------------------------------------------------------------------------
SECTION 3B: GLOBAL UI RULES (apply to ALL UI elements, always)
--------------------------------------------------------------------------------

These rules apply to every UI element in every file. Treat violations as bugs.

1. MONOSPACE FONT
   Every <button>, <input>, <select>, <textarea>, <label>, and status text must
   explicitly set: font-family: "JetBrains Mono"
   Elements do NOT inherit font-family from body in GAS/browser — always set it.

2. UI/UX SYMMETRY
   Spacing, padding, alignment, and sizing must be consistent and balanced:
   - Matching elements in the same context must have identical padding/margin
   - Button/input sizes within a group must be uniform
   - Alignment must be consistent (no accidental mix of left/center/right)
   - Visual weight must be balanced across all sides of a layout
   - Hover/focus/active interaction states must be consistent across similar controls
   When adding one element to a group, check all siblings for symmetry.

4. HD ICONS (SVG only)
   All icons must be inline SVG vectors — never Unicode characters (◎ ▤ ✦ ☰ ✕)
   or emoji used as visual icons. Unicode chars blur at small sizes and misalign.
   - Use <svg width="14" height="14" viewBox="0 0 24 24" ...> for toolbar icon-toggle
   - Use <svg width="16" height="16" viewBox="0 0 24 24" ...> for larger icon-btn
   - Always set stroke="currentColor" so icons inherit button color for hover/active
   - Icon set: Feather/Lucide (already used throughout TopoAssist)
   - If a Unicode char was used as icon: replace with SVG + remove font-size CSS

3. CANVAS BOUNDARY
   No modal, tooltip, dropdown, panel, or overlay may render outside the
   viewport/canvas boundary. The GAS sidebar/dialog is ~490px wide.
   - All widths must use min(Xpx, calc(100vw - Npx)) not hard-coded px
   - Fixed/absolute positioned elements must include max-width: calc(100vw - Npx)
   - overflow: hidden on containers that have children which could overflow
   - Inputs that expand on focus must use max-width relative to parent, not fixed px
   - Never use min-width larger than the expected viewport width (490px)

--------------------------------------------------------------------------------
--------------------------------------------------------------------------------
SECTION 4: GOOGLE SHEET STRUCTURE
--------------------------------------------------------------------------------

The working sheet name defaults to "PortMapping". Stored in ScriptProperties
under key 'TARGET_SHEET_NAME'. Can be renamed via TopoAssist > Sheet View >
Change Sheet Name.

ROW 1: Device Names (merged across their attribute columns)
ROW 2: Attribute headers in format: <attribute_key>_<DeviceName>
  Example: int_Leaf1, po_Leaf1, sp_mode_Leaf1, ..., int_Leaf2, po_Leaf2, ...
ROWS 3+: Data rows (one row per physical port / connection)

ATTRIBUTE SCHEMA (DEFAULT ORDER):
  NOTE: Keys below are the internal schema keys (no trailing underscore).
  Column headers in the sheet are formatted as <key>_<DeviceName> (e.g. int_Leaf1).
  Key         Label           Dropdown Options
  -------     -----------     ----------------
  int         Interface       (free text: Et1, Po10, Vl10, Lo0, Ma0)
  po          Port-Channel    (free text: Po10, Po100)
  sp_mode     Mode            l2-et-access, l2-et-trunk, l2-po-access, l2-po-trunk,
                              l3-et-int, l3-et-sub-int, l3-po-int, l3-po-sub-int
  vlan        VLANs           (free text: 10, 10,20, 10-20; add nv<N> for trunk native VLAN e.g. 10,20,nv100)
  svi_vlan    SVI VLANs       all (all VLANs in vlan_), <id> or <id,id> (specific VLANs only)
  ip_type     IP Type         p2p, gw
  vrf         VRF             (free text)
  speed       Speed           auto, 1g, 10g, 25g, 40g-4, 50g-1, 50g-2, 100g-1,
                              100g-2, 100g-4, 200g-1, 200g-2, 200g-4, 400g-2,
                              400g-4, 400g-8, 800g-4, 800g-8, 1.6t-8, sfp-1000baset
  encoding    FEC             fire-code, reed-solomon
  xcvr        Transceiver     (free text: QSFP28, SFP+, OSFP, etc.)
  desc        Description     (free text)
  sd          SD              (free text)
  dp-pp-mp_   DP-PP-MP        (free text)

NON-ARISTA DEVICES (IXIA, Spirent, traffic generators):
  - Detected by name containing: ixia, traffic, spirent, tgen (case-insensitive)
  - Or explicitly registered as non-arista via Device Manager
  - Get only ONE column: int_<DeviceName>
  - No attribute columns, no config generation

--------------------------------------------------------------------------------
SECTION 5: MENU STRUCTURE
--------------------------------------------------------------------------------

TopoAssist (top-level menu)
├── Show Topology               → showTopologyWindow()
├── [separator]
├── Sheet View                → submenu
│   ├── Show All Columns/Devices        → viewShowAll()
│   ├── Cabling (Int)                   → viewCabling()
│   ├── Transceiver (Int + Xcvr)        → viewTransceiver()
│   ├── Speed (Int + Xcvr + Speed)      → viewSpeed()
│   ├── Po (Int + Po)                   → viewPo()
│   ├── Mode (Int + Po + Mode)          → viewMode()
│   ├── Vlan (Int + Po + Mode + Vlan)   → viewVlan()
│   └── Custom View...                  → showSheetAssistPanel()  (merged panel)
├── [separator]
├── Sheet Manager            → submenu
│   ├── ▤ Column & Formatting Manager → showDeviceDataUi()
│   ├── ▣ Change Sheet Name     → promptRenameSheet()
│   ├── [separator]
│   ├── ▦ Create Sheet Checkpoint → createTopologySnapshot()
│   ├── ▧ Restore Sheet Checkpoint → showRestoreWizard()
│   ├── [separator]
│   └── ▫ New Project — Reset All Data → showNewProjectDialog()
├── [separator]
├── Topology Manager            → submenu
│   └── ▥ Device Manager        → showDeviceManagerUi()
└── User Guide                  → openUserGuide()

--------------------------------------------------------------------------------
SECTION 6: UI MODES (Sidebar.html / initialMode)
--------------------------------------------------------------------------------

The Sidebar.html template is multipurpose, driven by template.initialMode:

  'topology'      — Main network topology visualizer (1600x900 dialog)
  'manager'       — Device Manager (600x800 dialog)
  'schema'        — Column & Formatting manager (700x800 dialog)
  'view_custom'   — Custom column visibility manager (now merged into SheetAssistPanel)

STANDALONE SHEET-SIDE PANELS (separate HTML files, not Sidebar modes):
  SheetAssistPanel.html — Merged Sheet View (490×420). Two-column layout combining
                          device visibility (left) and column view selector (right).
                          Opened via showSheetAssistPanel() or "Custom View..." menu item.

SHEET MANAGER PANEL (SheetAssistPanel.html):
  Light theme (white body, #f1f5f9 header). Modeless dialog, 490×420px, title "Sheet View".
  Two-column layout: col-left (230px, border-right) = Devices, col-right (flex:1) = Columns.
  Shared status bar at bottom. Minimize collapses panel-body and status-bar, sets height to 36px.

  GLOBAL SEARCH — single search bar between header and body (id=searchRow, input id=globalSearch).
    filterAll() filters both .device-item (by data-name/data-hostname) and .col-item (by data-label).
    Called on oninput; also called after renderDevices() and renderColItems(). Hidden on minimize.

  LEFT COLUMN — Devices:
    SHOW ALL / HIDE ALL bulk buttons. Scrollable device list.
    Each device row: eye icon → toggle(), device name → navigate().
    Eye and name use event.stopPropagation(); only their own handler fires.

  RIGHT COLUMN — Columns:
    SHOW ALL / HIDE ALL bulk buttons — same style as device panel (green/orange, btn-bulk classes).
    2-column grid (grid-template-columns: 1fr 1fr) of column type items with eye icons — same
    visual as device items (eye-open green = visible, eye-closed gray = hidden, line-through label).
    Every toggle auto-applies via applyCustomView(visibleKeys) instantly. No Apply button, no presets.
    Mandatory first item (always visible, dimmed, not clickable).
    Loads via getSchemaConfig() → getViewPreferences() on panel open; state kept in visibleKeys[].

GAS FUNCTIONS for Sheet View (Code.gs):
  showSheetAssistPanel()     — Opens SheetAssistPanel.html (490×420 modeless dialog, "Sheet View");
                               calls ensureOnChangeTrigger() + ensureOnOpenTrigger() in try/catch
  getSheetDeviceList()       — Returns [{name, hostname, isVisible, isArista}] — isVisible from SHEET_VIEW_HIDDEN; isArista = d.type !== 'non-arista'
  toggleSheetDevice(name)    — Updates SHEET_VIEW_HIDDEN, calls applyCustomView(getViewPreferences()) + refreshSheetRowVisibility()
  showAllSheetDevices()      — Clears SHEET_VIEW_HIDDEN; shows all device columns; refreshes rows
  hideAllSheetDevices()      — Writes all device names to SHEET_VIEW_HIDDEN; hides all device columns; refreshes rows

TOPOLOGY TOOLBAR (primary row):
  - Minimize/restore button
  - Refresh button (fetchData)
  - W/G/O/T number inputs (card width, device gap, cable offset, cable top margin)
  - Sort controls: SHEET | MODE | IP | ALPHA
  - Edit mode toggle (pencil icon)
  - Group rect mode toggle (rectangle icon)
  - Bold text toggle
  - Technology stack modal (BGP/VXLAN/EVPN toggles)
  - IP Config modal
  - PNG download (html2canvas)
  - Cabling export modal (openCablingModal):
      ⬡ Topology Patching List — Download Topology Patching (downloadCablingReport('netlist'))
                               — Copy CSV (copyCablingCsv()) / Copy Markdown (copyCablingMarkdown())
                                 both topology-order; sub-buttons sit below Download, 8px margin-top
      ◻ Device Patching List  — Download Device Patching (downloadCablingReport('device'))
  - Help/shortcuts modal
  - Dark/light theme toggle
  - Audit/validation mode toggle (shield icon)
  - Toolbar collapse toggle

TOPOLOGY TOOLBAR (secondary row):
  - View mode: ◎ Simple (holistic) | ▤ Linear | ✦ Flexible (physics/D3 force)
  - Search/filter input (supports VLAN tracing with "v<number>" syntax)
  - Strict search checkbox
  - Status text (device count, link count)

TOPOLOGY TOOLBAR (copy strip):
  - Shows after clicking a link/device
  - Split buttons: Device | Left | Right
  - Each has Copy (copies EOS config to clipboard) and View (opens config modal)

--------------------------------------------------------------------------------
SECTION 7: TOPOLOGY VIEW MODES
--------------------------------------------------------------------------------

LINEAR VIEW (default):
  - Device cards in fixed sheet order (left to right)
  - Each device shows a list of port rows
  - Color-coded cables drawn as SVG lines connecting matching ports
  - Suitable for cabling/patching documentation

DEVICE CARD COLUMN LAYOUT (port rows inside each node card):
  Grid: 62px 1fr 30px 40px 25px
  Columns: Et | Mode | Cat | Po | St
    Et   (62px) — interface name; left-aligned header + data (62px holds up to ~9-char names like "et6/66/66")
    Mode (1fr)  — sp_mode_ value (routed/trunk/access…); left-aligned; truncates with ellipsis
    Cat  (30px) — port category pill: P2P (violet) / GW (amber) / L2 (gray); centered
    Po   (40px) — port-channel ("Po1", "Po10", blank if none); left-aligned header + data
    St   (25px) — status dot: Design phase: connected=green(var(--success)), disconnected=gray(#94a3b8)+red-ring; Live phase (op-* override): op-up=green(#22c55e), op-down=amber(#f59e0b), op-errdisabled=red(#ef4444), op-disabled=gray(#94a3b8); centered
  Cat values: "P2P", "GW", "L2" (previously L2 showed "-"; now consistently labeled).
  Header alignment: .node-cols-header has border-left:3px solid transparent (matches
    port-row's border-left:3px) so Et/Mode/Po headers align with data rows.
    nth-child(1,2,4) overridden to justify-content:flex-start to match data alignment.
  Sort button "CAT" (id=btnSortIp, changeSort('priority')): sorts P2P→GW→L2 (was "IP").

SIMPLE (HOLISTIC) VIEW:
  - One node per device, rendered as a box
  - Lines between connected device pairs (one line per unique pair)
  - Group rects can be drawn over it to label logical groups
  - Suitable for high-level topology understanding

FLEXIBLE VIEW:
  - D3 force-directed graph
  - Nodes are draggable, positions are saved per session
  - Same visual style as Simple view but physics-based

DEVICE NODE LABELS:
  SPINE, LEAF, HARNESS, TRANSIT, DUT, IXIA, SPIRENT
  Each has a distinct background/text/border color scheme.

--------------------------------------------------------------------------------
SECTION 8: TOPOLOGY ENGINE (calculateGlobalTopology)
--------------------------------------------------------------------------------

Server-side function in Code.gs. Reads the full sheet data and headers.
Returns:
  mlagConfigPorts — Set<"DeviceName:PoX">  Port-Channels detected as MLAG config ports
  peerLinkPorts   — Set<"DeviceName:EtX" | "DeviceName:PoX">  MLAG peer-link ports
                    calculateGlobalTopology() returns an empty Set; populated by explicit
                    DEVICE_MLAG_PEERS override in getDeviceConfig() / applyVisualFormatting():
                    (1) Et-keyed entries added from globalLinkMap (direct links between peers);
                    (2) _extendPeerLinkPortsWithPo() then adds Po-keyed entries by scanning data
                        rows for the po_ value on the same row as each Et entry.
                    calculateConnectionBackgrounds() uses Po-keyed colorKeys when po_ is filled,
                    so both Et and Po keys are required for correct peer-link visual detection.
  mlagPeerMap     — Map<deviceName, partnerName>  MLAG pair lookup (both directions)
  globalLinkMap   — Map<"DeviceName:Port", {dev, port}>  full topology link index
  debugLogs       — Array<string>  diagnostic messages (not returned to client)

MLAG DETECTION (EXPLICIT ONLY — heuristic removed 2026-04-16):
  - calculateGlobalTopology() returns empty Sets for mlagConfigPorts, peerLinkPorts, mlagPeerMap
  - All three are populated exclusively by explicit DEVICE_MLAG_PEERS override code in
    getDeviceConfig() and applyVisualFormatting() — never from PO-count heuristics
  - poMap is still built by calculateGlobalTopology() (identifies shared PO bundles for mlagConfigPorts)

PEER-LINK DETECTION:
  - Direct link between two declared MLAG peers in globalLinkMap → Et-keyed peerLinkPorts entry
  - _extendPeerLinkPortsWithPo() adds matching Po-keyed entry (required for sheet visual styling)
  - detectMlagState() converts the Et port to Port-Channel form for mlag configuration peer-link

KEY HELPER: hasKey(setObj, key)
  - Case-insensitive Set/Map lookup
  - Always use hasKey() instead of .has() when device names are involved
  - Device names from the sheet are original-cased; keys in Sets use lowercase

DATA_VERSION polling:
  - getDataVersion() returns a timestamp from ScriptProperties
  - Client polls this every few seconds to detect sheet changes
  - On change: fetchData(false) is called to refresh topology without forcing

TOPOLOGY CACHE:
  - getTopologyData() caches the full topology JSON in CacheService.getUserCache()
  - Cache key: 'TOPO_' + DATA_VERSION
  - Invalidated on forceSync or when DATA_VERSION changes

--------------------------------------------------------------------------------
SECTION 9: SHEET REBUILD ENGINE (rebuildSheet)
--------------------------------------------------------------------------------

Core function: rebuildSheet(forcedOrderList, forcedSchemaList, applyFormatting)

STAGES:
  1. BACKUP — reads all existing data into memory (values + formulas)
  2. ANALYZE — maps existing data to device/attribute keys
  3. GENERATE — builds new Row 1 (device names), Row 2 (headers), body data
  4. COMMIT — writes to sheet (always runs)
  5. VALIDATION — applies dropdown validations (always runs)
  6. STRUCTURAL FORMATTING — font, alignment, borders, merges, adaptive column widths
                              (width = max(80, ceil(header.length * 7) + 16); Consolas 10px
                               ≈ 7px/char + 16px padding; auto-adapts to device name length)
  7. COLOR APPLICATION — device header colors (golden angle hslToHex),
                          connection backgrounds, conditional format rules

SYNC_STATUS PROGRESS (written to CacheService.getUserCache() at each stage):
  Phase 1/4: Reading sheet data  →  Phase 2/4: Writing to sheet
  Phase 3/4: Applying formatting →  Phase 4/4: Restoring column view  →  Sync complete
  Polled every 600ms via getSyncStatus() by three callers:
    - triggerSyncOnly() (Force Sync button)
    - saveSchemaChanges() — structural change path only (isStructuralChange=true)
    - saveDeviceChanges() — slow path only (isStructureDirty=true, calls processDeviceBatch)
  All three show global loading overlay during poll and hide it on completion/error.
  SCOPING NOTE (saveSchemaChanges): syncPollTimer is declared as `let` at function scope
  (null-initialized), NOT inside the withSuccessHandler callback, so the 30s schemaLockTimer
  timeout handler can clearInterval(syncPollTimer)+hideGlobalLoading() if server hangs.

POST-REBUILD VISIBILITY RESTORE (end of stage 7):
  rebuildSheet() calls refreshSheetRowVisibility() THEN applyCustomView(getViewPreferences()).
  ORDER IS CRITICAL: refreshSheetRowVisibility() calls ensureDummyColumn() first, inserting
  _sys_ at col 1 and shifting device columns to col 2+. applyCustomView() then reads the
  correct layout (col A = _sys_ always visible, cols B+ = device columns to show/hide).
  If applyCustomView() runs first (before _sys_ exists), col A is a device column that
  gets locked visible, and _sys_ is never created — the bug that caused force sync to
  delete col A without restore.
  Reason: clearContent() in stage 4 wipes _sys_ and all column hide state; without both
  calls in the correct order, _sys_ stays gone and row visibility is broken after every sync.
  NOTE: stage 4 clear chain must include .clearNote() — clearContent()+clearFormat() do NOT
  clear notes. Without it, the _sys_ note persists on col A after clear, then when
  ensureDummyColumn() inserts the new _sys_ column (pushing device data to col B), both
  col A (new _sys_) and col B (old col A with ghost note) show "Managed by TopoAssist".
  Wrapped in try/catch so a visibility error does not block the success toast.

SAFETY:
  - If new body data is empty but backup had rows → abort with error + rollback
  - On any error: rolls back to backup data
  - Orphaned columns (not in schema but in sheet) are detected client-side before sync;
    user sees confirm() with per-column data-loss warning; decision passed as deleteOrphans flag

DEVICE HEADER COLORS:
  - Uses golden angle (137.5°) hue rotation: hue = (200 + i * 137.5) % 360
  - Pastel: HSL with saturation=85, lightness=88

--------------------------------------------------------------------------------
SECTION 10: VISUAL FORMATTING ENGINE
--------------------------------------------------------------------------------

applyVisualFormatting():
  1. Reads data and headers from sheet
  2. Calls calculateGlobalTopology() for MLAG/peer-link detection
  3. Calls calculateConnectionBackgrounds() → returns color matrix + MLAG ranges
  4. Applies background color matrix to data range
  5. Applies black SOLID_MEDIUM borders to MLAG cells
  6. Calls buildConditionalRules() → applies all conditional format rules
  7. Sets Consolas font, size 10, middle/center alignment

CONNECTION COLORS (calculateConnectionBackgrounds):
  - Pairs up columns in order: every 2 filled int_ cells = one cable pair
  - Physical links: pastel color derived from port name hash
  - Port-Channel (regular): distinct pastel color from Po number
  - MLAG Port-Channel: different pastel + black border
  - Peer-Link: separate color
  - Orphan (unpaired): white (#ffffff)

CONDITIONAL FORMAT RULES (buildConditionalRules) — 6 priority tiers (first = highest):
  Priority 1 — AUDIT RED (#fca5a5/#991b1b): value in a field that is N/A for current mode/interface
    A1: po_ filled + ET mode (^l[23]-et)
    A2: svi_vlan_ filled + l3-* mode
    A3: ip_type_ filled + l2-* mode + no SVI
    A4: vlan_ has 2+ VLANs (contains comma or dash) + pure l3-* non-sub-int
        (single VLAN on l3-et/l3-po is NOT flagged — needed for IP derivation)
    A5: vrf_ filled + l2-* mode + no SVI
    A6: xcvr_speed_ filled + xcvr_type_ missing → RED on xcvr_speed_
    A-Vx: int_ starts with ^Vx → RED on any of et_speed_, xcvr_speed_, xcvr_type_,
          encoding_, sp_mode_, po_, ip_type_ when filled (Vx1 is a virtual VTEP port;
          none of these fields apply). Paired with N/A-5 and N/A-Vx gray rules —
          gray shows when empty, red overrides when user fills the field.
  Priority 2 — MISSING AMBER (#fef3c7/#92400e):
    A7: sp_mode_ empty when int_ is filled (mode required; Vx1 rows excluded)
    A8: vlan_ empty when int_ filled AND mode is set (vlan_ required for ALL modes)
  Priority 3 — INACTIVE GRAY (#c8d5e3/#94a3b8, same as N/A GRAY):
    N1: int_ empty → all SCHEMA_KEYS columns except int_ itself (int_ stays white —
        it is the fillable entry point; single multi-range rule per device)
  Priority 4 — N/A GRAY (#c8d5e3/#94a3b8): field not applicable for current mode/interface
    N/A-1: svi_vlan_ gray when mode is l3-* (A2 overrides RED when filled)
    N/A-2: po_ gray when mode is l2-et-* or l3-et-* (A1 overrides RED when filled)
    N/A-3: ip_type_ gray when mode is l2-* + no SVI (A3 overrides RED when filled)
    N/A-4: REMOVED — vlan_ is needed for ALL modes (IP derivation on l3-et/l3-po,
           dot1q tag on sub-int). A4 still catches 2+ VLANs on pure l3.
    N/A-5: et_speed_, xcvr_speed_, xcvr_type_, encoding_ gray when int_ starts ^Vx
           (virtual VTEP port; no physical transceiver). A-Vx overrides RED when filled.
    N/A-Vx: sp_mode_, po_, ip_type_ gray when int_ starts ^Vx.
           A-Vx overrides RED when filled.
    N/A-6: vrf_ gray when mode is l2-* + no SVI (A5 overrides RED when filled)
  Priority 5 — QUALITY WARNINGS:
    W1: REMOVED — was amber when et_speed_ === xcvr_speed_; removed because equal speeds
        is correct for native (non-breakout) ports and caused noise on every standard port.
    W2: xcvr_speed_ pale-orange (#fed7aa) when empty + xcvr_type_ set + et_speed_
        implies a different speed tier (hint to fill xcvr_speed_ for breakout)
  Priority 6 — MEMBER PORT: italic + muted text on int_ when int_ starts ^Et AND po_ set
  SNAKE-ROW (blue #dbeafe/#1e40af): when snake_int_<dev> is filled, all SCHEMA_KEYS columns
    for that device turn blue (multi-range rule, same allDevRanges pattern as INACTIVE GRAY).
    A-SNAKE RED (priority 1) and N/A-SNAKE GRAY (priority 4) override blue per-column.
  A-SNAKE RED (#fca5a5/#991b1b): when snake_int_ filled AND ip_type_/vrf_/svi_vlan_ not empty
    → RED on that column (fields are auto-managed for snake ports — user should not fill them).
  N/A-SNAKE GRAY (#c8d5e3/#94a3b8): when snake_int_ filled → ip_type_, vrf_, svi_vlan_ gray
    (auto-forced to p2p/SNAKE_VRF/no-SVI; these fields are N/A regardless of mode).

IMPORTANT: After editing buildConditionalRules, user must click Force Sync
(toolbar button in the sidebar) for conditional format changes to take effect.

--------------------------------------------------------------------------------
SECTION 11: CONFIG GENERATION
--------------------------------------------------------------------------------

Entry point: generateConfig(portName, d, ipPrefs, seenPos, netSettings)
  portName    — interface name (e.g. "Et1", "Po10", "Et1.100")
  d           — data object with all attribute values for that row
  ipPrefs     — IP preferences (base subnets, VRFs, etc.)
  seenPos     — Set of already-generated Port-Channels (prevents duplicates)
  netSettings — IP family flags object from getNetworkSettings(); passed to
                generateComplexL3Block() to gate IPv6/OSPF/unnumbered commands.
                INVARIANT: MUST be passed by every caller. Omitting silently drops
                per-protocol commands (IPv6 addressing, OSPF per-interface, etc.).

DESCRIPTION LOGIC (10-rule system):
  Rule 1: Manual override (d.desc_ is set) → use as-is
  Rule S: Snake self-loop (isSnakePrimary||isSnakeSecondary) → "-> SNAKE-PeerDev-PeerPort" (checked before Rules 2-4)
  Rule 2: Regular physical → "-> PeerDev-PeerPort"
  Rule 3: Physical peer-link (standalone) → "-> PeerDev-PeerPort-MLAG-PEER-LINK"
  Rule 4: Physical child of regular Po → "-> PeerDev-PeerPort-PoX"
  Rule 5: Po (regular) → "-> PeerDev [& PeerDev2]"
  Rule 6: Physical child of MLAG Po → "-> PeerDev-PeerPort-PoX"
  Rule 7: MLAG Po → "-> PeerDev & PeerDev2 (MLAG)"
  Rule 8: Physical peer-link child of Po → "-> PeerDev-PeerPort-PoX"
  Rule 9: Po peer-link → "-> PeerDev (MLAG-PEER-LINK)"

INVALID CONFIG GUARD:
  MLAG + any L3 mode (l3-*) is INVALID on Arista EOS.
  generateConfig() returns an ERROR comment block instead of real config.
  generateComplexL3Block() also guards: returns "" for all l3-* modes with MLAG.

OSPF UNDERLAY (generateOSPF + generateOSPFv3):
  generateOSPF(deviceSheetIndex, bgpNeighbors) — OSPFv2 (router ospf 1):
  - Generates OSPF process blocks for all VRFs (area 0, single-area leaf-spine).
  - router-id: <id>.<id>.<id>.<id> (same loopback pattern as BGP); shared across
    all VRF OSPF instances (EOS leaf-spine convention).
  - VRF grouping: non-MLAG P2P interfaces are grouped by p.vrf from peerParams.
    DEFAULT VRF → router ospf 1
    Non-default VRF → router ospf 1 vrf <name>  (one block per VRF, sorted)
    All blocks use: passive-interface default + no passive-interface per interface,
    bfd default, max-lsa 12000. All emitted in a single 090_OSPF_UNDERLAY string.
  - Vlan4093 always added to the DEFAULT VRF set when an MLAG peer exists.
  - Guard: called only when netSettings.ospf_ipv4 is true.

  generateOSPFv3(deviceSheetIndex, bgpNeighbors) — OSPFv3 (router ospf3 1):
  - Mirrors generateOSPF() exactly but uses "router ospf3 1" keyword.
  - Emitted as 091_OSPF3_UNDERLAY block (between OSPFv2 and BGP blocks).
  - Guard: called only when netSettings.ospf_ipv6 is true.

  Per-interface OSPF commands (generateComplexL3Block):
  - OSPFv2 (addOspfV4 = netSettings.ospf_ipv4): ip ospf network point-to-point,
    ip ospf area 0.0.0.0, ip ospf neighbor bfd — on all P2P modes:
      l2-* with svi_vlan_: commands on each Vlan<x> SVI
      -sub-int modes:       commands on each <port>.<vlan> sub-interface
      l3-et-int / l3-po-int: commands on the routed interface
  - OSPFv3 (addOspfV6 = netSettings.ospf_ipv6): ipv6 ospf 1 area 0.0.0.0
    added on same P2P interfaces.
  - No VRF guard — VRF interfaces are covered by per-VRF process blocks.
  - MLAG guard: generateOSPF/generateOSPFv3 called only when hasP2pForOspf —
    at least one non-MLAG peerParam. MLAG-only devices skip (avoids dead process).

BGP EVPN OVERLAY ONLY (generateBGPEvpnOverlay(deviceSheetIndex, deviceName, bgpNeighbors, gwVlans, peerRoles, settings, ipPrefs)):
  - Used when OSPF is underlay + EVPN is enabled
  - Generates overlay-only BGP: loopback-to-loopback peers, no P2P sessions
  - Peer groups: OVERLAY_<PEER>_V4_LO_IP with update-source Loopback0 + ebgp-multihop 3
  - address-family evpn only (no ipv4/ipv6 address families)
  - MLAG partners are filtered out (isMlagOnly check): per EOS design, MLAG peers do
    NOT have BGP EVPN sessions with each other — EVPN sessions are spine-only; the
    MLAG peer's Vlan4093 session (underlay only) is handled separately.
    bgpNeighbors always contains the MLAG partner (added by generateMlagConfig), so
    the filter is required to prevent a spurious loopback EVPN session between peers.
  - peerRoles filtering: isPeerEvpn() helper — only LEAF/SPINE peers get overlay sessions;
    all other roles (HARNESS, unset, etc.) are skipped; call site passes peerRoles from getDeviceConfig().

AP VLAN GW (int_ = vx1):
  AP VLANs (wireless SSID VLANs from APs tunneling VXLAN to the GW) are VXLAN-domain
  only — no physical port carries them. User places "vx1" in the int_ column.
  Multiple vx1 rows allowed — one per VRF/VLAN group.
  Only vlan_ and svi_ fields are evaluated for config; vrf_ used for SVI vrf line.
  ip_type_ is NOT used for vx1 rows.

  MLAG + AP VLAN GW: fully supported. MLAG AP VLAN GW pairs share the same anycast VTEP
  (Loopback1) and must have identical vx1 rows (same VLANs/VRFs on both peers).
  EVPN distributes SSID VLANs leaf-to-leaf via Type-2/Type-3 routes; spines are
  underlay-only and never appear in the VXLAN data plane.

  collectDeviceData() returns: { allVlans, gwVlans, vx1Vlans, vrfs, hasP2p }
    - allVlans: L2 rows (isL2=true) + Vx1 rows → system VLAN table (vlan N name stanza)
    - gwVlans:  Vx1 rows ONLY (local + MLAG peer) → VXLAN VNI mapping + BGP EVPN
      L2 trunk/access rows (including ip_type_=gw-*) do NOT feed gwVlans.
      L2 trunk rows with ip_type_=gw generate regular SVI IP (not anycast/virtual);
      they must not appear in VXLAN VNI mappings. The Vx1 row is the sole authoritative
      declaration of what gets VXLAN-extended (both L2 VNIs and L3 VNIs).
    - vx1Vlans: Vx1 rows ONLY → VTEP/Lo1 generation guard (zero = no Vx1 row = no VTEP).
    - vrf_: collected into vrfs set via existing analyzeRow() (system block creates VRF).
    - MLAG peer Vx1 rows: handled explicitly for both gwVlans and vx1Vlans because
      ip_type_ being empty on peer vx1 rows would cause analyzeRow() to skip them.

  getDeviceConfig() interface loop — Vx1 pre-collection (before main rows.forEach):
    - ALL vx1 rows collected into vx1Entries[] before main loop runs.
    - "Vx1" pre-added to deviceSeenPos → main loop skips all Vx1 rows cleanly.
    - Each entry: if svi_=yes, generates one interface VlanX block per VLAN.
    - vrf_ included as " vrf <name>" line if set (after "interface VlanX", before description).
    - All SVI blocks concatenated into configMap["050_Vx1"] ("AP VLAN SVI").
    - If svi_=no or vlan_ empty: VLANs still feed gwVlans; no SVI block generated.

  SVI format (vlan 510, vrf TENANT_A, gw_v4_first=100, gw_v4_last=1, anycast mode, gw_ipv6=true):
    interface Vlan510
     vrf TENANT_A
     description ANYCAST_GW_TENANT_A_510     ← ANYCAST_GW_<vrf>_<vlan> when VRF set
     ip address virtual 100.5.10.1/24        ← anycast (gw_l3_type='anycast')
     default ipv6 address virtual            ← only when gw_ipv6=true
     ipv6 address virtual 100:5:10::1/64    ← only when gw_ipv6=true
     no shutdown

  SVI format (vlan 510, no VRF, VARP mode):
    interface Vlan510
     description ANYCAST_GW_510              ← ANYCAST_GW_<vlan> when no VRF
     ip virtual-router address 100.5.10.1    ← VARP (gw_l3_type='varp')
     no shutdown

  GW type is controlled by gw_l3_type in network settings:
    anycast (default): ip address virtual  — requires EVPN enabled
    varp:              ip virtual-router address — requires ip virtual-router mac-address global
  vx1VlanSet: built from all vx1 SVI VLAN claims; passed to generateComplexL3Block() so
    front-panel SVIs for the same VLAN are skipped (vx1 takes precedence).

  VNI mapping (handled by 055_VXLAN block via gwVlans, VNI base from IP+VNI Config):
    vxlan vlan 510 vni <vni_base + 510>  (default base=10000 → vni 10510)

VTEP LEAF DETECTION & STATIC FLOOD LIST:
  computeVtepNames(allDevices, rows, headers, topo) → Set<string>:
    Scans all Arista devices; a device is a VTEP iff role === 'LEAF' || role === 'SPINE'.
    All other roles (HARNESS, unset, etc.) are never VTEPs.
    Called once in getDeviceConfig() when isVxlan=true; result passed to generateVxlanBlock().

  generateVxlanBlock(..., vniBase, vtepNames):
    Signature now includes vtepNames (Set<string>) as last param.
    Static flood list (non-EVPN only): filters allDevices to vtepNames members only.
    Guard: if vtepNames is empty/null, falls back to all Arista devices (safe default).
    MLAG peer always excluded from flood list (shared anycast IP deduped via Set).

MLAG INFRASTRUCTURE BLOCK (generateMlagConfig(localId, partnerObj, peerLinkName, bgpNeighbors, isVxlan, settings)):
  VLANs:
    - vlan 4094: trunk group MLAG_PEER (control plane, restricted to peer-link)
    - vlan 4093: trunk group MLAG_PEER (L3 peering, ALSO restricted to peer-link)
  SVIs:
    - Vlan4094: no autostate + ip address 169.254.0.x/31
    - Vlan4093: no autostate + ip address 169.254.1.x/31 + (ipv6 address when hasMlagIpv6)
      hasMlagIpv6 = !settings || settings.int_ipv6 || settings.int_ipv6_unnum
      (both SVIs need no autostate so they stay up during failover/reload)
    - Vlan4093 when settings.ospf_ipv4=true: adds ip ospf network point-to-point +
      ip ospf area 0.0.0.0 so OSPF adjacency forms directly between MLAG peers
      (Winnow/EOS confirmed required)
  reload-delay order: mlag 300 first, non-mlag 330 second (non-mlag must be higher
    so MLAG ports are established before non-MLAG ports come up)
  Virtual-router MAC: 0011.2233.<hexId> dotted-hex format, derived from lower peer ID.

IP ADDRESSING (VLAN-DERIVED):
  Middle octets derived from VLAN ID.
  Example: VLAN 1050 → .10.50
  Last octet derived from device DID (sequential Arista device index).
  IPv6: intentionally uses same pattern as IPv4 (not strict EOS syntax) — LAB ONLY.

  MLAG GW physical IP formula (generateComplexL3Block, ip_type_=gw, isMlag=true):
    phySuffix = gwLastV4 + sheetIndex  (same for V6)
    virtual-router last octet = gwLastV4 (same on both peers)
    Guarantees: (a) physical ≠ virtual-router since sheetIndex ≥ 1
                (b) physical IPs unique between peers since sheetIndex is unique
    Example: gwLastV4=1, Leaf1(id=1)→.2, Leaf2(id=2)→.3, virtual-router→.1
    Previous bug: phySuffix=sheetIndex bumped by +1 only when ==gwLastV4 — caused
    both peers to get the same physical last octet (e.g. Leaf1→.2, Leaf2→.2).

INTERFACE NAMING (Arista EOS):
  Et  → Ethernet
  Po  → Port-Channel
  Vl  → Vlan (SVI)
  Lo  → Loopback
  Ma  → Management

canonicalizeInterface():
  Normalizes interface abbreviations (e.g. "eth1" → "Et1", "po1" → "Po1").
  DUPLICATED INTENTIONALLY in both Code.gs (server) and Sidebar-js.html (client).

IP + VNI CONFIG MODAL (#ipConfigModal):
  Title: "IP + VNI Configuration". Button tooltip: "IP & VNI Config".
  Size: modal-medium (450px). Body: padding 16px, gap 12px.
  Sections: Point-to-Point, Gateway (SVI/Overlay), VXLAN VNI Mapping.
  IPv4 + IPv6 rows within each section wrapped in .ip-section-rows (gap:4px) so
  they cluster tightly together; section-to-section gap remains 12px.
  Info-box has legend grid (formula) + .ip-example line below it:
    "ex VLAN 1500 → octs 15.0 · P2P: 200.15.0.id · GW: 100.15.0.1 · VNI: 10000+1500=11500"
    (demonstrates oct2=floor(vlan/100), oct3=vlan%100 derivation)
  Field ip_vni_base (VNI Base Offset, default 10000).
  openIpConfigModal() populates ip_vni_base from prefs.vni_base.
  saveIpConfig() includes vni_base in the prefs object sent to saveIpPreferences().
  resetIpConfig() resets ip_vni_base to "10000".

IP PREFERENCES STORAGE:
  - Stored in UserProperties (per user, per spreadsheet)
  - getIpPreferences() / saveIpPreferences()
  - Keys: p2p_v4_first, p2p_v4_mask, p2p_v6_first, p2p_v6_mask,
          gw_v4_first, gw_v4_last, gw_v4_mask, gw_v6_first, gw_v6_last, gw_v6_mask,
          vni_base (default '10000')

NETWORK FEATURE FLAGS (IP FAMILY PER PROTOCOL):
  - Stored in DocumentProperties (shared, per-spreadsheet)
  - getNetworkSettings() / saveNetworkSettings()
  - 16 boolean flags (stored as 'true'/'false' strings):
      P2P INTERFACE: INT_IPV4 (default true), INT_IPV6, INT_IPV6_UNNUM
      GATEWAY:       GW_IPV4 (default true), GW_IPV6
      BGP:           BGP_IPV4, BGP_IPV6, BGP_IPV6_UNNUM, BGP_RFC5549
      OSPF:          OSPF_IPV4, OSPF_IPV6, OSPF_IPV6_UNNUM
      VXLAN:         VXLAN_IPV4, VXLAN_IPV6
      EVPN:          EVPN_IPV4, EVPN_IPV6
  - Legacy migration: reads old NET_UNDERLAY/NET_VXLAN/NET_EVPN; INT_IPV4/GW_IPV4
    default true (all legacy projects had IPv4 always); GW_IPV6 defaults to int_ipv6.
  - P2P vs GW are FULLY DECOUPLED in generateComplexL3Block():
      P2P: p2pHasIpv4 (int_ipv4), p2pUseIpv6Unnum (int_ipv6_unnum),
           p2pUseIpv6Explicit (!unnum && int_ipv6)
      GW:  gwHasIpv4 (gw_ipv4), gwHasIpv6 (gw_ipv6)
      GW is ip_type_=gw — applies to SVI (l2-* with svi_vlan_), sub-interfaces
      (-sub-int), and L3 routed ports (l3-et-int / l3-po-int). GW NEVER uses
      IPv6 unnumbered — explicit IPs required for VARP.
  - IPv6 routing gates:
      hasP2pIpv6 = int_ipv6 || int_ipv6_unnum — gates Lo0 IPv6 addr, ipv6 router-id
      hasAnyIpv6 = hasP2pIpv6 || gw_ipv6     — gates VRF ipv6 unicast-routing
      generateGlobalBlock: ipv6 unicast-routing gated on int_ipv6||int_ipv6_unnum||gw_ipv6
  - BGP_RFC5549: gates pgV6Int peer groups; valid for any IPv6 P2P mode (link-local
    always present on any IPv6-enabled interface, including unnumbered).
  - addVtepIpv6 in generateVxlanBlock: gated on vxlan_ipv6 ONLY (not ||int_ipv6).
  - Feature derivation in config engine:
      isBgp  = bgp_ipv4 || bgp_ipv6 || bgp_ipv6_unnum || bgp_rfc5549
      isOspf = ospf_ipv4 || ospf_ipv6 || ospf_ipv6_unnum
      isVxlan = (vxlan_ipv4 || vxlan_ipv6) && isVxlanDevice
      isEvpn  = (evpn_ipv4  || evpn_ipv6)  && isEvpnDevice
  - Cross-validation (validateTechSettings() in Sidebar-js.html — 4 rules):
      1. gw_ipv6 + no IPv6 routing protocol (bgp_ipv6/rfc5549/bgp_unnum/ospf_ipv6/
         ospf_ipv6_unnum/evpn_ipv4/evpn_ipv6) → error
      2. vxlan_ipv4 + no IPv4 underlay (bgp_ipv4/bgp_unnum/rfc5549/ospf_ipv4) → error
      3. vxlan_ipv6 + no IPv6 underlay (bgp_ipv6/bgp_unnum/rfc5549/ospf_ipv6/
         ospf_ipv6_unnum) → error
      4. evpn (any) + no BGP (any variant) → error
    Errors shown in #techValidationErrors banner; Save button disabled.
    BGP Unnum and RFC5549 count for IPv4 underlay (both negotiate ipv4 unicast AF).
  - UI: Tech modal (#techModal) — width 580px:
      GATEWAY (top): chkGwIpv4, chkGwIpv6 — always enabled, no protocol gating
      P2P INTERFACE: radio group (name=p2pMode) — 5 values:
        ipv4 / ipv6 / ipv6unnum / ipv4ipv6 / ipv4ipv6unnum
      Protocol grid (4 cols, gated by P2P mode):
        BGP: chkBgpIpv4, chkBgpIpv6, chkBgpIpv6Unnum, chkBgpRfc5549
        OSPF: chkOspfIpv4, chkOspfIpv6, chkOspfIpv6Unnum
        VXLAN: chkVxlanIpv4, chkVxlanIpv6
        EVPN: chkEvpnIpv4, chkEvpnIpv6
      TECH_COMPAT (5 entries) + P2P_MODE_FLAGS (5 entries, radio→{int_ipv4,int_ipv6,
        int_ipv6_unnum}) defined as module-level constants in Sidebar-js.html.
  - Config blocks: 090_OSPF_UNDERLAY (ospf_ipv4), 091_OSPF3_UNDERLAY (ospf_ipv6)
    both between VXLAN and BGP sections. generateConfig() 5th param (netSettings)
    passed from BOTH getDeviceConfig() AND getTopologyData() (as globalNetSettings).

GLOBAL CONFIG TEMPLATE:
  - Stored in DocumentProperties under key 'GLOBAL_DEVICE_CONFIG_TEMPLATE'
  - Edited via sidebar toolbar button (lines/magnifier SVG, between IP Config and VLAN Summary)
  - Applied as a base template before per-device config generation (via generateGlobalBlock())
  - Sidebar modal #globalCfgModal (modal-std modal-medium): openGlobalCfgModal() loads
    current config via getGlobalConfig(), shows textarea. saveGlobalCfgFromSidebar() saves
    via saveGlobalConfig(), then clears configCache (sole config store) +
    refreshes active device config immediately (same pattern as saveTechSettings).
    closeGlobalCfgModal() hides modal + overlay. No Close button in header — footer Cancel
    is the only dismiss control (avoids duplicated close affordances).
  - Find bar (.gcfg-find-bar) sits between modal-header and textarea: search input
    (#globalCfgSearch), match counter (#globalCfgSearchCount), Prev/Next chevron SVG buttons.
    globalCfgSearchUpdate() builds _gcfgMatches[] via indexOf loop. _gcfgJumpTo(idx) uses
    setSelectionRange + scrollTop calculation (linesBefore × lineHeight). Enter=next,
    Shift+Enter=prev, Escape=clear. openGlobalCfgModal() resets all find state on open.
  - Editor area always light mode: wrapper bg #f8fafc, textarea bg #ffffff, color #1e293b,
    border #cbd5e1 (focus: #3b82f6). Hardcoded — not CSS vars — so readable in both themes.
  - globalCfgModal registered in modalOrder, closeFuncs (topologyKeyHandler), and
    closeAllModals() array so Escape and closeAllModals() dismiss it correctly.

--------------------------------------------------------------------------------
SECTION 12: DEVICE MANAGER
--------------------------------------------------------------------------------

showDeviceManagerUi() → 600x800 modeless dialog in 'manager' mode.

HEADER: flex row — "Device Manager" label (left) + ✕ close button (.reorder-header-close)
  on right. Close triggers attemptCloseManager() (warns on unsaved changes).

ADD DEVICES:
  - Text input supports individual names ("Leaf1") or ranges ("Leaf[1-4]")
  - parseAndExpandDevices() handles range expansion with zero-padding support
  - Max 50 devices total, max range size of 50
  - Device types: 'full' (Arista) or 'non-arista'

DEVICE ORDER:
  - Devices can be reordered via SortableJS drag-and-drop
  - Order saved to Document Properties and used for sheet column order
  - On reorder, orphaned schema columns are always preserved (no prompt)
  - Drag resets pendingDeleteIndex to -1 (cancels any in-flight delete confirmation)

DEVICE VISIBILITY (3 independent mechanisms — see Section 25 for full detail):
  - Sheet View (temporary): SHEET_VIEW_HIDDEN UserProperties — hides sheet columns only
  - Topology View (temporary): in-memory JS Set — hides topology nodes only
  - Device Manager (permanent): TOPOLOGY_HIDDEN_DEVICES ScriptProperties — persistent node visibility

DEVICE DID (Device ID):
  - Sequential integer assigned to each Arista device
  - Used for last-octet IP derivation
  - Non-Arista devices do not get a DID

INLINE RENAME (promptRenameDevice / applyDeviceEdit):
  - Clicking ✎ inserts a <li id="deviceEditRow"> after the target row with two inputs:
    DNS Name (required) and Hostname (optional)
  - Apply button → applyDeviceEdit(index): validates, updates currentDeviceList, calls
    closeDeviceEditor() + checkForChanges() + renderManagerList(), then scrolls the
    re-rendered item into view via scrollIntoView({block:'nearest',behavior:'smooth'})
    so the user can see the updated device without manually scrolling back
  - Cancel button / Escape → closeDeviceEditor() removes the editor row without commit

INLINE DELETE (removeDeviceFromList / confirmRemoveDevice / cancelRemoveDevice):
  - State variable: pendingDeleteIndex (module-level, default -1)
  - Clicking ✕ sets pendingDeleteIndex = index and calls renderManagerList()
  - Affected row renders with red-tinted background + outline; delete cell shows
    ✓ (red confirm) and ✕ (cancel) buttons instead of the trash button
  - ✓ → confirmRemoveDevice(): splices list, resets index, checkForChanges(), re-renders
  - ✕ → cancelRemoveDevice(): resets index, re-renders
  - No window.confirm() used anywhere in delete flow

getExistingDevices():
  Reads current devices from sheet Row 1 (device name headers).
  Returns array of { name, type, isVisible, labels, did }.

getNonAristaList():
  Returns list of explicitly registered non-Arista device names.
  Stored in DocumentProperties under 'NON_ARISTA_DEVICES'.

--------------------------------------------------------------------------------
SECTION 13: SCHEMA MANAGER
--------------------------------------------------------------------------------

showDeviceDataUi() → 700x800 modeless dialog in 'schema' mode.

Schema is stored as a JSON array in DocumentProperties under 'SCHEMA_CONFIG_ARRAY'.
Falls back to DEFAULT_SCHEMA_ARRAY if missing/corrupt.

getSchemaConfig() / saveSchemaConfig(newArray) / resetSchemaToDefaults()
resetSchemaToDefaults(): confirm dialog clarifies "resets column definitions only — sheet data not affected".

SCHEMA MODAL UI (schemaContainer, #schemaBody):
  Close button: btn-mono (matches all other modals).
  Add-row inputs: "Field key (e.g. rack_id)" + "Display label" placeholders.
  Footer (modal-actions, 3 footer-groups, ui-equal-size=128px):
    Left:   Reset Defaults (btn-danger)
    Center: "Apply Formatting" checkbox (chkApplyFormatting, title explains connection
            colors/conditional rules/MLAG borders/speed coding) + Check Sync (SVG
            magnifier) + Force Sync (SVG refresh)
    Right:  Save & Sync (btn-success, orange+asterisk when dirty, green when clean)
  Footer buttons use inline SVG icons (13×13) — no emoji.
  modified-highlight: border-bottom 2px solid #f59e0b + background rgba(245,158,11,0.08)
    (dark-mode compatible; previously hardcoded #fffbeb cream).

Schema columns can be:
  - Reordered (affects sheet column order after sync)
  - Have custom dropdown options added/removed
  - Have columns added (custom attributes)
  - Have columns deleted (with orphan safety prompt during sync)

syncSchemaPreservingOrder(optionalForcedSchema, applyFormatting, deleteOrphans):
  Main sync function. deleteOrphans: true=delete orphaned cols, false=keep, null=ui.alert fallback.
  Callers (triggerSyncOnly, saveSchemaChanges) pre-check via getOrphanedColumnsInfo() and
  pass explicit deleteOrphans so the sidebar loading overlay never hides the prompt.
  Then calls rebuildSheet() to apply the new structure.

auditSchemaVsSheet():
  Returns { extra, missing, conflicts } comparing expected headers vs actual.
  conflict detection is done client-side (conflicts array is always empty from server).
  IMPORTANT: actualHeaders filters out DUMMY_VIS_HEADER (_sys_) before comparison,
  so the system column is never flagged as "Extra" in the Column Sync status.

--------------------------------------------------------------------------------
SECTION 14: COLUMN VISIBILITY (VIEW MODES)
--------------------------------------------------------------------------------

SYSTEM VISIBILITY COLUMN (_sys_):
  DUMMY_VIS_HEADER = '_sys_' constant in Code.gs.
  _sys_ is ALWAYS VISIBLE at col A — never hidden. Eliminates all "can't hide all columns"
  GAS errors. Col A stays clean (no bullet markers written — col is empty except headers).
  On creation, ensureDummyColumn sets:
    - Row 1, col 1: '↕' (hint label)
    - Row 2, col 1: '_sys_' (detection header)
    - Cell note: 'Managed by TopoAssist — do not edit. This column tracks row visibility
      and is required for the hide/show feature to work correctly.'
    - Column width: 36px
  ensureDummyColumn(sheet): ensures _sys_ exists at col 1. No hide logic.
    - If col 1 header already equals _sys_: skips insert but still applies gray styling.
    - Otherwise: insertColumnBefore(1), sets headers/note/width, then applies gray styling.
    - Always sets background #e2e8f0 on rows 1..max(lastRow,3) of col 1 so the column is
      visually marked as reserved regardless of whether it was just created or already existed.
  _applyDeviceCols() — REMOVED. toggleSheetDevice now uses applyCustomView(getViewPreferences())
    for both hide and show, same as column toggle. This ensures device show/hide respects the
    current column type view without stale mode-string logic.
  refreshSheetRowVisibility(): calls ensureDummyColumn() FIRST (before reading headers),
    then re-reads lastCol and headers. Scans visible int_ columns; computes toShow/toHide
    row lists; hides/shows rows via _batchRowOp(). Does NOT write anything to col A.
  Device columns always start at col 2+. applyCustomView skips col 1 (_sys_) via colIdx===1 guard.

viewShowAll():
  Calls saveSheetViewHidden([]) + applyCustomView(all schema keys) + refreshSheetRowVisibility().
  Clears all Sheet View hidden devices AND shows all column types AND refreshes row visibility.
  Menu label: "Show All Columns/Devices".

Sheet View menu items — all use applyCustomView() (same as Sheet View panel):
  viewCabling()     → applyCustomView(['int'])                              + showSheetAssistPanel()
  viewTransceiver() → applyCustomView(['int','xcvr'])                       + showSheetAssistPanel()
  viewSpeed()       → applyCustomView(['int','xcvr','et_speed','xcvr_speed'])+ showSheetAssistPanel()
  viewPo()          → applyCustomView(['int','po'])                         + showSheetAssistPanel()
  viewMode()        → applyCustomView(['int','po','sp_mode'])               + showSheetAssistPanel()
  viewVlan()        → applyCustomView(['int','po','sp_mode','vlan'])        + showSheetAssistPanel()

  Sidebar presets (loadViewPreset in Sidebar-js.html) mirror these 6 keys:
    cabling / transceiver / speed / po / mode / vlan

setColumnVisibility() — REMOVED. All callers now use applyCustomView().
  showAllSheetDevices(): saveSheetViewHidden([]) + applyCustomView(getViewPreferences()) + refresh
  hideAllSheetDevices(): saveSheetViewHidden(all) + applyCustomView(getViewPreferences()) + refresh

Custom View (merged into SheetAssistPanel.html right column):
  - No longer a separate dialog; now the right column of the Sheet View panel (490×420).
  - SHOW ALL / HIDE ALL buttons — same style as device panel; no presets.
  - Key mapping used by menu view functions:
      cabling → ['int']
      po      → ['int', 'po']
      mode    → ['int', 'po', 'sp_mode']
      vlan    → ['int', 'po', 'vlan']
      xcvr    → ['int', 'xcvr']
  - applyCustomView(selectedKeys):
      A. Saves selectedKeys to CUSTOM_VIEW_PREFS (UserProperties) + SHEET_VIEW_MODE='CUSTOM'.
      B. Reads row-2 headers; computes target[i] (true=show, false=hide) per column:
           col 1 (_sys_) → always true
           column type:  header must start with one of selectedKeys + '_'
           device:       header must NOT end with '_' + a hiddenDevs entry
      C. Applies show/hide in batched consecutive runs (NO blanket showColumns reset) to
         avoid the all-columns-expand flash — each run calls
         sheet.showColumns(start, count) or sheet.hideColumns(start, count) once.
      Does NOT call showSheetAssistPanel() — panel callers keep the panel open; menu callers
      (viewIntOnly etc.) call showSheetAssistPanel() explicitly after applyCustomView.
  - showCustomViewUi() is DEAD CODE in Code.gs — no callers exist anywhere; menu item calls showSheetAssistPanel() instead. Safe to delete.

--------------------------------------------------------------------------------
SECTION 15: SNAPSHOTS / CHECKPOINTS
--------------------------------------------------------------------------------

createTopologySnapshot():
  - Prompts user for a name via ui.prompt()
  - Creates tab named SNAP_<unixMs>_<safeName>
  - Rows 1-2 = metadata; rows 3+ = original sheet content (formulas → static)
  - Tab color: orange

getSnapshotList():
  - Returns [{name, label, dateStr}] — newest first
  - label = parts after SNAP_<ts>_ joined by underscore
  - dateStr = new Date(ts).toLocaleString()

getSnapshotDiff(snapshotName):
  - Snapshot structure: row 1-2 = metadata, row 3 = device names,
    row 4 = int_ headers, rows 5+ = data
  - Reads live row 2 and snapshot row 4 for device list (int_ headers)
  - Data comparison: live rows 3+ vs snapshot rows 5+ (overlapping area)
  - Returns: { devicesAdded, devicesRemoved, liveDataRows, snapDataRows,
               changedCells, rowDelta }

showRestoreWizard():
  - 420x430 GAS modal dialog (separate from Sidebar.html modal system)
  - Dropdown shows "${label} — ${dateStr}" per snapshot
  - On dropdown change: calls getSnapshotDiff() via google.script.run
  - Diff panel shows row counts, delta (color-coded), changed cells,
    devices added/removed
  - Restore button DISABLED until diff loads successfully
  - On restore: calls restoreFromSnapshot(), closes on success

restoreFromSnapshot(snapshotName):
  - Reads snapshot from row 3 onward (skips 2 metadata rows)
  - Validates snapData is non-empty BEFORE touching live sheet
  - Backs up current live sheet; then clears + writes snapshot data
  - Calls syncSchemaPreservingOrder() to re-apply formatting
  - On any error: rolls back live backup; throws with rollback status in message
  - INVARIANT: live sheet must never be left empty due to a failed restore

showNewProjectDialog():
  - 400x330 GAS modal dialog (HtmlService, separate from Sidebar modal system)
  - Shows a red warning box: data will be permanently erased
  - Checkbox "Keep one device as starting point" (default: checked)
  - Device name text input (default: "Spine1"), hidden when checkbox unchecked
  - "Reset & Start New Project" (red) + "Cancel" buttons
  - On confirm: calls resetToNewProject(keepDevice, deviceName)

resetToNewProject(keepDevice, deviceName):
  - Gets the working sheet by SHEET_DATA name; throws if not found
  - Validates deviceName regex BEFORE clearing (avoids clear+fail leaving blank sheet)
  - Backs up live data; calls sheet.clear(); then optionally rebuildSheet()
  - If keepDevice=true: rebuildSheet([{name: deviceName, type: 'full'}], null, false)
    creates a fresh one-device structure with default schema
  - If keepDevice=false: leaves sheet empty (intentional new-project reset)
  - On any error: rolls back live backup; throws with rollback status in message
  - INVARIANT: if rebuild fails, original data is restored; sheet never stays blank unexpectedly

--------------------------------------------------------------------------------
SECTION 16: AUDIT / VALIDATION ENGINE
--------------------------------------------------------------------------------

Client-side in Sidebar-js.html: runValidation() / toggleValidationMode()

Audit checks (runValidation() in Sidebar-js.html):

CHECK GROUP 1 — Link-based (per-link in globalLinks):
  A. Po Mismatch — both sides have po_ set but to different values
       → flagErrorOnPort on both link.u and link.v
       msg: "✗ Po Mismatch: DevA has Po1 but DevB has Po2"
  B. Po Asymmetry — one side has po_ set, peer has po_ blank
       → flagErrorOnPort on both ports
       msg: "✗ Po Asymmetry: DevA has Po1 but peer has no po_ set"
  C. MLAG Destination — same Po number points to different remote device names
  D. VLAN Mismatch — vlan_ values differ across a cable (Arista-only links)
  E. Mode Incompatible — sp_mode_ pair is not in VALID_MODE_PAIRS
  F. Speed Mismatch — et_speed_ values differ across a cable (Arista-only links)
       Skips blank and 'auto'. Direct comparison only: 25g ↔ 10g flagged, 25g ↔ 25g passes.
       No breakout-aware logic (xcvr_speed_ carries explicit physical speed for cabling).
       msg: "Speed Mismatch: DevA (spA) ↔ DevB (spB)"

CHECK GROUP 2 — Node configuration:
  G. Invalid Interface Name — int_ doesn't match standard prefix regex
  H. Missing Po for Port-Channel mode — mode is po-based but po_ is blank
  I. Stale Po value on Ethernet mode — mode is et-based but po_ has a value
  J. Stale IP Type on L2 port — mode is l2-* with no SVI but ip_type_ has a value

CHECK GROUP 3 — SVI / Gateway:
  Cross-device VLAN gateway consistency (same VLAN, different GW IPs = error)

CHECK GROUP 5 — Duplicate Interface:
  Same int_ name appears more than once for the same device
  (uses globalPortFrequency server-side map: "DeviceName:PortName" → count)

CHECK GROUP 6 — VLAN usage consistency (per device)

CHECK GROUP 7 — Duplicate P2P VLAN across sheet
  Scans globalLinks for any link where either endpoint has ip_type_ = "p2p".
  Collapses Po member links to a logical identity before comparing:
    - Both sides have po_ set → logicalKey = sorted([DevA:PoX, DevB:PoY]).join("|")
      All physical child links of the same Po share the same VLAN intentionally —
      they collapse to one entry and are never flagged.
    - Standalone link (no po_) → logicalKey = link.id
  Maps each VLAN to Map<logicalKey, Set<link.id>>.
  Flags any VLAN with more than 1 distinct logical key — same VLAN on two different
  logical links → same derived subnet (oct2/oct3) → IP conflict in generated config.
  msg: "✗ Duplicate P2P VLAN <N>: Used on <X> logical links — causes IP conflict in generated config"
  Flags all physical link IDs belonging to each affected logical link.

UI: Shield icon in toolbar. Badge shows error count when issues found.
Clicking a flagged port highlights it in red on the topology.
flagErrorOnPort(pid, msg): adds msg to all links connected to port pid;
  deduplicates (includes check) so calling it for both ports on one link
  only adds the error once per link.

auditSchemaVsSheet() (server-side): checks extra/missing columns vs schema.

--------------------------------------------------------------------------------
SECTION 17: CABLING EXPORT & GENERATE ALL CONFIGS
--------------------------------------------------------------------------------

CABLING EXPORT:
openCablingModal() → client-side modal in Sidebar-js.html
Exports a structured cabling table (Device A | Port A | Device B | Port B)
based on paired connections in the topology.
Download as plain text OR copy to clipboard as CSV or Markdown table.

buildCableGroups()
  Shared helper that groups globalLinks into canonical cable pairs.
  Returns { cableGroups, sheetIndexMap }.
  Used by downloadCablingReport(), copyCablingCsv(), copyCablingMarkdown().

  Speed lookup: speedX_raw = xcvr_speed_X || et_speed_X || ""
  aggX = speedX_raw.match(/^(\d+)g-(\d+)$/i)  ← aggregate format is the ONLY reliable
         QSFP-DD breakout signal. Slash notation alone is ambiguous (card/port on tuf317,
         IXIA; QSFP-DD lanes on bkd566) and must NOT be used as the sole trigger.

  Non-Arista suppression: if device type === 'non-arista', isBreakout forced false
         regardless of slash (IXIA/Spirent use card/port slash, not QSFP-DD lanes).
         phyPort kept as full port name for non-Arista devices.

  GroupKey logic (xcvr_speed_ aggregate + slash combination):
    isBreakoutA = !isSelfLoop && !devAIsNonArista && portA.includes('/')
    isBreakoutB = !isSelfLoop && !devBIsNonArista && portB.includes('/')
    phyPortA = (non-Arista or no slash) ? full port : getPhysicalPortParent(port)
    phyPortB = (non-Arista or no slash) ? full port : getPhysicalPortParent(port)

  getPhysicalPortParent(portName): strips lane suffix → Et14/4 → Et14, Et1/1/2 → Et1/1.
    Implementation: split('/'), pop() last segment if numeric, join('/').
    Bug that was fixed: previous impl set parts[last]="1" instead of pop() → returned Et14/1.
    DUPLICATED in Code.gs for server-side testing. Last synced: 2026-04-19.

    if isBreakoutA && !isBreakoutB && aggA           → "DEV_A:phyPortA <-> DEV_B"
    if isBreakoutB && !isBreakoutA && aggB           → "DEV_A <-> DEV_B:phyPortB"
    if isBreakoutA && isBreakoutB && aggA && !aggB   → "DEV_A:phyPortA <-> DEV_B"
    if isBreakoutA && isBreakoutB && aggB && !aggA   → "DEV_A <-> DEV_B:phyPortB"
    if isBreakoutA && isBreakoutB && aggA && aggB    → "DEV_A:phyPortA <-> DEV_B:phyPortB" (Scenario A)
    else (no aggregate on breakout side, or no slash) → "DEV_A:portA <-> DEV_B:portB"

  Without aggX guard: tuf317 Et3/1-3 ↔ bri459 Et1-3 (no QSFP-DD, BASE-T)
    would collapse into one Scenario C group instead of 3 individual cables.

_sortCableGroups(cableGroups, sheetIndexMap)
  Sorts cable groups and their internal links. Used by copyCablingCsv() and copyCablingMarkdown().
  1. Sorts g.links within each group by portA then portB (natural order) — breakout lane
     sub-ports appear in sequence (Et14/1, Et14/2, Et14/3, Et14/4).
  2. Sorts groups anchored on devA (left side): primary = sheetIndexMap[devA]; secondary = phyA
     natural sort (Et1 < Et2 < ... < Et10 < Et11). devA is always the alphabetically-first device
     (fixed by buildCableGroups localeCompare), so this is consistently left-side anchored.

_breakoutSides(g)
  Determines which side is QSFP aggregate (bo=false, collapsed port display, no label)
  vs lane endpoint (bo=true, individual ports shown, xcvr gets "(Breakout)" suffix).
  Primary: lane-speed format (e.g. 100g-4 on one side, 25g on other).
  Fallback (when both sides are same speed, e.g. 25g ↔ 25g):
    if isBreakoutA && !isBreakoutB → {a:false, b:true} (A is QSFP aggregate, B is lane end)
    if isBreakoutB && !isBreakoutA → {a:true, b:false}
    else                            → {a:false, b:false}

copyCablingCsv()
  Builds CSV text: header row "Device A,Port A,Xcvr A,Speed A,Speed B,Xcvr B,Port B,Device B".
  Calls handleCopy() targeting #btnCablingCsv.
  Row count per group:
    Scenario B (count=1): one row, individual ports.
    Scenario C (count>1, one QSFP side): one row per link; QSFP side collapses to phyPort,
      lane side shows individual port.
    Scenario A (count>1, both isBreakoutA && isBreakoutB): ONE row (one physical cable).
      Physical rule: splitter cables are always 1-to-N for any QSFP/OSFP variant (QSFP, QSFP+,
      QSFP28, QSFP-DD, QSFP112, OSFP, etc.): one aggregate connector → 2, 4, or 8 individual
      SFP ends. Two QSFP/OSFP connectors facing each other = laser lane split = ONE cable.
      linksToRender = [g.links[0]] → one row.
      xcvr_speed_ = 400g-4 on both sides; et_speed_ = 100g-1 per lane (not shown in CSV).

copyCablingMarkdown()
  Builds Markdown table with same columns as copyCablingCsv().
  Same Scenario A single-row logic (linksToRender = [g.links[0]] when both sides breakout).
  Calls handleCopy() targeting #btnCablingMd.

Modal buttons: "Copy CSV" (#btnCablingCsv) and "Copy Markdown" (#btnCablingMd)
  in a flex row below the two download buttons, after a divider.

DEVICE BRIDGE MODAL (#lldpModal, 480px):
  Internal ID kept as lldpModal/openLldpModal/closeLldpModal (too many references to rename).
Toolbar button: shield+lines SVG, onclick="openLldpModal()", title="Device Bridge".
  Placed in Export group after "Export Cabling" button.
Purpose: SSH bridge to lab devices — LLDP verification, device status, and future features.
  Uses a local bridge process (device_bridge.py) running on the user's machine. The sidebar
  calls http://localhost:8765 via browser fetch(). http://localhost is exempt from browser
  mixed-content policy (W3C Secure Contexts spec — loopback is "potentially trustworthy"),
  so no HTTPS/cert setup is needed. The bridge SSHes to devices with admin/no-password.
  Modal footer: "Download Bridge" (btn-mono, calls downloadBridgeScript()) + "Run Check"
  (btn-primary-mono, id="btnRunLldp", calls runLldpCheck(), disabled until bridge healthy).
  Audit flags grid (bridge-audit-flags-grid): 4 rows — LLDP/chkBridgeAuditMismatch "Flag
  mismatched neighbour" (default on; gates mismatch flagging in runValidation), LLDP/
  chkBridgeAuditMissing "Flag missing neighbors" (default on), VLAN/chkBridgeAuditInternalVlan
  "Flag internal VLAN conflicts" (default on), BRIDGE/chkBridgeEnabled "Enable on topology
  bring-up" (default on). State vars: bridgeAuditFlagMismatch/Missing/InternalVlan in
  localStorage. Toggle fns: toggleBridgeAuditMismatch, toggleBridgeAuditMissing,
  toggleBridgeAuditInternalVlan. No standalone "Mismatches always flagged" badge — mismatch
  flagging is now user-controlled via chkBridgeAuditMismatch.
  Run status: CSS spinner (::before rotating circle) + live text "Querying N devices…"
  updating to "…(LLDP done)" / "…(LLDP + status done)" as each parallel fetch completes.
  downloadBridgeScript(): embeds device_bridge.py as a JS template literal string, creates
  Blob, triggers browser download as 'device_bridge.py'. MUST keep embedded copy in sync
  with the actual device_bridge.py whenever the bridge script is updated.

device_bridge.py (topoassist/device_bridge.py):
  Run: python3 device_bridge.py. Listens on http://127.0.0.1:8765. CORS on every response.
  GET  /health → {"status":"ok","version":"2.0","port":8765,"method":"ssh"}
  Transports (set METHOD at top of file):
    ssh  — subprocess sshpass via JUMP_HOST or direct; stdlib only (default)
    eapi — Arista eAPI JSON-RPC HTTPS POST /command-api; stdlib urllib; same JSON as SSH
    rest — RESTCONF GET /restconf/data/{path}; stdlib urllib; OpenConfig YANG (EOS 4.22+)
    gnmi — gNMI GET via pygnmi; OpenConfig YANG; port 6030 (EOS 4.22+; pip install pygnmi)
  ssh+eapi: _run_cmds(ip, *cmds) runs EOS show commands, returns list of EOS JSON dicts
  rest+gnmi: separate _rest_get/_gnmi_get; OpenConfig responses normalized via helpers:
    _oc_lldp_to_eos(raw), _oc_version(raw), _oc_platform(raw), _oc_iface_status(raw)
    _gnmi_val(response) — extracts val from pygnmi notification[0].update[0].val
  IMPORTANT: downloadBridgeScript() in Sidebar-js.html embeds device_bridge.py verbatim.
    When device_bridge.py changes, update the embedded copy in downloadBridgeScript() too.
  POST /lldp   → body: {ipMap:{deviceName:ip}}
               → for each device (when JUMP_HOST set, default "bus-home"):
                   ssh {JUMP_HOST} 'sshpass -p "" ssh -o StrictHostKeyChecking=no
                   -o ConnectTimeout=8 admin@{ip} "show lldp neighbors detail | json"'
                 Bridge runs on local Mac; jump server does the lab SSH.
                 No SSH tunnel needed; no sshpass needed on Mac.
                 (when JUMP_HOST="": direct sshpass mode — sshpass must be on Mac)
               → devices queried in parallel threads (threading.Thread); TIMEOUT=15s
               → returns {deviceName:{ok,neighbors}} or {ok:false,error}
  Error cases: SSH timeout, CalledProcessError, JSON decode error, ssh not in PATH.
  Suppress HTTP log noise (log_message overridden to compact one-liner).

Client globals (Sidebar-js.html):
  LLDP_BRIDGE_URL = 'http://localhost:8765'  (module-level const)
  lldpPortStatus = {}     — "dev:Et1" → 'match'|'mismatch'|'missing'; reset on each check
  lldpAuditFlagMissing    — persisted in localStorage; controls missing-dot color + audit flag
  devStatusMap = {}       — devId → {ok, version, platform, interfaces:{FullName:{linkStatus}}}

Client functions (Sidebar-js.html):
  openLldpModal()         — resets state, calls _populateLldpDevTable() + _checkBridgeHealth();
                            Run btn starts DISABLED until bridge health confirmed
  closeLldpModal()        — hides modal+overlay
  _checkBridgeHealth()    — GET /health; on success: dot-green, msg="Running on port N", enables Run btn;
                            on failure: dot-red, msg="Not running", shows lldpBridgeCmd yellow box, disables Run btn
  _getLldpMgmtIps()       — scans allNodesData for Ma*/Management* ports with ip_ → {dev: ip (no prefix)}
  _populateLldpDevTable() — renders lldp-dev-row grid: device name | host/IP input | status dot
                            Defaults to device name (DNS-resolvable) via autoIps[n.id] || n.id;
                            Ma0 IP takes priority if present; only Arista devices shown
  runLldpCheck()          — collects IPs; fires Promise.all([/lldp, /devstatus]) in parallel;
                            updates per-device dots; stores dsData→devStatusMap; calls
                            _compareLldpData + _renderLldpResults + applyLldpIndicators +
                            applyDevStatusToCards; on fetch error calls _checkBridgeHealth()
  _expandIfaceName(name)  — maps short EOS port names to full form for devstatus lookup:
                            Et→Ethernet, Ma→Management, Po→Port-Channel, Vl→Vlan
  applyDevStatusToCards() — updates .dev-version-line divs ("EOS 4.x.y · Platform") and
                            overrides port St dots with live op-status from devStatusMap:
                            connected→op-up (green), notconnect→op-down (amber),
                            errdisabled→op-errdisabled (red), other→op-disabled (gray);
                            also drives card-header .dev-bridge-dot reachability indicator:
                            ds.ok===true → dot-green (reachable via bridge SSH);
                            ds.ok===false → dot-orange (unreachable); absent → hidden;
                            called after render() and after each runLldpCheck()/runBridgeCheck()
  _normSysName(sysName)   — extracts short device name from LLDP FQDN: strips domain (split
                            on '.')[0]) then takes last dash-segment (split('-').pop()).
                            "DUT-L-Leaf1-VG-cal423.sjc.aristanetworks.com" → "cal423".
                            IMPORTANT: EOS reports full FQDN in systemName; topology device
                            IDs are short names — must normalize before comparison.
  _normLldpIface(name)    — full EOS names → canonical short: Ethernet→Et, Management→Ma,
                            Port-Channel→Po, Loopback→Lo, Vxlan→Vx
  _compareLldpData(lldpData) — iterates globalLinks; builds lldpMap {DevName:NormIface →
                            {remoteDevice (via _normSysName), remoteIface}};
                            NOTE: uses interfaceId_v2 (no embedded quotes) with interfaceId
                            fallback + quote-strip — interfaceId from EOS has literal `"`
                            chars: '"Ethernet1"' which breaks _normLldpIface matching;
                            skips: non-Arista devices, non-Et interfaces (PO/Vx/Lo etc.);
                            statuses: match / mismatch (shows actual LLDP seen) /
                            partial (one side not queried) / unknown (neither queried)
  _renderLldpResults(results, orphans) — renders #bridgeResultSections with grouped collapsible sections:
                            Issues (New link / Mismatch / No neighbour), OK (N/checkable), No EOS.
                            Classification by bridgePortStatus enum ('mismatch'/'missing') — NOT r.msg content.
                            secHtml(key, title, badgeCls, badgeText, body, hidden) builds each section.
                            subHtml(label, count, bodyHtml) builds each sub-section.
                            Collapse state from localStorage 'bridgeSec_issues/ok/noeos'; Issues open by default.
  _toggleBrSec(key) — toggles collapse on .br-sec-body + .br-chev; persists to localStorage

Modal UI structure:
  Bridge bar: dot + status msg + Recheck button (lldp-bridge-bar)
  Bridge cmd box: shown only when bridge not running — yellow box with start command (lldp-bridge-cmd)
  Management IPs table: lldp-dev-row grid (device name | IP input | status dot); Arista only
  Status line: shown during fetch ("Bridge is querying devices via SSH…")
  Results: #bridgeResultSections with three collapsible sections (Issues / OK / No EOS)
  Per-device dot (bridge modal): dot-gray=idle/no IP, dot-green=SSH OK, dot-red=SSH failed (device unreachable)
  Per-device dot (card header, .dev-bridge-dot): hidden=not checked, dot-green=reachable, dot-orange=unreachable
  Toolbar icon states: bridge-up (green) · bridge-partial (dark-yellow #d97706, ≥1 device SSH-failed) ·
                        bridge-down (red) · bridge-auth-expired (orange) · bridge-disabled (dimmed)
  _bridgeHasDeviceFailures: module-level flag; reset false at runBridgeCheck() start; set true on any
                             bridge modal dot-red; read by _checkBridgeHealth()/_silentBridgeCheck() to
                             re-assert 'partial' vs 'up' on periodic health polls

Result row statuses:
  match    — SVG check, green bg — LLDP confirms the cable
  mismatch — SVG X, red bg — LLDP sees different neighbor (detail line shows actual)
  partial  — SVG ?, amber bg — one device queried OK, other not queried
  unknown  — SVG ?, gray bg — neither device queried
  skip     — dash SVG, faded — non-Arista or non-Ethernet interface

lldpModal added to: modalOrder (Escape key), closeFuncs map, closeAllModals() list.

CSS classes (Sidebar-css.html section 13):
  .lldp-bridge-bar (flex row, bg-secondary bg), .lldp-bridge-msg, .lldp-recheck-btn
  .lldp-bridge-cmd (amber/yellow warning box), .lldp-cmd-code (bold, user-select:text)
  .lldp-field-label, .lldp-section-hdr, .lldp-hint
  .lldp-dev-row (grid: 1fr 140px 18px), .lldp-dev-name, .lldp-ip-inp, .lldp-dev-dot
  .dot-green, .dot-red, .dot-gray
  .lldp-empty, .lldp-run-status, .lldp-summary-badge (.badge-ok / .badge-fail)
  .lldp-result-list, .lldp-result-row (.lldp-row-ok/fail/part/unk/skip)
  .lldp-row-icon, .lldp-row-cable, .lldp-row-detail
  .col-lldp (16px flex col, port row LL indicator), .lldp-port-dot (7px circle)
  .lldp-port-match (green), .lldp-port-mismatch (amber), .lldp-port-missing (red),
  .lldp-port-missing-quiet (gray, when audit-missing flag off)
  .lldp-audit-row, .lldp-audit-always, .lldp-chk-label (modal audit toggle row)
  .dev-version-line — 9px JetBrains Mono, rgba(255,255,255,0.7) on colored headers,
    #64748b on plain headers; shows "EOS {version}  ·  {platform}" under hostname
  .dot.op-up (green #22c55e !important), .dot.op-down (amber #f59e0b),
  .dot.op-errdisabled (red #ef4444), .dot.op-disabled (gray #94a3b8) — live op-status
    overrides for port St dot applied by applyDevStatusToCards()

Note: Code.gs retains checkLldpNeighbors(ipMap,protocol) as a standalone server function
  (eAPI-based, for setups where devices have public IPs). Not called by the sidebar.

GENERATE ALL CONFIGS:
Toolbar button: file-text SVG icon in the Export group (after cabling button).
onclick="openGenerateAllModal()" → static modal #generateAllModal (600px wide).

Server functions (Code.gs):
  formatConfigMap(configMap)
    Server-side equivalent of client formatConfigText(). Sorts keys
    numerically, joins .full values with \n!\n.

CONFIGMAP KEY FORMAT (source of truth for both Configs sheet and tooltip):
  getDeviceConfig() returns configMap with prefixed keys for deterministic ordering:
    "000_BASE"          — hostname + mandatory device baseline (ip routing, service routing protocols, varp mac)
    "000_GTPL"          — user's custom template / Global Template (NTP, AAA, interface defaults/mtu, etc.) — omitted if getGlobalConfig() is empty
                          key chosen so G < L → sorts between 000_BASE and 000_LO0 (template visible near top of View Config)
    "001_SYSTEM"        — loopback, router general, VRFs, VLANs
    "050_<port>"        — physical interface (e.g. "050_Et1", "050_Ma0")
    "050_Vx1"           — AP VLAN SVI blocks (see AP VLAN GW section)
    "060_<port>"        — logical/Port-Channel (e.g. "060_Po10")
    "055_VXLAN"         — VXLAN block
    "070_MLAG"          — MLAG block
    "090_OSPF_UNDERLAY" — OSPF underlay
    "100_BGP_UNDERLAY" / "100_BGP_EVPN" — BGP blocks
    "999_EOF"           — configure + write memory
  formatConfigMap() / formatConfigText() sort numerically → same order in Configs
  sheet and full device view in sidebar. SINGLE SOURCE OF TRUTH: both paths call
  getDeviceConfig() and sort/join the same configMap.

  Tooltip per-port config: processDeviceConfig() maps configData keys to allNodesData.
  allNodesData keys are "DeviceName:Et1" (no prefix). MUST strip "^\d+_" prefix
  before canonicalizeInterface() — "050_Et1" → "Et1" → matches "SPINE1:Et1".
  Without the strip, canonicalizeInterface("050_Et1") returns "050_Et1" unchanged,
  no allNodesData entry is found, and tooltip right pane always shows "Loading...".

  writeConfigsToSheet(configs)
    Creates or overwrites "Configs" sheet tab (tab color #6366f1 indigo).
    Layout: one column per device. Row 1 = device names (bold, bg #1e1b4b, text #e0e7ff,
    frozen). Rows 2+ = config lines. All columns 500px wide. Font: Consolas size 10.
    Trims sheet to exactly (maxLines+1) rows × numDevices cols before setValues to stay
    under the GAS 10M cell limit (sheet.clear() does not trim dimensions).

  getAllDeviceConfigs()
    Filters getExistingDevices() → type='full' AND isVisible=true.
    Calls getDeviceConfig(dev.name) per device (sequential loop).
    Returns { configs: [{name, text}], sheetName } or { error }.

Client functions (Sidebar-js.html):
  openGenerateAllModal()    — resets body to intro text, shows modal + overlay
  closeGenerateAllModal()   — hides modal + overlay
  runGenerateAll()          — calls getAllDeviceConfigs(), renders per-device summary
  copyAllConfigs()          — copies window._generateAllText via handleCopy()

#generateAllModal added to closeAllModals() array and Sidebar-css.html hidden list.

VLAN SUMMARY PANEL:
Toolbar button: three-horizontal-rects SVG icon, onclick="openVlanSummaryModal()".
Static modal #vlanSummaryModal (680px wide) with filter input + scrollable table.
Added to closeAllModals() array and Sidebar-css.html hidden list.

Client functions (Sidebar-js.html):
  openVlanSummaryModal()      — clears filter, calls buildVlanSummary(), shows modal
  closeVlanSummaryModal()     — hides modal + overlay
  buildVlanSummary()          — iterates allNodesData; classifies each port L2/L3
                                using same logic as Check Group 6 (mode includes
                                'l3', hasIp, name starts 'vlan'); expands vlan_ and
                                via parseVlanInput() + parseVlanWithNative(); builds vlanMap and
                                stores in window._vlanSummaryData
  renderVlanSummaryTable()    — renders sticky-header table (VLAN|Type|VRF|Ports);
                                row click: closeVlanSummaryModal() then traceVlan(vid)
  filterVlanSummary()         — filters sorted keys by VLAN ID, VRF, or device/port name

IP CONFIG MODAL (#ipConfigModal):
Toolbar button in Modals group. openIpConfigModal(btn) populates fields from
  ipPrefs (PropertiesService). Fields stored in scriptProperties as JSON.
Legend info-box (ip-info-box): shows IP pattern with colored octets:
  .f-var (blue) = user-configurable fixed value  .f-dyn (amber) = dynamic (VLAN, Device ID)
  P2P pattern: [First].[VLAN].[VLAN].[Device ID]   Device ID = device's sheet index
  GW  pattern: [First].[VLAN].[VLAN].[Last]
CSS: ip-info-box, info-title, section-header, ip-legend-grid, legend-label all use
  CSS variables (var(--bg-secondary), var(--border), var(--text-main)) for dark-mode compat.
  (Previously hardcoded #eff6ff, #0f172a etc — fixed to use theme variables.)
Inputs: P2P (First Octet, Mask for v4; Prefix, Mask for v6)
        GW  (First Octet, Last Octet, Mask for v4; Prefix, Last Hextet, Mask for v6)
saveIpConfig() — validates, persists to scriptProperties, calls applyIpPrefs().
resetIpConfig() — resets to defaults (P2P 200//24//64, GW 100//24//64 last=1).

CARD COLLAPSE:
Device header double-click toggles .is-collapsed class on the .node element.
Single click → selectDevice(node.id). Double-click → toggle collapse.
selectDevice() shows splitDevice toolbar with Copy / View buttons. No sheet navigation side-effect.
CSS: .node.is-collapsed hides .node-body and .node-add-btn; removes header border-bottom.
Cables auto-disappear for collapsed devices: redrawLinks() skips ports whose
  row.offsetParent is null (hidden parent), so collapsed ports never enter posCache.
State persisted in localStorage key 'nb_collapsed' as {deviceId: 1} map.
Restored on each render (after el.style.top assignment in node-building loop).

EXPAND ALL / COLLAPSE ALL (single toggle):
Single button id="btnToggleCards" in secondary toolbar segmented-group, after the
3 layout-mode buttons, separated by a thin <div> divider (NOT a <span> — inline-block
span caused a blinking text cursor in the browser; pointer-events:none on the div).
Uses SVG chevron icons (14×14) — direction shows what WILL happen on click:
  ▴ (chevron-up) = "click to collapse" (initial state when cards are expanded)
  ▾ (chevron-down) = "click to expand" (shown after all cards are collapsed)
toggleAllCards() — checks .node[data-id]:not(.is-collapsed) count:
  any expanded → collapseAllCards(), button becomes ▾ "Expand all cards"
  all collapsed → expandAllCards(), button becomes ▴ "Collapse all cards"
collapseAllCards() — adds .is-collapsed to all .node[data-id], writes localStorage.
expandAllCards()   — removes .is-collapsed from all, writes '{}' to localStorage.
SVG constants: _svgChevronUp / _svgChevronDown defined at module level.
DISABLED IN SIMPLE VIEW: updateTopoButtons() sets btnToggleCards.disabled=true,
  opacity=0.3, cursor=not-allowed when activeMode==='simple' (holistic/simple view
  has no cards). Restored to normal for 'linear' and 'flexible' modes.

AUDIT-ONLY VIEW (ERR button):
Button id="btnAuditOnly" inside primary toolbar audit-group-box (alongside shield).
Class: .icon-btn (28×28, matches shield). Initially hidden (style="display:none").
Shown as display:flex when audit mode enabled (toggleValidationMode).
Icon: alert-circle SVG (circle with ! marks).
Active state CSS: #btnAuditOnly.active { background:#ef4444; color:white; }
  + svg { fill:currentColor; stroke:transparent; } → solid filled circle when active.
Visibility synced in applyValidationState() (called on init + toggle) so ERR appears
  correctly when isValidationMode=true by default on page load.
isAuditOnlyView (boolean, module-level) tracks toggle state.
toggleAuditOnlyView() — flips isAuditOnlyView, toggles .active on button,
  calls applyAuditOnlyView().
applyAuditOnlyView() — if audit off or isAuditOnlyView=false: restores all
  .node elements (clears el.dataset.auditHidden). If on: scans validationErrors,
  builds devsWithErrors Set from linkMap, hides non-error devices with
  el.style.display='none' and el.dataset.auditHidden='1'.
Called at end of runValidation() and in toggleValidationMode() when disabling audit.

PRIMARY TOOLBAR GROUPS (right-group, left→right):
  [✏ ⬜] | [B 🌙] | [🔷 🖥 ☰] | [📷 🌐 📄] | [🛡 ⊙] | ? | [▴]
  Edit    Appear   Modals       Export        Audit      Help  Collapse
  Edit:    editModeBtn (pencil), groupRectBtn (rect)
  Appear:  boldTextBtn (B), themeToggleBtn (moon)
  Modals:  techModal (layers), ipConfig (server), vlanSummary (rows)
  Export:  snapshot (camera), cabling (network), generateAll (file)
  Audit:   validateBtn (shield) + btnAuditOnly (alert-circle) in .audit-group-box
  Help:    openHelpModal (?)
  Collapse: toolbarCollapseBtn (chevron, toggles secondary row)

ROLE PILL FILTER:
Each span.dev-label-pill in the node header gets an onclick handler (added in
node-building loop after span.textContent = lbl).
Click pill → sets searchInp.value to role label (lowercase compare); if already
  set to that role, clears the search (toggle). Calls filterTopology().
Cursor pointer set via CSS (.dev-label-pill { cursor: pointer; }).

PO GROUP HIGHLIGHT:
clearPoHighlight() — removes .po-group-highlight from all .port-row elements.
applyPoHighlight(portId) — clears, then checks allNodesData[portId].details.po_.
  If set, iterates all allNodesData and adds .po-group-highlight to all ports
  sharing the same po_ value (numeric string match).
CSS: .port-row.po-group-highlight { outline: 2px solid #f59e0b; outline-offset: -2px;
  background: rgba(245,158,11,0.08) !important; }
Called: applyPoHighlight(portId) at top of selectPortOrLink() for every port click.
Cleared: clearPoHighlight() in Escape handler (step 2 global reset).

ESCAPE KEY (modal LIFO):
modalOrder and closeFuncs in topologyKeyHandler include generateAllModal, vlanSummaryModal,
and globalCfgModal so Escape closes them in order before resetting selection.
clearPoHighlight() also called in step 2 global reset.

CONFIG AGE INDICATOR:
allDevicesData[name].fetchEpoch = Date.now() stored in processDeviceConfig().
formatAge(epochMs) → 'just now' / 'Xm ago' / 'Xh Xm ago' (relative only).
formatAgeLabel(epochMs) → 'HH:MM · just now' / 'HH:MM · 3m ago' — combines
  the fetch clock time (HH:MM 24h) with the relative age, separated by ' · '.
  Used in all green-state setSplitBtnState() callsites. Fallback to getTimestamp()
  (raw clock string) if fetchEpoch not yet set.
  Affected callsites: processDeviceConfig() (device + left + right), selectDevice(),
  selectLine(), selectPortOrLink() (disconnected port), processInterfaceConfig().
refreshConfigAges() rebuilds labels from stored data every 30s via setInterval.
  Uses selectedDeviceId / selectedLineId + linkMap to reconstruct the full label
  text ([sheetId] Device:Config:Port [HH:MM · age]) directly — avoids regex-replace
  which caused "just ago|Copy|View" bug when label was in unexpected state.

--------------------------------------------------------------------------------
SECTION 18: SEARCH & VLAN TRACING
--------------------------------------------------------------------------------

searchInp (input in secondary toolbar):
  - Filters topology to show only matching devices/ports
  - "v<number>" syntax (e.g. "v10") → triggers VLAN trace mode
  - Highlights all ports carrying that VLAN
  - Strict mode checkbox: exact match only (not substring)
  - Enter key (with no autocomplete item focused) → jump-to-device: scrolls to first
    visible (non-dimmed) .node[data-id] whose data-id contains the search text, with
    a 1s blue outline pulse.
  - Role pill click on device header → sets searchInp.value to role label (or clears
    if already set) and calls filterTopology(). Toggle behavior.

debouncedFilterTopology() — 300ms debounce on filterTopology()
debouncedTraceVlan() — 300ms debounce on traceVlan()

Autocomplete: search-autocomplete-list (hidden div) shows matching device names.
  keydown handler in IIFE (line ~8000): ArrowUp/Down navigate, Enter (selectedIdx>=0)
  clicks item, Enter (selectedIdx<0) triggers jump-to-device above.

--------------------------------------------------------------------------------
SECTION 19: SMART POLLING
--------------------------------------------------------------------------------

Client polls getDataVersion() (server) every N seconds (configurable via toolbar).
If DATA_VERSION changed → fetchData(false) to refresh topology.

smartPollTimer / localDataVersion track polling state.
fetchData(forceSync):
  - Calls google.script.run.getTopologyData(forceSync, isColorEnabled)
  - On success: re-renders topology, updates status bar

SMART POLLING GUARDS (startSmartPolling — all 4 must pass before polling fires):
  1. Edit mode guard:    isEditMode === true → skip (user is dragging/editing nodes)
  2. Fetch guard:        isFetching === true → skip (prevents concurrent GAS executions)
  3. Loading overlay:    loading overlay visible → skip (initial load in progress)
  4. Visibility guard:   document.hidden === true → skip (saves quota when tab backgrounded)

BACKGROUND PREFETCH SYSTEM:
  Globals: prefetchQueue (uncached devices only), prefetchTotal (ALL Arista devices),
    prefetchInterval (setInterval handle or null), prefetchStartTimer,
    _prefetchHold (true while _autoRunBridge LLDP+devstatus scan is running),
    _prefetchEverRan (true after first startBackgroundPrefetch() call).

  fetchData() status message rules:
    - Cold start (isFirstLoad=true): shows "🚀 Requesting Topology Data.. no local cache"
    - All subsequent calls (isFirstLoad=false): message is NOT overwritten — calling
      code (smart poll, save handlers, Device Manager etc.) already set a specific status
      before calling fetchData (e.g. "Sheet changed — refreshing topology…").
    - isFirstLoad is set false in the fetchData success handler after first successful fetch.
    - Guard: _fetchGuard fires at 30s (GAS execution limit) → "retrying…" + auto-retry
      after 5s. _fetchGuardFired flag discards late GAS responses after guard fires.

  scheduleBackgroundPrefetch(forceRestart) — smart scheduler; signature:
    - If _prefetchEverRan && !forceRestart: return — skip smart-poll restarts when
      nothing changed (avoids re-caching after all configs are warm).
    - If forceRestart && prefetchInterval !== null: calls stopBackgroundPrefetch() first
      so the queue is rebuilt with newly-invalidated devices (leaving it running would
      orphan them — deleted from configCache but not in the running queue).
    - Waits for D3/holistic alpha < 0.1, isFetching=false, _prefetchHold=false,
      then calls startBackgroundPrefetch().
    Call sites:
      - fetchData() success: scheduleBackgroundPrefetch(_invalidated !== 0)
        (_invalidated = return value of _selectiveConfigInvalidate; 0=no change)
      - saveIpConfig / saveTechSettings / saveGlobalConfig success: scheduleBackgroundPrefetch(true)
        (force=true because save wipes configCache entirely)

  startBackgroundPrefetch() — sets _prefetchEverRan=true; builds prefetchQueue from
    uncached Arista devices; sets prefetchTotal = ALL Arista count (so counter reads
    "6 of 10" when 4 were already cached, not "1 of 6"); fetches 1 config/3 seconds;
    skips if pendingFetches active; updates status "Background caching: DEV (N of total)".

  stopBackgroundPrefetch() — clears prefetchInterval; resets prefetchQueue/prefetchTotal.

  _selectiveConfigInvalidate(oldDevs, newDevs) — returns invalidate.size (0=unchanged,
    >0=N devices invalidated, -1=error/full clear). Return value used by fetchData to
    decide whether to forceRestart the prefetcher.

  _prefetchHold protocol:
    _autoRunBridge() sets _prefetchHold=true at start; clears via _done() on
    .then() and .catch() — prevents prefetch from starting during bridge scan.

--------------------------------------------------------------------------------
SECTION 20: DARK/LIGHT THEME
--------------------------------------------------------------------------------

toggleTheme():
  - Toggles 'dark' class on document.body
  - Saves preference to localStorage under 'topology_theme'
  - On load: reads localStorage and applies saved theme

All colors defined in CSS variables in Sidebar-css.html.
Variables: --bg, --surface, --border, --text, --text-muted, --accent, etc.

--------------------------------------------------------------------------------
SECTION 21: LAYOUT PREFERENCES
--------------------------------------------------------------------------------

Saved to localStorage:
  - topology_card_width (W input)
  - topology_device_gap (G input)
  - topology_cable_offset (O input)
  - topology_cable_top (T input)
  - topology_refresh_interval
  - topology_auto_refresh (checkbox)
  - topology_view_mode (linear/simple/flexible)
  - topology_sort_mode (sheet/mode/priority/alpha)
  - topology_node_positions (flexible mode node coordinates)
  - topology_group_rects (group rectangle definitions)
  - topology_bold_labels

Server-side: getUiSettings() / saveUiSettings() for refresh and auto settings.

--------------------------------------------------------------------------------
SECTION 22: GROUP RECTS (SIMPLE/HOLISTIC VIEW ONLY)
--------------------------------------------------------------------------------

Draw rectangles over the topology to label logical device groups.
Active in: simple (holistic), flexible, and linear modes. "New Group" button in toolbar.

Each rect has: id, x, y, w, h, color, label, textPos, members:[deviceId].
Saved to localStorage under 'holistic_group_rects'.
groupRects[] array; rendered into per-view SVG overlays:
  - Simple/holistic: #svg-layer .group-rects
  - Flexible: #flex-grect-svg (appended to canvas-surface)
  - Linear: #linear-grect-svg (appended to canvas-surface, created in requestAnimationFrame after render())

Member rects: bounds computed live from node positions by _computeGroupBounds(); not draggable.
Manual rects: freely draggable, resize handles on corners.
refreshGroupRects(): refreshes all three SVG layers (holistic + flex + linear).
toggleViewMode() cleanup removes both #flex-grect-svg and #linear-grect-svg explicitly.

Edit a rect by double-clicking it → opens edit modal (label, color, members, text position).

--------------------------------------------------------------------------------
SECTION 23: IXIA INTEGRATION
--------------------------------------------------------------------------------

SHEETNAME(dummy_cell) function in Code.gs:
  Returns the name of the working sheet (SHEET_DATA).
  NOT called by the script. Used in the IXIA TAB of the Google Sheet as a formula.
  MUST NOT be removed.

Non-Arista devices (IXIA, Spirent) get only int_ columns.
They appear in the topology as nodes (labeled IXIA or SPIRENT) but no config
is generated for them.

--------------------------------------------------------------------------------
SECTION 24: HARD DESIGN CONSTRAINTS (DO NOT CHANGE)
--------------------------------------------------------------------------------

1. IPv6 SYNTAX: Does NOT follow Arista EOS syntax. This is intentional.
   IPv6 format aligns with IPv4 for LAB testing convenience.

2. MLAG IS EXPLICIT ONLY: MLAG pairs are declared via Device Manager (stored in
   DEVICE_MLAG_PEERS DocumentProperty). The old PO-count heuristic (>= 4 occurrences)
   was fully removed from calculateGlobalTopology(). Do NOT re-introduce any
   count-based MLAG detection. If MLAG behavior is wrong, fix the Device Manager
   declaration, not the topology engine.

3. SHEETNAME(dummy_cell) function: Must not be removed. Used by IXIA tab formula.

4. hasKey(setObj, key): Must be used instead of .has() for case-insensitive
   device name lookups. Device names in Sets are lowercase; sheet names are
   original-cased.

5. canonicalizeInterface() is duplicated in Code.gs AND Sidebar-js.html.
   This is intentional (server needs it for config gen; client needs it for
   topology rendering without round-trip). Both copies carry a
   "// DUPLICATED ... last synced: <date>" comment — update the date
   whenever either copy is changed, and keep both in sync manually.

6. generateConfig() signature: exactly 5 params (portName, d, ipPrefs, seenPos, netSettings).
   The 5th param is the full netSettings object (13 IP family flags). MUST be passed by every
   caller — both getTopologyData() (as globalNetSettings) and getDeviceConfig() (as netSettings).
   Omitting it silently drops all protocol-family-gated commands from that config path.
   generateBGP() has 9 params — settings is the 8th, ipPrefs is the 9th (provides loBase
   for loopback/ASN derivation); all call sites must pass both.
   generateBGPEvpnOverlay() has 7 params — settings is the 6th, ipPrefs is the 7th.

7. VRRP is REMOVED: MLAG + L3 is invalid on Arista EOS. The guard covers all
   l3-* modes in generateConfig() and generateComplexL3Block().

8. JetBrains Mono must be explicitly set on ALL UI elements in Sidebar-css.html.
   It does NOT inherit from body in Google Apps Script dialogs.

9. WINNOW FIRST: For any Arista/EOS-specific question (protocol behavior, config
   syntax, feature availability, known bugs, IXIA integration, CloudVision APIs),
   query Winnow MCP tools BEFORE guessing or using web search.
   Use winnow_query for complex questions, winnow_quick_query for fast lookups.
   CLAUDE.md at the project root enforces this.

10. FULLSCREEN IS NOT POSSIBLE: GAS dialogs have a hard size cap (~1050px wide,
    ~95% viewport height). google.script.host.setWidth/setHeight cannot exceed
    these limits. Native browser Fullscreen API is blocked by iframe sandboxing.
    Do NOT attempt to implement fullscreen — it was tried and reverted (2026-04-17).

--------------------------------------------------------------------------------
SECTION 25: KEY UTILITY FUNCTIONS (Code.gs)
--------------------------------------------------------------------------------

hasKey(setObj, key)
  Case-insensitive Set/Map lookup. Always prefer over .has() for device names.

hslToHex(h, s, l)
  Converts HSL color to hex string. Used for device header colors.

columnToLetter(col)
  Converts 1-based column index to A1 letter notation.

normalizePo(rawPo)
  Normalizes Port-Channel name (e.g. "port-channel10" → "Po10").

getExistingAttributes(sheet)
  Returns array of attribute header keys from Row 2 of the sheet.

getColumnIndices(headers, deviceName)
  Returns map of attribute keys to column indices for a given device.

getIpPreferences() / saveIpPreferences(prefs)
  Read/write IP subnet preferences from UserProperties.

getUiSettings() / saveUiSettings(settings)
  Read/write UI layout preferences (card width, gap, refresh) from UserProperties.

THREE DEVICE VISIBILITY MECHANISMS — fully independent:

  1. SHEET VIEW (temporary, per-user) — SheetAssistPanel.html left column
     Storage: SHEET_VIEW_HIDDEN (UserProperties)
     Functions: getSheetViewHidden() / saveSheetViewHidden(list)
     Effect: hides/shows device columns in the spreadsheet only
     Used by: toggleSheetDevice, showAllSheetDevices, hideAllSheetDevices, batchToggleSheetDevices, viewShowAll,
              applyCustomView (reads getSheetViewHidden for device filter),
              refreshSheetRowVisibility (reads getSheetViewHidden for row filter),
              getSheetDeviceList (derives isVisible from getSheetViewHidden)

     SHEET VIEW FILTERS (AND conditions stacked; each filter row is a 4-column grid):
     All three filters share the same ip-section / ip-section-header / ip-section-list CSS classes.
     ip-section-list uses display:grid; grid-template-columns:repeat(4,1fr) — items side-by-side.
     Items use device-item / eye-icon / item-name / is-hidden / ip-filter-item classes.
     All filters: OR within filter values, AND across filters. Default: all values selected.
     toggleMinimize() hides all three sections; load() fetches all three saved states.
     Dialog height 550px (Code.gs setHeight + google.script.host.setHeight in toggleMinimize).

     MULTI-SELECT (batch hide/show):
     var selectedDevices = new Set() tracks selected device names (client-side only).
     sel-row below action-row: SEL ALL checkbox (chkSelAll, indeterminate support) + SHOW/HIDE sel buttons.
     Checkbox (.sel-chk) prepended to each device-item in renderDevices(); onclick stopPropagation + toggleDevSel().
     toggleDevSel(name) — toggles selectedDevices Set, updates .is-selected class + checkbox, calls updateSelBtns().
     updateSelBtns() — enables/disables sel buttons, shows count "(N)" in label, syncs chkSelAll checked/indeterminate.
     selectAllVis(checked) — selects/deselects all visible (not filtered) items.
     hideSelected() / showSelected() — optimistic UI, calls batchToggleSheetDevices(names, hidden), clears selection on success.
     setServerBusy() calls updateSelBtns() so sel buttons are disabled while server call is in-flight.
     doShowAll/doHideAll success handlers call selectedDevices.clear() so selection resets on bulk ops.

     SERVER BUSY LOCK (race condition prevention):
     var serverBusy = false tracks whether a google.script.run call is in-flight.
     setServerBusy(on) sets flag + toggles body.server-busy class + disables bulk/minimize/sel buttons.
     Every mutating entry point guards with `if (serverBusy) return` before dispatching:
       toggle(), doShowAll(), doHideAll(), hideSelected(), showSelected(), toggleColItem(), applyColBulk(),
       toggleIpFilter(), toggleIntModeFilter(), toggleSviFilter()
     navigate() is NOT blocked (device name stays clickable) but its success/failure handlers
       guard with if(!serverBusy) before setStatus() — avoids clearing a live "Applying filter…" message.
     CSS body.server-busy rules: .ip-filter-item → pointer-events:none + opacity:0.45;
       .device-item:not(.is-toggling) .eye-icon → pointer-events:none + opacity:0.4;
       .col-item:not(.mandatory):not(.is-toggling) → pointer-events:none + opacity:0.5.
     The currently-toggling item's animation is unaffected (excluded from CSS rules).
     setServerBusy(false) called in BOTH success and failure handlers — always released.

     ip_type: SHEET_VIEW_IP_FILTER (UserProperties); values: p2p, gw, blank; 3 real + 1 dummy.
       Functions: getSheetViewIpFilter / saveSheetViewIpFilter / applySheetIpFilter
       Logic: refreshSheetRowVisibility builds devIpTypeCol map. Checks ip_type_ ONLY for devices
              with non-blank int_ on that row — prevents non-participating devices from bypassing filter.
              Normalization: ipRaw = ip_type_.toLowerCase().trim(); ipVal = (ipRaw==='p2p'||ipRaw==='gw') ? ipRaw : 'blank'
              (any unexpected value including '' maps to 'blank' — consistent with Topology View).

     int_mode: SHEET_VIEW_INT_MODE_FILTER (UserProperties); 8 specific sp_mode_ values; 8 real items (4×2 grid).
       Values: l2-et-access, l2-et-trunk, l2-po-access, l2-po-trunk, l3-et-int, l3-et-sub-int, l3-po-int, l3-po-sub-int.
       Labels: l2ea, l2et, l2pa, l2pt, l3ei, l3es, l3pi, l3ps (4-char abbrev; full value shown as tooltip).
       Functions: getSheetViewIntModeFilter / saveSheetViewIntModeFilter / applySheetIntModeFilter
       Logic: refreshSheetRowVisibility builds devSpModeCol map (sp_mode_ columns).
              Exact string match: intModeFilter.includes(sp). Blank sp_mode_ always passes (return true).
              Missing sp_mode_ column also passes (return true). Only checked for devices with non-blank int_.
       initIntModeFilter validates saved values against current item list; falls back to all 8 if stale
              (handles migration from old 4-group format ['l2','l3','et','po']).

     svi_vlan: SHEET_VIEW_SVI_FILTER (UserProperties); values: active (any svi_vlan_ value), blank; 2 real + 2 dummies.
       Functions: getSheetViewSviFilter / saveSheetViewSviFilter / applySheetSviFilter
       Logic: refreshSheetRowVisibility builds devSviCol map (svi_ columns).
              svi_='yes'→'yes', anything else→'blank'. Only checked for devices with non-blank int_.

  2. TOPOLOGY VIEW (temporary, in-memory) — Sidebar.html devVisPanel
     Storage: JavaScript Set hiddenDevices (lost on window close, never persisted)
     Functions: toggleDeviceVis, showAllDevices, hideAllDevices, applyDeviceVisibility (all client-side)
     Effect: hides/shows device nodes in topology diagram DOM only
     No server calls, no DATA_VERSION bump

     MULTI-SELECT (batch hide/show):
     let selectedInDevVis = new Set() tracks selected device IDs (client-side, in-memory).
     dev-vis-sel-row below dev-vis-action-row: SEL ALL checkbox (devVisSelAllChk) + SHOW/HIDE buttons (btnDevVisShowSel/btnDevVisHideSel).
     .dev-vis-chk checkbox prepended to each dev-vis-item in populateDevVisPanel(); click stopPropagation + change → toggleDevVisSelect(id).
     Item body click (not eye/nav/chk) also calls toggleDevVisSelect(id) — whole row is clickable for selection.
     toggleDevVisSelect(id) — toggles selectedInDevVis, updates .is-selected + checkbox, calls _updateDevVisSelUI().
     _updateDevVisSelUI() — enables/disables sel buttons, shows count "(N)" in label, syncs devVisSelAllChk indeterminate.
     selectAllDevVis(checked) — selects/deselects all visible (not filtered) items.
     showSelectedDevVis() — deletes all selected from hiddenDevices, clears selection, repopulates panel.
     hideSelectedDevVis() — adds all selected to hiddenDevices, clears selection, repopulates panel.
     showAllDevices/hideAllDevices clear selectedInDevVis before repopulating panel.

     TOPOLOGY VIEW PORT FILTERS (AND conditions; in-memory; lost on window close):
     All three filters use dev-vis-ip-section / dev-vis-ip-header / dev-vis-ip-list CSS classes.
     dev-vis-ip-list uses display:grid; grid-template-columns:repeat(4,1fr) — items side-by-side.
     dev-vis-ip-list .dev-vis-item overrides padding to 4px 5px; gap 4px for compact grid fit in 230px panel.
     Items use dev-vis-item / dev-vis-eye / dev-vis-name / is-hidden classes (same as device list).
     is-minimized CSS rule hides all dev-vis-ip-section elements when panel is minimized.
     initApp() calls all three render functions; fetchData() non-silent reset restores all defaults.
     Post-render: applyTopoIpTypeFilter() + applyTopoIntModeFilter() + applyTopoSviFilter() called
                  before requestAnimationFrame(redrawLinks) after topology render completes.
     isHidden check in redrawLinks() posCache building includes hidden-ip-type, hidden-int-mode,
     hidden-svi alongside trace-hidden/hidden-local/hidden-global — excluded ports drop from posCache
     so their cables are not drawn on canvas.
     CSS: .port-row.hidden-ip-type/.hidden-int-mode/.hidden-svi { display:none !important }

     ip_type: topoIpFilter=['p2p','gw','blank']; 3 real + 1 dummy; section id=devVisIpSection.
       applyTopoIpTypeFilter() reads details.ip_type_ → normalized p2p/gw/blank → hidden-ip-type class.
       Normalization: (ipRaw==='p2p'||ipRaw==='gw') ? ipRaw : 'blank' — identical to Sheet View Code.gs.
       No separate validLinks filter — posCache exclusion is the sole mechanism for cable hiding.

     int_mode: topoIntModeFilter=all 8 sp_mode_ values; 8 real items (4×2 grid); section id=devVisIntModeSection.
       Values: l2-et-access, l2-et-trunk, l2-po-access, l2-po-trunk, l3-et-int, l3-et-sub-int, l3-po-int, l3-po-sub-int.
       Labels: l2ea/l2et/l2pa/l2pt/l3ei/l3es/l3pi/l3ps; full value as tooltip title attribute.
       applyTopoIntModeFilter() reads details.sp_mode_ → exact match vs topoIntModeFilter → hidden-int-mode class.
       Blank sp_mode_ always shown (classList.remove, early return) — fixes bug where GW/NA ports with no
       sp_mode_ were hidden even when all modes selected.

     svi: topoSviFilter=['yes','blank']; 2 real + 2 dummies; section id=devVisSviSection.
       applyTopoSviFilter() reads details.svi_ → 'yes' or 'blank' → hidden-svi class.

  3. DEVICE MANAGER (permanent, shared) — Sidebar.html device manager tab
     Storage: TOPOLOGY_HIDDEN_DEVICES (ScriptProperties)
     Functions: getHiddenDevices() / saveDeviceVisibility(list)
     Effect: persistent node visibility used by getExistingDevices() → isVisible field read
             by topology rendering and Device Manager UI; triggers full fetchData(true) inline in save handler; saveDeviceLabels bumps DATA_VERSION in that chain

  saveDeviceVisibility does NOT bump DATA_VERSION (Device Manager callers call fetchData(true)
  explicitly). Sheet View functions never touch TOPOLOGY_HIDDEN_DEVICES.

runFullSheetCleanup()
  Called automatically before topology read. Normalizes interface names,
  clears invalid values, enforces schema consistency.
  Active cleanup rules:
    A. Interface name normalization (canonicalizeInterface): eth→Et, po→Po, vl→Vl, etc.
    B. SVI_VLAN standardization: true/1/y/yes → 'all' (legacy migration)
    Po format normalization (normalizePo): port-channel10/PO10 → Po10
  Removed cleanup rules (audit flags these instead — user may want to rollback):
    C. MLAG+L3 conflict: was clearing sp_mode_ when po_ matched mlagConfigPorts; audit Check B flags it
    D. L3 stale SVI: was clearing svi_ when mode is l3-*; audit Check C flags it
    E. L2 stale ip_type_: was clearing ip_type_ when mode is l2 and SVI off; audit Check I flags it
    F. ET stale PO: was clearing po_ when mode is et-based; audit Check H flags it

validateAndCleanData(header, value, rowData, headers, activeSviValue)
  Called from onEdit Phase A for every edited cell. Active rules:
  - Rule 4: VLAN format validation (rejects non-numeric/range formats)
  - Rule 6: Po format auto-correction (port-channel10 → Po10)
  Removed rules (audit flags instead; user may want to rollback):
  - Rule 1: L3 + svi_ set → was rejecting/clearing; audit Check C flags it
  - Rule 2: L2 + no SVI + ip_type_ set → was rejecting/clearing; audit Check I flags it
  - Rule 3: ET mode + po_ set → was rejecting/clearing; audit Check H flags it

onOpen()
  Simple trigger: fires when the spreadsheet opens (AuthMode.LIMITED).
  - Builds the TopoAssist custom menu and adds it to the UI via addToUi().
  - Does NOT call showModelessDialog() — AuthMode.LIMITED forbids it; the dialog call would
    fail silently. Auto-open is handled by onOpenInstallable() (installable trigger) instead.
  - Does NOT call ScriptApp.newTrigger() — also forbidden at AuthMode.LIMITED.

onOpenInstallable()
  INSTALLABLE onOpen trigger handler — runs with full auth, can open dialogs.
  - Auto-opens Sheet View if a column filter is active:
      if getViewPreferences().length < getSchemaConfig().length, calls showSheetAssistPanel().
      Wrapped in try/catch — silently ignored on first run before trigger is installed.
  - Wired up by ensureOnOpenTrigger(), called from showTopologyWindow() / showSheetAssistPanel()
    on first menu use (full auth context).

ensureOnOpenTrigger()
  Installs the onOpen installable trigger pointing at onOpenInstallable if not already present.
  - Checks ScriptApp.getProjectTriggers() for an existing ON_OPEN trigger on this spreadsheet.
  - If missing, calls ScriptApp.newTrigger('onOpenInstallable').forSpreadsheet(ss).onOpen().create().
  - Called from showTopologyWindow() and showSheetAssistPanel() inside try/catch.

onEdit(e)
  Trigger: fires on cell edit.
  - Col A guard (FIRST, before startRow<3): if startCol===1, reverts the edit and shows toast
    "Column A is reserved for TopoAssist row visibility markers and cannot be edited." Returns immediately.
    Row 2: restores DUMMY_VIS_HEADER via setValue (clearing it would cause ensureDummyColumn to
    insertColumnBefore(1) on next call, duplicating col A). All other rows: clearContent.
  - Bumps DATA_VERSION in ScriptProperties (triggers client polling refresh)
  - Phase A: runs validateAndCleanData per cell (format validation + Po normalization only)
  - Phase B mode column: updates cell background colours only (po_ grey for ET, svi_ grey for L3)
  Phase B auto-clears removed (audit flags instead; user may want to rollback):
  - Was clearing po_ when mode → ET (audit Check H)
  - Was clearing svi_ when mode → L3 (audit Check C)
  - Was clearing ip_type_ (gw) when mode → L3 non-po (audit Check I)
  - Was clearing ip_type_ when mode → L2 + no SVI (audit Check I)
  - Was clearing ip_type_ when SVI disabled on L2 (audit Check I)

onStructuralChange(e)
  INSTALLABLE trigger handler (not a simple trigger — must be wired up via ensureOnChangeTrigger).
  - Only acts on changeType === 'REMOVE_COLUMN' on the SHEET_DATA sheet; ignores all other events.
  - Calls ensureDummyColumn(sheet): if col 1 row 2 !== _sys_, inserts a new col before col 1 and
    restores header, note, row 1 label, 36px width, and gray background.
  - Shows toast: "Column A (_sys_) was deleted and has been automatically restored." (8 sec).
  - NOTE: onEdit cannot catch column deletion (structural change) — onChange installable is required.

ensureOnChangeTrigger()
  Installs the onChange installable trigger pointing at onStructuralChange if not already present.
  - Checks ScriptApp.getProjectTriggers() for an existing ON_CHANGE trigger on this spreadsheet.
  - If missing, calls ScriptApp.newTrigger('onStructuralChange').forSpreadsheet(ss).onChange().create().
  - NOT called from onOpen() — simple triggers run with AuthMode.LIMITED which cannot call
    ScriptApp.newTrigger(); the call would silently fail every time.
  - Called from showTopologyWindow() and showSheetAssistPanel() inside try/catch — these are
    menu-triggered functions that run with full auth, so the trigger installs on first panel open.

--------------------------------------------------------------------------------
SECTION 25B: PERSISTENCE KEYS INVENTORY
--------------------------------------------------------------------------------

SCRIPTPROPERTIES (server, shared across all users of this script deployment):
  DATA_VERSION              — integer string; incremented on every sheet edit (onEdit trigger)
  SHEET_VIEW_MODE           — 'CUSTOM' when a custom view is active
  CUSTOM_VIEW_PREFIXES      — JSON array of attribute key prefixes visible in custom view
  TOPOLOGY_HIDDEN_DEVICES   — JSON array of device names hidden from topology render

DOCUMENTPROPERTIES (server, per-spreadsheet):
  SCHEMA_CONFIG_ARRAY       — JSON array; the full ordered schema (attribute key list)
  NON_ARISTA_DEVICES        — JSON array; device names that are not Arista EOS
  GLOBAL_DEVICE_CONFIG_TEMPLATE — string; prepended to all device configs in Configs sheet
  DEVICE_HOSTNAMES          — JSON object {name: hostname}; maps device name → EOS hostname

USERPROPERTIES (server, per-user per-script):
  CUSTOM_VIEW_PREFS         — JSON array of attribute key prefixes for this user's custom view
  SHEET_VIEW_HIDDEN         — JSON array of device names the user hid from sheet rows
  SHEET_VIEW_IP_FILTER      — JSON array of ip_type values to show ('p2p','gw','blank')
  SHEET_VIEW_INT_MODE_FILTER — JSON array of int_mode values to show
  SHEET_VIEW_SVI_FILTER     — JSON array of svi values to show
  NET_UNDERLAY              — 'bgp'|'ospf'|'bgp+ospf'|'none'
  NET_VXLAN                 — 'true'|'false'
  NET_EVPN                  — 'true'|'false'
  NET_BGP (legacy)          — 'true' only; auto-migrated to NET_UNDERLAY='bgp' on read

USERCACHE (server, per-user, TTL 200s):
  TOPOLOGY_STATUS           — progress string shown in status bar during topology build
  TOPO_<VERSION>            — full JSON topology payload for DATA_VERSION=VERSION

LOCALSTORAGE (client, per-browser):
  topology_nodes_detailed_linear   — JSON node positions for detailed-linear view
  topology_nodes_detailed_flexible — JSON node positions for detailed-flexible view
  topology_nodes_simple            — JSON node positions for simple view
  camera_state_detailed_linear     — JSON {x,y,k} camera transform for detailed-linear
  camera_state_detailed_flexible   — JSON {x,y,k} camera transform for detailed-flexible
  camera_state_holistic            — JSON {x,y,k} camera transform for holistic view
  holistic_group_rects             — JSON array of group rect {id,x,y,width,height,label,color,textPos,...,members:[deviceId]} shared by Simple+Flex views; member rects compute bounds live from node positions
  nb_collapsed                     — JSON array of collapsed port IDs

SESSIONSTORAGE (client, per-tab):
  topology_snapshot_<viewMode>     — serialized render state for instant restore on reload

--------------------------------------------------------------------------------
SECTION 25C: GLOBAL STATE VARIABLES (Sidebar-js.html)
--------------------------------------------------------------------------------

KEY GLOBALS AND THEIR INVARIANTS:

  allNodesData        — Object keyed "DeviceName:PortName" (canonical). Cleared + repopulated
                        on every render() call. Do NOT persist state across renders here.

  configCache         — { [deviceName]: { data: configMap } }
                        configMap is the full prefixed key map (NOT a per-port string).
                        Access: configCache[dev].data["050_Et1"].full

  pendingFetches      — Set<deviceName>; deduplicates concurrent GAS calls for same device.
                        Always check before calling fetchFullConfig().

  selectedDeviceId    — string|null; currently selected device node.
  selectedLineId      — string|null; currently selected link.
                        INVARIANT: these two are mutually exclusive. Setting one clears the other.

  isFetching          — boolean; true while a google.script.run call is in flight.
                        Smart polling skips when true (prevents concurrent GAS executions).

  isEditMode          — boolean; true while user is dragging/editing topology nodes.
                        Smart polling skips when true.

  topoIpFilter        — Set<string>; ip_type values currently shown. Default: all values selected.
  topoIntModeFilter   — Set<string>; int_mode values currently shown. Default: all values selected.
  topoSviFilter       — Set<string>; svi values currently shown. Default: all values selected.
  INVARIANT: filters default to ALL values selected (show-all state) — empty set = show nothing.

  currentViewMode     — 'detailed_linear'|'detailed_flexible'|'simple'|'holistic'
                        Persisted to localStorage across page reloads.

  savedNodePositions  — {[portId]: {x,y}}; loaded from localStorage in loadStateFromStorage().
                        Used by D3 to restore node positions before simulation starts.

--------------------------------------------------------------------------------
SECTION 25D: redrawLinks() ALGORITHM
--------------------------------------------------------------------------------

redrawLinks() is the canvas rendering function. Called whenever nodes move or filters change.

4-STEP ALGORITHM:
  1. BUILD posCache (read phase)
     - Iterate all port rows in the DOM
     - Skip ports with any hidden-* CSS class (filter-excluded ports)
     - Read x,y from D3 node data → store in posCache[portId]
     - posCache never contains filtered-out ports

  2. BUILD validLinks
     - Iterate all declared links (from topology payload)
     - Include only links where BOTH endpoints exist in posCache
     - Links to filtered-out ports are silently excluded

  3. DYNAMIC RESIZE + DPR SCALE
     - Measure container size, apply devicePixelRatio scaling
     - Clears canvas

  4. DRAW IN 10 Z-ORDER LAYERS
     Layer 1:  base_bus      — LAG/bundle background traces
     Layer 2:  base_direct   — point-to-point physical links
     Layer 3:  non_eos       — links to/from non-Arista devices
     Layer 4:  regular_po    — regular Port-Channel aggregation lines
     Layer 5:  mlag          — MLAG Port-Channel lines
     Layer 6:  peer          — MLAG peer-link lines
     Layer 7:  trace         — VLAN trace highlight overlay
     Layer 8:  search        — search result highlight overlay
     Layer 9:  error         — invalid config highlight overlay
     Layer 10: selected      — currently selected link highlight

TOPOLOGY FILTER INTEGRATION:
  Filters (topoIpFilter, topoIntModeFilter, topoSviFilter) work by adding hidden-*
  CSS classes to port <tr> rows (e.g. hidden-ip-type, hidden-int-mode, hidden-svi).
  redrawLinks() excludes rows with these classes in step 1 (posCache build).
  This means filter changes MUST call redrawLinks() — CSS class toggle alone is not
  enough because posCache and validLinks must be rebuilt.

--------------------------------------------------------------------------------
SECTION 25E: initApp() INITIALIZATION FLOW (client startup)
--------------------------------------------------------------------------------

Runs once when the sidebar HTML page loads. Order is critical.

  1.  initTheme()                     — apply dark/light from localStorage
  2.  initToolbarCollapse()           — restore toolbar collapsed state
  3.  [if manager mode] → handle device manager UI; return early
  4.  [if schema mode]  → handle schema manager UI; return early
  5.  renderIpTypeList()              — populate IP type filter checkboxes
      renderIntModeList()             — populate int-mode filter checkboxes
      renderSviList()                 — populate SVI filter checkboxes
  6.  hideGlobalLoading()             — remove initial loading spinner
  7.  currentViewMode = localStorage  — restore last used view mode
  8.  loadStateFromStorage(mode)      — populate savedNodePositions from localStorage
  9.  loadGroupRects()                — restore holistic group rects from localStorage
  10. Try instant restore from sessionStorage → restore camera state if available
  11. fetchData(false, true)          — always runs; auto-verify=true triggers schema check
  12. bindGlobalSearchListeners()     — attach search input handlers
  13. initLayoutObserver()            — ResizeObserver for canvas resize
  14. startSmartPolling()             — begin DATA_VERSION polling loop

--------------------------------------------------------------------------------
SECTION 26: PROMPT TO RECREATE FROM SCRATCH
--------------------------------------------------------------------------------

Use the following prompt with an AI to recreate this project:

---

"Build a Google Apps Script project called TopoAssist — an Arista EOS Lab Workbench.

PROJECT OVERVIEW:
Create a 5-file GAS project (Code.gs, Sidebar.html, Sidebar-css.html,
Sidebar-js.html, UserGuide.html) that:
  - Adds a 'TopoAssist' custom menu to a Google Spreadsheet
  - Manages network device port-mapping data in a structured sheet
  - Renders a live network topology visualizer using D3.js in a modeless dialog
  - Generates Arista EOS device configurations from sheet data
  - Supports MLAG, VXLAN/EVPN, BGP, and L2/L3 interface modes

SHEET STRUCTURE:
Row 1 = Device names (merged). Row 2 = Attribute headers (format: <key>_<DeviceName>).
Rows 3+ = Port data. Default sheet name = 'PortMapping' (stored in ScriptProperties).
Schema keys: int_, po_, sp_mode_, vlan_, svi_vlan_, ip_type_, vrf_, et_speed_,
xcvr_speed_, encoding_, xcvr_, snake_int_, desc_, sd_, dp-pp-mp_ (in that order).
Native VLAN is encoded as nv<N> token inside vlan_ (e.g. "10,20,nv100") — no separate n_vlan_ column.
et_speed_: EOS interface speed command (e.g. "25g"); used by config gen and audit Check F.
xcvr_speed_: physical transceiver speed (e.g. "100g-4" for QSFP-DD); used by cabling grouping
  and report. Falls back to et_speed_ if blank.
Non-Arista devices (IXIA/Spirent) get only int_ column.

TOPOLOGY VISUALIZER (Sidebar-js.html + D3.js):
Three view modes: Linear (cable/patching), Simple/Holistic (node graph),
Flexible (D3 force-directed). Toolbar with sort controls (SHEET/MODE/IP/ALPHA),
search with VLAN trace (v10 syntax), edit mode for visual cabling, audit mode,
dark/light theme toggle, PNG export via html2canvas, config copy/view per device.
Device labels: SPINE, LEAF, HARNESS, TRANSIT, DUT, IXIA, SPIRENT.

MLAG LOGIC (EXPLICIT DECLARATION):
MLAG pairs are declared in Device Manager (DEVICE_MLAG_PEERS). The old >= 4
PO-count heuristic is removed. Peer-links are physical links between declared pairs.
MLAG + L3 is INVALID — output error comment block instead of config.

CONFIG GENERATION:
generateConfig(portName, d, ipPrefs, seenPos, netSettings) — 5 params. netSettings (13 IP family flags object) gates IPv6/OSPF/BGP/VXLAN/EVPN commands.
generateBGP(..., settings, ipPrefs) — 9 params; settings gates peer group emission per protocol family; ipPrefs provides loBase for loopback/ASN derivation.
generateBGPEvpnOverlay(..., settings, ipPrefs) — 7 params; settings and ipPrefs same roles as generateBGP.
Auto-description (9-rule): physical → 'PeerDev-PeerPort'; Po → neighbor list;
peer-link → append MLAG-PEER-LINK; child of Po → append PoX.
IP from VLAN: VLAN 1050 → .10.50. Last octet = device DID. IPv6 same pattern (lab).

VISUAL FORMATTING:
Connection pairs colored by pastel hash. MLAG cells get black SOLID_MEDIUM border.
Device headers use golden-angle HSL color rotation. Conditional rules:
member ports → italic; SVI/Po/IP grayed when mode makes them N/A.

CONSTRAINTS:
- hasKey(set, key) for all case-insensitive device name lookups
- canonicalizeInterface() duplicated in both Code.gs and Sidebar-js.html
- JetBrains Mono must be explicitly set on every UI element in CSS
- SHEETNAME(dummy_cell) function must exist (used by IXIA tab formula)
- rebuildSheet() must backup/rollback on error
- DATA_VERSION in ScriptProperties drives client polling for live refresh

Use PropertiesService (Script for global state, Document for sheet-level,
User for per-user prefs). CacheService for topology caching.
All dialogs use HtmlService.createTemplateFromFile with include() for CSS/JS."

---

================================================================================
SECTION 27: TOOLBAR DESIGN — RIBBON LAYOUT (Sidebar.html)
================================================================================

The toolbar uses a two-row ribbon pattern. All structural decisions here are
intentional and documented for future sessions.

PRIMARY ROW (toolbar-row primary-row)
──────────────────────────────────────
Left group (ui-group):
  Minimize button | Refresh button | W/G inputs (card width / device gap) | O/T inputs (cable offset / top)

Right group (ui-group right-group, align-items:stretch) — ribbon sections separated by .tb-sep:
  EDIT   → editModeBtn (pencil icon)
  CONFIG → techModal (stack layers), ipConfigModal (server rack), globalCfgModal (doc+pencil), generateAllModal (all configs)
  VIEW   → vlanSummaryModal (table rows), devVisPanelBtn (eye), downloadPNG (camera), cablingModal (tree)
  AUDIT  → btnBridgeToolbar (shield/lines) + audit-group-box (validateBtn + btnAuditOnly)
  THEME  → boldTextBtn (B), themeToggleBtn (moon)
  Utility (no label, align-self:flex-end) → openHelpModal (?), toolbarCollapseBtn (chevron)

Each ribbon section uses these classes:
  .tb-section  — flex-column wrapper (label above, buttons below)
  .tb-label    — 10px muted monospace category header (EDIT / CONFIG / VIEW / AUDIT / THEME)
  .tb-btns     — flex-row of icon buttons
  .tb-sep      — 1px vertical border, align-self:stretch, margins 4px

SECONDARY ROW (toolbar-row secondary-row)
──────────────────────────────────────────
Left group (ui-group left-group, align-items:stretch) — ribbon sections separated by .tb-sep:
  VIEW  → segmented-group: btnTopoSimple (holistic) / btnTopoLinear (linear) / btnTopoFlexible (flexible) + btnToggleCards (collapse all)
  SORT  → segmented-group: btnSortSheet / btnSortMode / btnSortIp (CAT) / btnSortAlpha
  search-container (align-self:center) → searchInp + searchClear + search-autocomplete
  chk-wrapper (align-self:center) → strictSearch checkbox

Right group:
  #status div (mono-status "Ready")

COPY ROW (toolbar-row copy-group-strip, display:none):
  splitDevice / splitLeft / splitRight split-buttons + selectionStatus
  Shown only when a link or device is selected.

HOLISTIC CANVAS CONTROLS (#holistic-controls):
  NOT in toolbar. Floating panel inside #viewport, position:absolute bottom-right 16px.
  Shown only when activeMode === 'simple' (holistic view).
  Contains: groupRectBtn (Draw Group), nodeSizeMinusBtn (−), nodeSizePlusBtn (+).
  JS: _updateViewModeButtons() shows/hides this panel.

CSS CLASSES (Sidebar-css.html):
  .tb-section  { flex-direction:column; align-items:center; justify-content:space-between; gap:3px; padding:2px 3px }
  .tb-label    { font-family:JetBrains Mono; font-size:10px; color:var(--text-muted); opacity:0.65; user-select:none }
  .tb-btns     { flex-direction:row; align-items:center; gap:2px }
  .tb-sep      { width:1px; background:var(--border); align-self:stretch; margin:4px 4px }
  .right-group { align-items:stretch } — required for tb-sep to span full toolbar height

DESIGN RATIONALE:
  - Sort moved from primary (center-group) to secondary so primary right-group has room for labeled sections
  - Holistic controls moved off toolbar to canvas so they are contextual and don't clutter
  - Bridge placed in AUDIT (not VIEW) — it validates physical wiring vs sheet data
  - Utility buttons (?, collapse) have no label and are bottom-aligned to not imply a category
  - Labels are 10px muted (opacity:0.65) to be visible but not compete with icons

================================================================================
TESTING
================================================================================

Three test suites cover the pure (non-GAS) logic:

── Python (device_bridge.py) ──────────────────────────────────────────────────
Run:  cd ~/claude/projects/topoassist && pytest tests/ -v
File: tests/test_device_bridge.py (91 tests)

Covered functions:
  _extract_session_diff  — EOS SSH diff parsing (7 cases)
  _oc_lldp_to_eos        — OpenConfig LLDP → EOS format (5 cases)
  _oc_version            — extract EOS software version (5 cases)
  _oc_platform           — extract platform/model (4 cases)
  _oc_iface_status       — OC interface status → linkStatus (7 cases)
  _gnmi_val              — extract val from pygnmi response (6 cases)

pytest installed via: pip3 install pytest
conftest.py adds project root to sys.path so device_bridge imports cleanly.

── GAS (Code.gs) ──────────────────────────────────────────────────────────────
Run:  Apps Script editor → select runAllTests → Run
File: Test-gs.gs  (was Tests.gs — renamed 2026-05-06)
Output: SpreadsheetApp alert (summary) + Execution Log (full detail)

Covered functions:
  canonicalizeInterface    — interface name normalization (18 cases)
  hasKey                   — case-insensitive Set lookup (6 cases)
  normalizePo              — Port-Channel number extraction (8 cases)
  isValidPort              — port string validation (8 cases)
  _parseSviVlans           — SVI VLAN subset selection (11 cases)
  _parseVrfList            — VRF list parsing (cases)
  _resolveVrfAtIndex       — VRF lookup by index (legacy; kept for tests)
  _buildVrfMap             — token-based VLAN→VRF map builder (13 cases)
  _resolveVrfForVlan       — VRF lookup by VLAN number (replaces _resolveVrfAtIndex in config gen)
  _auditVrfIssues          — VRF audit: all 6 conditions, error+warn+clean paths (24 cases)
  parseVlanWithNative      — nv<N> native-VLAN token split (11 cases)
  getPhysicalPortParent    — strip breakout lane suffix (cases)
  compressPortList         — sort + join port array (cases)
  _breakoutSides           — QSFP vs SFP lane side detection
  _buildCableGroupsForTest — cabling grouping: Scenario A/B/C + snake + non-Arista
  generateSnakeStaticConfig  — snake test config gen (~30 cases)
  generateSnakeTtlPbrConfig  — TTL PBR config gen
  generateSnakeTtlTrafficPolicyConfig — TTL traffic-policy config gen
  findAttrKey              — attribute key lookup
  deviceMetadata           — JSON round-trip + null-prop fallback (8 cases)
  generateGlobalBlock      — ip routing/ipv6/multi-agent/VARP MAC (6 cases)
  generateComplexL3Block   — GW type anycast/VARP, vx1 exclusion (5 cases)
  _compressVniLines        — consecutive VNI range compression (8 cases)
  deviceIdSnapshot         — shift detection + isFirstRun (5 cases)
  expandVlanString         — VLAN string → Set<int>; NaN + reversed range (10 cases)
  compressVlanRanges       — Set<int> → compressed range string (8 cases)
  parseAndExpandDevices    — "leaf[01-03]" → list; zero-pad, dedup, >50 skip (7 cases)

── Client-side JS (Sidebar-js.html) ──────────────────────────────────────────
Run:  cd ~/claude/projects/topoassist && node test-js.js
File: test-js.js  (was tests-client-node.js — renamed 2026-05-06; 22 tests)
Output: pass/fail per test, summary line

Also: Tests-client.html (browser-runnable; macOS: open Tests-client.html)

Covered functions (closures inside _updateGwExample / _readGwDeviceOverrides):
  applyHint(el, inheritedVal)          — 4 cases
  lockFirst(el, wide, fallback, force) — 10 cases (wide + exit-wide)
  g(f, inputMap)                       — 4 cases (disabled / hint / explicit)
  computeGlb4firstForNonWide(...)      — 4 cases

SYNC rule: applyHint and lockFirst in test-js.js and Tests-client.html are
verbatim copies of the closures in Sidebar-js.html. Both carry // SYNC:
comments. Update all three whenever either function changes.

Hook: topoassist-client-test-check.sh fires on every Sidebar-js.html edit —
detects new top-level functions, checks test-js.js for coverage, and reminds
to run: node test-js.js

── What is NOT tested ─────────────────────────────────────────────────────────
GAS-entangled functions (PropertiesService, SpreadsheetApp, UrlFetchApp) and
network transport functions (_ssh_cmds, _eapi_cmds, etc.) are not unit-tested.
They require integration testing against a live sheet or real devices.

================================================================================
SECTION 26: UI ELEMENT INVENTORY
--------------------------------------------------------------------------------

A. NATIVE SHEETS DIALOGS (ui.prompt / ui.alert — server-side, block execution)
   These open as browser-native dialogs, not sidebar overlays.

   ui.prompt (user types a value):
     promptRenameSheet()         "Change Sheet Name"         — new sheet name input
     createTopologySnapshot()    "Create Sheet Checkpoint"   — checkpoint label input

   ui.alert YES/NO (user confirms):
     syncSchemaColumns()         "Sync Schema: Orphaned Columns" — confirm column delete

   ui.alert OK (info/error):
     promptRenameSheet() flow    — validation errors, success confirmation (5 alerts)
     createTopologySnapshot()    — failure message

B. SIDEBAR MODALS — modal-std (standard floating overlay, Sidebar.html)

   ID                          Header Title                      Type
   ─────────────────────────────────────────────────────────────────────
   networkStackModal            Network Stack                     modal-std
   autoConfigModal             Auto Config                       modal-std
   deviceBridgeModal           Device Bridge                     modal-std
   pushConfirmModal             Config Push                       modal-std
   exportCablingModal           Export Cabling                    modal-std (tab layout: Export tab=#cablingControlsPanel, Topology List/Device List tabs=#cablingViewRight; _cablingShowTab(tab) is unified switch)
   cablingControlsPanel         (static)                          cabling-panel-controls (Export tab body: bridge filter, topology section, device section, AI Validate)
   cablingViewRight             (find bar + textarea)             cabling-panel-view (shown on Topology List/Device List tabs; find bar + Copy + Download; _cablingShowTab drives visibility)
   helpModal                   Shortcuts Help                    modal-std
   guiEditModal                Edit Interface                    modal-std
   auditModal                  Configuration Audit               modal-std
   groupRectModal              Group                             modal-std
   configModal                 (device name — set dynamically)  modal-std
   configCenterModal           Config Center                     modal-std
   vlanSummaryModal            VLAN Summary                      modal-std
   auditSettingsModal          Audit Checks                      modal-std

   Full-page containers (modal-std modal-large — slide in from right, cover canvas):
   deviceManagerContainer      Device Manager
   sheetColumnManagerContainer Column & Formatting Manager
   sheetVisContainer           Sheet Custom View Managers


C. SIDEBAR PANELS (non-modal, always-present UI regions)

   ID                    Name              Location / Behaviour
   ──────────────────────────────────────────────────────────────
   appTopologyContainer  App Topology      Main canvas; always visible in topology mode
   devVisPanel           DevVis            Floating panel (dev-vis-panel), toggled by
                                           devVisPanelBtn; has own minimize behaviour
   #modalDock            Modal Dock        Fixed bar at bottom; shows chips for each
                                           .modal-std.modal-minimized modal
   #activeModalChip      Active Chip       Amber pill in APP toolbar; shows name of
                                           currently open modal (hidden when none open
                                           or when active modal is minimized)

D. CHIP / DOCK NAME SOURCE
   Both #activeModalChip (_updateActiveChip) and #modalDock chips (_updateMinimizedDockChip)
   read the modal name from: modal.querySelector('.modal-header-title').textContent.trim()
   No hardcoded name map — chip label always matches what the modal header displays.

================================================================================
END OF INSTRUCTIONS
================================================================================
