# EOS CLI Tricks Lookup

Read the cheatsheet at `~/claude/projects/eos-tricks/EOS_CLI_Tricks.md` and answer the user's question using the content there.

The user's question is: $ARGUMENTS

Instructions:
- Search the cheatsheet for relevant commands/tricks matching the question.
- Show the exact commands from the file, formatted as code blocks.
- If multiple sections are relevant, show all of them.
- If nothing matches, say so and suggest the closest related section.
- Do not invent commands not in the cheatsheet — only cite what's there.
