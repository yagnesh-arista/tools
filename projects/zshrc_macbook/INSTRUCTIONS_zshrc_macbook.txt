INSTRUCTIONS — zshrc_macbook project
Last updated: 2026-04-27 (bus(): fixed parse error — apostrophe in comment ("don't") inside expect -c '...' block closed the zsh single-quoted string early, causing zsh to parse Tcl as shell code; replaced with "do not"; also: (1) removed -t from session-list fetch spawn to stop PTY escape sequences leaking into expect_out(buffer); (2) added regsub to strip non-digit chars from user_input, filtering ^[[O/[[I sequences iTerm2 sends in raw mode; (3) added \x1b filter in session loop to skip escape-containing lines)
Previous: 2026-04-21 (busclaude: wrap command in bash -l -c to source login profile so ~/.local/bin/claude is in PATH)
Previous: 2026-04-21 (Added busclaude(): like bus0 but ssh -t bus-home 'cd ~/claude && ./lclaude')
Previous: 2026-04-18 (Review fixes: bus0 title; ls GNU/BSD conditional; drop redundant HIST_IGNORE_DUPS + HIST_APPEND)
Previous: 2026-04-17 (Up arrow bound to fzf-history-widget in section 4, after source <(fzf --zsh); two bindkey lines: ^[[A (standard) and ^[OA (application mode/tmux) — matches bus-home up arrow behaviour)
Previous: 2026-04-17 (Added CLAUDE.md with critical constraints; added .claude/commands/deploy.md (/deploy command))
Previous: 2026-04-16 (fixed section numbering: 1-9 sequential)

PROJECT OVERVIEW
================
.zshrc configuration for the macbook (yagnes76D0W4m).
Shell: zsh (macOS default). This is the single source of truth — .bashrc is no longer used.
Source file lives at: ~/claude/projects/zshrc_macbook/.zshrc
Deploy target: ~/.zshrc on macbook via scp (no auto-hook — remote machine)

Deploy command:
  scp ~/claude/projects/zshrc_macbook/.zshrc yagnesh@<macbook-hostname>:~/.zshrc

FILE STRUCTURE
==============
~/claude/projects/zshrc_macbook/
  .zshrc                          — The actual zshrc config
  INSTRUCTIONS_zshrc_macbook.txt  — This file (AI rebuild prompt)
  CLAUDE.md                       — Auto-loaded constraints summary (no auto-deploy, source order, precmd_functions, zsh-only)
  .claude/commands/
    deploy.md                     — /deploy: show scp command + source order verification

CURRENT .zshrc SECTIONS
========================
1. bus0()      — Quick direct shell on bus-home: sets terminal title "bus-home", arista-ssh check-auth || login, then ssh bus-home
   busclaude() — Like bus0 but opens Claude Code: sets title "bus-claude", auth, then ssh -t bus-home 'bash -l -c "cd ~/claude && ./lclaude"'
                 bash -l required: non-login SSH shells skip ~/.bashrc so ~/.local/bin (claude binary) is missing from PATH
   bus()       — Interactive tmux session menu for jump host (bus-home) via SSH multiplexing + expect
                 INVARIANT: session-list fetch must NOT use -t (no PTY) — PTY causes terminal init
                 escape sequences to leak into expect_out(buffer) and corrupt/empty the session list.
                 INVARIANT: user_input must be stripped with `regsub -all {[^0-9]} $user_input "" user_input`
                 after expect_user read — iTerm2 sends ^[[O/[[I escape sequences in raw mode that
                 otherwise get captured as part of the choice string.
                 INVARIANT: session loop must skip lines matching *\x1b* to filter any remaining
                 escape-containing output before adding entries to session_list.
2. wsc()  — Remote Wireshark live capture via SSH + named pipe (FIFO) to Wireshark app
           NOTE: ssh_error uses 2>&1 >/dev/null to capture stderr for host key change detection
3. General Settings + PATH + CDPATH:
   - ~/go/bin prepended to PATH (fzf, gnmi, and other go-installed binaries)
   - fzf keybindings via source <(fzf --zsh) — Ctrl+R history, Ctrl+T file, Alt+C dir
   - Up arrow bound to fzf-history-widget (^[[A + ^[OA) immediately after source <(fzf --zsh)
     so the widget exists before bindkey runs; matches bus-home up arrow behaviour
   - FZF_DEFAULT_OPTS: --height 30% --layout=reverse --border
   - FZF_TMUX_HEIGHT=30% (controls fzf internal default height — must be set)
   - FZF_CTRL_R_OPTS: --height 30% --no-sort --exact (force height override, preserve order)
   - FZF_CTRL_T_OPTS: bat preview right:50% (Ctrl+T file search with syntax-highlighted preview)
   - FZF_ALT_C_OPTS: tree fallback ls preview right:40% (Alt+C dir jump with tree or ls preview)
   - Up arrow: fzf-history-widget (both ^[[A and ^[OA); Ctrl+R also works
   (fzf installed via: go install github.com/junegunn/fzf@latest)
   - zsh-autosuggestions: cloned to ~/.zsh/zsh-autosuggestions/, sourced after fzf
     ZSH_AUTOSUGGEST_HIGHLIGHT_STYLE=fg=cyan set in General Settings
   - stty -ixon                              (disable Ctrl+S/Q flow control)
   - setopt IGNORE_EOF + IGNOREEOF=2         (require 3 Ctrl+D presses to exit)
   - TZ=Asia/Kolkata                         (timezone)
   - ZSH_AUTOSUGGEST_HIGHLIGHT_STYLE=fg=cyan (autosuggestion color, visible in tmux)
   - EDITOR/VISUAL=vim
4. History: HISTSIZE=100000, SAVEHIST=200000, SHARE_HISTORY (syncs across tmux panes, implies HIST_APPEND),
            HIST_IGNORE_ALL_DUPS (dedup; supersedes HIST_IGNORE_DUPS — not set),
            HIST_FIND_NO_DUPS (up/down arrow skips duplicates)
5. Aliases: ls/ll/la/lrtha use GNU --color=auto if available, else BSD -G (auto-detected at startup); grep --color=auto; .., ..., mkcd()
6. Prompt: green user@host, blue cwd, git branch via vcs_info
   NOTE: uses precmd_functions+=(vcs_info_precmd) NOT precmd() directly — avoids conflict with iTerm2
7. iTerm2 shell integration: sourced AFTER prompt so it appends to precmd_functions cleanly
   Skips itself automatically inside tmux ($TERM == tmux-256color)

INSTRUCTIONS FOR AI
====================
- Read this file before making any changes to the project.
- After every change, update the relevant section(s) of this file and bump
  the "Last updated" date at the top.
- This is zsh on macOS — use zsh syntax (setopt, not set -o; [[ ]] not [ ]).
- Do not add bloat — only include what the user explicitly asks for.
- No auto-deploy hook. Remind user to scp after every change.
